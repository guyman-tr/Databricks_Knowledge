---
description: "Phase 11 - Generate Documentation (Wiki Only) for DWH Semantic Documentation. Wiki + sidecar + lineage output only — ALTER/deployment handled by Phase 11W (11w-write-objects.mdc)."
globs:
alwaysApply: false
---

# Phase 11: Generate Documentation

## Goal

Any AI assistant or analyst reading this document MUST be able to:
- **Understand** what the table represents in the business
- **Query it** effectively with the right JOINs and filters
- **Interpret** every column value in business terms (no raw IDs left unexplained)
- **Trace** where the data comes from (production lineage)
- **Avoid** performance pitfalls (distribution keys, large scans)

This is NOT a code review document. It's a **query brain** -- the knowledge an expert analyst would have after years of working with this data.

## Prerequisites

ALL applicable phases must be complete before generating.

## Pre-Read: Upstream Wiki (Tier 1 Source) — MANDATORY

Phase 13 identifies the production source table via the Generic Pipeline mapping. Before generating, **read the upstream production wiki** for that source table:

1. From Phase 13 results, get: `database_name`, `schema_name`, `table_name` (the production source)
2. Build the upstream wiki path: `C:\Users\guyman\Documents\github\DB_Schema\etoro\Wiki\{schema_name}\Tables\{schema_name}.{table_name}.md`
   - Also check: `DB_Schema\etoro\Wiki\{schema_name}\Views\{schema_name}.{table_name}.md`
3. **Read the upstream wiki file**. Extract the `## 4. Elements` table.
4. For EVERY DWH column that matches an upstream wiki column by name:
   - Copy the upstream description **VERBATIM** as Tier 1
   - Tag it `(Tier 1 — upstream wiki, {schema_name}.{table_name})`
   - Do NOT paraphrase, summarize, or rewrite
   - Only APPEND DWH-specific notes prefixed with "DWH note:" if the DWH transforms that column
5. If no upstream wiki exists for the mapped source, note it and fall through to Tier 2-4

> **This is the #1 quality driver.** Without this step, every column defaults to Tier 3-4 inference and the documentation is unreliable. Phase 13 MUST have run before this step.

## Pre-Read: Cross-Object Knowledge

Before generating, execute the Pre-Read step from `12-cross-object-enrichment.mdc`:
1. Collect all related objects discovered in earlier phases
2. Check if any already have `.md` docs in `knowledge/synapse/Wiki/`
3. Read existing docs and extract: element descriptions, value maps, relationships
4. Use absorbed knowledge for consistency across docs

## Output Path — THREE FILES PER OBJECT

```
knowledge/synapse/Wiki/{Schema}/{ObjectType}/{ObjectName}.md              ← wiki doc
knowledge/synapse/Wiki/{Schema}/{ObjectType}/{ObjectName}.review-needed.md ← review sidecar
knowledge/synapse/Wiki/{Schema}/{ObjectType}/{ObjectName}.lineage.md       ← production lineage map (from Phase 13 Steps 2-6)
```

Object type folders: `Tables/`, `Views/`

> Stored Procedures and Functions are not documentation targets. They are analyzed as inputs during table/view documentation (Phases 8, 9, 9B) but do not produce their own wiki files.

All three files are mandatory outputs. The `.lineage.md` file captures the production-to-DWH column mapping from Phase 13 Steps 2-6 (SP code lineage analysis).

> **ALTER scripts, downstream propagation, and deployment reports** are produced separately by Phase 11W (`11w-write-objects.mdc`) via the `write-objects-dwh` command. They are NOT generated during wiki build.

Write the wiki doc first, then the review sidecar, then the lineage file.

## STRICT RULES

1. **Use the query-brain template below** for ALL object types
2. **Copy the template EXACTLY** -- do not change section names, numbering, or order
3. **No environment-specific statistics** -- no row counts, percentages, date ranges
4. **DO include lookup value mappings** -- these define the system
5. **DO include query advisory** -- this is the core differentiator
6. **Every `{PLACEHOLDER}` must be replaced** with real content
7. **If a section does not apply**, write "N/A for {object type}." -- do NOT remove the section
8. **Elements table must list EVERY element** -- no skipping, no grouping
9. **CODE IS KING** -- when sources conflict, the hierarchy is: upstream wiki > Synapse live code > live data > metadata > Confluence/Jira. The upstream wiki has already gone through strict code-is-king analysis on production sources and is our authoritative starting point. Synapse code wins only for DWH-specific transformations and derived columns. If Phase 2 sampling found 5 enum values but Confluence documents only 3, include all 5 and note the undocumented ones. Confluence provides business meaning and intent; upstream wiki and Synapse code/data provide truth.
10. **VERBATIM UPSTREAM INHERITANCE** -- when a column exists in an upstream wiki, copy its description verbatim into the Elements table. Do not paraphrase, summarize, or rewrite. Only APPEND DWH-specific notes (prefixed with "DWH note:") if the DWH transforms that column differently. The upstream wiki's descriptions have already been validated through the full code-is-king pipeline — rewriting them introduces errors and loses context.
11. **CONFIDENCE TIERS** -- every element description must be traceable to a source tier: Tier 1 (upstream wiki verbatim — 4 stars), Tier 2 (Synapse SP code / downstream CASE patterns — 3 stars), Tier 3 (live data distribution — 2 stars), Tier 4 (column name inference — 1 star, must be flagged `[UNVERIFIED]`), Tier 5 (domain expert confirmed — 5 stars, highest confidence). When generating the Elements table, Tier 4 descriptions must carry the `[UNVERIFIED]` prefix so human reviewers can prioritize corrections. A Confidence Tier Legend table MUST be included in Section 4 before the Elements table, documenting what each tier means, its star rating, and how the description was derived.
12. **REVIEW SIDECAR** -- alongside every wiki file, produce a `.review-needed.md` companion file listing all Tier 4 items and specific questions for domain experts. Path: same directory as the wiki file, named `{ObjectName}.review-needed.md`. This is not optional.
13. **FULL UPSTREAM SEARCH** -- before labeling any column as "DWH-specific" or writing a Tier 4 description, grep the entire upstream wiki folder (tables, views, related objects, all schemas) for the column name. Columns frequently originate from views, related tables, or history schemas — not just the source table. Only after an exhaustive search returns no results may a column be flagged as DWH-specific or given a Tier 4 description.
14. **REVIEWER CORRECTIONS (RERUN)** -- before generating, check if a `.review-needed.md` already exists for this object. If it contains a `## Reviewer Corrections` section with entries, read every row and apply them as **Tier 5** overrides. Tier 5 is absolute — it overrides all other tiers including upstream wiki (Tier 1). After applying, carry corrections forward in the new `.review-needed.md` with a `[RESOLVED]` marker so they persist across future reruns. **STALENESS CHECK**: For each Tier 5 override, compare the current Tier 1–3 description against the description that was current when the Tier 5 was written (recorded in the "Current (wrong)" column of the corrections table). If the Tier 1–3 description has materially changed (new computation, renamed column, different transformation logic, changed FK target — not just wording/formatting), the Tier 5 may be stale. In this case: (a) still apply the Tier 5 for now, but (b) add the column to a new `## Tier 5 Re-Review Needed` section in the sidecar, showing the original context, the Tier 5 correction, and the new Tier 1–3 description, so a domain expert can confirm or update. Tag the column `(Tier 5 — domain expert, RE-REVIEW)` in the wiki until confirmed.
15. **DOMAIN GLOSSARY** -- before generating, read `knowledge/glossary.md`. All acronym expansions, term definitions, and value maps in the glossary are Tier 5 authority. The pipeline MUST NOT infer alternative meanings for any term that has a glossary entry. Apply glossary terms wherever the term appears in element descriptions, business logic sections, or query advisory.
16. **TIER 5 IN ELEMENTS TABLE** -- when a description comes from reviewer corrections or the glossary, tag it `(Tier 5 — domain expert, {original source})` — preserving the original production source from the lineage file (e.g., `Trade.PositionTbl`, `ETL-computed`, `Trade.OpenPositionEndOfDay → join-enriched`). The Tier 5 label indicates the *description quality* was expert-confirmed; the source indicates *where the data comes from*. Both are required for traceability. Tier 5 entries are never flagged `[UNVERIFIED]` and never appear in the review sidecar's Tier 4 section.
17. **ALL 8 section headers MUST exist as separate `## N.` lines** -- collapsing sections (e.g., `## 2–8.`) is FORBIDDEN and produces an invalid doc. See "Batch Processing & Subagent Quality Enforcement" section below for full enforcement rules.
18. **Batch size limits and minimum line counts are mandatory** -- Tables: max 3/subagent, Views: max 5. Minimum lines: Tables 100+, Views 80+. See Rules B1 and B3 below.

---

## QUERY-BRAIN TEMPLATE

Copy this EXACTLY for every object type. Replace only `{PLACEHOLDER}` values.

````markdown
# {Schema}.{ObjectName}

> {One-sentence business description: what this object represents or does in plain English}

| Property | Value |
|----------|-------|
| **Schema** | {Schema} |
| **Object Type** | {Table / View} |
| **Production Source** | {Production table name or "Derived" or "Multiple sources"} |
| **Refresh** | {Hourly / Daily / Real-time / Unknown} |
| | |
| **Synapse Distribution** | {HASH(column) / ROUND_ROBIN / REPLICATE / N/A} |
| **Synapse Index** | {CLUSTERED COLUMNSTORE / CLUSTERED INDEX / HEAP / N/A} |
| | |
| **UC Target** | _Pending — resolved during write-objects_ |
| **UC Format** | _Pending — resolved during write-objects_ |
| **UC Partitioned By** | _Pending — resolved during write-objects_ |
| **UC Table Type** | _Pending — resolved during write-objects_ |

---

## 1. Business Meaning

{Paragraph 1: WHAT this object represents in the business. Explain it as if talking to an analyst who knows SQL but not this specific dataset. What real-world concept does it model? What question does it answer?}

{Paragraph 2: WHERE this data comes from. Trace the lineage: production source table -> lake export -> Synapse ETL -> this table. Reference the upstream production wiki if applicable: "Source: `Trade.PositionTbl` (see upstream wiki: Trade/Tables/Trade.PositionTbl.md)"}

{Paragraph 3: HOW FRESH is this data and how is it loaded. ETL procedure name, load pattern (full/incremental/merge), refresh frequency, typical lag behind production.}

---

## 2. Business Logic

{For EACH business concept discovered in Phases 6, 9, and 10, create a numbered subsection.}

### 2.1 {Concept Name}

**What**: {One sentence: what business concept this represents}

**Columns Involved**: `{Column1}`, `{Column2}`, `{Column3}`

**Rules**:
- {Rule 1: plain English description}
- {Rule 2: what specific values mean in combination}

**Diagram**:
```
{ASCII diagram showing the relationship, hierarchy, state machine, or flow}
```

---

## 3. Query Advisory

{THIS IS THE KEY DIFFERENTIATOR from the production pipeline output. This section tells analysts HOW to use the table effectively.}

{**VIEW-SPECIFIC**: For views, include a **Data Preview** subsection (3.0) before the distribution/index subsections. Query the view directly via MCP (`SELECT TOP 5 ... FROM [{Schema}].[{ViewName}]`) and show 3-5 representative rows with a Meaning column explaining what each row represents in terms of the view's business purpose. Pick rows that show the variety of data within the view's filter (e.g., different settlement types, instruments, copy-trade vs independent). This preview helps analysts understand what the view actually produces before diving into query patterns.}

### 3.1 Synapse Distribution & Index

{**Explicitly label as Synapse.** Distribution column, index type, and what they mean for Synapse query performance. E.g., "**In Synapse**, this table is HASH-distributed on `PositionID` with a CLUSTERED INDEX. Always include `PositionID` in JOINs or WHERE clauses for optimal Synapse query performance."}

### 3.1b UC (Databricks) Storage & Partitioning

{**Explicitly label as UC/Databricks.** Format, table type, partition columns, and what they mean for Databricks query performance. E.g., "**In Databricks**, this table is stored as Delta (MANAGED), partitioned by `etr_y`, `etr_ym`, `etr_ymd`. Always filter on these partition columns for partition pruning. Consider Z-ORDER for additional clustering."}

### 3.2 Common Query Patterns

{List the 3-5 most common ways analysts query this table, with recommended approaches.}

| Analyst Question | Recommended Approach |
|-----------------|---------------------|
| {e.g., "All open positions for a customer"} | {e.g., "Filter StatusID = 1 AND CID = @cid"} |
| {e.g., "Position P&L for a date range"} | {e.g., "JOIN with fact_pnl on PositionID, filter on date"} |

### 3.3 Common JOINs

| Join To | Join Condition | Purpose |
|---------|---------------|---------|
| {Schema.Table} | {ON condition} | {Why you'd join} |

### 3.4 Gotchas

{Things that trip up analysts:}
- {e.g., "StatusID=2 means 'Being Closed' -- a brief transitional state. For 'closed', look in the history table."}
- {e.g., "TreeID = PositionID means root position. TreeID != PositionID means copy-trade child."}
- {e.g., "MirrorID = 0 means manually opened. MirrorID > 0 means copy-trade."}

---

## 4. Elements

{EVERY column with a clear, meaningful description. No raw IDs left unexplained.}
{For Tables: list every column. For Views: list every output column. For Procedures: list every parameter. For Functions: list parameters + return columns.}
{Order must match the DDL definition order.}

{**VIEW-SPECIFIC RULES -- Views must be as meaning-rich as tables:**}
{1. List EVERY output column individually -- parse the SELECT list from the Phase 1 DDL analysis and enumerate each one. If a view has 60 columns, the table has 60 rows.}
{2. **Inherit descriptions from base table docs**: For each view column, use the column-to-source mapping from Phase 1 to identify its base table (e.g., `dp.CurrencyID` -> `DWH_dbo.Dim_Position.CurrencyID`). Read the existing `.md` doc for that base table and COPY the rich description from there. Adapt slightly for view context if needed, but keep the full business meaning. The tier tag inherits too -- if the base table doc says `(Tier 1 — upstream wiki)`, the view column inherits that tier.}
{3. For computed/derived columns in the view (CASE, ISNULL, arithmetic expressions), document BOTH the formula AND the business purpose: "Computed in view: `ISNULL(InitialUnits, AmountInUnitsDecimal)`. Returns the original unit count at open, falling back to current units for positions opened before InitialUnits was added."}
{4. For columns sourced from a JOIN table (not the primary base table), note the source: "From DWH_dbo.Dim_Instrument via InstrumentID JOIN. Financial instrument traded in this position."}

| # | Element | Type | Nullable | Description |
|---|---------|------|----------|-------------|
| {n} | {Name} | {SQL data type} | {YES / NO} | {MEANINGFUL business description. For ID columns: include the resolved value map: "Position state: 1=Open, 2=Closed (from Dictionary.PositionStatus)". For amounts: include units. For dates: explain when set. For flags: explain both values.} |

---

## 5. Lineage

### 5.1 Production Sources

| Synapse Column | Production Source | Source Column | Transform |
|---------------|-------------------|---------------|-----------|
| {Col} | {Production Schema.Table} | {Source Col} | {None / Expression / CASE mapping} |

{Link to upstream wiki: "Full production documentation: see upstream wiki {path} (source configured in dwh-semantic-doc-config.json)"}

### 5.2 ETL Pipeline

```
{Production Table} -> Generic Pipeline (lake export) -> {Staging Table} -> {ETL SP} -> {This Table}
```

| Step | Object | Description |
|------|--------|-------------|
| Source | {Production.Table} | {Production table description} |
| Lake | {Lake path if known} | {Export frequency} |
| Staging | {Staging table} | {Raw import} |
| ETL | {SP name} | {Transformations applied} |
| Target | {This table} | {Final DWH table} |

---

## 6. Relationships

### 6.1 References To (this object points to)

| Element | Related Object | Description |
|---------|---------------|-------------|
| {Col} | {Schema.Object} | {Business meaning of the relationship} |

### 6.2 Referenced By (other objects point to this)

| Source Object | Source Element | Description |
|--------------|---------------|-------------|
| {Schema.Object} | {Col} | {Business meaning} |

---

## 7. Sample Queries

{Three practical queries that analysts would actually use. Each query should answer a real business question.}

### 7.1 {Business question as title}
```sql
{Practical query answering this question}
```

### 7.2 {Business question as title}
```sql
{Another useful query}
```

### 7.3 {Business question with JOINs resolving IDs to names}
```sql
{A JOIN query that resolves IDs to human-readable names}
```

---

## 8. Atlassian Knowledge Sources

| Source | Type | Key Knowledge Extracted |
|--------|------|------------------------|
| [{Page title or ticket key}]({URL}) | {Confluence / Jira} | {One-sentence summary} |

{If Phase 10 found nothing, write "No Atlassian sources found for this object."}

---

*Generated: {YYYY-MM-DD} | Quality: {N.N}/10 ({star_grade}) | Phases: {N}/{Total}*
*Tiers: {N} T1, {N} T2, {N} T3, {N} T4 [UNVERIFIED], {N} T5 | Elements: {N}/10, Logic: {N}/10, Relationships: {N}/10, Sources: {N}/10*
*Object: {Schema}.{ObjectName} | Type: {Object Type} | Production Source: {Production table path or "N/A"}*
````

---

## Review Sidecar Template

The `.review-needed.md` file MUST follow this structure. This is an **offline review artifact** — the pipeline does NOT stop here. Domain experts review it at their own pace, and corrections trigger a selective review-rerun (Phase 11 + ALTER re-deploy only).

```markdown
# {Schema}.{ObjectName} — Review Needed

> Items flagged for offline domain expert review. The pipeline continues end-to-end; review happens asynchronously. Add corrections to `## Reviewer Corrections` and trigger a review-rerun to regenerate and re-deploy.

## Reviewer Corrections

> **Instructions**: Add corrections below. Each row becomes a Tier 5 (domain-expert confirmed)
> override on the next pipeline rerun. Use `glossary` in the Scope column if the term should
> also be added to `knowledge/glossary.md`.

| Column / Topic | Current (wrong) | Correction | Scope | Reviewer | Date |
|----------------|-----------------|------------|-------|----------|------|

## Tier 4 (UNVERIFIED) Columns

{All Tier 4 items from the Elements table}

## Columns Needing Clarification

{Items where the pipeline found conflicting or ambiguous evidence, regardless of tier}

## Structural Questions

{Questions about the object's design, schema, or ETL that cannot be answered from code alone}

## Tier 5 Re-Review Needed

> Tier 5 (domain expert) overrides whose underlying Tier 1–3 source has materially changed
> since the correction was made. The Tier 5 is still applied, but a domain expert should
> confirm it remains valid given the new upstream definition.

| Column | Tier 5 Correction | Was Based On (old Tier 1–3) | New Tier 1–3 | Change Summary |
|--------|-------------------|----------------------------|--------------|----------------|
```

On **rerun** (full or review-rerun): If a previous `.review-needed.md` exists, read `## Reviewer Corrections` first.
Apply all entries as Tier 5 overrides. In the new sidecar, carry resolved corrections forward
with `[RESOLVED]` prefix in the "Current (wrong)" column. Remove resolved items from
Tier 4 / Clarification sections. **Staleness check**: For each Tier 5, compare the "Current
(wrong)" column (the old Tier 1–3 text the correction was based on) against the freshly
generated Tier 1–3 description. If the Tier 1–3 has materially changed (not just formatting),
populate the `## Tier 5 Re-Review Needed` section and tag the column `(Tier 5 — domain expert, RE-REVIEW)`.
Once a reviewer confirms the Tier 5 still holds, they update the "Current (wrong)" column to
match the new Tier 1–3, clearing the staleness flag on the next run.

**Review-rerun mode**: When triggered by the review-rerun scope (not a full pipeline run), Phase 11
uses the previously gathered phase data (Phases 1–10 are NOT re-run). Only the documentation
generation and ALTER deployment are refreshed. This makes review-rerun fast (~2-3 min per table).

## Post-Generation

1. **Update `_semantic_index.md`**: Map business concepts from Sections 2 and 3 to this object
2. **Generate `.lineage.md`**: Run Phase 13 Steps 2-6 (SP code lineage analysis) to produce the column lineage map. This file captures production-to-DWH column mappings from ETL SPs. See `13-production-lineage-mapping.mdc` for the file format and column lineage template

> **NOTE**: UC Object Resolution, ALTER Script Generation, Table Tags, PII Tags, Downstream Propagation, ALTER Execution, and Deployment Reports have been moved to **Phase 11W** (`11w-write-objects.mdc`). They are executed via the `write-objects-dwh` command, not during wiki build.

## Quality Score Calculation

Calculate a quality score (0-10 scale) from four dimensions. Include in the wiki doc footer. The score measures **documentation completeness**, not data quality — it tells you how thorough the pipeline's analysis was for this object.

### Dimensions

| Dimension | Weight | How to Score |
|-----------|--------|-------------|
| **Elements** | 40% | `(1 - (Tier4_count / total_elements)) × 10`. Perfect = 0 Tier 4 [UNVERIFIED] elements. Each Tier 4 reduces the score. |
| **Business Logic** | 25% | Based on Section 2 richness: 0 concepts = 2/10, 1 concept = 5/10, 2 concepts = 7/10, 3+ concepts with diagrams = 10/10. |
| **Relationships** | 20% | Based on Section 6 completeness: No relationships = 2/10, only "References To" = 5/10, both directions populated = 8/10, both directions + business descriptions = 10/10. |
| **Sources** | 15% | Based on evidence breadth: Only DDL = 2/10, +live data = 4/10, +Synapse SP logic = 6/10, +Atlassian = 7/10, +upstream wiki = 9/10, +upstream wiki AND Atlassian = 10/10. |

### Formula

```
Overall = (Elements × 0.40) + (Logic × 0.25) + (Relationships × 0.20) + (Sources × 0.15)
```

Round to one decimal place.

### Star Grade (1-5 Scale)

Map the 0-10 score to a compact 1-5 star grade for the ALTER script tag. This is the **only** quality signal that goes into UC — the full breakdown stays in the wiki.

| Score | Stars | Visual | Tag Value |
|-------|-------|--------|-----------|
| 9.0-10.0 | 5 | ★★★★★ | `5` |
| 7.0-8.9 | 4 | ★★★★☆ | `4` |
| 5.0-6.9 | 3 | ★★★☆☆ | `3` |
| 3.0-4.9 | 2 | ★★☆☆☆ | `2` |
| 0.0-2.9 | 1 | ★☆☆☆☆ | `1` |

The star visual goes into the wiki footer (e.g., `Quality: 8.2/10 (★★★★☆)`). The numeric grade goes into the ALTER script tag.

### Quality Thresholds

| Grade | Rating | Action |
|-------|--------|--------|
| ★★★★★ (5) | Excellent | No action needed |
| ★★★★☆ (4) | Good | Acceptable for most objects |
| ★★★☆☆ (3) | Fair | Review Tier 4 elements, consider re-running with more grep against upstream wikis |
| ★★☆☆☆ (2) | Poor | Investigate why phases produced thin results; may need manual enrichment |
| ★☆☆☆☆ (1) | Insufficient | Pipeline likely failed or was run with missing prerequisites |

---

## CRITICAL: Batch Processing & Subagent Quality Enforcement

When documentation is generated in bulk (multiple objects per subagent), these rules are **NON-NEGOTIABLE**. Every violation produces a broken doc that must be regenerated from scratch. Our pipeline produces 3 files per object, making quality degradation under batch pressure a significant risk.

### Rule B1: Maximum Batch Sizes Per Subagent

| Object Type | Max Per Subagent | Rationale |
|-------------|-----------------|-----------|
| **Tables** | 3 | Full pipeline with MCP queries, Grep, upstream wiki search, and 3 output files (wiki + sidecar + lineage) |
| **Views** | 5 | Must inherit from base table docs, trace JOINs, 3 output files |

**NEVER exceed these limits.** A subagent processing 10 tables WILL produce skeleton wikis with missing sections and inadequate lineage maps.

### Rule B2: Every Doc MUST Have All 8 Section Headers

After writing each wiki doc, the subagent MUST verify that **ALL** of these markdown headers exist as separate lines in the file:

```
## 1. Business Meaning
## 2. Business Logic
## 3. Query Advisory
## 4. Elements
## 5. Lineage
## 6. Relationships
## 7. Sample Queries
## 8. Atlassian Knowledge Sources
```

**FORBIDDEN patterns** -- these indicate a subagent cut corners:
- `## 2–8.` or `## 2-8.` (collapsed all sections into one)
- `## 2. Business Logic, Query Advisory, Elements` (merged section titles)
- Missing `## 4. Elements` entirely
- Missing `## 3. Query Advisory` entirely (this is our key differentiator)
- Missing `## 5. Lineage` entirely

If ANY required header is missing, the doc is **INVALID** and must be rewritten before the subagent reports success.

### Rule B3: Minimum Line Counts (Wiki File)

| Object Type | Minimum Lines | Rationale |
|-------------|--------------|-----------|
| **Tables** | 100 | 8 sections + Elements table with all columns + Query Advisory + Lineage |
| **Views** | 80 | 8 sections + all output columns + Query Advisory |

A wiki file below the minimum is **automatically suspect**. The subagent must check line count before reporting completion and re-expand any section that was too brief.

### Rule B4: Elements Table Must List Every Element Individually

The `## 4. Elements` section MUST contain a markdown table with one row per element:
- **Tables**: One row per column (a 40-column table = 40 rows in Elements)
- **Views**: One row per output column
- **Functions**: One row per parameter + one row per return column
- **Procedures**: One row per parameter

**FORBIDDEN**: Grouping multiple elements into one row, listing elements in prose instead of a table, or writing "See DDL for full column list."

This reinforces Strict Rule 8 but is restated here because batch execution is where violations occur most.

### Rule B5: Subagent Prompts Must Reference the Spec

The orchestrator (parent agent) MUST include this instruction in every subagent prompt:

> "Read and follow the template in `.cursor/rules/dwh-semantic-doc/11-generate-documentation.mdc` EXACTLY. Every doc must have all 8 section headers, an Elements table with one row per element, tier tags on every description, and all 3 output files (wiki + sidecar + lineage). Do NOT abbreviate, collapse, or skip sections."

**FORBIDDEN orchestrator prompt patterns**:
- "write concise MD (50-100 lines)" -- this actively instructs the subagent to violate the template
- "write a brief doc" -- same problem
- "sections 1-8 + footer" -- too vague; subagent will shortcut
- "just generate the lineage file" -- the lineage file requires the full wiki context to be meaningful

### Rule B6: Post-Write Validation Gate

After writing each object's output files, the subagent MUST run this validation **before moving to the next object**:

1. **Section check**: Confirm all 8 `## N.` headers exist in the wiki file
2. **Elements check**: Confirm `## 4. Elements` contains a markdown table with `|` delimiters
3. **Line count check**: Confirm the wiki file meets the minimum for its object type (Rule B3)
4. **Tier tag check**: Confirm every element description includes a `(Tier N` tag or `[UNVERIFIED]` prefix
5. **Quality score check**: Confirm the footer contains a `Quality:` score and star grade
6. **Output file check**: Confirm all required output files exist:
   - All object types: wiki `.md` + sidecar `.review-needed.md` + `.lineage.md`

If any check fails, the subagent MUST fix the doc immediately -- not move on and report partial success.

### Rule B7: Orchestrator Must Verify Subagent Output

After a subagent completes, the orchestrator MUST spot-check at least 1 random doc from the batch:
1. Read the wiki file
2. Verify it has all 8 section headers
3. Verify the Elements table has individual rows with tier tags
4. Verify the `.lineage.md` file exists with column lineage mapping
5. If the spot-check fails, assume ALL docs from that subagent are suspect and re-dispatch

---

## Shared Modules (Committed, Not Generated)

These files live in `knowledge/synapse/Wiki/` and are **committed to the repo** — unlike `_deploy.py` which is generated and deleted per run:

| File | Purpose |
|------|---------|
| `_deep_propagate_lib.py` | Shared library for deep lineage discovery and batched execution. Imported by per-table `{Object}_deep_propagate.py` scripts. |
| `_build_dependency_order.py` | Builds bottom-up Synapse table processing order from SSDT dependencies + SP write target parsing. Re-run when DataPlatform repo is updated. |
| `_dependency_order.json` | Generated output: 2,134 Synapse objects with dependency depth. Used by the pipeline to determine processing order. |
| `_broadcast_propagate.py` | Standalone script for propagating blacklisted ETL column descriptions across all UC instances. |

## Completion — THREE FILES REQUIRED

Phase 11 is NOT complete until ALL three files exist:

1. **Wiki file**: `knowledge/synapse/Wiki/{Schema}/{ObjectType}/{ObjectName}.md`
2. **Review sidecar**: `knowledge/synapse/Wiki/{Schema}/{ObjectType}/{ObjectName}.review-needed.md`
3. **Lineage map**: `knowledge/synapse/Wiki/{Schema}/{ObjectType}/{ObjectName}.lineage.md`

The wiki file is the primary knowledge artifact — it contains the structured documentation that feeds ALTER scripts during the deployment phase (Phase 11W via `write-objects-dwh`). The review sidecar may only be omitted if there are genuinely ZERO Tier 4 columns, ZERO clarification questions, and ZERO structural questions — which is almost never the case. The lineage map is produced from Phase 13 Steps 2-6.

> **ALTER scripts, downstream propagation, deployment reports, and UC execution** are NOT part of Phase 11. They are handled by Phase 11W (`11w-write-objects.mdc`) via the `write-objects-dwh` command.

Mark `[x] Phase 11: Documentation Generated` in the checklist after all three files are written.
