---
description: Create a new Jira issue in the DA project (Epic, Story, Sub-task, Sub-Dev, Bug)
---

# Create Jira Issue

Read the skill file at `C:\Users\guyman\.cursor\skills\jira-da\SKILL.md`, then follow **Flow 1: Create Issue**.

Start by asking the user what type of issue to create using AskQuestion. Apply all defaults documented in the skill. Walk through the conversational flow, preview the issue, and create it via the Atlassian MCP.

If the user provided context in their message (e.g. "create a story for migrating X"), use that to pre-fill the summary and description instead of asking.
