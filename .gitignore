.env
.venv
__pycache__
node_modules
.cache
fetched_*.md
fetched_*.json
jira-creds.json
.mcp.json
_upstream_wiki_routing.json

# regen-harness writes .bak siblings beside live wikis when overwriting
*.bak
.tmp_*.ps1
.tmp_*.txt


# Auto-generated backup files from UC rewriter
*.uc-rewrite.bak

# Eval-harness MCP-only override rule — placed at run start, removed at run end
.cursor/rules/eval-mcp-only.mdc

# Local workspace backup snapshots (created by skill-edit safety nets, never commit)
.local_workspace_backup/

# Microsoft Office lock files (created while a doc is open in Word/Excel/PowerPoint)
~$*

# Audit harness outputs — large, regenerable, never committed
audits/_tier1_audit_cache/
audits/_sql_grounded_audit_cache/
audits/_llm_column_audit_*/
audits/_apply_report_*
audits/_sql_grounded_phase*/
audits/regen-sample/

# Per-skill local stage (sync_to_databricks.py target)
# (workspace destination, never commit local mirror)

