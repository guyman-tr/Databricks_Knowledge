## Review: eMoney_Tribe.CardsSnapshots_CardSnapshot-140457

### Per-Dimension Scores

**Dimension 1 — Tier Accuracy: 10/10**
All 67 columns tagged Tier 3. No upstream wiki exists (confirmed by bundle's `_no_upstream_found.txt`). Spot-checked @Created, CardStatusCode, CountryCode, SynapseUpdateDate, partition_date — all correctly Tier 3. SynapseUpdateDate and partition_date are ETL-generated (could be Tier 2), but with no upstream wiki the writer's Tier 3 tag with "grounded in DDL" justification is defensible. No mismatches.

**Dimension 2 — Upstream Fidelity: 7/10**
Neutral score — no upstream wiki was available in the bundle, so no Tier 1 inheritance was possible. The writer correctly avoided fabricating Tier 1 tags. No missed inheritance.

**Dimension 3 — Completeness: 9/10 → score 8**
All 8 sections present. 67 elements match 67 DDL columns. All element rows have 5 cells with Tier annotations. Property table complete. ETL diagram uses real names. Footer has tier breakdown. Row count + date range in Section 1. CardStatusCode lists 8 key=value pairs inline. Review-needed sidecar does NOT have `## 4. Elements`. One miss: no dictionary-style inline listing for DefaultCardCurrency in the elements table (only 2 values: GBP/EUR — listed in Section 2.5 but the element description says "GBP=UK cards (78%), EUR=EU cards (22%)" which actually does cover it). On reflection, 10/10 checklist items pass. Score: 10.

**Dimension 4 — Business Meaning: 9/10**
Section 1 is specific and actionable: names the domain (eToro Money debit cards), row grain (one row per card per snapshot date), row count (86.2M), date range (2021-09-05 to 2026-04-26), ETL consumer (SP_eMoney_Reconciliation_ETLs → ETL_CardSnapshot), refresh pattern (daily append), two card programs with volume splits. PII warning included. Strong.

**Dimension 5 — Data Evidence: 7/10**
Row count and date range present. CardStatusCode distribution with percentages. Currency/program splits. PII masking observations. However, there is no explicit Phase Gate Checklist section — the footer just says "Phases: 11/14" without identifying which 3 were skipped. The specific distributions suggest live data was queried, so I'll trust the evidence but note the missing formalism.

**Dimension 6 — Shape Fidelity: 8/10**
Numbered sections 1–8, tier legend in Section 4, three real SQL samples in Section 7, footer with quality score and tier breakdown. Minor deviations: no explicit Phase Gate Checklist section, Section 8 is essentially a placeholder ("No Atlassian sources searched").

### T1 Fidelity Table

No Tier 1 columns exist — the upstream bundle contained no wiki. This is correct and expected.

| Column | Upstream Quote | Wiki Quote | Match | Loss |
|--------|---------------|------------|-------|------|
| *(none — 0 Tier 1 columns)* | — | — | — | — |

### Top 5 Issues

1. **Medium — Address (element #36)**: Description is self-contradictory: "not passed through to ETL_CardSnapshot by SP_eMoney_Reconciliation_ETLs (selected into temp table but present in final output)." "Not passed through" and "present in final output" cannot both be true. Writer should clarify which is accurate.

2. **Low — SynapseUpdateDate / partition_date tier**: Both are ETL-generated (GETDATE() / derived from snapshot date). These fit the Tier 2 definition ("ETL-computed") better than Tier 3 ("no source traceable"). However, given the entire table has no upstream wiki, this is a minor classification edge case rather than a substantive error.

3. **Low — Section 8 placeholder**: "No Atlassian sources searched (regen harness mode)" provides zero value. Either populate with relevant Confluence/Jira links or state "None identified" cleanly.

4. **Low — Missing Phase Gate Checklist**: Footer claims "Phases: 11/14" but no section lists which phases were completed or skipped. This makes it impossible to verify data evidence claims.

5. **Low — @Created vs Created ambiguity**: Element #67 (Created) says "May represent the source system creation time vs pipeline creation time" — the word "may" is a hedge. The review-needed sidecar correctly flags this, but the wiki description should commit to "semantics unknown" rather than speculating.

### Regeneration Feedback

1. Fix the contradictory description for Address (#36) — determine from SP code whether it IS or IS NOT passed through to ETL_CardSnapshot.
2. Consider re-tagging SynapseUpdateDate and partition_date as Tier 2 (ETL-computed) since they are generated by the pipeline, not passthrough from FiatDwhDB.
3. Add an explicit Phase Gate Checklist section listing completed vs. skipped phases.
4. Remove speculative hedging from Created (#67) — state "semantics vs @Created unclear" directly.
5. Clean up Section 8 — either find relevant Atlassian links or reduce to a single-line "None identified."

### Weighted Score Calculation

```
weighted = 0.25×10 + 0.20×7 + 0.20×10 + 0.15×9 + 0.10×7 + 0.10×8
         = 2.50 + 1.40 + 2.00 + 1.35 + 0.70 + 0.80
         = 8.75
```

**Verdict: PASS**

<JUDGE_VERDICT>
{
  "schema": "eMoney_Tribe",
  "object": "CardsSnapshots_CardSnapshot-140457",
  "weighted_score": 8.75,
  "verdict": "PASS",
  "dimension_scores": {
    "tier_accuracy": 10,
    "upstream_fidelity": 7,
    "completeness": 10,
    "business_meaning": 9,
    "data_evidence": 7,
    "shape_fidelity": 8
  },
  "t1_fidelity_table": [],
  "issues": [
    {
      "severity": "medium",
      "column_or_section": "Address (#36)",
      "problem": "Self-contradictory description: says 'not passed through to ETL_CardSnapshot' but then '(selected into temp table but present in final output)'. These statements conflict — writer must verify from SP code whether Address appears in the final ETL_CardSnapshot output."
    },
    {
      "severity": "low",
      "column_or_section": "SynapseUpdateDate (#65), partition_date (#66)",
      "problem": "Tagged Tier 3 but both are ETL-generated (GETDATE() and derived date respectively). These fit Tier 2 (ETL-computed) better than Tier 3 (no source traceable). Minor classification edge case given no upstream wiki exists."
    },
    {
      "severity": "low",
      "column_or_section": "Section 8",
      "problem": "Placeholder text 'No Atlassian sources searched (regen harness mode)' provides no value. Should either contain relevant links or a clean 'None identified' statement."
    },
    {
      "severity": "low",
      "column_or_section": "Footer / Phase Gate",
      "problem": "Footer claims 'Phases: 11/14' but no Phase Gate Checklist section exists to identify which 3 phases were skipped. Cannot verify data evidence claims without this."
    },
    {
      "severity": "low",
      "column_or_section": "Created (#67)",
      "problem": "Description hedges with 'May represent the source system creation time vs pipeline creation time'. Should commit to 'semantics vs @Created unclear, both carry identical values in sampled data' rather than speculating."
    }
  ],
  "regeneration_feedback": "Minor improvements only (wiki passes): (1) Fix contradictory Address (#36) description — verify from SP code whether it reaches ETL_CardSnapshot final output. (2) Consider re-tagging SynapseUpdateDate and partition_date as Tier 2 (ETL-computed). (3) Add explicit Phase Gate Checklist section listing completed vs skipped phases. (4) Remove speculative hedging from Created (#67). (5) Clean up Section 8 placeholder.",
  "stats_check": {
    "table_level_stats_in_descriptions": ["CardStatusCode distributions (A=41%, E=29%, N=26%, S=2.6%, L=1%)", "DefaultCardCurrency split (GBP=78%, EUR=22%)", "CountryCode distribution (826=78%)"],
    "skipped_phases": ["Cannot determine — no Phase Gate Checklist section exists, footer only says 11/14"]
  }
}
</JUDGE_VERDICT>
