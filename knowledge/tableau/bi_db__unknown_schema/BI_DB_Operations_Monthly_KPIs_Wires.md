# Tableau Usage — BI_DB_Operations_Monthly_KPIs_Wires

> Generated by `tools/tableau/extract_table_metadata.py`. Do not edit by hand.

## Identity

- Full name: `BI_DB_Operations_Monthly_KPIs_Wires`
- Database: `bi_db` (sqlserver)
- Schema: ``
- Connection type: `sqlserver`
- Tableau metadata id: `06216c95-7314-58a3-f5e9-ae1c1583d438`

## Summary

- Custom SQL queries: **10**
- Downstream workbooks: **8**
- Downstream calculated fields: **323**

## Custom SQL queries referencing this table

### 1. Custom SQL Query

- Tableau id: `194ac8e5-3f53-5fe7-1ee7-e3c908f8ddba`

```sql
SELECT  * FROM BI_DB_Operations_Monthly_KPIs_Wires omkw
```

### 2. Custom SQL Query

- Tableau id: `87950b24-7f56-6238-34aa-f3c60b587233`

```sql
SELECT
	c.*
   ,dc1.Name AS Country,bd.IsFTD,
case when Regulation in ('ASIC & GAML') THEN 'ASIC' 
when Regulation in ('eToroUS', 'FinCEN+FINRA') THEN 'FinCEN' ELSE Regulation end as Regulation1
FROM
BI_DB_Operations_Monthly_KPIs_Wires  c
	JOIN DWH..Dim_Customer dc
		ON c.CID = dc.RealCID
	JOIN DWH..Dim_Country dc1
		ON dc.CountryID = dc1.CountryID
join DWH.dbo.Fact_BillingDeposit bd on bd.DepositID=c.DepositID
where FundingTypeName in ('WireTransfer')
```

### 3. Custom SQL Query

- Tableau id: `f80be0de-7915-fe1f-d401-2da79badcc62`

```sql
SELECT  * FROM BI_DB_Operations_Monthly_KPIs_Wires omkw
```

### 4. Custom SQL Query

- Tableau id: `0229941f-2d59-2ef6-b25f-2689fa0c34a2`

```sql
select * from [dbo].[BI_DB_Operations_Monthly_KPIs_Wires]
where ModificationDate>=dateadd(month,-5,dateadd(month,datediff(month,0,getdate()),0))
```

### 5. Custom SQL Query

- Tableau id: `0cb0bbf8-e63b-2ad0-e9c5-ac171cc97888`

```sql
SELECT
	c.*
   ,dc1.Name AS Country
FROM
BI_DB_dbo.BI_DB_Operations_Monthly_KPIs_Wires  c
	JOIN DWH_dbo.Dim_Customer dc
		ON c.CID = dc.RealCID
	JOIN DWH_dbo.Dim_Country dc1
		ON dc.CountryID = dc1.CountryID
where FundingTypeID=2
```

### 6. Custom SQL Query

- Tableau id: `7479aa92-cbe7-c2b1-3d5e-fa9ec036ac9f`

```sql
SELECT
	c.*
   ,dc1.Name AS Country
FROM
BI_DB_dbo.BI_DB_Operations_Monthly_KPIs_Wires  c
	JOIN DWH_dbo.Dim_Customer dc
		ON c.CID = dc.RealCID
	JOIN DWH_dbo.Dim_Country dc1
		ON dc.CountryID = dc1.CountryID
where FundingTypeID=2
```

### 7. Custom SQL Query

- Tableau id: `96738065-7423-cf58-ebbf-dae395f2c2ca`

```sql
SELECT
	c.*
   ,dc1.Name AS Country
FROM
BI_DB_dbo.BI_DB_Operations_Monthly_KPIs_Wires  c
	JOIN DWH_dbo.Dim_Customer dc
		ON c.CID = dc.RealCID
	JOIN DWH_dbo.Dim_Country dc1
		ON dc.CountryID = dc1.CountryID
where FundingTypeID=2
```

### 8. Custom SQL Query

- Tableau id: `b5f943ee-447e-21fa-b391-0e2a979c31fd`

```sql
select * from [dbo].[BI_DB_Operations_Monthly_KPIs_Wires]
where ModificationDate>=dateadd(month,-5,dateadd(month,datediff(month,0,getdate()),0))
```

### 9. Custom SQL Query

- Tableau id: `d2c357d9-2f86-fcfc-437a-16b7f59b3ddf`

```sql
SELECT
	c.*
   ,dc1.Name AS Country
   ,r.Name as DesignatedRegulation
   ,EOMONTH(c.ModificationDate) AS ModificationDateEOMonth
FROM
BI_DB_dbo.BI_DB_Operations_Monthly_KPIs_Wires  c
	JOIN DWH_dbo.Dim_Customer dc
		ON c.CID = dc.RealCID
	JOIN DWH_dbo.Dim_Country dc1
		ON dc.CountryID = dc1.CountryID
		JOIN DWH_dbo.Dim_Regulation r on r.ID = dc.DesignatedRegulationID
where FundingTypeID=2
```

### 10. Custom SQL Query

- Tableau id: `f3775f6d-f3be-be42-b520-7ff5c0dd69c5`

```sql
SELECT
	c.*
   ,dc1.Name AS Country
   ,r.Name as DesignatedRegulation
   ,EOMONTH(c.ModificationDate) AS ModificationDateEOMonth
FROM
BI_DB_dbo.BI_DB_Operations_Monthly_KPIs_Wires  c
	JOIN DWH_dbo.Dim_Customer dc
		ON c.CID = dc.RealCID
	JOIN DWH_dbo.Dim_Country dc1
		ON dc.CountryID = dc1.CountryID
		JOIN DWH_dbo.Dim_Regulation r on r.ID = dc.DesignatedRegulationID
where FundingTypeID=2 and c.ModificationDate >= '2025-01-01'
```

## Downstream workbooks

_Attribution: 3 via direct table-drag, 8 via custom SQL (workbooks can use both paths)._

| # | Workbook | Project | Owner | Via | Last updated | URL |
|---|---|---|---|---|---|---|
| 1 | OPS Weekly KPIs China | 14-02-2024 | Maor Hacun | custom_sql,direct | 2024-02-14T08:17:11Z | [open](https://reports.etorocorp.com/#/workbooks/OPSWeeklyKPIsChina) |
| 2 | OPS Weekly KPIs China full 6 months (Delete when finished) | 14-02-2024 | Guy Manova | custom_sql,direct | 2024-02-14T08:17:11Z | [open](https://reports.etorocorp.com/#/workbooks/OPSWeeklyKPIsChinafull6monthsDeletewhenfinished) |
| 3 | Funnel (Onboarding to MIMO) | 8. Various ad hoc reports | Georgios Kyriakou | custom_sql | 2025-09-08T10:11:56Z | [open](https://reports.etorocorp.com/#/workbooks/Funnel-checkifok) |
| 4 | OPS key measurements and targets | ARCHIVED | Pavlina Masoura | custom_sql,direct | 2023-09-12T11:08:49Z | [open](https://reports.etorocorp.com/#/workbooks/OPSkeymeasurementsandtargets) |
| 5 | Ticket Analysis -  All Tickets | ARCHIVED | Pavlina Masoura | custom_sql | 2023-07-10T11:12:55Z | [open](https://reports.etorocorp.com/#/workbooks/TicketAnalysis-All) |
| 6 | Ticket Analysis -  Assigned to OPS | ARCHIVED | Pavlina Masoura | custom_sql | 2023-07-10T11:12:23Z | [open](https://reports.etorocorp.com/#/workbooks/TicketAnalysis-AssignedtoOPS) |
| 7 | Funnel | Operations -Archived | Pavlina Masoura | custom_sql | 2025-07-20T08:10:25Z | [open](https://reports.etorocorp.com/#/workbooks/Funnel) |
| 8 | OPS Monthly and Weekly KPIs | Operations -Archived | Pavlina Masoura | custom_sql | 2026-05-31T10:20:53Z | [open](https://reports.etorocorp.com/#/workbooks/OPSWeeklyKPIs) |

### View URLs (per workbook)

- **OPS Weekly KPIs China** (14-02-2024)
  - [Ops Weekly KPIs (new)](https://reports.etorocorp.com/#/views/OPSWeeklyKPIsChina/OpsWeeklyKPIsnew)
  - [Ops Monthly KPI (new)](https://reports.etorocorp.com/#/views/OPSWeeklyKPIsChina/OpsMonthlyKPInew)
  - [Ops Weekly KPIs w/Regs (2)](https://reports.etorocorp.com/#/views/OPSWeeklyKPIsChina/OpsWeeklyKPIswRegs2)
  - [Ops Monthly KPIs w/Regs](https://reports.etorocorp.com/#/views/OPSWeeklyKPIsChina/OpsMonthlyKPIswRegs)
  - [Raw Data Verifications](https://reports.etorocorp.com/#/views/OPSWeeklyKPIsChina/RawDataVerifications)
  - [Raw Data Cashouts](https://reports.etorocorp.com/#/views/OPSWeeklyKPIsChina/RawDataCashouts)
  - [Raw Data Wires](https://reports.etorocorp.com/#/views/OPSWeeklyKPIsChina/RawDataWires)
- **OPS Weekly KPIs China full 6 months (Delete when finished)** (14-02-2024)
  - [Ops Weekly KPIs (new)](https://reports.etorocorp.com/#/views/OPSWeeklyKPIsChinafull6monthsDeletewhenfinished/OpsWeeklyKPIsnew)
  - [Ops Monthly KPI (new)](https://reports.etorocorp.com/#/views/OPSWeeklyKPIsChinafull6monthsDeletewhenfinished/OpsMonthlyKPInew)
  - [Ops Weekly KPIs w/Regs (2)](https://reports.etorocorp.com/#/views/OPSWeeklyKPIsChinafull6monthsDeletewhenfinished/OpsWeeklyKPIswRegs2)
  - [Ops Monthly KPIs w/Regs](https://reports.etorocorp.com/#/views/OPSWeeklyKPIsChinafull6monthsDeletewhenfinished/OpsMonthlyKPIswRegs)
  - [Raw Data Verifications](https://reports.etorocorp.com/#/views/OPSWeeklyKPIsChinafull6monthsDeletewhenfinished/RawDataVerifications)
  - [Raw Data Cashouts](https://reports.etorocorp.com/#/views/OPSWeeklyKPIsChinafull6monthsDeletewhenfinished/RawDataCashouts)
  - [Raw Data Wires](https://reports.etorocorp.com/#/views/OPSWeeklyKPIsChinafull6monthsDeletewhenfinished/RawDataWires)
- **Funnel (Onboarding to MIMO)** (8. Various ad hoc reports)
  - [Funnel  ](https://reports.etorocorp.com/#/views/Funnel-checkifok/Funnel)
  - [Per Country Metrics](https://reports.etorocorp.com/#/views/Funnel-checkifok/PerCountryMetrics)
  - [Country timelines ](https://reports.etorocorp.com/#/views/Funnel-checkifok/Countrytimelines)
- **OPS key measurements and targets** (ARCHIVED)
  - [MONEY OUT - OPS Key Measurements and Targets](https://reports.etorocorp.com/#/views/OPSkeymeasurementsandtargets/MONEYOUT-OPSKeyMeasurementsandTargets)
  - [RISK - OPS Key Measurements and Targets](https://reports.etorocorp.com/#/views/OPSkeymeasurementsandtargets/RISK-OPSKeyMeasurementsandTargets)
  - [AFFILIATES - OPS Key Measurements and Targets](https://reports.etorocorp.com/#/views/OPSkeymeasurementsandtargets/AFFILIATES-OPSKeyMeasurementsandTargets)
  - [KYC - OPS Key Measurements and Targets](https://reports.etorocorp.com/#/views/OPSkeymeasurementsandtargets/KYC-OPSKeyMeasurementsandTargets)
  - [MONEY IN - OPS Key Measurements and Targets](https://reports.etorocorp.com/#/views/OPSkeymeasurementsandtargets/MONEYIN-OPSKeyMeasurementsandTargets)
- **Ticket Analysis -  All Tickets** (ARCHIVED)
  - [Avg. No of Touches - Per SubType](https://reports.etorocorp.com/#/views/TicketAnalysis-All/Avg_NoofTouches-PerSubType)
  - [Avg. CSAT Score - Per SubType](https://reports.etorocorp.com/#/views/TicketAnalysis-All/Avg_CSATScore-PerSubType)
  - [Avg. Days To Resolve - Per SubType](https://reports.etorocorp.com/#/views/TicketAnalysis-All/Avg_DaysToResolve-PerSubType)
  - [Tickets KPIs per Type](https://reports.etorocorp.com/#/views/TicketAnalysis-All/TicketsKPIsperType)
  - [Weekly Avg NoOfTouches and CSAT per ActionType](https://reports.etorocorp.com/#/views/TicketAnalysis-All/WeeklyAvgNoOfTouchesandCSATperActionType)
  - [Weekly Tickets Analysis -Opened By](https://reports.etorocorp.com/#/views/TicketAnalysis-All/WeeklyTicketsAnalysis-OpenedBy)
  - [Tickets Closed vs Open](https://reports.etorocorp.com/#/views/TicketAnalysis-All/TicketsClosedvsOpen)
  - [Avg CSAT](https://reports.etorocorp.com/#/views/TicketAnalysis-All/AvgCSAT)
  - [Monthly %Variance CSAT,Touches,Days](https://reports.etorocorp.com/#/views/TicketAnalysis-All/MonthlyVarianceCSATTouchesDays)
- **Ticket Analysis -  Assigned to OPS** (ARCHIVED)
  - [Tickets Opened Per SubType - Assigned to OPS](https://reports.etorocorp.com/#/views/TicketAnalysis-AssignedtoOPS/TicketsOpenedPerSubType-AssignedtoOPS)
  - [Monthly tickets with Metrics -Assigned to OPS](https://reports.etorocorp.com/#/views/TicketAnalysis-AssignedtoOPS/MonthlyticketswithMetrics-AssignedtoOPS)
  - [Avg. No of Touches Per Subtype - Assigned to OPS](https://reports.etorocorp.com/#/views/TicketAnalysis-AssignedtoOPS/Avg_NoofTouchesPerSubtype-AssignedtoOPS)
  - [Avg.csat Per SubType - Assigned to OPS](https://reports.etorocorp.com/#/views/TicketAnalysis-AssignedtoOPS/Avg_csatPerSubType-AssignedtoOPS)
  - [Avg. No of Touches % change - Assigned to OPS](https://reports.etorocorp.com/#/views/TicketAnalysis-AssignedtoOPS/Avg_NoofToucheschange-AssignedtoOPS)
  - [Avg. CSAT  % change - Assigned to OPS](https://reports.etorocorp.com/#/views/TicketAnalysis-AssignedtoOPS/Avg_CSATchange-AssignedtoOPS)
  - [Avg. DaysToResolve % change - Assigned to OPS](https://reports.etorocorp.com/#/views/TicketAnalysis-AssignedtoOPS/Avg_DaysToResolvechange-AssignedtoOPS)
  - [Tickets Closed vs Open - Assigned to OPS](https://reports.etorocorp.com/#/views/TicketAnalysis-AssignedtoOPS/TicketsClosedvsOpen-AssignedtoOPS)
  - [SLA Analysis - Assigned to OPS](https://reports.etorocorp.com/#/views/TicketAnalysis-AssignedtoOPS/SLAAnalysis-AssignedtoOPS)
  - [Tickets KPIs Assigned to OPS](https://reports.etorocorp.com/#/views/TicketAnalysis-AssignedtoOPS/TicketsKPIsAssignedtoOPS)
  - [Weekly Tickets ](https://reports.etorocorp.com/#/views/TicketAnalysis-AssignedtoOPS/WeeklyTickets)
  - [Weekly Tickets New](https://reports.etorocorp.com/#/views/TicketAnalysis-AssignedtoOPS/WeeklyTicketsNew)
- **Funnel** (Operations -Archived)
  - [Funnel  ](https://reports.etorocorp.com/#/views/Funnel/Funnel_1)
- **OPS Monthly and Weekly KPIs** (Operations -Archived)
  - [OPS Weekly KPIs](https://reports.etorocorp.com/#/views/OPSWeeklyKPIs/OPSWeeklyKPIs)
  - [OPS Monthly KPIs](https://reports.etorocorp.com/#/views/OPSWeeklyKPIs/OPSMonthlyKPIs)
  - [OPS Weekly KPIs w/Regs](https://reports.etorocorp.com/#/views/OPSWeeklyKPIs/OPSWeeklyKPIswRegs)
  - [OPS Monthly KPIs w/Regs](https://reports.etorocorp.com/#/views/OPSWeeklyKPIs/OPSMonthlyKPIswRegs)
  - [Raw Data Verifications](https://reports.etorocorp.com/#/views/OPSWeeklyKPIs/RawDataVerifications)
  - [Raw Data Cashouts](https://reports.etorocorp.com/#/views/OPSWeeklyKPIs/RawDataCashouts)
  - [Raw Data Wires](https://reports.etorocorp.com/#/views/OPSWeeklyKPIs/RawDataWires)

## Downstream calculated fields (in embedded datasources)

### Workbook: OPS key measurements and targets

#### Datasource: Affiliates KPIs

- **CO - % Fundings Processed within SLA24**

  ```
  [CO - WID Processed Within SLA24]/[CO - Total WID Requests]
  ```

- **CO - % Fundings Processed within SLA48**

  ```
  sum([CO - Fundings Processed Within SLA48])/sum([CO - Total Fundings Processed])
  ```

- **CO - % Fundings Processed within SLA5days**

  ```
  [CO - WID Processed Within SLA5days]/[CO - Total WID Requests]
  ```

- **CO - % WID Processed within SLA24**

  ```
  countd(if [WD_ID_SLA] = "OverallSLA"
  then [WithdrawID] else 0 end)
  /COUNTD([WithdrawID])
  ```

- **CO - % WID Processed within SLA48**

  ```
  sum(if [WD_ID_SLA48] = "OverallSLA48"
  then 1 else 0 end)
  /count([WithdrawID])
  ```

- **CO - % WID Processed within SLA5days**

  ```
  sum(if [WD_ID_SLA5days] = "OverallSLA5days"
  then 1 else 0 end)
  /count([WithdrawID])
  ```

- **CO - Fundings Processed Within SLA24**

  ```
  if [CashoutStatusID] = 3 
      and [SLA] = 1
  then [Number of Records] END
  ```

- **CO - Fundings Processed Within SLA48**

  ```
  if [CashoutStatusID] = 3 
      and [SLA48] = 1
  then [Number of Records] END
  ```

- **CO - Fundings Processed Within SLA5days**

  ```
  if [CashoutStatusID] = 3 
      and [SLA5days]=1
  then [Number of Records] END
  ```

- **CO - Total Funding Actions**

  ```
  [Number of Records]
  ```

- **CO - Total Fundings Processed**

  ```
  if [CashoutStatusID] = 3 then [Number of Records] END
  ```

- **CO - Total WID Processed**

  ```
  COUNTD(
  if [Approved] = 1 then [WithdrawID] 
  END)
  ```

- **CO - Total WID Requests**

  ```
  COUNTD([WithdrawID])
  ```

- **CO - WID Processed Within SLA24**

  ```
  COUNTD(
  if [CashoutStatusID] = 3 
      and [WD_ID_SLA] = "OverallSLA"
  then [WithdrawID] END
  )
  ```

- **CO - WID Processed Within SLA48**

  ```
  countd(
  if [CashoutStatusID] = 3 
      and [WD_ID_SLA48] = "OverallSLA48"
  then [WithdrawID] END
  )
  ```

- **CO - WID Processed Within SLA5days**

  ```
  countd(
  if [CashoutStatusID] = 3 
      and [WD_ID_SLA5days] = "OverallSLA5days"
  then [WithdrawID] END
  )
  ```

- **difference from target affiliate co sla**

  ```
  [CO - % WID Processed within SLA24]-[target affiliate co sla]
  ```

- **filter - last 6 months (modDate)**

  ```
  if [ModificationDate] >= DATEADD('month', -5, TODAY()) then 1 else 0 END
  ```

- **KPI ffiliate co sla **

  ```
  if ([CO - % WID Processed within SLA24]-[target affiliate co sla])>0 then 'Above' else 'Below' end
  ```

- **ModDateYear_Month**

  ```
  str(year([ModificationDate])) + "_" + str(month([ModificationDate]))
  ```

- **Number of Records**

  ```
  1
  ```

- **PI Payment/Affiliate Payment**

  ```
  CASE [CashoutReasonID] WHEN 14 THEN 'PI Payment' WHEN 15 THEN 'Affiliate Payment' END
  ```

- **target affiliate co sla**

  ```
  0.95
  ```

#### Datasource: affiliates Verificaiton SLA

- **aff target verification sla**

  ```
  0.9
  ```

- **diff from targe aff ver sla**

  ```
  COUNT([RealCID]) / TOTAL(COUNT([RealCID]))-[aff target verification sla]
  ```

- **KPI target aff ver sla **

  ```
  if [diff from targe aff ver sla]>0 then 'Above' else 'Below' end
  ```

- **sla %**

  ```
  sum([SLA])/count(RealCID)
  ```

- **YearMonth**

  ```
  Left(DATENAME('month',[VerificationLevel3Date]), 3)+"-"+DATENAME('year',[VerificationLevel3Date])
  ```

#### Datasource: cashout STP

- **% STP E2E**

  ```
  [STP EndToEnd]/COUNT([WithdrawID])
  ```

- **Auto Execution %**

  ```
  sum([AutoExexution numeric])/count([WithdrawID])
  ```

- **Auto Preparation % **

  ```
  sum([AutoPreparation numeric ])/count([WithdrawID])
  ```

- **AutoApproval %**

  ```
  sum([AutoApprovals numeric]) /count([WithdrawID])
  ```

- **AutoApprovals numeric**

  ```
  CASE [AutoApproval] WHEN 'Auto Approval' then 1 
  when 'Manual ' then 0 else 0 end
  ```

- **AutoExexution numeric**

  ```
  case  [ExecutionApproval] when 'Auto Execute' then 1 else 0 end
  ```

- **AutoPreparation numeric **

  ```
  if  [Preparation]='Mass Auto Create' then 1 
  elseif [Preparation]='Auto Create' then 1 else 0 end
  ```

- **Calculation1**

  ```
  COUNT([CID]) / TOTAL(COUNT([CID]))
  ```

- **exclude today**

  ```
  if datediff('day',[ModificationDate],today())=0 then 'exclude' else 'include' end
  ```

- **improvement stp cashouts**

  ```
  ([% STP E2E]-[Target cashouts stp])
  ```

- **KPI  stp cashouts **

  ```
  IF ([% STP E2E]-[Target cashouts stp])<0 THEN 'Below' else 'Above' end
  ```

- **Prep**

  ```
  case  [Preparation] when 'Manual' then 'Manual' 
  when NULL then 'Manual' else 'Auto' end
  ```

- **STP EndToEnd**

  ```
  sum(IF [AutoApprovals numeric]=1 AND [AutoExexution numeric]=1 and ([AutoPreparation numeric ])=1 then 1 else 0 end)
  ```

- **Target cashouts stp**

  ```
  0.5
  ```

- **YearMonth**

  ```
  Left(DATENAME('month',[RequestDate]), 3)+"-"+DATENAME('year',[RequestDate])
  ```

#### Datasource: Cashouts

- **Amount (copy)**

  ```
  [Amount]
  ```

- **CO - % Fundings Processed within SLA24**

  ```
  [CO - WID Processed Within SLA24]/[CO - Total WID Requests]
  ```

- **CO - % Fundings Processed within SLA48**

  ```
  sum([CO - Fundings Processed Within SLA48])/sum([CO - Total Fundings Processed])
  ```

- **CO - % Fundings Processed within SLA5days**

  ```
  [CO - WID Processed Within SLA5days]/[CO - Total WID Requests]
  ```

- **CO - % WID Processed within SLA24**

  ```
  countd(if [WD_ID_SLA] = "OverallSLA"
  then [WithdrawID] else 0 end)
  /COUNTD([WithdrawID])
  ```

- **CO - % WID Processed within SLA48**

  ```
  sum(if [WD_ID_SLA48] = "OverallSLA48"
  then 1 else 0 end)
  /count([WithdrawID])
  ```

- **CO - % WID Processed within SLA5days**

  ```
  sum(if [WD_ID_SLA5days] = "OverallSLA5days"
  then 1 else 0 end)
  /count([WithdrawID])
  ```

- **CO - Fundings Processed Exceed SLA24**

  ```
  if [CashoutStatusID] = 3 
      and [SLA] = 0
  then [Number of Records] END
  ```

- **CO - Fundings Processed Exceed SLA48**

  ```
  if [CashoutStatusID] = 3 
      and [SLA48] = 0
  then [Number of Records] END
  ```

- **CO - Fundings Processed Exceed SLA5days**

  ```
  if [CashoutStatusID] = 3 
      and [SLA5days] = 0
  then [Number of Records] END
  ```

- **CO - Fundings Processed Within SLA24**

  ```
  if [CashoutStatusID] = 3 
      and [SLA] = 1
  then [Number of Records] END
  ```

- **CO - Fundings Processed Within SLA48**

  ```
  if [CashoutStatusID] = 3 
      and [SLA48] = 1
  then [Number of Records] END
  ```

- **CO - Fundings Processed Within SLA5days**

  ```
  if [CashoutStatusID] = 3 
      and [SLA5days]=1
  then [Number of Records] END
  ```

- **CO - Total Funding Actions**

  ```
  [Number of Records]
  ```

- **CO - Total Fundings Canceled**

  ```
  if [CashoutStatusID] = 4 then [Number of Records] END
  ```

- **CO - Total Fundings Processed**

  ```
  if [CashoutStatusID] = 3 then [Number of Records] END
  ```

- **CO - Total WID Canceled**

  ```
  COUNTD(
  if [CashoutStatusID] = 4 then [WithdrawID] 
  END)
  ```

- **CO - Total WID Processed**

  ```
  COUNTD(
  if [Approved] = 1 then [WithdrawID] 
  END)
  ```

- **CO - Total WID Requests**

  ```
  COUNTD([WithdrawID])
  ```

- **CO - WID Processed Exceed SLA24**

  ```
  countd
  (
  if [CashoutStatusID] = 3 
      and [WD_ID_SLA] = "OverallNotSLA"
  then [WithdrawID] END
  )
  ```

- **CO - WID Processed Exceed SLA48**

  ```
  COUNTD(
  if [CashoutStatusID] = 3 
      and [WD_ID_SLA48] = "OverallNotSLA48"
  then [WithdrawID] END
  )
  ```

- **CO - WID Processed Exceed SLA5days**

  ```
  COUNTD(
  if [CashoutStatusID] = 3 
      and [WD_ID_SLA5days] = "OverallNotSLA5days"
  then [WithdrawID] END
  )
  ```

- **CO - WID Processed Within SLA24**

  ```
  COUNTD(
  if [CashoutStatusID] = 3 
      and [WD_ID_SLA] = "OverallSLA"
  then [WithdrawID] END
  )
  ```

- **CO - WID Processed Within SLA48**

  ```
  countd(
  if [CashoutStatusID] = 3 
      and [WD_ID_SLA48] = "OverallSLA48"
  then [WithdrawID] END
  )
  ```

- **CO - WID Processed Within SLA5days**

  ```
  countd(
  if [CashoutStatusID] = 3 
      and [WD_ID_SLA5days] = "OverallSLA5days"
  then [WithdrawID] END
  )
  ```

- **CO SLA 8H**

  ```
  sum([Less than 8Hrs])/COUNTD([WithdrawID])
  ```

- **Difference of actual from target: CO SLA 8H**

  ```
  [CO SLA 8H]-[Target - CO SLA 8H]
  ```

- **filter - Exclude Current Week**

  ```
  if [RequestDate] >= DATEADD('week', -1, TODAY()) then 1 else 0 END
  ```

- **filter - Exclude Current Week (modDate)**

  ```
  if [ModificationDate] >= DATEADD('week', -1, TODAY()) then 1 else 0 END
  ```

- **filter - last 6 months**

  ```
  if [RequestDate] >= DATEADD('month', -5, TODAY()) then 1 else 0 END
  ```

- **filter - last 6 months (modDate)**

  ```
  if [ModificationDate] >= DATEADD('month', -5, TODAY()) then 1 else 0 END
  ```

- **filter - last 6 weeks**

  ```
  if [RequestDate] >= DATEADD('week', -6, TODAY()) then 1 else 0 END
  ```

- **filter - last 6 weeks (modDate)**

  ```
  if datepart('week',today())-datepart('week',[ModificationDate])<=6 and 
  (datepart('week',today())-datepart('week',[ModificationDate])>0) then 1 else 0 END
  ```

- **FilterView**

  ```
  lookup(max(([HoursBetween])),0)
  ```

- **KPI**

  ```
  IF ([CO SLA 8H]-[Target - CO SLA 8H])<0 THEN 'BELOW' ELSE 'OVER' END
  ```

- **Less than 8Hrs**

  ```
  IF [HoursBetween]<8 THEN 1 ELSE 0 END
  ```

- **ModDateYear_Month**

  ```
  str(year([ModificationDate])) + "_" + str(month([ModificationDate]))
  ```

- **Number of Records**

  ```
  1
  ```

- **RequestDateYear_Month**

  ```
  str(year([RequestDate])) + "_" + str(month([RequestDate]))
  ```

- **SLA (copy)**

  ```
  [SLA]
  ```

- **SLA48 (copy)**

  ```
  [SLA48]
  ```

- **Target - CO SLA 8H**

  ```
  0.5
  ```

- **Weeknumber**

  ```
  DATEPART('week', [RequestDate])
  ```

- **Weeknumber (modDate)**

  ```
  DATEPART('week', [ModificationDate])
  ```

- **WithdrawID (copy)**

  ```
  [WithdrawID]
  ```

- **YearMonth**

  ```
  Left(DATENAME('month',[ModificationDate]), 3)+"-"+DATENAME('year',[ModificationDate])
  ```

#### Datasource: chbk loss

- **Final CHB Loss**

  ```
  [Final]*-1
  ```

#### Datasource: Finance Deposits

- **CHB loss out of Total Deposits**

  ```
  sUM([federated.0ctwefz1cpdt1e13cdhsk19l7ju4].[Final])/sum([TotalDeposits])
  ```

#### Datasource: Monthly Chargeback Report

- **CHB $Amount**

  ```
  case [Refund / CHB] when 'CHB' THEN [CHB/Refund $ Amount] ELSE 0 END
  ```

- **CHBK $ over Total Deposits**

  ```
  SUM([CHB $Amount])/SUM([federated.1j71f7d1pxdaoq1e1n5sb0gmawdo].[TotalDeposits])
  ```

- **improvement chbk**

  ```
  ([target chbk]-[CHBK $ over Total Deposits])
  ```

- **improvement loss**

  ```
  ([target loss]-[federated.1j71f7d1pxdaoq1e1n5sb0gmawdo].[Calculation_-3954441893248290816])
  ```

- **kpi improvement chbk **

  ```
  if (([target chbk]-[CHBK $ over Total Deposits]))<0 then 'below' else 'above' end
  ```

- **KPIi improvement loss **

  ```
  IF(([target loss]-[federated.1j71f7d1pxdaoq1e1n5sb0gmawdo].[Calculation_-3954441893248290816]))<0 THEN 'BELOW' ELSE 'ABOVE' END
  ```

- **Refund $ over Total Deposits**

  ```
  SUM([Refund $Amount])/SUM([federated.1j71f7d1pxdaoq1e1n5sb0gmawdo].[TotalDeposits])
  ```

- **Refund $Amount**

  ```
  case [Refund / CHB] when 'Refund' THEN [CHB/Refund $ Amount] ELSE 0 END
  ```

- **target chbk**

  ```
  0.0015
  ```

- **target loss**

  ```
  0.0008
  ```

#### Datasource: Risk alerts

- **% False Positive**

  ```
  sum([False Positive])/countd([AlertID])
  ```

- **Calculation1**

  ```
  COUNTD([AlertID]) / TOTAL(COUNTD([AlertID]))
  ```

- **False Positive**

  ```
  if [StatusReason]='False positive' then 1 else 0 end
  ```

- **filter for view**

  ```
  lookup(max(([Hours - TimeToModification])),0)
  ```

- **Hours - TimeToModification**

  ```
  datediff('hour',[CreationDate],[ModificationDate])
  ```

- **Improvement risk alert outcome**

  ```
  ([Target Risk Alert]-[% False Positive])
  ```

- **KPI  risk alert outcome **

  ```
  IF ([Target Risk Alert]-[% False Positive])<0 THEN 'Below' else 'Above' end
  ```

- **Target Risk Alert**

  ```
  0.5
  ```

- **TimeToFolowUp**

  ```
  datediff('hour',[CreationDate],[FollowUpDate])
  ```

- **YearMonth**

  ```
  Left(DATENAME('month',[ModificationDate]), 3)+"-"+DATENAME('year',[ModificationDate])
  ```

#### Datasource: SLAs 

- **Calculation1**

  ```
  COUNTD([TaskID]) / TOTAL(COUNTD([TaskID]))
  ```

- **diff from target kyc dov verification**

  ```
  (COUNTD([TaskID]) / TOTAL(COUNTD([TaskID])))-[KYC doc verification sla target]
  ```

- **kpi target kyc doc ver**

  ```
  if [diff from target kyc dov verification]>0 then 'Above' else 'Below' end
  ```

- **KYC doc verification sla target**

  ```
  0.95
  ```

- **Priority **

  ```
  [Priority]
  ```

- **Team **

  ```
  [Team]
  ```

- **Time in HR filter view**

  ```
  lookup(max(([TimeinHr])),0)
  ```

- **YearMonth**

  ```
  Left(DATENAME('month',[CreateDate]), 3)+"-"+DATENAME('year',[CreateDate])
  ```

#### Datasource: TICKETS PER PROCESS

- **# out of SLA**

  ```
  SUM(CASE [SLA] WHEN 'Out of SLA' THEN 1 ELSE 0 END)
  ```

- **# solved **

  ```
  sum(case [Open/Closed TicketStatus] when 'Solved' then 1 else 0 end)
  ```

- **% improvement csat**

  ```
  avg([LastCsat])-[Target csat score]
  ```

- **% In SLA**

  ```
  SUM(CASE [SLA] WHEN 'In SLA' THEN 1 ELSE 0 END) /COUNT([CaseNumber])
  ```

- **% solved**

  ```
  sum(case [Open/Closed TicketStatus] when 'Solved' then 1 else 0 end)/count([CaseNumber])
  ```

- **%NoOfTouches>3**

  ```
  sum([NoOfTouches>3])/count([CaseNumber])
  ```

- **%Out of SLA **

  ```
  SUM(CASE [SLA] WHEN 'Out of SLA' THEN 1 ELSE 0 END) /COUNT([CaseNumber])
  ```

- **Calculation1**

  ```
  (ZN(AVG([LastCsat])) - LOOKUP(ZN(AVG([LastCsat])), -1)) / ABS(LOOKUP(ZN(AVG([LastCsat])), -1))
  ```

- **Calculation2**

  ```
  (ZN(AVG([LastCsat])) - LOOKUP(ZN(AVG([LastCsat])), -1)) / ABS(LOOKUP(ZN(AVG([LastCsat])), -1))
  ```

- **Calculation3**

  ```
  ZN(AVG([LastCsat])) - LOOKUP(ZN(AVG([LastCsat])), -1)
  ```

- **Calculation4**

  ```
  ZN(AVG([LastCsat])) - LOOKUP(ZN(AVG([LastCsat])), -1)
  ```

- **Calculation5**

  ```
  ZN(AVG([LastCsat])) - LOOKUP(ZN(AVG([LastCsat])), -1)
  ```

- **Calculation6**

  ```
  (ZN(AVG([LastCsat])) - LOOKUP(ZN(AVG([LastCsat])), -1)) / ABS(LOOKUP(ZN(AVG([LastCsat])), -1))
  ```

- **Calculation7**

  ```
  (ZN(AVG([LastCsat])) - LOOKUP(ZN(AVG([LastCsat])), -1)) / ABS(LOOKUP(ZN(AVG([LastCsat])), -1))
  ```

- **Calculation8**

  ```
  (ZN(AVG([NumberOfTouches])) - LOOKUP(ZN(AVG([NumberOfTouches])), -1)) / ABS(LOOKUP(ZN(AVG([NumberOfTouches])), -1))
  ```

- **Calculation9**

  ```
  (ZN(AVG([NumberOfTouches])) - LOOKUP(ZN(AVG([NumberOfTouches])), -1)) / ABS(LOOKUP(ZN(AVG([NumberOfTouches])), -1))
  ```

- **DIFF FROM TARGET REMOVE TICKETS**

  ```
  COUNTD([CaseNumber])-[TARGET TO REMOVE TICKETS]
  ```

- **diff fromt arget kyc scat**

  ```
  AVG([LastCsat])-[kyc target csat]
  ```

- **diff fromt arget verificaiton scat**

  ```
  AVG([LastCsat])-[verification target csat ]
  ```

- **DIFF MONEY IN FROM TARGET**

  ```
  AVG([LastCsat])-[TARGET CSAT MONEY IN]
  ```

- **Improvement csat**

  ```
  [Target csat score]-AVG([LastCsat])
  ```

- **KPI csat money out**

  ```
  IF (avg([LastCsat])-[Target csat score])<0 THEN 'Below' else 'Above' end
  ```

- **kpi MONEY IN FROM TARGET**

  ```
  if [DIFF MONEY IN FROM TARGET]<0 then 'below' else 'above' end
  ```

- **kpi remove tickets**

  ```
  if [DIFF FROM TARGET REMOVE TICKETS]<0 then 'below' else 'above' end
  ```

- **KPI risk Improvement csat **

  ```
  IF (AVG([LastCsat])-[risk Target csat score ])<0 THEN 'BELOW' ELSE 'ABOVE' END
  ```

- **kpi target verificaiton scat**

  ```
  if [diff fromt arget verificaiton scat]>0 then 'Above' else 'Below' end
  ```

- **kpis target kyc scat**

  ```
  if [diff fromt arget kyc scat]>0 then 'Above' else 'Below' end
  ```

- **kyc target csat**

  ```
  3.76
  ```

- **NoOfTouches>3**

  ```
  if [NumberOfTouches]>3 then 1 else 0 end
  ```

- **risk % improvement csat **

  ```
  ([risk Target csat score ]-avg([LastCsat]))/avg([LastCsat])
  ```

- **risk Improvement csat **

  ```
  AVG([LastCsat])-[risk Target csat score ]
  ```

- **risk Target csat score **

  ```
  3.82
  ```

- **TARGET CSAT MONEY IN**

  ```
  4
  ```

- **Target csat score**

  ```
  3.88
  ```

- **TARGET TO REMOVE TICKETS**

  ```
  6500
  ```

- **verification target csat **

  ```
  4.00
  ```

- **YearMonth**

  ```
  Left(DATENAME('month',[CreatedDate]), 3)+"-"+DATENAME('year',[CreatedDate])
  ```

#### Datasource: Wires

- **DIFFERENCE WIRE SLA TO TARGET**

  ```
  [Wires - % ProcessTime within SLA] - [TARGET WIRES SLA]
  ```

- **exclude current month**

  ```
  if DATEPART('month', [Modification Date]) = DATEPART('month', today())
  and 
  DATEPART('year', [Modification Date]) = DATEPART('year', today())
   then 1 else 0 END
  ```

- **filter - Exclude Current Week**

  ```
  if DATEPART('week', [ValueDate]) = DATEPART('week', today())
  and 
  DATEPART('year', [ValueDate]) = DATEPART('year', today())
   then 1 else 0 END
  ```

- **filter - Exclude Current Week (modDate)**

  ```
  if DATEPART('week', [Modification Date]) = DATEPART('week', today())
  and 
  DATEPART('year', [Modification Date]) = DATEPART('year', today())
   then 1 else 0 END
  ```

- **filter - last 4 months**

  ```
  if [ValueDate] >= DATEADD('month', -3, TODAY()) then 1 else 0 END
  ```

- **filter - last 6 months (modDate)**

  ```
  if [Modification Date] >= DATEADD('month', -5, TODAY()) then 1 else 0 END
  ```

- **filter - last 6 weeks**

  ```
  if [ValueDate] >= DATEADD('week', -6, TODAY()) then 1 else 0 END
  ```

- **filter - last 6 weeks (modDate)**

  ```
  if [Modification Date] <= DATEADD("week", -6, TODAY()) then 0 else 1 end
  ```

- **kpi  WIRE SLA TO TARGET**

  ```
  if [DIFFERENCE WIRE SLA TO TARGET]<0 then 'below' else 'above' end
  ```

- **ModYearMonth (modDate)**

  ```
  str(year([Modification Date])) + "_" + str(month([Modification Date]))
  ```

- **Number of Records**

  ```
  1
  ```

- **Redeposits**

  ```
  case [IsFTD] when 0 then 1 else 0 end
  ```

- **TARGET WIRES SLA**

  ```
  0.98
  ```

- **ValueYearMonth**

  ```
  str(year([ValueDate])) + "_" + str(month([ValueDate]))
  ```

- **Weeknumber**

  ```
  DATEPART('week', [ValueDate])
  ```

- **Weeknumber (modDate)**

  ```
  DATEPART('week', [Modification Date])
  ```

- **Wires - % ProcessTime within SLA**

  ```
  sum([Wires - ProcessTime within SLA])/sum([Wires - Wire Count])
  ```

- **Wires - Avg. Days Start to Finish**

  ```
  if [Funding Type ID] = 2 then
  [FromStartToFinish] 
  end
  ```

- **Wires - Avg. Days WireTransfer**

  ```
  if [Funding Type ID] = 2 then
  [HandlingDays]
  END
  ```

- **Wires - ProcessTime within SLA**

  ```
  if [HandlingDays] <= 1 
      and [Funding Type ID] = 2
  then 1 else 0 END
  ```

- **Wires - Wire Count**

  ```
  if [Funding Type ID] = 2 then
  [Number of Records] END
  ```

### Workbook: OPS Weekly KPIs China

#### Datasource: Cashouts

- **Amount (copy)**

  ```
  [Amount]
  ```

- **Calculation1**

  ```
  COUNTD([WithdrawID (copy)]) / TOTAL(COUNTD([WithdrawID (copy)]))
  ```

- **Calculation2**

  ```
  COUNTD([WithdrawID (copy)]) / TOTAL(COUNTD([WithdrawID (copy)]))
  ```

- **Calculation3**

  ```
  COUNTD([WithdrawID (copy)]) / TOTAL(COUNTD([WithdrawID (copy)]))
  ```

- **Calculation4**

  ```
  COUNTD([WithdrawID (copy)]) / TOTAL(COUNTD([WithdrawID (copy)]))
  ```

- **CO - % Fundings Processed within SLA24**

  ```
  [CO - WID Processed Within SLA24]/[CO - Total WID Requests]
  ```

- **CO - % Fundings Processed within SLA48**

  ```
  sum([CO - Fundings Processed Within SLA48])/sum([CO - Total Fundings Processed])
  ```

- **CO - % WID Processed within SLA24**

  ```
  countd(if [WD_ID_SLA] = "OverallSLA"
  then [WithdrawID] else 0 end)
  /COUNTD([WithdrawID])
  ```

- **CO - % WID Processed within SLA48**

  ```
  sum(if [WD_ID_SLA48] = "OverallSLA48"
  then 1 else 0 end)
  /count([WithdrawID])
  ```

- **CO - Fundings Processed Exceed SLA24**

  ```
  if [CashoutStatusID] = 3 
      and [SLA] = 0
  then [Number of Records] END
  ```

- **CO - Fundings Processed Exceed SLA48**

  ```
  if [CashoutStatusID] = 3 
      and [SLA48] = 0
  then [Number of Records] END
  ```

- **CO - Fundings Processed Within SLA24**

  ```
  if [CashoutStatusID] = 3 
      and [SLA] = 1
  then [Number of Records] END
  ```

- **CO - Fundings Processed Within SLA48**

  ```
  if [CashoutStatusID] = 3 
      and [SLA48] = 1
  then [Number of Records] END
  ```

- **CO - Total Funding Actions**

  ```
  [Number of Records]
  ```

- **CO - Total Fundings Canceled**

  ```
  if [CashoutStatusID] = 4 then [Number of Records] END
  ```

- **CO - Total Fundings Processed**

  ```
  if [CashoutStatusID] = 3 then [Number of Records] END
  ```

- **CO - Total WID Canceled**

  ```
  COUNTD(
  if [CashoutStatusID] = 4 then [WithdrawID] 
  END)
  ```

- **CO - Total WID Processed**

  ```
  COUNTD(
  if [Approved] = 1 then [WithdrawID] 
  END)
  ```

- **CO - Total WID Requests**

  ```
  COUNTD([WithdrawID])
  ```

- **CO - WID Processed Exceed SLA24**

  ```
  countd
  (
  if [CashoutStatusID] = 3 
      and [WD_ID_SLA] = "OverallNotSLA"
  then [WithdrawID] END
  )
  ```

- **CO - WID Processed Exceed SLA48**

  ```
  COUNTD(
  if [CashoutStatusID] = 3 
      and [WD_ID_SLA48] = "OverallNotSLA48"
  then [WithdrawID] END
  )
  ```

- **CO - WID Processed Within SLA24**

  ```
  COUNTD(
  if [CashoutStatusID] = 3 
      and [WD_ID_SLA] = "OverallSLA"
  then [WithdrawID] END
  )
  ```

- **CO - WID Processed Within SLA48**

  ```
  countd(
  if [CashoutStatusID] = 3 
      and [WD_ID_SLA48] = "OverallSLA48"
  then [WithdrawID] END
  )
  ```

- **filter - Exclude Current Week**

  ```
  if [RequestDate] >= DATEADD('week', -1, TODAY()) then 1 else 0 END
  ```

- **filter - last 6 months**

  ```
  if [RequestDate] >= DATEADD('month', -5, TODAY()) then 1 else 0 END
  ```

- **filter - last 6 weeks**

  ```
  if [RequestDate] >= DATEADD('week', -6, TODAY()) then 1 else 0 END
  ```

- **Number of Records**

  ```
  1
  ```

- **RequestDateYear_Month**

  ```
  str(year([RequestDate])) + "_" + str(month([RequestDate]))
  ```

- **SLA (copy)**

  ```
  [SLA]
  ```

- **SLA48 (copy)**

  ```
  [SLA48]
  ```

- **Weeknumber**

  ```
  DATEPART('week', [RequestDate])
  ```

- **WithdrawID (copy)**

  ```
  [WithdrawID]
  ```

#### Datasource: Verifications

- **Calculation1**

  ```
  COUNT([Number of Records]) / TOTAL(COUNT([Number of Records]))
  ```

- **Calculation2**

  ```
  COUNT([Number of Records]) / TOTAL(COUNT([Number of Records]))
  ```

- **Calculation3**

  ```
  COUNT([Number of Records]) / TOTAL(COUNT([Number of Records]))
  ```

- **Calculation4**

  ```
  COUNT([Number of Records]) / TOTAL(COUNT([Number of Records]))
  ```

- **EffectiveDateYear_Month**

  ```
  str(year([EffectiveAddDate])) + "_" + str(month([EffectiveAddDate]))
  ```

- **filter - Exclude Current Week**

  ```
  if [EffectiveAddDate] >= DATEADD('week', -1, TODAY()) then 1 else 0 END
  ```

- **filter - last 6 months**

  ```
  if [EffectiveAddDate] >= DATEADD('month', -5, TODAY()) then 1 else 0 END
  ```

- **filter - last 6 weeks**

  ```
  if [EffectiveAddDate] >= DATEADD('week', -6, TODAY()) then 1 else 0 END
  ```

- **Number of Records**

  ```
  1
  ```

- **Verification - Verification Method**

  ```
  if [VerificationLevelID] = 3 and [EvMatchStatus] = 2 then "byEV" else "byDocs" end
  ```

- **Verification - Verified by Docs**

  ```
  IF [VerificationLevelID] = 3 and [EvMatchStatus] != 2 
  then 1 else 0 end
  ```

- **Verification - Verified by EV**

  ```
  IF [VerificationLevelID] = 3 and [EvMatchStatus] = 2 
  then 1 else 0 end
  ```

- **Verifications - % First Review in SLA**

  ```
  sum([Verifications - First Review in SLA])/sum([Verifications - Total Count (inc leads)])
  ```

- **Verifications - % FTDs Verified in SLA**

  ```
  sum(if [IsDepositor] = 1 and [VerificationLevelID] = 3 then
  [Verifications - Total Verified in SLA] end)
  /
  sum (if [IsDepositor] = 1 and [VerificationLevelID] = 3 then
  [Verifications - Total Verified (inc. leads)] end)
  ```

- **Verifications - % Verified in SLA**

  ```
  sum([Verifications - Total Verified in SLA])/sum([Verifications - Total Verified (inc. leads)])
  ```

- **Verifications - % verified of FTDs**

  ```
  sum([Verifications - Total Verified from FTDs])/
  sum([Verifications - Total FTDs])
  ```

- **Verifications - % verified of Total Count (inc. leads)**

  ```
  sum([Verifications - Total Verified (inc. leads)])/
  sum([Verifications - Total Count (inc leads)])
  ```

- **Verifications - Avg. Days to First Review**

  ```
  [FirstTouch]
  ```

- **Verifications - Avg. Days to Verify**

  ```
  [WorkingDaysToVerify]
  ```

- **Verifications - Closed after 15 Days**

  ```
  [Liquidated]
  ```

- **Verifications - First Review in SLA**

  ```
  if [FirstTouchSLA] = 1 then 1 else 0 end
  ```

- **Verifications - Total Count (inc leads)**

  ```
  [Number of Records]
  ```

- **Verifications - Total FTDs**

  ```
  if [IsDepositor] = 1
  then [Number of Records]
  else 0 END
  ```

- **Verifications - Total Not Verified (inc. leads)**

  ```
  if [VerificationLevelID] != 3
  then [Number of Records]
  else 0 END
  ```

- **Verifications - Total Not Verified and FTDd**

  ```
  if [VerificationLevelID] != 3
  and [IsDepositor] = 1
  then [Number of Records]
  else 0 END
  ```

- **Verifications - Total Verified (inc. leads)**

  ```
  if [VerificationLevelID] = 3
  then [Number of Records]
  else 0 END
  ```

- **Verifications - Total Verified from FTDs**

  ```
  if [VerificationLevelID] = 3
  and [IsDepositor] = 1
  then [Number of Records]
  else 0 END
  ```

- **Verifications - Total Verified in SLA**

  ```
  if [VerificationLevelID] = 3
  and [VerificationSLA] = 1
  then [Number of Records]
  else 0 END
  ```

- **Weeknumber**

  ```
  DATEPART('week', [EffectiveAddDate])
  ```

#### Datasource: Wires

- **filter - Exclude Current Week**

  ```
  if [Modification Date] >= DATEADD('week', -1, TODAY()) then 1 else 0 END
  ```

- **filter - Exclude Current Week (copy)**

  ```
  if [Weeknumber] = DATEPART('week', TODAY()) then 1 else 0 end
  ```

- **filter - last 6 months**

  ```
  if [ValueDate] >= DATEADD('month', -5, TODAY()) then 1 else 0 END
  ```

- **filter - last 6 weeks**

  ```
  if [ValueDate] >= DATEADD('week', -6, TODAY()) then 1 else 0 END
  ```

- **Number of Records**

  ```
  1
  ```

- **ValueYearMonth**

  ```
  str(year([ValueDate])) + "_" + str(month([ValueDate]))
  ```

- **Weeknumber**

  ```
  DATEPART('week', [Modification Date])
  ```

- **Wires - % ProcessTime within SLA**

  ```
  sum([Wires - ProcessTime within SLA])/sum([Wires - Wire Count])
  ```

- **Wires - Avg. Days Start to Finish**

  ```
  if [Funding Type ID] = 2 then
  [FromStartToFinish] 
  end
  ```

- **Wires - Avg. Days WireTransfer**

  ```
  if [Funding Type ID] = 2 then
  [HandlingDays]
  END
  ```

- **Wires - ProcessTime within SLA**

  ```
  if [HandlingDays] <= 1 
      and [Funding Type ID] = 2
  then 1 else 0 END
  ```

- **Wires - Wire Count**

  ```
  if [Funding Type ID] = 2 then
  [Number of Records] END
  ```

### Workbook: OPS Weekly KPIs China full 6 months (Delete when finished)

#### Datasource: Cashouts

- **Amount (copy)**

  ```
  [Amount]
  ```

- **Calculation1**

  ```
  COUNTD([WithdrawID (copy)]) / TOTAL(COUNTD([WithdrawID (copy)]))
  ```

- **Calculation2**

  ```
  COUNTD([WithdrawID (copy)]) / TOTAL(COUNTD([WithdrawID (copy)]))
  ```

- **Calculation3**

  ```
  COUNTD([WithdrawID (copy)]) / TOTAL(COUNTD([WithdrawID (copy)]))
  ```

- **Calculation4**

  ```
  COUNTD([WithdrawID (copy)]) / TOTAL(COUNTD([WithdrawID (copy)]))
  ```

- **CO - % Fundings Processed within SLA24**

  ```
  [CO - WID Processed Within SLA24]/[CO - Total WID Requests]
  ```

- **CO - % Fundings Processed within SLA48**

  ```
  sum([CO - Fundings Processed Within SLA48])/sum([CO - Total Fundings Processed])
  ```

- **CO - % WID Processed within SLA24**

  ```
  countd(if [WD_ID_SLA] = "OverallSLA"
  then [WithdrawID] else 0 end)
  /COUNTD([WithdrawID])
  ```

- **CO - % WID Processed within SLA48**

  ```
  sum(if [WD_ID_SLA48] = "OverallSLA48"
  then 1 else 0 end)
  /count([WithdrawID])
  ```

- **CO - Fundings Processed Exceed SLA24**

  ```
  if [CashoutStatusID] = 3 
      and [SLA] = 0
  then [Number of Records] END
  ```

- **CO - Fundings Processed Exceed SLA48**

  ```
  if [CashoutStatusID] = 3 
      and [SLA48] = 0
  then [Number of Records] END
  ```

- **CO - Fundings Processed Within SLA24**

  ```
  if [CashoutStatusID] = 3 
      and [SLA] = 1
  then [Number of Records] END
  ```

- **CO - Fundings Processed Within SLA48**

  ```
  if [CashoutStatusID] = 3 
      and [SLA48] = 1
  then [Number of Records] END
  ```

- **CO - Total Funding Actions**

  ```
  [Number of Records]
  ```

- **CO - Total Fundings Canceled**

  ```
  if [CashoutStatusID] = 4 then [Number of Records] END
  ```

- **CO - Total Fundings Processed**

  ```
  if [CashoutStatusID] = 3 then [Number of Records] END
  ```

- **CO - Total WID Canceled**

  ```
  COUNTD(
  if [CashoutStatusID] = 4 then [WithdrawID] 
  END)
  ```

- **CO - Total WID Processed**

  ```
  COUNTD(
  if [Approved] = 1 then [WithdrawID] 
  END)
  ```

- **CO - Total WID Requests**

  ```
  COUNTD([WithdrawID])
  ```

- **CO - WID Processed Exceed SLA24**

  ```
  countd
  (
  if [CashoutStatusID] = 3 
      and [WD_ID_SLA] = "OverallNotSLA"
  then [WithdrawID] END
  )
  ```

- **CO - WID Processed Exceed SLA48**

  ```
  COUNTD(
  if [CashoutStatusID] = 3 
      and [WD_ID_SLA48] = "OverallNotSLA48"
  then [WithdrawID] END
  )
  ```

- **CO - WID Processed Within SLA24**

  ```
  COUNTD(
  if [CashoutStatusID] = 3 
      and [WD_ID_SLA] = "OverallSLA"
  then [WithdrawID] END
  )
  ```

- **CO - WID Processed Within SLA48**

  ```
  countd(
  if [CashoutStatusID] = 3 
      and [WD_ID_SLA48] = "OverallSLA48"
  then [WithdrawID] END
  )
  ```

- **filter - Exclude Current Week**

  ```
  if [RequestDate] >= DATEADD('week', -1, TODAY()) then 1 else 0 END
  ```

- **filter - last 6 months**

  ```
  if [RequestDate] >= DATEADD('month', -5, TODAY()) then 1 else 0 END
  ```

- **filter - last 6 weeks**

  ```
  if [RequestDate] >= DATEADD('week', -6, TODAY()) then 1 else 0 END
  ```

- **Number of Records**

  ```
  1
  ```

- **RequestDateYear_Month**

  ```
  str(year([RequestDate])) + "_" + str(month([RequestDate]))
  ```

- **SLA (copy)**

  ```
  [SLA]
  ```

- **SLA48 (copy)**

  ```
  [SLA48]
  ```

- **Weeknumber**

  ```
  DATEPART('week', [RequestDate])
  ```

- **WithdrawID (copy)**

  ```
  [WithdrawID]
  ```

#### Datasource: Verifications

- **Calculation1**

  ```
  COUNT([Number of Records]) / TOTAL(COUNT([Number of Records]))
  ```

- **Calculation2**

  ```
  COUNT([Number of Records]) / TOTAL(COUNT([Number of Records]))
  ```

- **Calculation3**

  ```
  COUNT([Number of Records]) / TOTAL(COUNT([Number of Records]))
  ```

- **Calculation4**

  ```
  COUNT([Number of Records]) / TOTAL(COUNT([Number of Records]))
  ```

- **EffectiveDateYear_Month**

  ```
  str(year([EffectiveAddDate])) + "_" + str(month([EffectiveAddDate]))
  ```

- **filter - Exclude Current Week**

  ```
  if [EffectiveAddDate] >= DATEADD('week', -1, TODAY()) then 1 else 0 END
  ```

- **filter - last 6 months**

  ```
  if [EffectiveAddDate] >= DATEADD('month', -5, TODAY()) then 1 else 0 END
  ```

- **filter - last 6 weeks**

  ```
  if [EffectiveAddDate] >= DATEADD('week', -6, TODAY()) then 1 else 0 END
  ```

- **Number of Records**

  ```
  1
  ```

- **Verification - Verification Method**

  ```
  if [VerificationLevelID] = 3 and [EvMatchStatus] = 2 then "byEV" else "byDocs" end
  ```

- **Verification - Verified by Docs**

  ```
  IF [VerificationLevelID] = 3 and [EvMatchStatus] != 2 
  then 1 else 0 end
  ```

- **Verification - Verified by EV**

  ```
  IF [VerificationLevelID] = 3 and [EvMatchStatus] = 2 
  then 1 else 0 end
  ```

- **Verifications - % First Review in SLA**

  ```
  sum([Verifications - First Review in SLA])/sum([Verifications - Total Count (inc leads)])
  ```

- **Verifications - % FTDs Verified in SLA**

  ```
  sum(if [IsDepositor] = 1 and [VerificationLevelID] = 3 then
  [Verifications - Total Verified in SLA] end)
  /
  sum (if [IsDepositor] = 1 and [VerificationLevelID] = 3 then
  [Verifications - Total Verified (inc. leads)] end)
  ```

- **Verifications - % Verified in SLA**

  ```
  sum([Verifications - Total Verified in SLA])/sum([Verifications - Total Verified (inc. leads)])
  ```

- **Verifications - % verified of FTDs**

  ```
  sum([Verifications - Total Verified from FTDs])/
  sum([Verifications - Total FTDs])
  ```

- **Verifications - % verified of Total Count (inc. leads)**

  ```
  sum([Verifications - Total Verified (inc. leads)])/
  sum([Verifications - Total Count (inc leads)])
  ```

- **Verifications - Avg. Days to First Review**

  ```
  [FirstTouch]
  ```

- **Verifications - Avg. Days to Verify**

  ```
  [WorkingDaysToVerify]
  ```

- **Verifications - Closed after 15 Days**

  ```
  [Liquidated]
  ```

- **Verifications - First Review in SLA**

  ```
  if [FirstTouchSLA] = 1 then 1 else 0 end
  ```

- **Verifications - Total Count (inc leads)**

  ```
  [Number of Records]
  ```

- **Verifications - Total FTDs**

  ```
  if [IsDepositor] = 1
  then [Number of Records]
  else 0 END
  ```

- **Verifications - Total Not Verified (inc. leads)**

  ```
  if [VerificationLevelID] != 3
  then [Number of Records]
  else 0 END
  ```

- **Verifications - Total Not Verified and FTDd**

  ```
  if [VerificationLevelID] != 3
  and [IsDepositor] = 1
  then [Number of Records]
  else 0 END
  ```

- **Verifications - Total Verified (inc. leads)**

  ```
  if [VerificationLevelID] = 3
  then [Number of Records]
  else 0 END
  ```

- **Verifications - Total Verified from FTDs**

  ```
  if [VerificationLevelID] = 3
  and [IsDepositor] = 1
  then [Number of Records]
  else 0 END
  ```

- **Verifications - Total Verified in SLA**

  ```
  if [VerificationLevelID] = 3
  and [VerificationSLA] = 1
  then [Number of Records]
  else 0 END
  ```

- **Weeknumber**

  ```
  DATEPART('week', [EffectiveAddDate])
  ```

#### Datasource: Wires

- **filter - Exclude Current Week**

  ```
  if [Modification Date] >= DATEADD('week', -1, TODAY()) then 1 else 0 END
  ```

- **filter - Exclude Current Week (copy)**

  ```
  if [Weeknumber] = DATEPART('week', TODAY()) then 1 else 0 end
  ```

- **filter - last 6 months**

  ```
  if [ValueDate] >= DATEADD('month', -5, TODAY()) then 1 else 0 END
  ```

- **filter - last 6 weeks**

  ```
  if [ValueDate] >= DATEADD('week', -6, TODAY()) then 1 else 0 END
  ```

- **Number of Records**

  ```
  1
  ```

- **ValueYearMonth**

  ```
  str(year([ValueDate])) + "_" + str(month([ValueDate]))
  ```

- **Weeknumber**

  ```
  DATEPART('week', [Modification Date])
  ```

- **Wires - % ProcessTime within SLA**

  ```
  sum([Wires - ProcessTime within SLA])/sum([Wires - Wire Count])
  ```

- **Wires - Avg. Days Start to Finish**

  ```
  if [Funding Type ID] = 2 then
  [FromStartToFinish] 
  end
  ```

- **Wires - Avg. Days WireTransfer**

  ```
  if [Funding Type ID] = 2 then
  [HandlingDays]
  END
  ```

- **Wires - ProcessTime within SLA**

  ```
  if [HandlingDays] <= 1 
      and [Funding Type ID] = 2
  then 1 else 0 END
  ```

- **Wires - Wire Count**

  ```
  if [Funding Type ID] = 2 then
  [Number of Records] END
  ```
