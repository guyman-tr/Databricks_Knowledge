# Tableau Usage — BI_DB_VerificationStatus30Days

> Generated by `tools/tableau/extract_table_metadata.py`. Do not edit by hand.

## Identity

- Full name: ``
- Database: `` ()
- Schema: ``
- Connection type: ``
- Tableau metadata id: ``

## Summary

- Custom SQL queries: **2**
- Downstream workbooks: **1**
- Downstream calculated fields: **0**

## Custom SQL queries referencing this table

### 1. Custom SQL Query

- Tableau id: `1a962c2f-e6fe-a381-6d4f-5f510c5a7ad1`

```sql
select RealCID, 
		CONVERT(DATE, FirstDepositDate) FirstDepositDate,
		Country, 
		Region,
		CurrentVerificationLevel,
		DidCO,
		UploadDocs,
		SuggestedPOA,
		SuggestedPOI,
		PendingClosureStatusID,
		CurrentPlayerStatus,
		PlayerStatusReasonID,
		Closed,
		EvVerified,
                NewUpload,
		case when DATEADD(day, 15, convert(date, FirstDepositDate)) <= convert(date, getdate()) 
			and	CurrentVerificationLevel < 3
			and DidCO = 0
			and PendingClosureStatusID < 3
			then 1 else 0 end as Not_Closed_15_Plus,
		RegulationID,
		EvMatchStatus,
		FTD_Plus_14,
		[14_Days_RE],
        [14_Days_Deposits],
		[Priority],
		IsDepositor,
		--WorldCheckID, 
		IsWalletUser,
                dr.Name as Regulation
        FROM [BI_DB_dbo].[BI_DB_VerificationStatus30Days]
join DWH_dbo.Dim_Regulation dr on dr.ID=RegulationID
```

### 2. Custom SQL Query

- Tableau id: `f2098275-699c-f5f4-0d7d-cc1a89f79463`

```sql
SELECT RealCID,
	Convert(DATE, FirstDepositDate) AS FTD_Date,
	CASE WHEN NewUpload = 1 THEN 'UploadedOnlyOne' ELSE 'UploadedNone' END AS Problem,
	DATEDIFF(day, FirstDepositDate, getdate()) AS DaysFromFTD
FROM [BI_DB_dbo].[BI_DB_VerificationStatus30Days]
WHERE EvVerified = 0 
	AND CurrentVerificationLevel < 3 
	AND Closed = 0 AND ((NewUpload = 1 
	AND (SuggestedPOA + SuggestedPOI < 2 
			OR SuggestedPOA IS NULL 
			OR SuggestedPOI IS NULL))
	OR SuggestedPOA IS NULL 
	OR SuggestedPOI IS NULL)
```

## Downstream workbooks

_Attribution: 0 via direct table-drag, 1 via custom SQL (workbooks can use both paths)._

| # | Workbook | Project | Owner | Via | Last updated | URL |
|---|---|---|---|---|---|---|
| 1 | 15-Day Verification Status dashboard | Verifications | Pavlina Masoura | custom_sql | 2025-02-14T11:03:54Z | [open](https://reports.etorocorp.com/#/workbooks/15-DayVerificationStatusdashboard) |

### View URLs (per workbook)

- **15-Day Verification Status dashboard** (Verifications)
  - [Ver-Map 15 days](https://reports.etorocorp.com/#/views/15-DayVerificationStatusdashboard/Ver-Map15days)

## Downstream calculated fields (in embedded datasources)

_None._