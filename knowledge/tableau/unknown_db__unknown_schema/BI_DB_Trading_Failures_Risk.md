# Tableau Usage — BI_DB_Trading_Failures_Risk

> Generated by `tools/tableau/extract_table_metadata.py`. Do not edit by hand.

## Identity

- Full name: ``
- Database: `` ()
- Schema: ``
- Connection type: ``
- Tableau metadata id: ``

## Summary

- Custom SQL queries: **2**
- Downstream workbooks: **1**
- Downstream calculated fields: **0**

## Custom SQL queries referencing this table

### 1. Custom SQL Query

- Tableau id: `2b812f5b-75fe-04d8-fe7e-61c7e6dfb460`

```sql
SELECT bdtfr.Date,
    bdtfr.InstrumentID,
    bdtfr.ErrorCode,
    bdtfr.Regulation,
    bdtfr.Copy_Manual, 
    bdtfr.Type, 
    SUM(bdtfr.Orders_Positions) AS NumberOfTransactions
FROM BI_DB_dbo.BI_DB_Trading_Failures_Risk bdtfr
GROUP BY Date,
    bdtfr.InstrumentID,
    bdtfr.ErrorCode,
    bdtfr.Regulation,
    bdtfr.Copy_Manual, 
    bdtfr.Type
```

### 2. Custom SQL Query

- Tableau id: `3d594c90-fb8a-2613-9995-65d21be375cb`

```sql
SELECT bdtfr.ErrorCode,
    bdtfr.Regulation,
    bdtfr.InstrumentID,
    bdtfr.Copy_Manual, 
    bdtfr.Type, 
    SUM(CASE WHEN bdtfr.Date = DATEADD(DAY, -1, CAST(GETDATE() AS DATE)) THEN bdtfr.Orders_Positions ELSE 0 END) AS Yesterday,
    SUM(CASE WHEN bdtfr.Date >= DATEADD(DAY, 1 - DATEPART(WEEKDAY, GETDATE()), CAST(GETDATE() AS DATE)) THEN bdtfr.Orders_Positions ELSE 0 END) AS WeekToDate,
    SUM(CASE WHEN bdtfr.Date >= DATEADD(DAY, 1 - DAY(GETDATE()), CAST(GETDATE() AS DATE)) THEN bdtfr.Orders_Positions ELSE 0 END) AS MonthToDate,
    SUM(CASE WHEN bdtfr.Date >= DATEADD(QUARTER, DATEDIFF(QUARTER, 0, GETDATE()), 0) THEN bdtfr.Orders_Positions ELSE 0 END) AS QuarterToDate
FROM BI_DB_dbo.BI_DB_Trading_Failures_Risk bdtfr
GROUP BY 
    bdtfr.ErrorCode,
    bdtfr.Regulation,
    bdtfr.InstrumentID,
    bdtfr.Copy_Manual, 
    bdtfr.Type
```

## Downstream workbooks

_Attribution: 0 via direct table-drag, 1 via custom SQL (workbooks can use both paths)._

| # | Workbook | Project | Owner | Via | Last updated | URL |
|---|---|---|---|---|---|---|
| 1 | Latency & Slippage Summary Report | Archive | Adar Cahlon | custom_sql | 2025-01-21T09:18:26Z | [open](https://reports.etorocorp.com/#/workbooks/LatencySlippageSummaryReport) |

### View URLs (per workbook)

- **Latency & Slippage Summary Report** (Archive)
  - [Quality of Execution Summary](https://reports.etorocorp.com/#/views/LatencySlippageSummaryReport/QualityofExecutionSummary)
  - [Raw Data](https://reports.etorocorp.com/#/views/LatencySlippageSummaryReport/RawData)
  - [Gil Daily New Report- Total Latency for Shiri](https://reports.etorocorp.com/#/views/LatencySlippageSummaryReport/GilDailyNewReport-TotalLatencyforShiri)
  - [Daily New Report- Total Slippage (2)](https://reports.etorocorp.com/#/views/LatencySlippageSummaryReport/DailyNewReport-TotalSlippage2)

## Downstream calculated fields (in embedded datasources)

_None._