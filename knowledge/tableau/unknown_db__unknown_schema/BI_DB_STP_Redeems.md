# Tableau Usage — BI_DB_STP_Redeems

> Generated by `tools/tableau/extract_table_metadata.py`. Do not edit by hand.

## Identity

- Full name: ``
- Database: `` ()
- Schema: ``
- Connection type: ``
- Tableau metadata id: ``

## Summary

- Custom SQL queries: **5**
- Downstream workbooks: **5**
- Downstream calculated fields: **0**

## Custom SQL queries referencing this table

### 1. Custom SQL Query

- Tableau id: `18626a79-383f-4c03-b21c-dc3097b5e8d8`

```sql
select 
distinct stp.RedeemID,
stp.CID,
stp.AmountOnRequest,
stp.LastModificationDate,
stp.RedeemStatus,
stp.OPSApproved,
stp.RiskApproved,
stp.TradingApproved,
stp.AMLApproved,
stp.AmdinistratorsApproved,
stp.Approval,
stp.PlayerLevel,
stp.Regulation,
stp.ExecutionApproval,
sum(stp.Units) as Units
, left(di.Name,3) AS Coin, fbr.RequestDate
FROM BI_DB_dbo.BI_DB_STP_Redeems stp
JOIN DWH_dbo.Fact_BillingRedeem fbr ON fbr.RedeemID=stp.RedeemID
JOIN DWH_dbo.Dim_Position dp ON dp.PositionID=fbr.PositionID
JOIN DWH_dbo.Dim_Instrument di ON di.InstrumentID=dp.InstrumentID
group by
stp.RedeemID,
stp.CID,
stp.AmountOnRequest,
stp.LastModificationDate,
stp.RedeemStatus,
stp.OPSApproved,
stp.RiskApproved,
stp.TradingApproved,
stp.AMLApproved,
stp.AmdinistratorsApproved,
stp.Approval,
stp.PlayerLevel,
stp.Regulation,
stp.ExecutionApproval
, left(di.Name,3), fbr.RequestDate
```

### 2. Custom SQL Query

- Tableau id: `557a7ea6-af08-4cfd-655c-bc1b88562ce6`

```sql
select r.*,
country.Name AS Country
FROM BI_DB.dbo.BI_DB_STP_Redeems r
LEFT JOIN DWH.dbo.Dim_Customer c ON c.RealCID=r.CID
LEFT JOIN DWH.dbo.Dim_Country country ON country.CountryID=c.CountryID
```

### 3. Custom SQL Query

- Tableau id: `84907a0a-b402-8cf9-635a-ef424baafbcb`

```sql
select 
distinct stp.RedeemID,
stp.CID,
stp.AmountOnRequest,
stp.LastModificationDate,
stp.RedeemStatus,
MAX(CASE WHEN stp.OPSApproved=1 THEN 1 ELSE 0 END) AS OPSApproved,
MAX(CASE WHEN stp.RiskApproved=1 THEN 1 ELSE 0 END) AS RiskApproved,
MAX(CASE WHEN stp.TradingApproved=1 THEN 1 ELSE 0 END) AS TradingApproved,
MAX(CASE WHEN stp.AMLApproved=1 THEN 1 ELSE 0 END) AS AMLApproved,
MAX(CASE WHEN stp.AmdinistratorsApproved=1 THEN 1 ELSE 0 END) AS AmdinistratorsApproved,
stp.Approval,
stp.PlayerLevel,
stp.Regulation,
MAX ( CASE WHEN wtf.ManagerID=0 and wtf.CashoutStatusID=3 then 'Auto' else 'Manual' END) as ExecutionApproval,
MAX(stp.Units) as Units
, left(di.Name,3) AS Coin, fbr.RequestDate
FROM BI_DB_dbo.BI_DB_STP_Redeems stp
JOIN DWH_dbo.Fact_BillingRedeem fbr ON fbr.RedeemID=stp.RedeemID
JOIN DWH_dbo.Dim_Position dp ON dp.PositionID=fbr.PositionID
JOIN DWH_dbo.Dim_Instrument di ON di.InstrumentID=dp.InstrumentID
left join [BI_DB_dbo].[External_etoro_Billing_Redeem] red on red.RedeemID=stp.RedeemID
LEFT JOIN BI_DB_dbo.External_etoro_BackOffice_RedeemApproval R ON R.RedeemID=red.RedeemID
left join [BI_DB_dbo].[External_etoro_Billing_vWithdrawToFunding] wtf on red.WithdrawToFundingID=wtf.ID
--where stp.RedeemID=1176597 
group by
stp.RedeemID,
stp.CID,
stp.AmountOnRequest,
stp.LastModificationDate,
stp.RedeemStatus,
stp.Approval,
stp.PlayerLevel,
stp.Regulation
, left(di.Name,3), fbr.RequestDate
```

### 4. Custom SQL Query

- Tableau id: `8a8b9bc1-bcdb-663b-b246-d844ed874808`

```sql
SELECT 
    stp.RedeemID,
    stp.CID,
    stp.AmountOnRequest,
    stp.LastModificationDate,
    stp.RedeemStatus,
    MAX(CASE WHEN stp.OPSApproved = 1 THEN 1 ELSE 0 END) AS OPSApproved,
    MAX(CASE WHEN stp.RiskApproved = 1 THEN 1 ELSE 0 END) AS RiskApproved,
    MAX(CASE WHEN stp.TradingApproved = 1 THEN 1 ELSE 0 END) AS TradingApproved,
    MAX(CASE WHEN stp.AMLApproved = 1 THEN 1 ELSE 0 END) AS AMLApproved,
    MAX(CASE WHEN stp.AmdinistratorsApproved = 1 THEN 1 ELSE 0 END) AS AmdinistratorsApproved,
    stp.Approval,
    stp.PlayerLevel,
    stp.Regulation,
    case when wtf.ManagerID=0 and wtf.CashoutStatusID=3 then 'Auto' else 'Manual' end as ExecutionApproval,
    MAX(stp.Units) AS Units,
    LEFT(di.Name, 3) AS Coin,
    fbr.RequestDate
FROM 
    BI_DB_dbo.BI_DB_STP_Redeems stp
JOIN 
    DWH_dbo.Fact_BillingRedeem fbr ON fbr.RedeemID = stp.RedeemID
JOIN 
    DWH_dbo.Dim_Position dp ON dp.PositionID = fbr.PositionID
JOIN 
    DWH_dbo.Dim_Instrument di ON di.InstrumentID = dp.InstrumentID
 join  [BI_DB_dbo].[External_etoro_Billing_Redeem] red  on red.RedeemID=stp.RedeemID
left join [BI_DB_dbo].[External_etoro_Billing_vWithdrawToFunding] wtf on red.WithdrawToFundingID=wtf.ID
GROUP BY 
    stp.RedeemID,
    stp.CID,
    stp.AmountOnRequest,
    stp.LastModificationDate,
    stp.RedeemStatus,
    stp.Approval,
    stp.PlayerLevel,
    stp.Regulation,
    LEFT(di.Name, 3),
    fbr.RequestDate,case when wtf.ManagerID=0 and wtf.CashoutStatusID=3 then 'Auto' else 'Manual' end
```

### 5. Custom SQL Query

- Tableau id: `95b3e81d-c445-b6a2-a327-8f011ebc7cec`

```sql
SELECT 
    stp.RedeemID,
    stp.CID,
    stp.AmountOnRequest,
    stp.LastModificationDate,
    stp.RedeemStatus,
    MAX(CASE WHEN stp.OPSApproved = 1 THEN 1 ELSE 0 END) AS OPSApproved,
    MAX(CASE WHEN stp.RiskApproved = 1 THEN 1 ELSE 0 END) AS RiskApproved,
    MAX(CASE WHEN stp.TradingApproved = 1 THEN 1 ELSE 0 END) AS TradingApproved,
    MAX(CASE WHEN stp.AMLApproved = 1 THEN 1 ELSE 0 END) AS AMLApproved,
    MAX(CASE WHEN stp.AmdinistratorsApproved = 1 THEN 1 ELSE 0 END) AS AmdinistratorsApproved,
    case when R.Comment is NULL then 'Auto Approval' else stp.Approval end as Approval,
    stp.PlayerLevel,
    stp.Regulation,
    case when wtf.ManagerID=0 and wtf.CashoutStatusID=3 then 'Auto' else 'Manual' end as ExecutionApproval,
    MAX(stp.Units) AS Units,
    LEFT(di.Name, 3) AS Coin,
    fbr.RequestDate
FROM 
    BI_DB_dbo.BI_DB_STP_Redeems stp
JOIN 
    DWH_dbo.Fact_BillingRedeem fbr ON fbr.RedeemID = stp.RedeemID
JOIN 
    DWH_dbo.Dim_Position dp ON dp.PositionID = fbr.PositionID
JOIN 
    DWH_dbo.Dim_Instrument di ON di.InstrumentID = dp.InstrumentID
 join  [BI_DB_dbo].[External_etoro_Billing_Redeem] red  on red.RedeemID=stp.RedeemID
left join [BI_DB_dbo].[External_etoro_Billing_vWithdrawToFunding] wtf on red.WithdrawToFundingID=wtf.ID
LEFT JOIN 
	BI_DB_dbo.External_etoro_BackOffice_RedeemApproval R ON R.RedeemID=stp.RedeemID
GROUP BY 
    stp.RedeemID,
    stp.CID,
    stp.AmountOnRequest,
    stp.LastModificationDate,
    stp.RedeemStatus,
    case when R.Comment is NULL then 'Auto Approval' else stp.Approval end,
    stp.PlayerLevel,
    stp.Regulation,
    LEFT(di.Name, 3),
    fbr.RequestDate,case when wtf.ManagerID=0 and wtf.CashoutStatusID=3 then 'Auto' else 'Manual' end
```

## Downstream workbooks

_Attribution: 0 via direct table-drag, 5 via custom SQL (workbooks can use both paths)._

| # | Workbook | Project | Owner | Via | Last updated | URL |
|---|---|---|---|---|---|---|
| 1 | Redeems STP | 2.6.24 migration | Pavlina Masoura | custom_sql | 2024-06-02T16:45:44Z | [open](https://reports.etorocorp.com/#/workbooks/RedeemsSTP) |
| 2 | OPS Monthly Report - Redeems | Redeems | Georgios Kyriakou | custom_sql | 2026-05-31T10:05:50Z | [open](https://reports.etorocorp.com/#/workbooks/OPSMonthlyReport-Redeems) |
| 3 | OPS Presentation for Management | Unused Content | Izmini Nicolaou | custom_sql | 2024-02-20T08:23:24Z | [open](https://reports.etorocorp.com/#/workbooks/OPSPresentationforManagement) |
| 4 | Redeems STP - synapse WIP | Unused Content | Pavlina Masoura | custom_sql | 2024-02-20T08:30:23Z | [open](https://reports.etorocorp.com/#/workbooks/RedeemsSTP-synapseWIP) |
| 5 | Redeems STP- synapse | Unused Content | Pavlina Masoura | custom_sql | 2024-04-17T10:32:19Z | [open](https://reports.etorocorp.com/#/workbooks/RedeemsSTP-synapse) |

### View URLs (per workbook)

- **Redeems STP** (2.6.24 migration)
  - [Redeems STP](https://reports.etorocorp.com/#/views/RedeemsSTP/RedeemsSTP)
  - [% Auto-Approval - Daily graph](https://reports.etorocorp.com/#/views/RedeemsSTP/Auto-Approval-Dailygraph)
  - [% Auto-Execution - Daily graph](https://reports.etorocorp.com/#/views/RedeemsSTP/Auto-Execution-Dailygraph)
  - [Omnibus  Consumption and Threshold](https://reports.etorocorp.com/#/views/RedeemsSTP/OmnibusConsumptionandThreshold)
  - [Threshold 14DAvg](https://reports.etorocorp.com/#/views/RedeemsSTP/Threshold14DAvg)
- **OPS Monthly Report - Redeems** (Redeems)
  - [Redeems](https://reports.etorocorp.com/#/views/OPSMonthlyReport-Redeems/Redeems)
  - [Redeems Hourly Monthly Analysis](https://reports.etorocorp.com/#/views/OPSMonthlyReport-Redeems/RedeemsHourlyMonthlyAnalysis)
  - [Redeems STP](https://reports.etorocorp.com/#/views/OPSMonthlyReport-Redeems/RedeemsSTP)
  - [Redeems % Auto-Approval - Daily graph](https://reports.etorocorp.com/#/views/OPSMonthlyReport-Redeems/RedeemsAuto-Approval-Dailygraph)
  - [Redeems % Auto-Execution - Daily graph](https://reports.etorocorp.com/#/views/OPSMonthlyReport-Redeems/RedeemsAuto-Execution-Dailygraph)
- **OPS Presentation for Management** (Unused Content)
  - [Cashout STP](https://reports.etorocorp.com/#/views/OPSPresentationforManagement/CashoutSTP)
  - [Redeems STP](https://reports.etorocorp.com/#/views/OPSPresentationforManagement/RedeemsSTP)
  - [Docs STP](https://reports.etorocorp.com/#/views/OPSPresentationforManagement/DocsSTP)
  - [% STP Onboarding ](https://reports.etorocorp.com/#/views/OPSPresentationforManagement/STPOnboarding)
- **Redeems STP - synapse WIP** (Unused Content)
  - [Redeems STP](https://reports.etorocorp.com/#/views/RedeemsSTP-synapseWIP/RedeemsSTP)
  - [% Auto-Approval - Daily graph](https://reports.etorocorp.com/#/views/RedeemsSTP-synapseWIP/Auto-Approval-Dailygraph)
  - [% Auto-Execution - Daily graph](https://reports.etorocorp.com/#/views/RedeemsSTP-synapseWIP/Auto-Execution-Dailygraph)
  - [Omnibus  Consumption and Threshold](https://reports.etorocorp.com/#/views/RedeemsSTP-synapseWIP/OmnibusConsumptionandThreshold)
  - [Threshold 14DAvg](https://reports.etorocorp.com/#/views/RedeemsSTP-synapseWIP/Threshold14DAvg)
- **Redeems STP- synapse** (Unused Content)
  - [Redeems STP](https://reports.etorocorp.com/#/views/RedeemsSTP-synapse/RedeemsSTP)
  - [% Auto-Approval - Daily graph](https://reports.etorocorp.com/#/views/RedeemsSTP-synapse/Auto-Approval-Dailygraph)
  - [% Auto-Execution - Daily graph](https://reports.etorocorp.com/#/views/RedeemsSTP-synapse/Auto-Execution-Dailygraph)
  - [Omnibus  Consumption and Threshold](https://reports.etorocorp.com/#/views/RedeemsSTP-synapse/OmnibusConsumptionandThreshold)
  - [Threshold 14DAvg](https://reports.etorocorp.com/#/views/RedeemsSTP-synapse/Threshold14DAvg)

## Downstream calculated fields (in embedded datasources)

_None._