# Tableau Usage — Dealing_Duco_EODRecon

> Generated by `tools/tableau/extract_table_metadata.py`. Do not edit by hand.

## Identity

- Full name: ``
- Database: `` ()
- Schema: ``
- Connection type: ``
- Tableau metadata id: ``

## Summary

- Custom SQL queries: **8**
- Downstream workbooks: **2**
- Downstream calculated fields: **0**

## Custom SQL queries referencing this table

### 1. Custom SQL Query

- Tableau id: `1c778174-f2a7-0f8e-dd73-43dae1ccd463`

```sql
SELECT EOD.Date,DATENAME(Month, EOD.[Date]) [Period], Sum(ABS(EOD.eToroUSDAmount/Conv.rate)) M_NOP
from [AZR-WE-DWH-02].[Dealing].[dbo].[Dealing_Duco_EODRecon] EOD

left  join (
			select  *
			,concat(datepart(yy,reporttime),datepart(mm,reporttime)) YearMm
			from
				(select  * ,row_number() over (partition by concat(datepart(yy,reporttime),datepart(mm,reporttime)) order by reporttime desc) RN
				FROM [RegReportDB_Prod].[dbo].[Reg_Py_eurofxref_daily]
				where to_currency='USD') aa
				where RN=1
			) Conv  on Conv.YearMm =concat(datepart(yy,EOD.Date),datepart(mm,EOD.Date))

JOIN [Reg_Instruments_SCD] SCD on EOD.InstrumentID = SCD.InstrumentID
--Where EOD.Date = EOMONTH(Date)
Where EOD.Date in ('2022-07-29','2022-08-31','2022-09-30','2022-10-31','2022-11-30',
'2022-12-30','2023-01-31','2023-02-28','2023-03-31','2023-04-28','2023-05-30','2023-06-30')
AND LiquidityAccountName not like '%Real%'
AND LiquidityAccountName not like '%REAL%'
AND SCD.InstrumentTypeID = '1' 
AND EOD.Date between SCD.ValidFrom and SCD.ValidTo
--AND EOD.Date > '2022-06-30'
--AND EOD.Date <= '2023-06-30'
Group By EOD.Date
UNION
select '9999-12-31' as [Date]
     , 'Average' [Period]
    ,avg(A.M_NOP) as AVG_M_NOP
    from
	(SELECT EOD.Date,DATENAME(Month, EOD.[Date]) [Period], Sum(ABS(EOD.eToroUSDAmount/Conv.rate)) M_NOP
from [AZR-WE-DWH-02].[Dealing].[dbo].[Dealing_Duco_EODRecon] EOD

left  join (
			select  *
			,concat(datepart(yy,reporttime),datepart(mm,reporttime)) YearMm
			from
				(select  * ,row_number() over (partition by concat(datepart(yy,reporttime),datepart(mm,reporttime)) order by reporttime desc) RN
				FROM [RegReportDB_Prod].[dbo].[Reg_Py_eurofxref_daily]
				where to_currency='USD') aa
				where RN=1
			) Conv  on Conv.YearMm =concat(datepart(yy,EOD.Date),datepart(mm,EOD.Date))

LEFT JOIN [Reg_Instruments_SCD] SCD on EOD.InstrumentID = SCD.InstrumentID
--Where EOD.Date = EOMONTH(Date)
Where EOD.Date in ('2022-07-29','2022-08-31','2022-09-30','2022-10-31','2022-11-30',
'2022-12-30','2023-01-31','2023-02-28','2023-03-31','2023-04-28','2023-05-30','2023-06-30')
AND LiquidityAccountName not like '%Real%'
AND LiquidityAccountName not like '%REAL%'
AND SCD.InstrumentTypeID = '1'
AND EOD.Date between SCD.ValidFrom and SCD.ValidTo
--AND Date > '2022-06-30'
--AND Date <= '2023-06-30'
Group By EOD.Date) as A
```

### 2. Custom SQL Query

- Tableau id: `1f8fcbaa-ccfc-94e6-1c31-d3e2b4854108`

```sql
SELECT EOD.Date,DATENAME(Month, EOD.[Date]) [Period], Sum(ABS(EOD.eToroUSDAmount/Conv.rate)) M_NOP
from [AZR-WE-DWH-02].[Dealing].[dbo].[Dealing_Duco_EODRecon] EOD

left  join (
			select  *
			,concat(datepart(yy,reporttime),datepart(mm,reporttime)) YearMm
			from
				(select  * ,row_number() over (partition by concat(datepart(yy,reporttime),datepart(mm,reporttime)) order by reporttime desc) RN
				FROM [RegReportDB_Prod].[dbo].[Reg_Py_eurofxref_daily]
				where to_currency='USD') aa
				where RN=1
			) Conv  on Conv.YearMm =concat(datepart(yy,EOD.Date),datepart(mm,EOD.Date))

JOIN [Reg_Instruments_SCD] SCD on EOD.InstrumentID = SCD.InstrumentID
--Where EOD.Date = EOMONTH(Date)
Where EOD.Date in ('2022-07-29','2022-08-31','2022-09-30','2022-10-31','2022-11-30',
'2022-12-30','2023-01-31','2023-02-28','2023-03-31','2023-04-28','2023-05-30','2023-06-30')
AND LiquidityAccountName not like '%Real%'
AND LiquidityAccountName not like '%REAL%'
AND SCD.InstrumentTypeID in (2,10)
AND EOD.Date between SCD.ValidFrom and SCD.ValidTo
--AND EOD.Date > '2022-06-30'
--AND EOD.Date <= '2023-06-30'
Group By EOD.Date
UNION
select '9999-12-31' as [Date]
     , 'Average' [Period]
    ,avg(A.M_NOP) as AVG_M_NOP
    from
	(SELECT EOD.Date,DATENAME(Month, EOD.[Date]) [Period], Sum(ABS(EOD.eToroUSDAmount/Conv.rate)) M_NOP
from [AZR-WE-DWH-02].[Dealing].[dbo].[Dealing_Duco_EODRecon] EOD

left  join (
			select  *
			,concat(datepart(yy,reporttime),datepart(mm,reporttime)) YearMm
			from
				(select  * ,row_number() over (partition by concat(datepart(yy,reporttime),datepart(mm,reporttime)) order by reporttime desc) RN
				FROM [RegReportDB_Prod].[dbo].[Reg_Py_eurofxref_daily]
				where to_currency='USD') aa
				where RN=1
			) Conv  on Conv.YearMm =concat(datepart(yy,EOD.Date),datepart(mm,EOD.Date))

LEFT JOIN [Reg_Instruments_SCD] SCD on EOD.InstrumentID = SCD.InstrumentID
--Where EOD.Date = EOMONTH(Date)
Where EOD.Date in ('2022-07-29','2022-08-31','2022-09-30','2022-10-31','2022-11-30',
'2022-12-30','2023-01-31','2023-02-28','2023-03-31','2023-04-28','2023-05-30','2023-06-30')
AND LiquidityAccountName not like '%Real%'
AND LiquidityAccountName not like '%REAL%'
AND SCD.InstrumentTypeID in (2,10)
AND EOD.Date between SCD.ValidFrom and SCD.ValidTo
--AND Date > '2022-06-30'
--AND Date <= '2023-06-30'
Group By EOD.Date) as A
```

### 3. Custom SQL Query

- Tableau id: `57dd5149-e34c-a4b3-cd83-322b06510f23`

```sql
SELECT 
EOD.Date,
DATENAME(Month, EOD.[Date]) [Period], 
SCD.InstrumentTypeID,
Sum(ABS(EOD.eToroUSDAmount/Conv.rate)) M_NOP
from [AZR-WE-DWH-02].[Dealing].[dbo].[Dealing_Duco_EODRecon] EOD

left join (
select *
,concat(datepart(yy,reporttime),datepart(mm,reporttime)) YearMm
from
(select * ,row_number() over (partition by concat(datepart(yy,reporttime),datepart(mm,reporttime)) order by reporttime desc) RN
FROM [RegReportDB_Prod].[dbo].[Reg_Py_eurofxref_daily]
where to_currency='USD') aa
where RN=1
) Conv on Conv.YearMm =concat(datepart(yy,EOD.Date),datepart(mm,EOD.Date))

JOIN [Reg_Instruments_SCD] SCD on EOD.InstrumentID = SCD.InstrumentID
Where EOD.Date in ('2022-07-29','2022-08-31','2022-09-30','2022-10-31','2022-11-30',
'2022-12-30','2023-01-31','2023-02-28','2023-03-31','2023-04-28','2023-05-31','2023-06-30')
AND LiquidityAccountName not like '%Real%'
AND LiquidityAccountName not like '%REAL%'
--AND SCD.InstrumentTypeID in (4,5,6)
AND EOD.Date between SCD.ValidFrom and SCD.ValidTo
Group By EOD.Date,SCD.InstrumentTypeID
```

### 4. Custom SQL Query

- Tableau id: `6bf0e400-ee4b-bd8a-23e5-0c3c7dc6d1a6`

```sql
SELECT [Date]
     
      ,[LP]
  ,count(distinct [InstrumentID]) eToro_Positions_EOD
  FROM [Dealing_dbo].[Dealing_Duco_EODRecon]
 where [Date] >= cast (Getdate () -10 as Date)
and LP <> ''
 and LP in ('FXCM','FD NDFs ToroHedge22 Deals','Global Prime','GS','GS COMMOD','IB CFD','IG','JPM','MarketMaker Direct CFDbook Real','Saxo CFD','Saxo FX&Commod','UBS','EDF')
and LiquidityAccountID is not null
group by [Date]
     
      ,[LP]
```

### 5. Custom SQL Query

- Tableau id: `747b8d71-3e0e-87a7-aee3-b1a5d81dadac`

```sql
SELECT EOD.Date,DATENAME(Month, EOD.[Date]) [Period], Sum(ABS(EOD.eToroUSDAmount/Conv.rate)) M_NOP
from [AZR-WE-DWH-02].[Dealing].[dbo].[Dealing_Duco_EODRecon] EOD

left  join (
			select  *
			,concat(datepart(yy,reporttime),datepart(mm,reporttime)) YearMm
			from
				(select  * ,row_number() over (partition by concat(datepart(yy,reporttime),datepart(mm,reporttime)) order by reporttime desc) RN
				FROM [RegReportDB_Prod].[dbo].[Reg_Py_eurofxref_daily]
				where to_currency='USD') aa
				where RN=1
			) Conv  on Conv.YearMm =concat(datepart(yy,EOD.Date),datepart(mm,EOD.Date))

JOIN [Reg_Instruments_SCD] SCD on EOD.InstrumentID = SCD.InstrumentID
--Where EOD.Date = EOMONTH(Date)
Where EOD.Date in ('2022-07-29','2022-08-31','2022-09-30','2022-10-31','2022-11-30',
'2022-12-30','2023-01-31','2023-02-28','2023-03-31','2023-04-28','2023-05-30','2023-06-30')
AND LiquidityAccountName not like '%Real%'
AND LiquidityAccountName not like '%REAL%'
AND SCD.InstrumentTypeID in (4,5,6)
AND EOD.Date between SCD.ValidFrom and SCD.ValidTo
--AND EOD.Date > '2022-06-30'
--AND EOD.Date <= '2023-06-30'
Group By EOD.Date
UNION
select '9999-12-31' as [Date]
     , 'Average' [Period]
    ,avg(A.M_NOP) as AVG_M_NOP
    from
	(SELECT EOD.Date,DATENAME(Month, EOD.[Date]) [Period], Sum(ABS(EOD.eToroUSDAmount/Conv.rate)) M_NOP
from [AZR-WE-DWH-02].[Dealing].[dbo].[Dealing_Duco_EODRecon] EOD

left  join (
			select  *
			,concat(datepart(yy,reporttime),datepart(mm,reporttime)) YearMm
			from
				(select  * ,row_number() over (partition by concat(datepart(yy,reporttime),datepart(mm,reporttime)) order by reporttime desc) RN
				FROM [RegReportDB_Prod].[dbo].[Reg_Py_eurofxref_daily]
				where to_currency='USD') aa
				where RN=1
			) Conv  on Conv.YearMm =concat(datepart(yy,EOD.Date),datepart(mm,EOD.Date))

LEFT JOIN [Reg_Instruments_SCD] SCD on EOD.InstrumentID = SCD.InstrumentID
--Where EOD.Date = EOMONTH(Date)
Where EOD.Date in ('2022-07-29','2022-08-31','2022-09-30','2022-10-31','2022-11-30',
'2022-12-30','2023-01-31','2023-02-28','2023-03-31','2023-04-28','2023-05-30','2023-06-30')
AND LiquidityAccountName not like '%Real%'
AND LiquidityAccountName not like '%REAL%'
AND SCD.InstrumentTypeID in (4,5,6)
AND EOD.Date between SCD.ValidFrom and SCD.ValidTo
--AND Date > '2022-06-30'
--AND Date <= '2023-06-30'
Group By EOD.Date) as A
```

### 6. Custom SQL Query

- Tableau id: `857ac2b9-4db9-a09f-6141-0cd5c347f114`

```sql
SELECT EOD.Date,DATENAME(Month, EOD.[Date]) [Period], Sum(ABS(EOD.eToroUSDAmount/Conv.rate)) M_NOP, SCD.InstrumentTypeID
from [AZR-WE-DWH-02].[Dealing].[dbo].[Dealing_Duco_EODRecon] EOD

left  join (
			select  *
			,concat(datepart(yy,reporttime),datepart(mm,reporttime)) YearMm
			from
				(select  * ,row_number() over (partition by concat(datepart(yy,reporttime),datepart(mm,reporttime)) order by reporttime desc) RN
				FROM [RegReportDB_Prod].[dbo].[Reg_Py_eurofxref_daily]
				where to_currency='USD') aa
				where RN=1
			) Conv  on Conv.YearMm =concat(datepart(yy,EOD.Date),datepart(mm,EOD.Date))

JOIN [Reg_Instruments_SCD] SCD on EOD.InstrumentID = SCD.InstrumentID
--Where EOD.Date = EOMONTH(Date)
Where EOD.Date in ('2023-07-31','2023-08-31','2023-09-29','2023-10-31','2023-11-30',
'2023-12-29','2024-01-31','2024-02-29','2024-03-29','2024-04-30','2024-05-31','2024-06-28')
AND LiquidityAccountName not like '%Real%'
AND LiquidityAccountName not like '%REAL%'
AND SCD.InstrumentID in  (312,313,314)
AND EOD.Date between SCD.ValidFrom and SCD.ValidTo
--AND EOD.Date > '2022-06-30'
--AND EOD.Date <= '2023-06-30'
Group By EOD.Date ,SCD.InstrumentTypeID
UNION
select '9999-12-31' as [Date]
     , 'Average' [Period]
    ,avg(A.M_NOP) as AVG_M_NOP, InstrumentTypeID
    from
	(SELECT EOD.Date,DATENAME(Month, EOD.[Date]) [Period], Sum(ABS(EOD.eToroUSDAmount/Conv.rate)) M_NOP,SCD.InstrumentTypeID
from [AZR-WE-DWH-02].[Dealing].[dbo].[Dealing_Duco_EODRecon] EOD

left  join (
			select  *
			,concat(datepart(yy,reporttime),datepart(mm,reporttime)) YearMm
			from
				(select  * ,row_number() over (partition by concat(datepart(yy,reporttime),datepart(mm,reporttime)) order by reporttime desc) RN
				FROM [RegReportDB_Prod].[dbo].[Reg_Py_eurofxref_daily]
				where to_currency='USD') aa
				where RN=1
			) Conv  on Conv.YearMm =concat(datepart(yy,EOD.Date),datepart(mm,EOD.Date))

LEFT JOIN [Reg_Instruments_SCD] SCD on EOD.InstrumentID = SCD.InstrumentID
--Where EOD.Date = EOMONTH(Date)
Where EOD.Date in  ('2023-07-31','2023-08-31','2023-09-29','2023-10-31','2023-11-30',
'2023-12-29','2024-01-31','2024-02-29','2024-03-29','2024-04-30','2024-05-31','2024-06-28')
AND LiquidityAccountName not like '%Real%'
AND LiquidityAccountName not like '%REAL%'
AND SCD.InstrumentID in  (312,313,314)
AND EOD.Date between SCD.ValidFrom and SCD.ValidTo
--AND Date > '2022-06-30'
--AND Date <= '2023-06-30'
Group By EOD.Date, SCD.InstrumentTypeID) as A
Group By A.InstrumentTypeID
```

### 7. Custom SQL Query

- Tableau id: `9e79133f-fde8-0eab-6ec8-5e85404def20`

```sql
SELECT EOD.Date,DATENAME(Month, EOD.[Date]) [Period], Sum(ABS(EOD.eToroUSDAmount/Conv.rate)) M_NOP
from [AZR-WE-DWH-02].[Dealing].[dbo].[Dealing_Duco_EODRecon] EOD

left  join (
			select  *
			,concat(datepart(yy,reporttime),datepart(mm,reporttime)) YearMm
			from
				(select  * ,row_number() over (partition by concat(datepart(yy,reporttime),datepart(mm,reporttime)) order by reporttime desc) RN
				FROM [RegReportDB_Prod].[dbo].[Reg_Py_eurofxref_daily]
				where to_currency='USD') aa
				where RN=1
			) Conv  on Conv.YearMm =concat(datepart(yy,EOD.Date),datepart(mm,EOD.Date))

Where EOD.Date in ('2022-07-29','2022-08-31','2022-09-30','2022-10-31','2022-11-30',
'2022-12-30','2023-01-31','2023-02-28','2023-03-31','2023-04-28','2023-05-30','2023-06-30')
AND LiquidityAccountName not like '%Real%'
AND LiquidityAccountName not like '%REAL%'
AND InstrumentID in  (312,313,314)
Group By EOD.Date
UNION
select '9999-12-31' as [Date]
     , 'Average' [Period]
    ,avg(A.M_NOP) as AVG_M_NOP
    from
	(SELECT EOD.Date,DATENAME(Month, EOD.[Date]) [Period], Sum(ABS(EOD.eToroUSDAmount/Conv.rate)) M_NOP
from [AZR-WE-DWH-02].[Dealing].[dbo].[Dealing_Duco_EODRecon] EOD

left  join (
			select  *
			,concat(datepart(yy,reporttime),datepart(mm,reporttime)) YearMm
			from
				(select  * ,row_number() over (partition by concat(datepart(yy,reporttime),datepart(mm,reporttime)) order by reporttime desc) RN
				FROM [RegReportDB_Prod].[dbo].[Reg_Py_eurofxref_daily]
				where to_currency='USD') aa
				where RN=1
			) Conv  on Conv.YearMm =concat(datepart(yy,EOD.Date),datepart(mm,EOD.Date))

Where EOD.Date in ('2022-07-29','2022-08-31','2022-09-30','2022-10-31','2022-11-30',
'2022-12-30','2023-01-31','2023-02-28','2023-03-31','2023-04-28','2023-05-30','2023-06-30')
AND LiquidityAccountName not like '%Real%'
AND LiquidityAccountName not like '%REAL%'
AND InstrumentID in  (312,313,314)
Group By EOD.Date) as A
```

### 8. Custom SQL Query

- Tableau id: `a5bb9718-770c-bcd1-2f73-bf8d3613e553`

```sql
SELECT EOD.Date,DATENAME(Month, EOD.[Date]) [Period], Sum(ABS(EOD.eToroUSDAmount/Conv.rate)) M_NOP, SCD.InstrumentTypeID
from [AZR-WE-DWH-02].[Dealing].[dbo].[Dealing_Duco_EODRecon] EOD

left  join (
			select  *
			,concat(datepart(yy,reporttime),datepart(mm,reporttime)) YearMm
			from
				(select  * ,row_number() over (partition by concat(datepart(yy,reporttime),datepart(mm,reporttime)) order by reporttime desc) RN
				FROM [RegReportDB_Prod].[dbo].[Reg_Py_eurofxref_daily]
				where to_currency='USD') aa
				where RN=1
			) Conv  on Conv.YearMm =concat(datepart(yy,EOD.Date),datepart(mm,EOD.Date))

JOIN [Reg_Instruments_SCD] SCD on EOD.InstrumentID = SCD.InstrumentID
--Where EOD.Date = EOMONTH(Date)
Where EOD.Date in ('2023-07-31','2023-08-31','2023-09-29','2023-10-31','2023-11-30',
'2023-12-29','2024-01-31','2024-02-29','2024-03-29','2024-04-30','2024-05-31','2024-06-28')
AND LiquidityAccountName not like '%Real%'
AND LiquidityAccountName not like '%REAL%'
--AND SCD.InstrumentTypeID = '1' 
AND EOD.Date between SCD.ValidFrom and SCD.ValidTo
--AND EOD.Date > '2022-06-30'
--AND EOD.Date <= '2023-06-30'
Group By EOD.Date ,SCD.InstrumentTypeID
UNION
select '9999-12-31' as [Date]
     , 'Average' [Period]
    ,avg(A.M_NOP) as AVG_M_NOP, InstrumentTypeID
    from
	(SELECT EOD.Date,DATENAME(Month, EOD.[Date]) [Period], Sum(ABS(EOD.eToroUSDAmount/Conv.rate)) M_NOP,SCD.InstrumentTypeID
from [AZR-WE-DWH-02].[Dealing].[dbo].[Dealing_Duco_EODRecon] EOD

left  join (
			select  *
			,concat(datepart(yy,reporttime),datepart(mm,reporttime)) YearMm
			from
				(select  * ,row_number() over (partition by concat(datepart(yy,reporttime),datepart(mm,reporttime)) order by reporttime desc) RN
				FROM [RegReportDB_Prod].[dbo].[Reg_Py_eurofxref_daily]
				where to_currency='USD') aa
				where RN=1
			) Conv  on Conv.YearMm =concat(datepart(yy,EOD.Date),datepart(mm,EOD.Date))

LEFT JOIN [Reg_Instruments_SCD] SCD on EOD.InstrumentID = SCD.InstrumentID
--Where EOD.Date = EOMONTH(Date)
Where EOD.Date in  ('2023-07-31','2023-08-31','2023-09-29','2023-10-31','2023-11-30',
'2023-12-29','2024-01-31','2024-02-29','2024-03-29','2024-04-30','2024-05-31','2024-06-28')
AND LiquidityAccountName not like '%Real%'
AND LiquidityAccountName not like '%REAL%'
--AND SCD.InstrumentTypeID = '1'
AND EOD.Date between SCD.ValidFrom and SCD.ValidTo
--AND Date > '2022-06-30'
--AND Date <= '2023-06-30'
Group By EOD.Date, SCD.InstrumentTypeID) as A
Group By A.InstrumentTypeID
```

## Downstream workbooks

_Attribution: 0 via direct table-drag, 2 via custom SQL (workbooks can use both paths)._

| # | Workbook | Project | Owner | Via | Last updated | URL |
|---|---|---|---|---|---|---|
| 1 | EMIR Clearing Threshold | Global | Ahmad Fayad | custom_sql | 2025-08-19T08:56:04Z | [open](https://reports.etorocorp.com/#/workbooks/ExternalHedgeFlow-AssetsInstrumentsMonitor) |
| 2 | Transaction Reporting | Global | Nativ Jorge Makmel | custom_sql | 2025-08-19T08:52:22Z | [open](https://reports.etorocorp.com/#/workbooks/AuditvsTrAnaReconciliationDashboard) |

### View URLs (per workbook)

- **EMIR Clearing Threshold** (Global)
  - [External Hedge Flow](https://reports.etorocorp.com/#/views/ExternalHedgeFlow-AssetsInstrumentsMonitor/ExternalHedgeFlow_1)
  - [LPs By Asset](https://reports.etorocorp.com/#/views/ExternalHedgeFlow-AssetsInstrumentsMonitor/LPsByAsset)
  - [Clearing Threshold Dashboard 2022-2023](https://reports.etorocorp.com/#/views/ExternalHedgeFlow-AssetsInstrumentsMonitor/ClearingThresholdDashboard_1)
  - [Clearing Threshold Dashboard 2023-2024](https://reports.etorocorp.com/#/views/ExternalHedgeFlow-AssetsInstrumentsMonitor/ClearingThresholdDashboard2023-2024)
- **Transaction Reporting** (Global)
  - [T-R Consolidated View](https://reports.etorocorp.com/#/views/AuditvsTrAnaReconciliationDashboard/T-RConsolidatedView_1)
  - [Audit vs BestEx](https://reports.etorocorp.com/#/views/AuditvsTrAnaReconciliationDashboard/ConsolidatedAuditvsTrAnadashboard-BetaVersion)
  - [ASIC TR](https://reports.etorocorp.com/#/views/AuditvsTrAnaReconciliationDashboard/ASICTRDashboard_1)
  - [Field by Field Reconciliation](https://reports.etorocorp.com/#/views/AuditvsTrAnaReconciliationDashboard/FieldbyFieldReconciliation)
  - [MiFIR UK](https://reports.etorocorp.com/#/views/AuditvsTrAnaReconciliationDashboard/MiFIRUK)
  - [MiFIR UK Pending/Rejected UTIs](https://reports.etorocorp.com/#/views/AuditvsTrAnaReconciliationDashboard/MiFIRUKPendingRejectedUTIs)
  - [EMIR](https://reports.etorocorp.com/#/views/AuditvsTrAnaReconciliationDashboard/EMIREUDashboard_1)
  - [EMIR Rejected UTIs List](https://reports.etorocorp.com/#/views/AuditvsTrAnaReconciliationDashboard/EMIRRejectedUTIsList)
  - [MiFIR EU](https://reports.etorocorp.com/#/views/AuditvsTrAnaReconciliationDashboard/MiFIREU)
  - [MiFIR EU Pending/Rejected UTIs](https://reports.etorocorp.com/#/views/AuditvsTrAnaReconciliationDashboard/MiFIREUPendingRejectedUTIs)
  - [EMIR LP](https://reports.etorocorp.com/#/views/AuditvsTrAnaReconciliationDashboard/EMIRLP)
  - [Dashboard 9](https://reports.etorocorp.com/#/views/AuditvsTrAnaReconciliationDashboard/Dashboard9)

## Downstream calculated fields (in embedded datasources)

_None._