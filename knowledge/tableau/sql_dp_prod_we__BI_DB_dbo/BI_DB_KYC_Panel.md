# Tableau Usage — BI_DB_KYC_Panel

> Generated by `tools/tableau/extract_table_metadata.py`. Do not edit by hand.

## Identity

- Full name: `BI_DB_dbo.BI_DB_KYC_Panel`
- Database: `sql_dp_prod_we` (azure_sql_dw)
- Schema: `BI_DB_dbo`
- Connection type: `azure_sql_dw`
- Tableau metadata id: `c0c1eecc-bfd6-da3f-2e60-3cc46ae6e76d`

## Summary

- Custom SQL queries: **15**
- Downstream workbooks: **12**
- Downstream calculated fields: **278**

## Custom SQL queries referencing this table

### 1. KYC

- Tableau id: `0d11b9e7-9dee-8b18-7760-7a362b32adb0`

```sql
SELECT RealCID
	  ,CountryName Country
FROM BI_DB_dbo.BI_DB_KYC_Panel
group by RealCID
	  ,CountryName
```

### 2. Custom SQL Query

- Tableau id: `67afb62d-63ba-9841-96d9-2f8d4361fff1`

```sql
SELECT bdkp.RealCID
	  ,bdkp.Q10_AnswerText AS 'Annual_Income'
	  ,bdkp.Q11_AnswerText AS 'Liquid_Assets'
	  ,bdkp.Q9_AnswerText AS 'Risk_Apetite'
	  ,bdkp.Q14_AnswerText AS 'Planned_Invested_Amount'
	  ,bdkp.Q8_AnswerText AS 'Trading_Primary_Purpose'
FROM BI_DB_dbo.BI_DB_KYC_Panel bdkp
```

### 3. Custom SQL Query1

- Tableau id: `7be58d98-3ff8-817c-f7e4-308f1ecfd4c5`

```sql
SELECT a.RealCID
,a.GCID
,EOMONTH(dc.FirstDepositDate) 'EOM_FTD'
,a.CountryName
,a.Age_On_Reg
	  , CASE WHEN a.Age_On_Reg<25 THEN 'Low - 25'
	         WHEN a.Age_On_Reg<31 THEN '26 - 30'
			 WHEN a.Age_On_Reg<36 THEN '31 - 35'
			 WHEN a.Age_On_Reg<41 THEN '36 - 40'
			 WHEN a.Age_On_Reg<51 THEN '41 - 50'
			 WHEN a.Age_On_Reg<61 THEN '51 - 60'
			 WHEN a.Age_On_Reg>=61 THEN  '61+'
			 ELSE 'No age' END AS 'AgeGroup'
,a.Gender
,a.Q3_Is_Professional_Knowledge 
,a.Q23_Is_Assessment_Pass
,a.Experience_Level
,CASE WHEN ISNULL(a.Q10_AnswerID, 0) IN (34, 35, 0) THEN 'Up_to_$50K'
 WHEN ISNULL(a.Q10_AnswerID, 0) IN (36, 79) THEN '$50K-$200K'
ELSE 'Above_$200K' END AS 'Annual_Income'
,CASE WHEN ISNULL(a.Q11_AnswerID, 0) IN (34, 35, 0) THEN 'Up_to_$50K'
 WHEN ISNULL(a.Q11_AnswerID, 0) IN (36, 79) THEN '$50K-$200K'
ELSE 'Above_$200K' END AS 'Liquid_Assets'
,a.Q9_AnswerText 'Q9_Risk_Reward_Senario'
,a.Q14_AnswerText 'Q14_Planned_Invested_Amount'
,a.Q5_AnswerText 'Q5_Trading_Strategy'
,a.Q8_AnswerText 'Q8_Trading_Primary_Purpose'
      ,CASE WHEN EOMONTH(dc.FirstDepositDate)>=EOMONTH(bdoofuk.DateTime_VL3) AND EOMONTH(dc.FirstDepositDate)=EOMONTH(a.FirstAction_Date)
	  THEN 'FundedFTDmonth' ELSE  'NotFundedFTDMonth' END AS 'Funded_Ind'
	  ,bdoofuk.MarketingRegion
FROM BI_DB_dbo.BI_DB_KYC_Panel a
INNER JOIN DWH_dbo.Dim_Customer dc ON a.RealCID = dc.RealCID AND dc.IsDepositor=1
INNER JOIN BI_DB_dbo.BI_DB_Operations_Onboarding_Flow_UserKPIs bdoofuk ON bdoofuk.CID=a.RealCID
LEFT JOIN BI_DB_dbo.BI_DB_CIDFirstDates bdcd ON bdcd.CID=a.RealCID


--WHERE a.Reg_Month >= 202101
```

### 4. KYC

- Tableau id: `84943b2d-e7f4-eb00-4640-39d65ee6594f`

```sql
SELECT RealCID
	  ,CountryName Country
FROM BI_DB_dbo.BI_DB_KYC_Panel
group by RealCID
	  ,CountryName
```

### 5. Custom SQL Query1

- Tableau id: `a759132f-650a-a00b-48b2-d720a963c862`

```sql
SELECT a.RealCID
      ,a.GCID
      ,EOMONTH(dc.FirstDepositDate) 'EOM_FTD'   
      ,a.CountryName
      ,a.Age_On_Reg
	  , CASE WHEN a.Age_On_Reg<25 THEN 'Low - 25'
	         WHEN a.Age_On_Reg<31 THEN '26 - 30'
			 WHEN a.Age_On_Reg<36 THEN '31 - 35'
			 WHEN a.Age_On_Reg<41 THEN '36 - 40'
			 WHEN a.Age_On_Reg<51 THEN '41 - 50'
			 WHEN a.Age_On_Reg<61 THEN '51 - 60'
			 WHEN a.Age_On_Reg>=61 THEN  '61+'
			 ELSE 'No age' END AS 'AgeGroup'
      ,a.Gender
      ,a.Q3_Is_Professional_Knowledge 
      ,a.Q23_Is_Assessment_Pass
      ,a.Experience_Level
      ,CASE WHEN ISNULL(a.Q10_AnswerID, 0) IN (34, 35, 0) THEN 'Up_to_$50K'
            WHEN ISNULL(a.Q10_AnswerID, 0) IN (36, 79) THEN '$50K-$200K'
       ELSE 'Above_$200K' END AS 'Annual_Income'
      ,CASE WHEN ISNULL(a.Q11_AnswerID, 0) IN (34, 35, 0) THEN 'Up_to_$50K'
            WHEN ISNULL(a.Q11_AnswerID, 0) IN (36, 79) THEN '$50K-$200K'
       ELSE 'Above_$200K' END AS 'Liquid_Assets'
      ,a.Q9_AnswerText 'Q9_Risk_Reward_Senario'
      ,a.Q14_AnswerText 'Q14_Planned_Invested_Amount'
      ,a.Q5_AnswerText 'Q5_Trading_Strategy'
      ,a.Q8_AnswerText 'Q8_Trading_Primary_Purpose'
      ,CASE WHEN EOMONTH(dc.FirstDepositDate)>=EOMONTH(bdoofuk.DateTime_VL3) AND EOMONTH(dc.FirstDepositDate)=EOMONTH(a.FirstAction_Date)
	  THEN 'FundedFTDmonth' ELSE  'NotFundedFTDMonth' END AS 'Funded_Ind'
	  ,bdoofuk.MarketingRegion
FROM BI_DB_dbo.BI_DB_KYC_Panel a
INNER JOIN DWH_dbo.Dim_Customer dc ON a.RealCID = dc.RealCID AND dc.IsDepositor=1
INNER JOIN BI_DB_dbo.BI_DB_Operations_Onboarding_Flow_UserKPIs bdoofuk ON bdoofuk.CID=a.RealCID
LEFT JOIN BI_DB_dbo.BI_DB_CIDFirstDates bdcd ON bdcd.CID=a.RealCID


--WHERE a.Reg_Month >= 202101
```

### 6. Custom SQL Query

- Tableau id: `b8379a97-ac74-d23d-c0ca-d5c45a7c7aa3`

```sql
SELECT bdkp.RealCID,
		CASE WHEN bdkp.Q9_AnswerID IN (25,26,24,23) THEN 1 ELSE 0 END AS 'Is_RiskApetite_5/-3',
		CASE WHEN bdkp.Q10_AnswerID IN (79,35,34,80,36,81) THEN 1 ELSE 0 END AS 'Is_AnnualIncome_Upto10K',
		CASE WHEN bdkp.Q8_AnswerID IN (22) THEN 1 ELSE 0 END AS 'Is_TradingPurpose_SavingForHome'
FROM BI_DB_dbo.BI_DB_KYC_Panel bdkp
```

### 7. Custom SQL Query

- Tableau id: `e29ddbca-3776-b140-64e7-9c69276aeecc`

```sql
SELECT p.RealCID
	  ,p.RegisteredReal
          ,p.FirstDepositDate
	  ,p.FTD_YYMM
	  ,p.Country
	  ,p.EU
	  ,p.RegulationID
          ,p.Regulation
	  ,k.Q34_Experience_Crypto
	  ,k.Q34_AnswerText
	  ,k.Q34_AnswerID
	  ,k.Q35_Experience_CFDs
	  ,k.Q35_AnswerText
	  ,k.Q35_AnswerID
	  ,k.Q3_Trading_Knowledge
	  ,k.Q3_AnswerText
	  ,k.Q23_Assessment
	  ,k.Q23_Is_Assessment_Pass
	  ,k.Q23_AnswerText
	  ,k.Q23_AnswerID
	  ,k.Q8_Trading_Primary_Purpose
	  ,k.Q8_AnswerText
	  ,k.Q8_AnswerID
	  ,k.Q9_Risk_Reward_Scenario
	  ,k.Q9_AnswerText
	  ,k.Q9_AnswerID
	  ,k.Q10_Annual_Income
	  ,k.Q10_AnswerText
	  ,k.Q10_AnswerID
	  ,k.Q11_Liquid_Assets
	  ,k.Q11_AnswerText
	  ,k.Q11_AnswerID
          ,case when p.Country='Spain' and RegisteredReal>='20250320' then 'Futures' else 'CFD' end Product_Type
FROM (
		SELECT  DISTINCT dc.RealCID
			,dc.RegisteredReal
                        ,dc.FirstDepositDate
			,YEAR(dc.FirstDepositDate)*100+MONTH(dc.FirstDepositDate) FTD_YYMM
			,dc1.Name Country
			,dc1.EU
			,dc.RegulationID
                        ,dr.Name Regulation
	FROM DWH_dbo.Dim_Customer dc
	LEFT JOIN DWH_dbo.Dim_Country dc1
		ON dc.CountryID=dc1.CountryID
	LEFT JOIN DWH_dbo.Dim_Position dp
		ON dc.RealCID=dp.CID
        LEFT JOIN DWH_dbo.Dim_Regulation dr
		ON dr.DWHRegulationID=dc.RegulationID
	WHERE dc.FirstDepositDate>='20240101'
		AND dc1.EU=1
		AND dc.IsValidCustomer=1
		AND dc.RegulationID=1    -- CySEC
		AND dp.IsSettled=0
		AND dp.MirrorID=0
		AND dp.CloseDateID=0
		) p
LEFT JOIN (
				SELECT  bdkp.RealCID
	    ,bdkp.Q34_Experience_Crypto
		,bdkp.Q34_AnswerText
		,bdkp.Q34_AnswerID
		
		,bdkp.Q35_Experience_CFDs
		,bdkp.Q35_AnswerText
		,bdkp.Q35_AnswerID

		,bdkp.Q3_Trading_Knowledge
		,bdkp.Q3_AnswerText

		,bdkp.Q23_Assessment
		,bdkp.Q23_Is_Assessment_Pass
		,bdkp.Q23_AnswerText
		,bdkp.Q23_AnswerID

		,bdkp.Q8_Trading_Primary_Purpose
		,bdkp.Q8_AnswerText
		,bdkp.Q8_AnswerID

		,bdkp.Q9_Risk_Reward_Scenario
		,bdkp.Q9_AnswerText
		,bdkp.Q9_AnswerID

		,bdkp.Q10_Annual_Income
		,bdkp.Q10_AnswerText
		,bdkp.Q10_AnswerID

		,bdkp.Q11_Liquid_Assets
		,bdkp.Q11_AnswerText
		,bdkp.Q11_AnswerID
FROM BI_DB_dbo.BI_DB_KYC_Panel bdkp
	 ) k
	ON p.RealCID = k.RealCID
```

### 8. KYC

- Tableau id: `386524a2-64f2-1652-09d0-39d423716bf7`

```sql
SELECT RealCID
	  ,Q3_Trading_Knowledge
	  ,Q3_Is_Professional_Knowledge
	  ,Q3_AnswerText
	  ,Q23_Assessment
	  ,Q23_Is_Assessment_Pass
	  ,Q23_AnswerText
	  ,Experience_Level
	  ,Q33_Experience_Equities
	  ,Q33_AnswerText
	  ,Q34_Experience_Crypto
	  ,Q34_AnswerText
	  ,Q35_Experience_CFDs
	  ,Q35_AnswerText
	  ,Q2_Experience
	  ,Q2_AnswerText
	  ,Q10_Annual_Income
	  ,Q10_AnswerText
	  ,Q11_Liquid_Assets
	  ,Q11_AnswerText
	  ,Q9_Risk_Reward_Scenario
	  ,Q9_AnswerText
	  ,Q14_Planned_Invested_Amount
	  ,Q14_AnswerText
	  ,Q27_Planned_Investment_Instrument
	  ,Is_PI_Stocks
	  ,Is_PI_Crypto
	  ,Is_PI_FX
	  ,Total_PI_Answers
	  ,Q5_Trading_Strategy
	  ,Q5_AnswerText
	  ,Q8_Trading_Primary_Purpose
	  ,Q8_AnswerText
	  ,Q15_Sources_of_Income
	  ,Q15_AnswerText
	  ,Q26_Sources_of_Funds
	  ,Q26_AnswerText
	  ,Q18_Occupation
	  ,Q18_AnswerText
FROM BI_DB.dbo.BI_DB_KYC_Panel
```

### 9. KYC

- Tableau id: `45c376bf-14f3-ec1f-79dc-7ab2a32b2ca5`

```sql
SELECT RealCID
	  ,CountryName Country
FROM BI_DB_dbo.BI_DB_KYC_Panel
group by RealCID
	  ,CountryName
```

### 10. Custom SQL Query

- Tableau id: `7d46f948-4828-b09f-9e4c-5e654168000b`

```sql
SELECT  pcp.[Date],
CONVERT(CHAR(8),CASE WHEN MONTH(pcp.Date) = MONTH(GETDATE()) THEN DATEADD(DAY,-1,GETDATE()) ELSE EOMONTH(pcp.Date) END,112) EOMonth,
        pcp.[DateID],
        pcp.[RealCID],
        pcp.[ClubTier],
        pcp.[Desk],
        pcp.[AM],
        pcp.[Country],
        pcp.[DaysSinceApplication],
        pcp.[UpdateDate],
        dd.[DateKey],
        dd.[Date] AS [dd_Date],
        dd.[CalendarYearMonth] AS [CalendarYearMonth],
        dd.[YearMonth] AS [YearMonth],
		td.ACC_TotalDeposits,
		vl.Liabilities + vl.ActualNWA AS Equity,
		TradingKnowledge,
	    TradingExperience,
	    TotalCashLiquid,
	    AnnualIncome,
		dc1.MarketingRegionManualName Region
FROM BI_DB_dbo.[BI_DB_ProfessionalCustomersPending] pcp
INNER JOIN [DWH_dbo].[Dim_Customer] dc WITH (NOLOCK)
ON pcp.RealCID = dc.RealCID
INNER JOIN [DWH_dbo].[Dim_Country] dc1 WITH (NOLOCK)
ON dc.CountryID = dc1.CountryID
  INNER JOIN (
  SELECT dd.DateKey
        ,dd.FullDate Date
        ,dd.CalendarYearMonth
        ,dd.MonthName+' '+ CAST(dd.CalendarYear AS VARCHAR(30)) AS YearMonth
  FROM [DWH_dbo].[Dim_Date] dd WITH (NOLOCK)
) dd 
ON pcp.[DateID] = dd.[DateKey]
--ORDER BY pcp.RealCID
LEFT JOIN  (
SELECT  CID 
       ,Active_Month
	   ,ACC_TotalDeposits
FROM BI_DB_dbo.BI_DB_CID_MonthlyPanel_FullData
) 
td
ON pcp.RealCID = td.CID
AND left(pcp.DateID,6) = td.Active_Month
LEFT JOIN DWH_dbo.V_Liabilities vl
ON pcp.RealCID = vl.CID
AND CONVERT(CHAR(8),CASE WHEN MONTH(pcp.Date) = MONTH(GETDATE()) THEN DATEADD(DAY,-1,GETDATE()) ELSE EOMONTH(pcp.Date) END,112) = vl.DateID
LEFT JOIN (
SELECT RealCID
      ,CASE WHEN KYC.Q3_Is_Professional_Knowledge = 0 THEN 'No' ELSE 'Yes' END TradingKnowledge
	  ,KYC.Q2_AnswerText TradingExperience
	  ,KYC.Q11_AnswerText TotalCashLiquid 
	  ,KYC.Q10_AnswerText AnnualIncome
FROM BI_DB_dbo.BI_DB_KYC_Panel KYC
)kyc
ON pcp.RealCID = kyc.RealCID
```

### 11. Transfer to Seychelles Regulation

- Tableau id: `8ac27b18-fddb-e450-e238-4674543a51f3`

```sql
SELECT fm.CID
     ,fm.AccountManager
	 ,kycp.CountryName
	 ,fm.EOM_Club
	 ,fm.EOM_Regulation CurrentRegulation
	 ,oa.Regulation PreviousRegulation
	 ,oa.RegulationChangeDate
	 ,fm.EOM_Equity Equity
	 ,fm.ACC_Revenue_Total Revenue
	 ,fm.ACC_TotalDeposits TotalDeposits
FROM [BI_DB].[dbo].[BI_DB_CID_MonthlyPanel_FullData] fm WITH (NOLOCK)
LEFT JOIN [BI_DB].[dbo].[BI_DB_KYC_Panel] kycp WITH (NOLOCK)
ON fm.CID = kycp.RealCID
OUTER APPLY
(
SELECT TOP 1 RealCID
            ,dr.Name Regulation
			,dd.FullDate RegulationChangeDate
FROM [DWH].[dbo].[Fact_SnapshotCustomer] fsc WITH (NOLOCK)
INNER JOIN [DWH].[dbo].[Dim_Range] dr1 WITH (NOLOCK)
ON fsc.DateRangeID = dr1.DateRangeID
INNER JOIN DWH.dbo.Dim_Regulation dr WITH (NOLOCK)
ON fsc.RegulationID = dr.ID
INNER JOIN [DWH].[dbo].[Dim_Date] dd WITH (NOLOCK)
ON dr1.ToDateID = dd.DateKey
WHERE fsc.RealCID = fm.CID
AND fsc.RegulationID !=9
AND dr1.ToDateID >=20210401
ORDER BY dr1.DateRangeID desc
) oa
WHERE fm.ActiveDate = DATEFROMPARTS(YEAR(getdate()-1),MONTH(getdate()-1),1)
AND fm.EOM_Regulation = 'FSA Seychelles'
```

### 12. Custom SQL Query

- Tableau id: `971181d7-0ed5-3f47-64ea-17d211a42364`

```sql
SELECT DISTINCT 
		bdkp.RealCID
		,bdkp.GCID
		,bdkp.Reg_Date
		,CASE WHEN bdkp.FTD_Month <= 200000 THEN NULL ELSE bdkp.FTD_Date END [FTD_Date]
		,CASE WHEN bdkp.FTD_Month > 200000 THEN DATEDIFF(DAY,bdkp.Reg_Date,bdkp.FTD_Date) ELSE NULL END [GapInDays_RegToFTD]
		,bdfa.FirstDepositAmount
		,bdkp.Gender
		,bdcd.Verified
		,bdcd.VerificationLevel1Date
		,bdcd.VerificationLevel2Date
		,bdcd.VerificationLevel3Date
		,bdkp.Q18_AnswerText [Occupation]
		,bdkp.Q3_Is_Professional_Knowledge [HaveTradingKnowledge]
		,bdkp.Q3_AnswerText [SourceOfTradingKnowledge]
		,bdkp.Q23_Is_Assessment_Pass [PassedKnowledgeAssessment]
		,bdkp.Experience_Level [TradingExperienceLevel]
		,bdkp.Q2_AnswerText [TradingExperienceTime]
		,bdkp.Q33_AnswerText [TradedBefore_Equities]
		,bdkp.Q47_AnswerText [TradedBefore_Equities_Amt]
		,bdkp.Q34_AnswerText [TradedBefore_Crypto]
		,bdkp.Q48_AnswerText [TradedBefore_Crypto_Amt]
		,bdkp.Q35_AnswerText [TradedBefore_LeveragedCFD]
		,bdkp.Q45_AnswerText [TradedBefore_LeveragedCFD_Amt]
		,bdkp.Q10_AnswerText [Annual_Income]
		,bdkp.Q15_AnswerText [MainIncome_Source]
		,bdkp.Q26_AnswerText [SourcesOfIncome]
		,bdkp.Q11_AnswerText [Liquid_Assets]
		,bdkp.Q9_AnswerText [Risk/Reward_ExpectationsTolerance]
		,bdkp.Q14_AnswerText [PlannedInvestment_Amt]
		,bdkp.Q5_AnswerText [TradingStrategy]
		,bdkp.Q8_AnswerText [PurposeOfTrading]
		,bdkp.Q29_AnswerText [TimeFrameForInvesting]
		,bdkp.Is_PI_Stocks [PlanToInvest_Stocks]
		,bdkp.Is_PI_Crypto [PlanToInvest_Crypto]
		,bdkp.Is_PI_FX [PlanToInvest_FX]
		,bdkp.Total_PI_Answers [PlanToInvest_TotalAssetTypes]
		,bdcd.NewMarketingRegion [MarketingRegion_Current]
		,bdcd.Region [Region_Current]
		,bdcd.Country [Country_Current]
		,bdcd.Club [Club_Current]
		,bdcd.Blocked [Blocked_Current]
		,DATEDIFF(YEAR, bdcd.BirthDate, GETDATE()) [Age_Current]
		,bdfa.FirstAction
		,bdfa.FirstActionDate
		,bdfa.FirstInstrument
		,bdfa.FirstAction_Detailed
		,bdfa.FirstActionTypeNew
		,bdfa.SecondAction
		,bdfa.SecondActionDate
		,bdfa.SecondInstrument
		,bdfa.SecondAction_Detailed
		,bdfa.ThirdAction
		,bdfa.ThirdActionDate
		,bdfa.ThirdAction_Detailed
		,bdfa.ThirdInstrument
		,bdfa.FourthAction
		,bdfa.FourthActionDate
		,bdfa.FourthAction_Detailed
		,bdfa.FifthAction
		,bdfa.FifthActionDate
		,bdfa.FifthAction_Detailed
		,bdfa.FirstLeverage
		,bdfa.SecondLeverage
		,bdfa.ThirdLeverage
		,bdfa.FourthLeverage
		,bdfa.FifthLeverage
		,bdfa.Deposit1day
		,bdfa.Deposit7days
		,bdfa.Deposit14days
		,bdfa.Deposit30days
		,bdfa.Deposit60days
		,bdfa.Deposit90days
		,bdfa.Deposit180days
		,bdfa.Deposit360days
		,bdfa.Equity1day
		,bdfa.Equity7days
		,bdfa.Equity14days
		,bdfa.Equity30days
		,bdfa.Equity60days
		,bdfa.Equity90days
		,bdfa.Equity180days
		,bdfa.Equity360days
		,bdfa.Revenue1day
		,bdfa.Revenue7days
		,bdfa.Revenue14days
		,bdfa.Revenue30days
		,bdfa.Revenue60days
		,bdfa.Revenue90days
		,bdfa.Revenue180days
		,bdfa.Revenue360days
		,bdfa.[Traded_Stocks/ETFs]
		,bdfa.TradedCrypto
		,bdfa.TradedCopy
		,bdfa.TradedCopyFund
		,bdfa.[Traded_FX/Commodities/Indices]
		,CASE WHEN bdcd.FirstDemoLoggedIn IS NOT NULL THEN 1 ELSE 0 END [Demo_HasLoginBefore]
		,bdcd.FirstDemoLoggedIn
		,CASE WHEN bdcd.FirstDemoPosOpenDate IS NOT NULL THEN 1 ELSE 0 END [Demo_OpenedPosBefore]
		,bdcd.FirstDemoPosOpenDate
		,GETDATE() [ScriptRunDate]
FROM BI_DB..BI_DB_KYC_Panel bdkp
LEFT JOIN BI_DB..BI_DB_CIDFirstDates bdcd ON bdkp.GCID = bdcd.GCID
LEFT JOIN BI_DB..BI_DB_First5Actions bdfa ON bdcd.CID = bdfa.CID
```

### 13. Custom SQL Query

- Tableau id: `a6666252-5f9e-4f7a-1fe5-1e49cccc8598`

```sql
SELECT  pcp.[Date],
CONVERT(CHAR(8),CASE WHEN MONTH(pcp.Date) = MONTH(GETDATE()) THEN DATEADD(DAY,-1,GETDATE()) ELSE EOMONTH(pcp.Date) END,112) EOMonth,
        pcp.[DateID],
        pcp.[RealCID],
        pcp.[ClubTier],
        pcp.[Desk],
        pcp.[AM],
        pcp.[Country],
        pcp.[DaysSinceApplication],
        pcp.[UpdateDate],
        dd.[DateKey],
        dd.[Date] AS [dd_Date],
        dd.[CalendarYearMonth] AS [CalendarYearMonth],
        dd.[YearMonth] AS [YearMonth],
		td.ACC_TotalDeposits,
		vl.Liabilities + vl.ActualNWA AS Equity,
		TradingKnowledge,
	    TradingExperience,
	    TotalCashLiquid,
	    AnnualIncome,
		dc1.MarketingRegionManualName Region
FROM BI_DB_dbo.[BI_DB_ProfessionalCustomersPending] pcp
INNER JOIN [DWH_dbo].[Dim_Customer] dc WITH (NOLOCK)
ON pcp.RealCID = dc.RealCID
INNER JOIN [DWH_dbo].[Dim_Country] dc1 WITH (NOLOCK)
ON dc.CountryID = dc1.CountryID
  INNER JOIN (
  SELECT dd.DateKey
        ,dd.FullDate Date
        ,dd.CalendarYearMonth
        ,dd.MonthName+' '+ CAST(dd.CalendarYear AS VARCHAR(30)) AS YearMonth
  FROM [DWH_dbo].[Dim_Date] dd WITH (NOLOCK)
) dd 
ON pcp.[DateID] = dd.[DateKey]
--ORDER BY pcp.RealCID
LEFT JOIN  (
SELECT  CID 
       ,Active_Month
	   ,ACC_TotalDeposits
FROM BI_DB_dbo.BI_DB_CID_MonthlyPanel_FullData
) 
td
ON pcp.RealCID = td.CID
AND left(pcp.DateID,6) = td.Active_Month
LEFT JOIN DWH_dbo.V_Liabilities vl
ON pcp.RealCID = vl.CID
AND CONVERT(CHAR(8),CASE WHEN MONTH(pcp.Date) = MONTH(GETDATE()) THEN DATEADD(DAY,-1,GETDATE()) ELSE EOMONTH(pcp.Date) END,112) = vl.DateID
LEFT JOIN (
SELECT RealCID
      ,CASE WHEN KYC.Q3_Is_Professional_Knowledge = 0 THEN 'No' ELSE 'Yes' END TradingKnowledge
	  ,KYC.Q2_AnswerText TradingExperience
	  ,KYC.Q11_AnswerText TotalCashLiquid 
	  ,KYC.Q10_AnswerText AnnualIncome
FROM BI_DB_dbo.BI_DB_KYC_Panel KYC
)kyc
ON pcp.RealCID = kyc.RealCID
```

### 14. Custom SQL Query

- Tableau id: `cee5f02f-93c5-6ec6-5474-858515a20104`

```sql
SELECT  pcp.[Date],
CONVERT(CHAR(8),CASE WHEN MONTH(pcp.Date) = MONTH(GETDATE()) THEN DATEADD(DAY,-1,GETDATE()) ELSE EOMONTH(pcp.Date) END,112) EOMonth,
        pcp.[DateID],
        pcp.[RealCID],
        pcp.[ClubTier],
        pcp.[Desk],
        pcp.[AM],
        pcp.[Country],
        pcp.[DaysSinceApplication],
        pcp.[UpdateDate],
        dd.[DateKey],
        dd.[Date] AS [dd_Date],
        dd.[CalendarYearMonth] AS [CalendarYearMonth],
        dd.[YearMonth] AS [YearMonth],
		td.ACC_TotalDeposits,
		vl.Liabilities + vl.ActualNWA AS Equity,
		TradingKnowledge,
	    TradingExperience,
	    TotalCashLiquid,
	    AnnualIncome,
		dc1.MarketingRegionManualName Region,
		pcap.SelectedCriteria SelectedCriteria,
td.ACC_Revenue_Total,
dr.Name Regulation 
FROM BI_DB_dbo.[BI_DB_ProfessionalCustomersPending] pcp
INNER JOIN [DWH_dbo].[Dim_Customer] dc WITH (NOLOCK)
ON pcp.RealCID = dc.RealCID
INNER JOIN [DWH_dbo].[Dim_Regulation] dr  WITH (NOLOCK)
ON dc.RegulationID = dr.ID
LEFT JOIN [BI_DB_dbo].[External_BI_OUTPUT_Customer_ProfessionalCustomers] pcap
ON dc.GCID = pcap.GCID
INNER JOIN [DWH_dbo].[Dim_Country] dc1 WITH (NOLOCK)
ON dc.CountryID = dc1.CountryID
  INNER JOIN (
  SELECT dd.DateKey
        ,dd.FullDate Date
        ,dd.CalendarYearMonth
        ,dd.MonthName+' '+ CAST(dd.CalendarYear AS VARCHAR(30)) AS YearMonth
  FROM [DWH_dbo].[Dim_Date] dd WITH (NOLOCK)
) dd 
ON pcp.[DateID] = dd.[DateKey]
--ORDER BY pcp.RealCID
LEFT JOIN  (
SELECT CID 
       ,Active_Month
	   ,ACC_TotalDeposits
	   ,ACC_Revenue_Total
FROM BI_DB_dbo.BI_DB_CID_MonthlyPanel_FullData
) 
td
ON pcp.RealCID = td.CID
AND left(pcp.DateID,6) = td.Active_Month
LEFT JOIN DWH_dbo.V_Liabilities vl
ON pcp.RealCID = vl.CID
AND CONVERT(CHAR(8),CASE WHEN MONTH(pcp.Date) = MONTH(GETDATE()) THEN DATEADD(DAY,-1,GETDATE()) ELSE EOMONTH(pcp.Date) END,112) = vl.DateID
LEFT JOIN (
SELECT RealCID
      ,CASE WHEN KYC.Q3_Is_Professional_Knowledge = 0 THEN 'No' ELSE 'Yes' END TradingKnowledge
	  ,KYC.Q2_AnswerText TradingExperience
	  ,KYC.Q11_AnswerText TotalCashLiquid 
	  ,KYC.Q10_AnswerText AnnualIncome
FROM BI_DB_dbo.BI_DB_KYC_Panel KYC
)kyc
ON pcp.RealCID = kyc.RealCID
WHERE dc.PlayerStatusID = 1
```

### 15. Custom SQL Query

- Tableau id: `d0447e57-f873-dd8f-c836-86dc5edb15d7`

```sql
SELECT

	EOMONTH(KYC.Reg_Date) AS Reg_Month,
	KYC.VerificationLevelID,
	KYC.Gender,
        KYC.Region,
        KYC.EU,
        KYC.CountryName,
        KYC.Club,
	KYC.IsFTD,
        KYC.Age_On_Reg,
	KYC.IsFirstAction,
	KYC.FirstAction,
	IIF(KYC.FirstAction IN ('Copy Fund','Copy','Stocks/ETFs'),'Stocks/ETFs/Copy',KYC.FirstAction ) AS FirstActionAdj,
	KYC.FirstInstrument,
	KYC.Q10_AnswerText AS Annual_Income,
	KYC.Q11_AnswerText AS Liquid_Assets,
	KYC.Q18_AnswerText,
	CASE WHEN KYC.Q10_AnswerText IN ('Up to $10K') THEN 'Up to $10K' WHEN KYC.Q10_AnswerText IS NULL THEN 'NULL' ELSE 'More than 10K' end AS Annual_Income_Adj,
	CASE WHEN KYC.Q11_AnswerText IN ('Up to $10K') THEN 'Up to $10K' WHEN KYC.Q11_AnswerText IS NULL THEN 'NULL' ELSE 'More than 10K' end AS Liquid_Assets_Adj,
	CASE WHEN ((KYC.Q35_AnswerID =49 AND KYC.Q34_AnswerID =49 AND KYC.Q33_AnswerID=49 ) OR  KYC.Q2_AnswerID=49) THEN 'Never Traded' ELSE 'Traded' END AS Expirience,
	KYC.Q2_AnswerID AS Expirince_Old,
	KYC.Q23_Is_Assessment_Pass,
        KYC.Q23_AnswerText,
    KYC.Q3_Is_Professional_Knowledge,
	CASE WHEN (KYC.Q23_Is_Assessment_Pass=1 AND Q3_Is_Professional_Knowledge=1) THEN 'Professional+Pass'
		 WHEN (KYC.Q23_Is_Assessment_Pass=1 AND Q3_Is_Professional_Knowledge=0) THEN 'NOTProfessional+Pass'
		 WHEN (KYC.Q23_Is_Assessment_Pass=0 AND Q3_Is_Professional_Knowledge=1) THEN 'Professional+NOTPass'
		 ELSE 'NOTProfessional+NOTPass' END AS IsPassProfessional,
	KYC.Q33_AnswerText AS Expirience_Equities,
	KYC.Q34_AnswerText AS Expirience_Crypto,
	KYC.Q35_AnswerText AS Expirience_CFD,
	CASE WHEN KYC.Is_PI_Stocks=1 THEN 'Stocks'
		 WHEN KYC.Is_PI_Crypto=1 THEN 'Crypto'
		 WHEN KYC.Is_PI_FX=1 THEN 'FX' END AS PI_Answer,
	KYC.Total_PI_Answers,
	KYC.FirstDepositAmount,
	KYC.Equity30days,
	KYC.Revenue30days,
        KYC.Equity14days,
	KYC.Revenue14days,
        COUNT(RealCID) AS CIDcount

FROM BI_DB..BI_DB_KYC_Panel KYC
WHERE EOMONTH(KYC.Reg_Date)>=EOMONTH(DATEADD(MONTH,-24,GETDATE())) AND EOMONTH(KYC.Reg_Date)<>EOMONTH(GETDATE())
GROUP BY 


	EOMONTH(KYC.Reg_Date),
	KYC.VerificationLevelID,
	KYC.Gender,
	KYC.IsFTD,
	KYC.IsFirstAction,
	KYC.FirstAction,
	IIF(KYC.FirstAction IN ('Copy Fund','Copy','Stocks/ETFs'),'Stocks/ETFs/Copy',KYC.FirstAction ),
	KYC.FirstInstrument,
	KYC.Q10_AnswerText,
	KYC.Q11_AnswerText,
	KYC.Q18_AnswerText,
	CASE WHEN KYC.Q10_AnswerText IN ('Up to $10K') THEN 'Other' ELSE 'More than 10K' end,
	CASE WHEN KYC.Q11_AnswerText IN ('Up to $10K') THEN 'Other' ELSE 'More than 10K' end ,
	CASE WHEN ((KYC.Q35_AnswerID =49 AND KYC.Q34_AnswerID =49 AND KYC.Q33_AnswerID=49 ) OR  KYC.Q2_AnswerID=49) THEN 'Never Traded' ELSE 'Traded' END,
	KYC.Q2_AnswerID ,
	KYC.Q23_Is_Assessment_Pass,
        KYC.Q3_Is_Professional_Knowledge,
	CASE WHEN (KYC.Q23_Is_Assessment_Pass=1 AND Q3_Is_Professional_Knowledge=1) THEN 'Professional+Pass'
		 WHEN (KYC.Q23_Is_Assessment_Pass=1 AND Q3_Is_Professional_Knowledge=0) THEN 'NOTProfessional+Pass'
		 WHEN (KYC.Q23_Is_Assessment_Pass=0 AND Q3_Is_Professional_Knowledge=1) THEN 'Professional+NOTPass'
		 ELSE 'NOTProfessional+NOTPass' END,
	KYC.Q33_AnswerText,
	KYC.Q34_AnswerText,
	KYC.Q35_AnswerText,
	CASE WHEN KYC.Is_PI_Stocks=1 THEN 'Stocks'
		 WHEN KYC.Is_PI_Crypto=1 THEN 'Crypto'
		 WHEN KYC.Is_PI_FX=1 THEN 'FX' END,
	KYC.Total_PI_Answers,
	KYC.FirstDepositAmount,
	KYC.Equity30days,
	KYC.Revenue30days,
        KYC.Q23_AnswerText,
        KYC.Age_On_Reg,
        KYC.Club,
        KYC.Region,
        KYC.EU,
        KYC.CountryName,
        KYC.Equity14days,
        KYC.Revenue14days
```

## Downstream workbooks

_Attribution: 7 via direct table-drag, 12 via custom SQL (workbooks can use both paths)._

| # | Workbook | Project | Owner | Via | Last updated | URL |
|---|---|---|---|---|---|---|
| 1 | KYC Data - Predicting High Potential Customers | 14-02-2024 | Clarence Tan | custom_sql | 2024-04-07T05:32:44Z | [open](https://reports.etorocorp.com/#/workbooks/KYCData-PredictingHighPotentialCustomers) |
| 2 | Regulation Transfer:Seychelles | 14-02-2024 | Tom Boksenbojm | custom_sql | 2024-02-14T09:05:00Z | [open](https://reports.etorocorp.com/#/workbooks/RegulationTransferSeychelles) |
| 3 | GenderAnalysis | 17.7.24 form sandbox | Tom Boksenbojm | custom_sql | 2024-07-17T10:39:54Z | [open](https://reports.etorocorp.com/#/workbooks/GenderAnalysis) |
| 4 | KYC monthly analysis | Compliance & AML | Zvika Solomon | custom_sql | 2025-12-11T15:11:02Z | [open](https://reports.etorocorp.com/#/workbooks/KYCmonthlyanalysis) |
| 5 | Professional Customers Report | Customer Facing | Tom Boksenbojm | custom_sql,direct | 2024-10-06T06:26:04Z | [open](https://reports.etorocorp.com/#/workbooks/ProfessionalCustomersReport) |
| 6 | Professional Customers: Pending Application  | Customer Facing & Support - Old | Tom Boksenbojm | custom_sql | 2024-11-02T10:01:53Z | [open](https://reports.etorocorp.com/#/workbooks/ProfessionalCustomersPendingApplication) |
| 7 | Onboarding Operations Excellence Report | Onboarding & KYC | Artyom Bogomolsky | custom_sql,direct | 2024-02-20T08:26:28Z | [open](https://reports.etorocorp.com/#/workbooks/OnboardingOperationsExcellenceReport) |
| 8 | Onboarding Operations Excellence Report - Synapse | Onboarding & KYC | Yarden Sabadra | custom_sql,direct | 2024-02-14T15:07:42Z | [open](https://reports.etorocorp.com/#/workbooks/OnboardingOperationsExcellenceReport-Synapse) |
| 9 | ASIC Monitoring CFD | Product Governance | Tal Cohen | custom_sql,direct | 2026-04-28T10:33:00Z | [open](https://reports.etorocorp.com/#/workbooks/ASICMonitoringCFD) |
| 10 | AT NM Dashboard 2024 | Product Governance | Tal Cohen | custom_sql,direct | 2025-03-10T15:15:57Z | [open](https://reports.etorocorp.com/#/workbooks/ATNMDashboard-Extanded) |
| 11 | CFD product review- CySEC | Product Governance | Tal Cohen | custom_sql,direct | 2025-06-04T08:54:04Z | [open](https://reports.etorocorp.com/#/workbooks/CFDproductreview-CySEC) |
| 12 | Professional Customers: Pending Applications  | Sandbox | Tom Boksenbojm | custom_sql,direct | 2023-08-31T09:10:08Z | [open](https://reports.etorocorp.com/#/workbooks/ProfessionalCustomersPendingApplications) |

### View URLs (per workbook)

- **KYC Data - Predicting High Potential Customers** (14-02-2024)
  - [Registered and Did FTD, based on Reg Year (3)](https://reports.etorocorp.com/#/views/KYCData-PredictingHighPotentialCustomers/RegisteredandDidFTDbasedonRegYear3)
  - [Registered and Did FTD, based on Reg Year](https://reports.etorocorp.com/#/views/KYCData-PredictingHighPotentialCustomers/RegisteredandDidFTDbasedonRegYear)
  - [Registered and Did FTD, based on Reg Year (2)](https://reports.etorocorp.com/#/views/KYCData-PredictingHighPotentialCustomers/RegisteredandDidFTDbasedonRegYear2)
  - [Annual Income Breakdown](https://reports.etorocorp.com/#/views/KYCData-PredictingHighPotentialCustomers/AnnualIncomeBreakdown)
  - [Annual Income Breakdown (Avg Rev)](https://reports.etorocorp.com/#/views/KYCData-PredictingHighPotentialCustomers/AnnualIncomeBreakdownAvgRev)
  - [Annual Income Breakdown (Sum Dep)](https://reports.etorocorp.com/#/views/KYCData-PredictingHighPotentialCustomers/AnnualIncomeBreakdownSumDep)
- **Regulation Transfer:Seychelles** (14-02-2024)
  - [Transfer to seychelles](https://reports.etorocorp.com/#/views/RegulationTransferSeychelles/Transfertoseychelles)
- **GenderAnalysis** (17.7.24 form sandbox)
  - [Distrubution gender](https://reports.etorocorp.com/#/views/GenderAnalysis/Distrubutiongender)
  - [Last Month Active User](https://reports.etorocorp.com/#/views/GenderAnalysis/LastMonthActiveUser)
  - [Pier: Active User (2)](https://reports.etorocorp.com/#/views/GenderAnalysis/PierActiveUser2)
  - [Trend: Users](https://reports.etorocorp.com/#/views/GenderAnalysis/TrendUsers)
  - [Trend: Active Users](https://reports.etorocorp.com/#/views/GenderAnalysis/TrendActiveUsers)
  - [Trend: Active Open Users](https://reports.etorocorp.com/#/views/GenderAnalysis/TrendActiveOpenUsers)
  - [Trend: Active](https://reports.etorocorp.com/#/views/GenderAnalysis/TrendActive)
  - [Trend: Active Open](https://reports.etorocorp.com/#/views/GenderAnalysis/TrendActiveOpen)
  - [Distribution Users](https://reports.etorocorp.com/#/views/GenderAnalysis/DistributionUsers)
  - [Distribution KPI](https://reports.etorocorp.com/#/views/GenderAnalysis/DistributionKPI)
  - [Age](https://reports.etorocorp.com/#/views/GenderAnalysis/Age)
  - [Age (2)](https://reports.etorocorp.com/#/views/GenderAnalysis/Age2)
  - [Age (3)](https://reports.etorocorp.com/#/views/GenderAnalysis/Age3)
  - [FTDs Distribution](https://reports.etorocorp.com/#/views/GenderAnalysis/FTDsDistribution)
  - [FTDs Distribution (2)](https://reports.etorocorp.com/#/views/GenderAnalysis/FTDsDistribution2)
  - [FTDA Difference](https://reports.etorocorp.com/#/views/GenderAnalysis/FTDADifference)
  - [Share of Woman by Region](https://reports.etorocorp.com/#/views/GenderAnalysis/ShareofWomanbyRegion)
  - [FirstActionDistribution (2)](https://reports.etorocorp.com/#/views/GenderAnalysis/FirstActionDistribution2)
  - [OTD_FTD](https://reports.etorocorp.com/#/views/GenderAnalysis/OTD_FTD)
  - [Club](https://reports.etorocorp.com/#/views/GenderAnalysis/Club)
  - [Sheet 13](https://reports.etorocorp.com/#/views/GenderAnalysis/Sheet13)
  - [Sheet 13 (2)](https://reports.etorocorp.com/#/views/GenderAnalysis/Sheet132)
  - [Sheet 14](https://reports.etorocorp.com/#/views/GenderAnalysis/Sheet14)
  - [Sheet 16](https://reports.etorocorp.com/#/views/GenderAnalysis/Sheet16)
  - [Sheet 17](https://reports.etorocorp.com/#/views/GenderAnalysis/Sheet17)
  - [Conversion Rate Reg FTD](https://reports.etorocorp.com/#/views/GenderAnalysis/ConversionRateRegFTD)
  - [Conversion Rate Reg FTD (2)](https://reports.etorocorp.com/#/views/GenderAnalysis/ConversionRateRegFTD2)
  - [Occupation](https://reports.etorocorp.com/#/views/GenderAnalysis/Occupation)
  - [Occupation (2)](https://reports.etorocorp.com/#/views/GenderAnalysis/Occupation2)
  - [Dashboard 1](https://reports.etorocorp.com/#/views/GenderAnalysis/Dashboard1)
  - [Dashboard 2](https://reports.etorocorp.com/#/views/GenderAnalysis/Dashboard2)
  - [Dashboard 3](https://reports.etorocorp.com/#/views/GenderAnalysis/Dashboard3)
  - [PnL Data](https://reports.etorocorp.com/#/views/GenderAnalysis/PnLData)
  - [Sheet 27](https://reports.etorocorp.com/#/views/GenderAnalysis/Sheet27)
  - [Registrations](https://reports.etorocorp.com/#/views/GenderAnalysis/Registrations)
  - [First Deposit](https://reports.etorocorp.com/#/views/GenderAnalysis/FirstDeposit)
- **KYC monthly analysis** (Compliance & AML)
  - [Expirience](https://reports.etorocorp.com/#/views/KYCmonthlyanalysis/Expirience)
  - [Planning investment](https://reports.etorocorp.com/#/views/KYCmonthlyanalysis/Planninginvestment)
  - [Knowledge](https://reports.etorocorp.com/#/views/KYCmonthlyanalysis/Knowledge)
  - [Economic Profile](https://reports.etorocorp.com/#/views/KYCmonthlyanalysis/EconomicProfile)
  - [GeneralINFO](https://reports.etorocorp.com/#/views/KYCmonthlyanalysis/GeneralINFO)
  - [Occupation ](https://reports.etorocorp.com/#/views/KYCmonthlyanalysis/Occupation)
- **Professional Customers Report** (Customer Facing)
  - [Professional Customers](https://reports.etorocorp.com/#/views/ProfessionalCustomersReport/ProfessionalCustomers)
  - [Approved Applications](https://reports.etorocorp.com/#/views/ProfessionalCustomersReport/ApprovedApplications)
  - [Number of Applications](https://reports.etorocorp.com/#/views/ProfessionalCustomersReport/NumberofApplications)
  - [Pending Applications](https://reports.etorocorp.com/#/views/ProfessionalCustomersReport/PendingApplications)
  - [Pending Applications Equity/Total Deposits](https://reports.etorocorp.com/#/views/ProfessionalCustomersReport/PendingApplicationsEquityTotalDeposits)
- **Professional Customers: Pending Application ** (Customer Facing & Support - Old)
  - [Pending Applications ](https://reports.etorocorp.com/#/views/ProfessionalCustomersPendingApplication/PendingApplications)
- **Onboarding Operations Excellence Report** (Onboarding & KYC)
  - [Onboarding Conversion](https://reports.etorocorp.com/#/views/OnboardingOperationsExcellenceReport/OnboardingConversion)
  - [General %STP / V2 to V3](https://reports.etorocorp.com/#/views/OnboardingOperationsExcellenceReport/GeneralSTPV2toV3)
  - [Screening, POI/POA- %STP](https://reports.etorocorp.com/#/views/OnboardingOperationsExcellenceReport/ScreeningPOIPOA-STP)
  - [Non Verified customers](https://reports.etorocorp.com/#/views/OnboardingOperationsExcellenceReport/NonVerifiedcustomers)
  - [Sub Reason for Non verification](https://reports.etorocorp.com/#/views/OnboardingOperationsExcellenceReport/SubReasonforNonverification)
  - [Heat MAP](https://reports.etorocorp.com/#/views/OnboardingOperationsExcellenceReport/HeatMAP)
  - [Conversion Flow](https://reports.etorocorp.com/#/views/OnboardingOperationsExcellenceReport/ConversionFlow)
  - [Management Summary](https://reports.etorocorp.com/#/views/OnboardingOperationsExcellenceReport/ManagementSummary)
  - [FTD analysis](https://reports.etorocorp.com/#/views/OnboardingOperationsExcellenceReport/FTDanalysis)
  - [Country KYC Flow](https://reports.etorocorp.com/#/views/OnboardingOperationsExcellenceReport/CountryKYCFlow)
- **Onboarding Operations Excellence Report - Synapse** (Onboarding & KYC)
  - [Onboarding Conversion](https://reports.etorocorp.com/#/views/OnboardingOperationsExcellenceReport-Synapse/OnboardingConversion)
  - [General %STP / V2 to V3](https://reports.etorocorp.com/#/views/OnboardingOperationsExcellenceReport-Synapse/GeneralSTPV2toV3)
  - [Screening, POI/POA- %STP](https://reports.etorocorp.com/#/views/OnboardingOperationsExcellenceReport-Synapse/ScreeningPOIPOA-STP)
  - [Non Verified customers](https://reports.etorocorp.com/#/views/OnboardingOperationsExcellenceReport-Synapse/NonVerifiedcustomers)
  - [Sub Reason for Non verification](https://reports.etorocorp.com/#/views/OnboardingOperationsExcellenceReport-Synapse/SubReasonforNonverification)
  - [Heat MAP](https://reports.etorocorp.com/#/views/OnboardingOperationsExcellenceReport-Synapse/HeatMAP)
  - [Conversion Flow](https://reports.etorocorp.com/#/views/OnboardingOperationsExcellenceReport-Synapse/ConversionFlow)
  - [Management Summary](https://reports.etorocorp.com/#/views/OnboardingOperationsExcellenceReport-Synapse/ManagementSummary)
  - [FTD analysis](https://reports.etorocorp.com/#/views/OnboardingOperationsExcellenceReport-Synapse/FTDanalysis)
  - [Country KYC Flow](https://reports.etorocorp.com/#/views/OnboardingOperationsExcellenceReport-Synapse/CountryKYCFlow)
- **ASIC Monitoring CFD** (Product Governance)
  - [ASIC Monitoring Dashboard](https://reports.etorocorp.com/#/views/ASICMonitoringCFD/ASICMonitoringDashboard)
- **AT NM Dashboard 2024** (Product Governance)
  - [AT NM Dashboard](https://reports.etorocorp.com/#/views/ATNMDashboard-Extanded/ATNMDashboard)
- **CFD product review- CySEC** (Product Governance)
  - [CFD product review- CID Level](https://reports.etorocorp.com/#/views/CFDproductreview-CySEC/CFDproductreview-CIDLevel)
  - [KYC Aggregated](https://reports.etorocorp.com/#/views/CFDproductreview-CySEC/KYCAggregated)
- **Professional Customers: Pending Applications ** (Sandbox)
  - [Pending Applications ](https://reports.etorocorp.com/#/views/ProfessionalCustomersPendingApplications/PendingApplications)

## Downstream calculated fields (in embedded datasources)

### Workbook: ASIC Monitoring CFD

#### Datasource: BI_DB_ASIC_Monitoring_CFD_W_Sun (BI_DB_dbo.BI_DB_ASIC_Monitoring_CFD_W_Sun)+ (sql_dp_prod_we)

- **Date Filter**

  ```
  [Date Parameter]
  ```

- **Date_Ind**

  ```
  [Date]=[Date Parameter]
  ```

- **Real CID (copy)**

  ```
  [CID]
  ```

- **Top filter**

  ```
  IF [# of Records] THEN 
  INDEX()<= 1000
  ELSE
  TRUE
  END
  ```

### Workbook: AT NM Dashboard 2024

#### Datasource: BI_DB_Scored_Appropriateness_Negative_Market (BI_DB_dbo.BI_DB_Scored_Appropriateness_Negative_Market)+ (sql_dp_prod_we)

- **1**

  ```
  1
  ```

- **1 (1)**

  ```
  1
  ```

- **AT_Calc**

  ```
  SUM([AT_Failed_Fixed])/SUM([AT_Fixed])
  ```

- **AT_Colored<50%**

  ```
  if [AT_Calc]<0.5 THEN [AT_Calc] ELSE 0.5 END
  ```

- **AT_Colored>50%**

  ```
  IF [AT_Calc]>0.5 THEN [AT_Calc]-0.5 END
  ```

- **AT_Failed_Fixed**

  ```
  {FIXED[Eom Ftd]:
  SUM(IF IFNULL([ApproprietnessScore Status],'NULL') = 'Failed' THEN 1 ELSE 0 END)}
  ```

- **AT_Fixed**

  ```
  {FIXED[Eom Ftd]:
  SUM(IF IFNULL([ApproprietnessScore Status],'NULL') != 'NULL' THEN 1 ELSE 0 END)}
  ```

- **AT_Grey<50%**

  ```
  IF [AT_Calc]<0.5 THEN 0.5-[AT_Calc] END
  ```

- **AT_Grey>50%**

  ```
  IF [AT_Calc]>0.5 THEN 1-[AT_Calc] ELSE 0.5 END
  ```

- **Bottom Half**

  ```
  MIN(1)
  ```

- **Calculation1**

  ```
  COUNTD([Gcid]) / TOTAL(COUNTD([Gcid]))
  ```

- **Category**

  ```
  IF [CFD Status]='CFD_Blocked' then 'CFD Blocked'
  ELSEIF [CFD Status]='CFD_Allowed' and [ApproprietnessScore Status (group)]='Failed' then 'Appropriateness Test Failed'
  ELSEIF [CFD Status]='CFD_Allowed' and [ApproprietnessScore Status (group)]='Passed' then 'No Restrication'
  ELSE NULL END
  ```

- **CFD Status (Release)**

  ```
  [CFD Status]
  ```

- **COUNTD_Gcid**

  ```
  IF COUNTD([Gcid]) >= 1000000 THEN
  //round for millions
  STR(ROUND(COUNTD([Gcid]) / 1000000,1)) + "M"
  ELSEIF COUNTD([Gcid]) >= 1000 THEN
  //round for thousands
  STR(ROUND(COUNTD([Gcid]) / 1000,1)) + "K"
  ELSE
  STR(ROUND(COUNTD([Gcid]),1))
  END
  ```

- **DateDiffBlockRelease_Group**

  ```
  IF [Date Diff Block Release]=0 then str([Date Diff Block Release])
  ELSEIF [Date Diff Block Release]>=1 and [Date Diff Block Release]<10 then '1-9'
  ELSEIF [Date Diff Block Release]>=10 and [Date Diff Block Release]<31 then '10-30'
  ELSEIF [Date Diff Block Release]>=31 and [Date Diff Block Release]<61 then '31-60'
  ELSEIF [Date Diff Block Release]>=61 and [Date Diff Block Release]<121 then '61-120'
  ELSE 'Above 120' end
  ```

- **Dummy**

  ```
  'Dummy'
  ```

- **Gcid (copy 2)**

  ```
  [Gcid]
  ```

- **Gcid (copy)**

  ```
  [Gcid]
  ```

- **IsKnockOutQuestionsNotNull**

  ```
  IF ISNULL([Is_RiskApetite_5/-3])=FALSE and ISNULL([Is_AnnualIncome_Upto10K])=FALSE and ISNULL([Is_TradingPurpose_SavingForHome])=FALSE
  then true else false end
  ```

- **KnockOutQuestionsKYCAnswers**

  ```
  if [Is_RiskApetite_5/-3]=1 and [Is_AnnualIncome_Upto10K]=0 and [Is_TradingPurpose_SavingForHome]=0 
  then 'Risk Apetite 5%/-3%'
  ELSEIF  [Is_RiskApetite_5/-3]=0 and [Is_AnnualIncome_Upto10K]=1 and [Is_TradingPurpose_SavingForHome]=0 
  then 'Annual Income Up to $10K'
  ELSEIF  [Is_RiskApetite_5/-3]=0 and [Is_AnnualIncome_Upto10K]=0 and [Is_TradingPurpose_SavingForHome]=1
  then 'Trading Purpose Saving For Home'
  ELSEIF  [Is_RiskApetite_5/-3]=1 and [Is_AnnualIncome_Upto10K]=1 and [Is_TradingPurpose_SavingForHome]=0
  then 'Risk Apetite 5%/-3% AND Annual Income Up to $10K'
  ELSEIF  [Is_RiskApetite_5/-3]=1 and [Is_AnnualIncome_Upto10K]=0 and [Is_TradingPurpose_SavingForHome]=1
  then 'Risk Apetite 5%/-3% AND Trading Purpose Saving For Home'
  ELSEIF  [Is_RiskApetite_5/-3]=0 and [Is_AnnualIncome_Upto10K]=1 and [Is_TradingPurpose_SavingForHome]=1
  then 'Annual Income Up to $10K AND Trading Purpose Saving For Home'
  ELSEIF  [Is_RiskApetite_5/-3]=1 and [Is_AnnualIncome_Upto10K]=1 and [Is_TradingPurpose_SavingForHome]=1
  then 'Risk Apetite 5%/-3% AND Annual Income Up to $10K AND Trading Purpose Saving For Home'
  ELSE 'N/A'
  END
  ```

- **Last12Months**

  ```
  DATEDIFF('month', [FTD Date], TODAY()) > 0    // Exclude current month
  AND
  DATEDIFF('month', [FTD Date], TODAY()) <= 12  // Include only last 12 months
  ```

- **LastMonth**

  ```
  DATEDIFF('month', [FTD Date], TODAY()) > 0    // Exclude current month
  AND
  DATEDIFF('month', [FTD Date], TODAY()) <= 1  // Include only last month
  ```

- **NM_Calc**

  ```
  SUM([NM_Failed_Fixed])/SUM([NM_Fixed])
  ```

- **NM_Colored<50%**

  ```
  if [NM_Calc]<0.5 THEN [NM_Calc] ELSE 0.5 END
  ```

- **NM_Colored>50%**

  ```
  IF [NM_Calc]>0.5 THEN [NM_Calc]-0.5 END
  ```

- **NM_Failed_Fixed**

  ```
  {FIXED[Eom Ftd]:
  SUM(IF IFNULL([Restriction Status Desc],'NULL') = 'Failed' THEN 1 ELSE 0 END)}
  ```

- **NM_Fixed**

  ```
  {FIXED[Eom Ftd]:
  SUM(IF IFNULL([Restriction Status Desc],'NULL') != 'NULL' THEN 1 ELSE 0 END)}
  ```

- **NM_Grey<50%**

  ```
  IF [NM_Calc]<0.5 THEN 0.5-[NM_Calc] END
  ```

- **NM_Grey>50%**

  ```
  IF [NM_Calc]>0.5 THEN 1-[NM_Calc] ELSE 0.5 END
  ```

- **Pointer 1**

  ```
  IF [NM_Calc]<=0.5
  THEN .005 END
  ```

- **Pointer 2**

  ```
  IF [NM_Calc]>0.5 THEN .005 END
  ```

- **PopUpsCount_Fix**

  ```
  IF [PopUpsCount]<11 then [PopUpsCount] else -1 end
  ```

- **releasedPopAll**

  ```
  IF [CFD Status]='CFD_Blocked' THEN 1
  ELSEIF ISNULL([Block Date])=False and ISNULL([Release Date])=False and [CFD Status]='CFD_Allowed' THEN 1
  ELSE 0 END
  ```

### Workbook: CFD product review- CySEC

#### Datasource: Custom SQL Query (sql_dp_prod_we)

- **min(0)**

  ```
  MIN(0)
  ```

### Workbook: Onboarding Operations Excellence Report

#### Datasource: Custom SQL Query (BI_DB)

- **Experienced Ind**

  ```
  IF[Experience_Level (group)]='Experienced' then 1 else 0 end
  ```

- **sum([Q3_Is_Professional_Knowledge])/count([RealCID])**

  ```
  sum([Q3_Is_Professional_Knowledge])/count([RealCID])
  ```

#### Datasource: Operations Onboarding KPIs

- **% of VL3 < 1Hr**

  ```
  SUM(
  IF [IsVL3] = 1 
  AND DATEDIFF('hour', [DateTime VL0], [DateTime VL3]) <= 1 
  THEN 1 ELSE 0 END)
  /
  SUM(
  IF [IsVL3] = 1 
  THEN 1 ELSE 0 END)
  ```

- **% of VL3 < 24Hr**

  ```
  SUM(
  IF [IsVL3] = 1 
  AND DATEDIFF('hour', [DateTime VL0], [DateTime VL3]) <= 24
  THEN 1 ELSE 0 END)
  /
  SUM(
  IF [IsVL3] = 1 
  THEN 1 ELSE 0 END)
  ```

- **% of VL3 > 3Day**

  ```
  SUM(
  IF [IsVL3] = 1 
  AND DATEDIFF('hour', [DateTime VL0], [DateTime VL3]) >= 72
  THEN 1 ELSE 0 END)
  /
  SUM(
  IF [IsVL3] = 1 
  THEN 1 ELSE 0 END)
  ```

- **%FA**

  ```
  [SUM FA]/[SUM Reg]
  ```

- **%STP POA**

  ```
  sum(if [POA IsResponseAutomatic]>0 and ([POA_Manager]=0 or [POA_Manager]=Null) then 1 else 0 end)/count([CID])
  ```

- **%STP POI**

  ```
  sum(if[POI IsResponseAutomatic]>0 and ([POI_Manager]=0 or [POI_Manager]=NULL) then 1 else 0 end )/count([CID])
  ```

- **%STP US**

  ```
  sum([Screening IsAutomatic])/count([CID])
  ```

- **calc: #Funded**

  ```
  sum(if [Period Ind]='5 Month
  Before' then ([calc: Funded]/5) else [calc: Funded] end)
  ```

- **calc: Dep.Attempt**

  ```
  sum(if [Period Ind]='5 Month
  Before' then ([DepositAttempt]/5) else [DepositAttempt] end)
  ```

- **calc: Funded**

  ```
  if [Is FTD]=1 and [IsVL3]=1 and [FirstAction]=1 then 1 else 0 end
  ```

- **calc:#FA**

  ```
  sum(if [Period Ind]='5 Month
  Before' then ([FirstAction]/5) else [FirstAction] end)
  ```

- **calc:#Reg**

  ```
  sum(if [Period Ind]='5 Month
  Before' then ([IsVL0]/5) else [IsVL0] end)
  ```

- **calc:#VL1**

  ```
  sum(if [Period Ind]='5 Month
  Before' then ([IsVL1]/5) else [IsVL1] end)
  ```

- **calc:#VL2**

  ```
  sum(if [Period Ind]='5 Month
  Before' then ([IsVL2]/5) else [IsVL2] end)
  ```

- **calc:#VL3**

  ```
  sum(if [Period Ind]='5 Month
  Before' then ([IsVL3]/5) else [IsVL3] end)
  ```

- **calc:%Funded**

  ```
  sum([calc: Funded])/[SUM Reg]
  ```

- **calc:FTDs**

  ```
  sum(if [Period Ind]='5 Month
  Before' then ([Is FTD]/5) else [Is FTD] end)
  ```

- **Calculation1**

  ```
  COUNT([CID]) / TOTAL(COUNT([CID]))
  ```

- **Calculation2**

  ```
  COUNT([CID]) / TOTAL(COUNT([CID]))
  ```

- **Calculation3**

  ```
  COUNT([CID]) / TOTAL(COUNT([CID]))
  ```

- **Calculation4**

  ```
  COUNT([CID]) / TOTAL(COUNT([CID]))
  ```

- **Calculation5**

  ```
  SUM([POA IsResponseAutomatic]) / TOTAL(SUM([POA IsResponseAutomatic]))
  ```

- **Calculation6**

  ```
  SUM([POA IsResponseAutomatic]) / TOTAL(SUM([POA IsResponseAutomatic]))
  ```

- **Calculation7**

  ```
  COUNT([CID (copy)]) / TOTAL(COUNT([CID (copy)]))
  ```

- **Calculation8**

  ```
  LOOKUP(COUNTD([CID (copy)]) / TOTAL(COUNTD([CID (copy)])),LAST())
  ```

- **Calculation9**

  ```
  LOOKUP([test 1],-1) - [test 1]
  ```

- **CID (copy)**

  ```
  [CID]
  ```

- **Closed Accounts**

  ```
  if [PlayerStatusID]=2 and [PlayerStatusReasonID]= 40 then 1 else 0 end
  ```

- **Country average LTV**

  ```
  { FIXED [Country],month([DateTime VL2]):sum([LTV])/sum(if [LTV]>0 then 1 else 0 end)}
  ```

- **Country Heat Value**

  ```
  [Country]+' : '+ [Non Verification Sub reason]
  ```

- **Date of Registration**

  ```
  DATE([DateTime VL0])
  ```

- **DaysFrom FTD**

  ```
  DATEDIFF('day',[DateTime FTD],TODAY())
  ```

- **DD Hours RegtoVL3**

  ```
  [DDMinutes VL0toVL3] / 60
  ```

- **DIFF non vere**

  ```
  ZN(COUNT([CID (copy)])) - LOOKUP(ZN(COUNT([CID (copy)])), -1)/ COUNT([CID (copy)])
  ```

- **Docs Flow**

  ```
  IF [VD HasDocuments] = 0 THEN "No Docs"
  ELSEIF [VD HasDocuments] = 1 AND [POI IsApproved] = 1 AND [POA IsApproved] = 1 THEN "Both Approved"
  ELSEIF [VD HasDocuments] = 1 AND [POI IsApproved] = 1 AND [POA IsApproved] = 0 THEN "POI Approved"
  ELSEIF [VD HasDocuments] = 1 AND [POI IsApproved] = 0 AND [POA IsApproved] = 1 THEN "POA Approved"
  ELSEIF [VD HasDocuments] = 1 AND [POI IsApproved] = 0 AND [POA IsApproved] = 0 THEN "Both Declined"
  ELSE "Error"
  END
  ```

- **Document SLA**

  ```
  if DATEDIFF('minute',[Max Document Upload Date],[Max Document Reviewed Date])<=2 then 'In 0-2 Minutes'
  ELSEIF DATEDIFF('minute',[Max Document Upload Date],[Max Document Reviewed Date])<=10 then'In 3-10 minutes'
  ELSEIF DATEDIFF('minute',[Max Document Upload Date],[Max Document Reviewed Date])<=180 then'11 min - 3 hours'
  ELSEIF DATEDIFF('minute',[Max Document Upload Date],[Max Document Reviewed Date])<=360 then '4-6 Hours'
  ELSEIF DATEDIFF('minute',[Max Document Upload Date],[Max Document Reviewed Date])<=720 then '7-12 Hours'
  ELSEIF DATEDIFF('minute',[Max Document Upload Date],[Max Document Reviewed Date])<=1440 then '13-24 Hours'
  ELSE 'More then 24 Hours'
  end
  ```

- **Effected VL2 customers**

  ```
  SUM([IsVL2])
  ```

- **EV Flow**

  ```
  IF [EV IsCountryEligible] = 1 AND ISNULL([EV MatchStatusID]) THEN "EV: Not Sent"
  ELSEIF [EV IsCountryEligible] = 1 AND [EV MatchStatusID] IN (1, 2) THEN "EV: Verified"
  ELSEIF [EV IsCountryEligible] = 1 AND [EV MatchStatusID] IN (0, 3) THEN "EV: Not Verified"
  ELSEIF [EV IsCountryEligible] = 0 THEN "Non-EV"
  ELSE "Error"
  END
  ```

- **EV MatchStatus (copy)**

  ```
  [EV MatchStatus]
  ```

- **EV verified #**

  ```
  sum (if [EV MatchStatus (copy)]='Verified' then 1 else 0 end)
  ```

- **EV verified or other verification**

  ```
  if [EV MatchStatus (copy)]='Verified' then 'EV'
   
   else 'Not sent to EV' end
  ```

- **EV vs. NonEv Category**

  ```
  IF IFNULL([EV MatchStatusID], 0) = 0 THEN "Not sent to EV"
  ELSEIF [EV MatchStatusID] IN (1, 2, 3) THEN "Sent to EV"
  END
  ```

- **FA in Reg Month (copy)**

  ```
  if  MONTH([Date of Registration])=MONTH([FirstActionDate])   then 1 else 0 end
  ```

- **FA&FTD in Reg Month (copy)**

  ```
  if MONTH([Date of Registration])=MONTH([DateTime FTD])
  and MONTH([Date of Registration])=MONTH([FirstActionDate])   then 1 else 0 end
  ```

- **First Deposit Attempt in Reg Month (copy)**

  ```
  if  month([Date of Registration])=month([FirstDepositAttemptDate])then 1 else 0 end
  ```

- **FirstAction Ind**

  ```
  [FirstAction]
  ```

- **FTD before V3 Ind**

  ```
  if [DateTime FTD]<[DateTime VL3] then 'FTD Before V3'
  ELSEIF [DateTime FTD]>[DateTime VL3] then 'FTD After V3' 
  end
  ```

- **FTD EV Group**

  ```
  if [EV MatchStatus (copy)]<>'Verified' then 'Not Approved'
  elseif [FirstAction Ind]=1 then 'EV Approved+FA'
  elseif [FirstAction Ind]=0 then 'EV Approved+No FA'
  else 'Not Relevant' end
  ```

- **FTD in Reg Month**

  ```
  if  month([Date of Registration])=month([DateTime FTD]) 
  and year([Date of Registration])= year([DateTime FTD]) then 1 else 0 end
  ```

- **Funded and not Funded drill**

  ```
  IF [Funded FTD month]=1 then 'Funded - V3+FA'
  elseif [IsVL3]=1 then 'V3 No F.Action'
  ELSE 'No V3'
  end
  ```

- **Funded FTD month**

  ```
  if [Is FTD]=1 and [IsVL3]=1 and [FirstAction]=1 and MONTH([DateTime FTD])=MONTH([FirstActionDate]) 
   and MONTH([DateTime FTD])>=MONTH([DateTime VL3])
  then 1 else 0 end
  ```

- **Funded FTD month (copy)**

  ```
  if [Is FTD]=1 and [IsVL3]=1 and [FirstAction]=1 and MONTH([DateTime FTD])=MONTH([FirstActionDate]) 
   and MONTH([DateTime FTD])>=MONTH([DateTime VL3])
  then 1 else 0 end
  ```

- **Funded in Reg Month**

  ```
  if MONTH([Date of Registration])=MONTH([DateTime VL3]) and MONTH([DateTime FTD])=MONTH([DateTime VL3]) 
  and MONTH([Date of Registration])=MONTH([FirstActionDate])   then 1 else 0 end
  ```

- **Heat Map Value (AVG LTV*CIDs)**

  ```
  avg([Country average LTV])*[SUM VL2]
  ```

- **Hours From FTD  to FA**

  ```
  DATEDIFF('hour',[DateTime FTD],[FirstActionDate])
  ```

- **Index for KYC**

  ```
  INDEX()
  ```

- **Index Heat Map**

  ```
  index()
  ```

- **IsVL3 (copy)**

  ```
  [IsVL3]
  ```

- **KYC Flow (copy)**

  ```
  [KYC Flow]
  ```

- **Max Document Reviewed Date**

  ```
  max([POA ResponseDateTime],[POI ResponseDateTime])
  ```

- **Max Document Upload Date**

  ```
  max([POA UploadDateTime],[POI UploadDateTime])
  ```

- **Non Verification Sub reason**

  ```
  if
  [NonVerificationReason]='Docs not Approved' and [POA IsApproved]=0 and [POI IsApproved]=0 then 'Both Docs Not Approved'
  ELSEIF [NonVerificationReason]='Docs not Approved' and [POA IsApproved]=1 and [POI IsApproved]=0 then 'POI Not Approved'
  ELSEIF [NonVerificationReason]='Docs not Approved' and [POA IsApproved]=0 and [POI IsApproved]=1 then 'POA Not Approved'
  
  ELSEIF [NonVerificationReason]='Missing Docs' and ZN([POA IsApproved])=0 and zn([POI IsApproved])=0 then 'Missing Both Docs'
  ELSEIF [NonVerificationReason]='Missing Docs' and ZN([POA IsApproved])=1 and zn([POI IsApproved])=0 then 'Missing POI'
  ELSEIF [NonVerificationReason]='Missing Docs' and ZN([POA IsApproved])=0 and zn([POI IsApproved])=1 then 'Missing POA'
  
  ELSEIF [NonVerificationReason]='User Screening Issue' and ((ZN([POA IsApproved])=1 and zn([POI IsApproved])=1) or [EV MatchStatus]='Verified') and
  ([Phone Verification] ='AutomaticallyVerified' or [Phone Verification] ='ManualyVerified') then 'User Screening issue only'
  ELSEIF [NonVerificationReason]='User Screening Issue' then 'User Screening with other issues'
  
  else [NonVerificationReason]
  end
  ```

- **Not Funded Group (FTD Month)**

  ```
  IF [Funded FTD month]=0 and [IsVL3]=0 then 'No V3'
  elseif [Funded FTD month]=0 and [IsVL3]=1 and [DateTime VL3]<= [DateTime FTD] then 'V3 Before FTD + No FA'
  else 'V3 After FTD + No FA'
  end
  ```

- **Period Ind**

  ```
  if DATEDIFF('month',[Date of Registration],TODAY()-1)= 0.00 then 'This
   Month'
  elseif DATEDIFF('month',[Date of Registration],TODAY()-1)= 1.00 then 'Curr
   Month'
  elseif DATEDIFF('month',[Date of Registration],TODAY()-1)= 2.00 then 'Prev
  Month'
   elseif DATEDIFF('month',[Date of Registration],TODAY()-1)> 2.00 then '5 Month
  Before'
  else 'Other Period'
  END
  ```

- **Period Ind (copy)**

  ```
  if DATEDIFF('month',[Date of Registration],TODAY()-1)= 0.00 then 'Curr
   Month'
  elseif DATEDIFF('month',[Date of Registration],TODAY()-1)= 1.00 then 'Prev
  Month'
   elseif DATEDIFF('month',[Date of Registration],TODAY()-1)> 1.00 then '5 Month
  Before'
  else 'Other Period'
  END
  ```

- **Period Ind _Week**

  ```
  if [Date of Registration] >= dateadd('day',-7,TODAY()) then 'Curr
   Week'
  elseif [Date of Registration] >= dateadd('day',-14,TODAY()) then 'Prev
  Week'
    elseif [Date of Registration] >= dateadd('day',-70,TODAY()) then '8 Weeks
  Before'
  else 'Other Period'
  END
  ```

- **Period Ind VL2**

  ```
  if DATEDIFF('month',[DateTime VL2],TODAY()-1)= 0.00 then 'This
   Month'
  elseif DATEDIFF('month',[DateTime VL2],TODAY()-1)= 1.00 then 'Curr
   Month'
  elseif DATEDIFF('month',[DateTime VL2],TODAY()-1)= 2.00 then 'Prev
  Month'
   elseif DATEDIFF('month',[DateTime VL2],TODAY()-1)> 2.00 then '5 Month
  Before'
  else 'Other Period'
  END
  ```

- **Period Ind_Summary**

  ```
  if DATEDIFF('month',[Date of Registration],TODAY()-1)= 0.00 then 'This
   Month'
  elseif DATEDIFF('month',[Date of Registration],TODAY()-1)= 1.00 then 'Curr
   Month'
  elseif DATEDIFF('month',[Date of Registration],TODAY()-1)= 2.00 then 'Prev
  Month'
   elseif DATEDIFF('month',[Date of Registration],TODAY()-1)> 2.00 then '5 Month
  Before'
  else 'Other Period'
  END
  ```

- **POA Decline Category**

  ```
  IF ISNULL([POA CountDeclines]) THEN "No Declines"
  ELSEIF [POA CountDeclines] = 1 THEN "One Decline"
  ELSEIF [POA CountDeclines] IN (2, 3) THEN "2 to 3 Declines"
  ELSEIF [POA CountDeclines] IN (4, 5) THEN "4 to 5 Declines"
  ELSEIF [POA CountDeclines] > 5 THEN "More than 5 Declines"
  ELSE "Error"
  END
  ```

- **POI Decline Category**

  ```
  IF ISNULL([POI CountDeclines]) THEN "No Declines"
  ELSEIF [POI CountDeclines] = 1 THEN "One Decline"
  ELSEIF [POI CountDeclines] IN (2, 3) THEN "2 to 3 Declines"
  ELSEIF [POI CountDeclines] IN (4, 5) THEN "4 to 5 Declines"
  ELSEIF [POI CountDeclines] > 5 THEN "More than 5 Declines"
  ELSE "Error"
  END
  ```

- **POI/POA STP User %**

  ```
  if [IsVL3] = 1
             and ([EV MatchStatus]='None' or isnull([EV MatchStatus])=TRUE 
                  )
             and [Screening Status]='NoMatch'     
             and [Phone Verification]='AutomaticallyVerified'
             and [DateTime VL2]>DATE("2022-08-14")
  and [POA IsApproved]+[POI IsApproved]=2 and [POA_Manager]+[POI_Manager]=0
   
      then 1 
  end
  ```

- **Regular STP User %**

  ```
  IF [IsVL3] = 1 
           and [EV MatchStatus]='Verified'
           and [Screening Status]='NoMatch'
           and [Phone Verification]='AutomaticallyVerified'
           and [Country ID]=219 
           and [Email Verification]=1
           THEN 1
       ELSEIF  [IsVL3] = 1 
               and [Phone Verification]='AutomaticallyVerified'
               and ifnull([Screening Status],'NoMatch')='NoMatch'
               and [EV MatchStatus]='Verified'
               and [Designated Regulation]='CYSEC'
               and [Country ID]!=219 
               then 1 
      elseif [IsVL3] = 1
             and [EV MatchStatus]='Verified'
             and [Screening Status]='NoMatch'     
             and [Phone Verification]='AutomaticallyVerified'
             and [Country ID]!=219
      then 1  
  
       else 0 END
  ```

- **STP 5 month diff abs**

  ```
  sum (if [Period Ind]='Curr
   Month' then zn([Regular STP User %])+zn([POI/POA STP User %]) else 0 end)/sum(if [Period Ind]='Curr
   Month' then 1 else 0 end)
  -
  
  sum (if [Period Ind]='5 Month
  Before' then [Regular STP User %]+zn([POI/POA STP User %]) else 0 end)/sum(if [Period Ind]='5 Month
  Before' then 1 else 0 end)
  ```

- **STP 5 month diff abs (copy)**

  ```
  sum (if [Period Ind]='Curr
   Month' then 1 else 0 end)/sum(if [Period Ind]='Curr
   Month' then 1 else 0 end)
  -
  
  sum (if [Period Ind]='5 Month
  Before' then [Regular STP User %]+zn([POI/POA STP User %]) else 0 end)/sum(if [Period Ind]='5 Month
  Before' then 1 else 0 end)
  ```

- **STP eToro %**

  ```
  SUM (IF [IsSTP eToro] = 1 THEN 1 ELSE 0 END) / COUNT([CID])
  ```

- **STP POA**

  ```
  if[POA IsResponseAutomatic]=1 and([POA_Manager]=0 or [POA_Manager]=Null) then 1 else 0 end
  ```

- **STP POI**

  ```
  if [POI IsResponseAutomatic]>0 and ([POI_Manager]=0 or [POI_Manager]= NULL) then 1 else 0 end
  ```

- **STP prev month diff abs**

  ```
  sum (if [Period Ind]='Curr
   Month' then zn([Regular STP User %])+zn([POI/POA STP User %]) else 0 end)/sum(if [Period Ind]='Curr
   Month' then 1 else 0 end)
  -
  
  sum (if [Period Ind]='Prev
  Month' then [Regular STP User %]+zn([POI/POA STP User %]) else 0 end)/sum(if [Period Ind]='Prev
  Month' then 1 else 0 end)
  ```

- **SUM Dep.Attempt**

  ```
  sum([DepositAttempt])
  ```

- **SUM FA**

  ```
  sum([FirstAction])
  ```

- **SUM FTD**

  ```
  SUM([Is FTD])
  ```

- **SUM Reg**

  ```
  SUM([IsVL0])
  ```

- **SUM Unverified FTD**

  ```
  SUM(IF [Is FTD] = 1 AND [IsVL3] = 0 THEN 1 ELSE 0 END)
  ```

- **SUM VL1**

  ```
  SUM([IsVL1])
  ```

- **SUM VL2**

  ```
  SUM([IsVL2])
  ```

- **SUM VL3**

  ```
  SUM([IsVL3])
  ```

- **sum([First Deposit Attempt in Reg Month (copy)])/count(CID)**

  ```
  sum([First Deposit Attempt in Reg Month (copy)])/count(CID)
  ```

- **sum([FTD in Reg Month])/sum([First Deposit Attempt in Reg Mon...**

  ```
  sum([FTD in Reg Month])/sum([First Deposit Attempt in Reg Month (copy)])
  ```

- **sum([Is FTD])/count([CID])**

  ```
  sum([Is FTD])/count([CID])
  ```

- **SUM([VL1 in Reg Month])/count([CID])**

  ```
  SUM([VL1 in Reg Month])/count([CID])
  ```

- **SUM([VL2 in Reg Month])/COUNT([CID])**

  ```
  SUM([VL2 in Reg Month])/COUNT([CID])
  ```

- **SUM([VL3&FTD in Reg Month])/count([CID])**

  ```
  SUM([VL3&FTD in Reg Month])/count([CID])
  ```

- **test 1**

  ```
  COUNTD([CID (copy)])/SUM([Total CID per month])
  ```

- **total**

  ```
  /*{FIXED [Period Ind VL2]:sum(if[Period Ind VL2] ='Curr
   Month' then 1 else 0 end)}/ {FIXED [Period Ind VL2]:COUNT([CID (copy)])}*/
  
   if[Period Ind (copy 2)_611082180667727873]='Curr
   Month' then { FIXED [Period Ind (copy 2)_611082180667727873]: sum(1)} else { FIXED [Period Ind (copy 2)_611082180667727873]: sum(0)} end
  ```

- **Total CID per month**

  ```
  { FIXED [Period Ind VL2] : COUNTD([CID (copy)]) }
  ```

- **VB: IsVL3<=72Hrs**

  ```
  IF [IsVL3] = 1
  AND DATEDIFF('hour', [DateTime VL0], [DateTime VL3]) <= 72
  THEN 'Yes' 
  ELSE 'No'
  END
  ```

- **VB: VL3 Categories**

  ```
  IF DATEDIFF('hour', [DateTime VL0], [DateTime VL3]) <= 1
  THEN '<= 1 Hour'
  
  ELSEIF DATEDIFF('hour', [DateTime VL0], [DateTime VL3]) > 1
  AND DATEDIFF('hour', [DateTime VL0], [DateTime VL3]) <= 24
  THEN '<= 24 Hrs'
  
  ELSEIF DATEDIFF('hour', [DateTime VL0], [DateTime VL3]) > 24
  THEN '> 24 Hrs'
  
  ELSE 'Error Check'
  END
  ```

- **Verification Categories**

  ```
  IF     IFNULL([IsVL0],0) = 1 AND IFNULL([IsVL1],0) = 0 AND IFNULL([IsVL2],0) = 0 AND IFNULL([IsVL3],0) = 0  AND IFNULL([Is FTD],0) = 0 THEN "Reg Only"
  ELSEIF IFNULL([IsVL0],0) = 1 AND IFNULL([IsVL1],0) = 1 AND IFNULL([IsVL2],0) = 0 AND IFNULL([IsVL3],0) = 0  AND IFNULL([Is FTD],0) = 0 THEN "In VL1"
  ELSEIF IFNULL([IsVL0],0) = 1 AND IFNULL([IsVL1],0) = 1 AND IFNULL([IsVL2],0) = 1 AND IFNULL([IsVL3],0) = 0  AND IFNULL([Is FTD],0) = 0 THEN "In VL2"
  ELSEIF IFNULL([IsVL0],0) = 1 AND IFNULL([IsVL1],0) = 1 AND IFNULL([IsVL2],0) = 1 AND IFNULL([IsVL3],0) = 1  AND IFNULL([Is FTD],0) = 0 THEN "In VL3"
  ELSEIF IFNULL([IsVL0],0) = 1 AND IFNULL([IsVL1],0) = 1 AND IFNULL([IsVL2],0) = 1 AND IFNULL([IsVL3],0) = 1  AND IFNULL([Is FTD],0) = 1 THEN "Verified FTD"
  ELSEIF IFNULL([IsVL0],0) = 1 AND IFNULL([IsVL1],0) = 1 AND IFNULL([IsVL2],0) = 1 AND IFNULL([IsVL3],0) = 0  AND IFNULL([Is FTD],0) = 1 THEN "Unverified FTD"
  ELSE "Error In Flow"
  END
  ```

- **VL1 in Reg Month**

  ```
  if MONTH([Date of Registration])=MONTH([DateTime VL1]) then 1 else 0 end
  ```

- **VL2 in Reg Month**

  ```
  if MONTH([Date of Registration])=MONTH([DateTime VL2]) then 1 else 0 end
  ```

- **VL3 from reg  5 month diff abs**

  ```
  sum (if [Period Ind]='Curr
   Month' then [VL3 in Reg Month] else 0 end)/sum(if [Period Ind]='Curr
   Month' then 1 else 0 end)
  -
  
  sum (if [Period Ind]='5 Month
  Before' then [VL3 in Reg Month] else 0 end)/sum(if [Period Ind]='5 Month
  Before' then 1 else 0 end)
  ```

- **VL3 from reg prev month diff abs **

  ```
  sum (if [Period Ind]='Curr
   Month' then [VL3 in Reg Month] else 0 end)/sum(if [Period Ind]='Curr
   Month' then 1 else 0 end)
  -
  
  sum (if [Period Ind]='Prev
  Month' then [VL3 in Reg Month] else 0 end)/sum(if [Period Ind]='Prev
  Month' then 1 else 0 end)
  ```

- **VL3 in Reg Month**

  ```
  if MONTH([Date of Registration])=MONTH([DateTime VL3]) then 1 else 0 end
  ```

- **VL3&FTD in Reg Month**

  ```
  if MONTH([Date of Registration])=MONTH([DateTime VL3]) 
  and MONTH([DateTime FTD])=MONTH([DateTime VL3]) 
  and year([DateTime FTD])=year([DateTime VL3]) 
    then 1 else 0 end
  ```

- **WoW eToro STP % Change**

  ```
  COUNT(IF WEEK([DateTime VL3]) = WEEK(TODAY()) AND [IsSTP eToro] = 1 THEN CID END)
  /
  COUNT(IF WEEK([DateTime VL3]) = WEEK(DATEADD('week', -1, TODAY())) AND [IsSTP eToro] = 1 THEN CID END)
  -1
  ```

### Workbook: Onboarding Operations Excellence Report - Synapse

#### Datasource: Custom SQL Query (BI_DB)

- **Experienced Ind**

  ```
  IF[Experience_Level (group)]='Experienced' then 1 else 0 end
  ```

- **sum([Q3_Is_Professional_Knowledge])/count([RealCID])**

  ```
  sum([Q3_Is_Professional_Knowledge])/count([RealCID])
  ```

#### Datasource: Operations Onboarding KPIs

- **% of VL3 < 1Hr**

  ```
  SUM(
  IF [IsVL3] = 1 
  AND DATEDIFF('hour', [DateTime VL0], [DateTime VL3]) <= 1 
  THEN 1 ELSE 0 END)
  /
  SUM(
  IF [IsVL3] = 1 
  THEN 1 ELSE 0 END)
  ```

- **% of VL3 < 24Hr**

  ```
  SUM(
  IF [IsVL3] = 1 
  AND DATEDIFF('hour', [DateTime VL0], [DateTime VL3]) <= 24
  THEN 1 ELSE 0 END)
  /
  SUM(
  IF [IsVL3] = 1 
  THEN 1 ELSE 0 END)
  ```

- **% of VL3 > 3Day**

  ```
  SUM(
  IF [IsVL3] = 1 
  AND DATEDIFF('hour', [DateTime VL0], [DateTime VL3]) >= 72
  THEN 1 ELSE 0 END)
  /
  SUM(
  IF [IsVL3] = 1 
  THEN 1 ELSE 0 END)
  ```

- **%FA**

  ```
  [SUM FA]/[SUM Reg]
  ```

- **%STP POA**

  ```
  sum(if [POA IsResponseAutomatic]>0 and ([POA_Manager]=0 or [POA_Manager]=Null) then 1 else 0 end)/count([CID])
  ```

- **%STP POI**

  ```
  sum(if[POI IsResponseAutomatic]>0 and ([POI_Manager]=0 or [POI_Manager]=NULL) then 1 else 0 end )/count([CID])
  ```

- **%STP US**

  ```
  sum([Screening IsAutomatic])/count([CID])
  ```

- **calc: #Funded**

  ```
  sum(if [Period Ind]='5 Month
  Before' then ([calc: Funded]/5) else [calc: Funded] end)
  ```

- **calc: Dep.Attempt**

  ```
  sum(if [Period Ind]='5 Month
  Before' then ([DepositAttempt]/5) else [DepositAttempt] end)
  ```

- **calc: Funded**

  ```
  if [Is FTD]=1 and [IsVL3]=1 and [FirstAction]=1 then 1 else 0 end
  ```

- **calc:#FA**

  ```
  sum(if [Period Ind]='5 Month
  Before' then ([FirstAction]/5) else [FirstAction] end)
  ```

- **calc:#Reg**

  ```
  sum(if [Period Ind]='5 Month
  Before' then ([IsVL0]/5) else [IsVL0] end)
  ```

- **calc:#VL1**

  ```
  sum(if [Period Ind]='5 Month
  Before' then ([IsVL1]/5) else [IsVL1] end)
  ```

- **calc:#VL2**

  ```
  sum(if [Period Ind]='5 Month
  Before' then ([IsVL2]/5) else [IsVL2] end)
  ```

- **calc:#VL3**

  ```
  sum(if [Period Ind]='5 Month
  Before' then ([IsVL3]/5) else [IsVL3] end)
  ```

- **calc:%Funded**

  ```
  sum([calc: Funded])/[SUM Reg]
  ```

- **calc:FTDs**

  ```
  sum(if [Period Ind]='5 Month
  Before' then ([Is FTD]/5) else [Is FTD] end)
  ```

- **Calculation1**

  ```
  COUNT([CID]) / TOTAL(COUNT([CID]))
  ```

- **Calculation2**

  ```
  COUNT([CID]) / TOTAL(COUNT([CID]))
  ```

- **Calculation3**

  ```
  COUNT([CID]) / TOTAL(COUNT([CID]))
  ```

- **Calculation4**

  ```
  COUNT([CID]) / TOTAL(COUNT([CID]))
  ```

- **Calculation5**

  ```
  SUM([POA IsResponseAutomatic]) / TOTAL(SUM([POA IsResponseAutomatic]))
  ```

- **Calculation6**

  ```
  SUM([POA IsResponseAutomatic]) / TOTAL(SUM([POA IsResponseAutomatic]))
  ```

- **Calculation7**

  ```
  COUNT([CID (copy)]) / TOTAL(COUNT([CID (copy)]))
  ```

- **Calculation8**

  ```
  LOOKUP(COUNTD([CID (copy)]) / TOTAL(COUNTD([CID (copy)])),LAST())
  ```

- **Calculation9**

  ```
  LOOKUP([test 1],-1) - [test 1]
  ```

- **CID (copy)**

  ```
  [CID]
  ```

- **Closed Accounts**

  ```
  if [PlayerStatusID]=2 and [PlayerStatusReasonID]= 40 then 1 else 0 end
  ```

- **Country average LTV**

  ```
  { FIXED [Country],month([DateTime VL2]):sum([LTV])/sum(if [LTV]>0 then 1 else 0 end)}
  ```

- **Country Heat Value**

  ```
  [Country]+' : '+ [Non Verification Sub reason]
  ```

- **Date of Registration**

  ```
  DATE([DateTime VL0])
  ```

- **DaysFrom FTD**

  ```
  DATEDIFF('day',[DateTime FTD],TODAY())
  ```

- **DD Hours RegtoVL3**

  ```
  [DDMinutes VL0toVL3] / 60
  ```

- **DIFF non vere**

  ```
  ZN(COUNT([CID (copy)])) - LOOKUP(ZN(COUNT([CID (copy)])), -1)/ COUNT([CID (copy)])
  ```

- **Docs Flow**

  ```
  IF [VD HasDocuments] = 0 THEN "No Docs"
  ELSEIF [VD HasDocuments] = 1 AND [POI IsApproved] = 1 AND [POA IsApproved] = 1 THEN "Both Approved"
  ELSEIF [VD HasDocuments] = 1 AND [POI IsApproved] = 1 AND [POA IsApproved] = 0 THEN "POI Approved"
  ELSEIF [VD HasDocuments] = 1 AND [POI IsApproved] = 0 AND [POA IsApproved] = 1 THEN "POA Approved"
  ELSEIF [VD HasDocuments] = 1 AND [POI IsApproved] = 0 AND [POA IsApproved] = 0 THEN "Both Declined"
  ELSE "Error"
  END
  ```

- **Document SLA**

  ```
  if DATEDIFF('minute',[Max Document Upload Date],[Max Document Reviewed Date])<=2 then 'In 0-2 Minutes'
  ELSEIF DATEDIFF('minute',[Max Document Upload Date],[Max Document Reviewed Date])<=10 then'In 3-10 minutes'
  ELSEIF DATEDIFF('minute',[Max Document Upload Date],[Max Document Reviewed Date])<=180 then'11 min - 3 hours'
  ELSEIF DATEDIFF('minute',[Max Document Upload Date],[Max Document Reviewed Date])<=360 then '4-6 Hours'
  ELSEIF DATEDIFF('minute',[Max Document Upload Date],[Max Document Reviewed Date])<=720 then '7-12 Hours'
  ELSEIF DATEDIFF('minute',[Max Document Upload Date],[Max Document Reviewed Date])<=1440 then '13-24 Hours'
  ELSE 'More then 24 Hours'
  end
  ```

- **Effected VL2 customers**

  ```
  SUM([IsVL2])
  ```

- **EV Flow**

  ```
  IF [EV IsCountryEligible] = 1 AND ISNULL([EV MatchStatusID]) THEN "EV: Not Sent"
  ELSEIF [EV IsCountryEligible] = 1 AND [EV MatchStatusID] IN (1, 2) THEN "EV: Verified"
  ELSEIF [EV IsCountryEligible] = 1 AND [EV MatchStatusID] IN (0, 3) THEN "EV: Not Verified"
  ELSEIF [EV IsCountryEligible] = 0 THEN "Non-EV"
  ELSE "Error"
  END
  ```

- **EV MatchStatus (copy)**

  ```
  [EV MatchStatus]
  ```

- **EV verified #**

  ```
  sum (if [EV MatchStatus (copy)]='Verified' then 1 else 0 end)
  ```

- **EV verified or other verification**

  ```
  if [EV MatchStatus (copy)]='Verified' then 'EV'
   
   else 'Not sent to EV' end
  ```

- **EV vs. NonEv Category**

  ```
  IF IFNULL([EV MatchStatusID], 0) = 0 THEN "Not sent to EV"
  ELSEIF [EV MatchStatusID] IN (1, 2, 3) THEN "Sent to EV"
  END
  ```

- **FA in Reg Month (copy)**

  ```
  if  MONTH([Date of Registration])=MONTH([FirstActionDate])   then 1 else 0 end
  ```

- **FA&FTD in Reg Month (copy)**

  ```
  if MONTH([Date of Registration])=MONTH([DateTime FTD])
  and MONTH([Date of Registration])=MONTH([FirstActionDate])   then 1 else 0 end
  ```

- **First Deposit Attempt in Reg Month (copy)**

  ```
  if  month([Date of Registration])=month([FirstDepositAttemptDate])then 1 else 0 end
  ```

- **FirstAction Ind**

  ```
  [FirstAction]
  ```

- **FTD before V3 Ind**

  ```
  if [DateTime FTD]<[DateTime VL3] then 'FTD Before V3'
  ELSEIF [DateTime FTD]>[DateTime VL3] then 'FTD After V3' 
  end
  ```

- **FTD EV Group**

  ```
  if [EV MatchStatus (copy)]<>'Verified' then 'Not Approved'
  elseif [FirstAction Ind]=1 then 'EV Approved+FA'
  elseif [FirstAction Ind]=0 then 'EV Approved+No FA'
  else 'Not Relevant' end
  ```

- **FTD in Reg Month**

  ```
  if  month([Date of Registration])=month([DateTime FTD]) 
  and year([Date of Registration])= year([DateTime FTD]) then 1 else 0 end
  ```

- **Funded and not Funded drill**

  ```
  IF [Funded FTD month]=1 then 'Funded - V3+FA'
  elseif [IsVL3]=1 then 'V3 No F.Action'
  ELSE 'No V3'
  end
  ```

- **Funded FTD month**

  ```
  if [Is FTD]=1 and [IsVL3]=1 and [FirstAction]=1 and MONTH([DateTime FTD])=MONTH([FirstActionDate]) 
   and MONTH([DateTime FTD])>=MONTH([DateTime VL3])
  then 1 else 0 end
  ```

- **Funded FTD month (copy)**

  ```
  if [Is FTD]=1 and [IsVL3]=1 and [FirstAction]=1 and MONTH([DateTime FTD])=MONTH([FirstActionDate]) 
   and MONTH([DateTime FTD])>=MONTH([DateTime VL3])
  then 1 else 0 end
  ```

- **Funded in Reg Month**

  ```
  if MONTH([Date of Registration])=MONTH([DateTime VL3]) and MONTH([DateTime FTD])=MONTH([DateTime VL3]) 
  and MONTH([Date of Registration])=MONTH([FirstActionDate])   then 1 else 0 end
  ```

- **Heat Map Value (AVG LTV*CIDs)**

  ```
  avg([Country average LTV])*[SUM VL2]
  ```

- **Hours From FTD  to FA**

  ```
  DATEDIFF('hour',[DateTime FTD],[FirstActionDate])
  ```

- **Index for KYC**

  ```
  INDEX()
  ```

- **Index Heat Map**

  ```
  index()
  ```

- **IsVL3 (copy)**

  ```
  [IsVL3]
  ```

- **KYC Flow (copy)**

  ```
  [KYC Flow]
  ```

- **Max Document Reviewed Date**

  ```
  max([POA ResponseDateTime],[POI ResponseDateTime])
  ```

- **Max Document Upload Date**

  ```
  max([POA UploadDateTime],[POI UploadDateTime])
  ```

- **Non Verification Sub reason**

  ```
  if
  [NonVerificationReason]='Docs not Approved' and [POA IsApproved]=0 and [POI IsApproved]=0 then 'Both Docs Not Approved'
  ELSEIF [NonVerificationReason]='Docs not Approved' and [POA IsApproved]=1 and [POI IsApproved]=0 then 'POI Not Approved'
  ELSEIF [NonVerificationReason]='Docs not Approved' and [POA IsApproved]=0 and [POI IsApproved]=1 then 'POA Not Approved'
  
  ELSEIF [NonVerificationReason]='Missing Docs' and ZN([POA IsApproved])=0 and zn([POI IsApproved])=0 then 'Missing Both Docs'
  ELSEIF [NonVerificationReason]='Missing Docs' and ZN([POA IsApproved])=1 and zn([POI IsApproved])=0 then 'Missing POI'
  ELSEIF [NonVerificationReason]='Missing Docs' and ZN([POA IsApproved])=0 and zn([POI IsApproved])=1 then 'Missing POA'
  
  ELSEIF [NonVerificationReason]='User Screening Issue' and ((ZN([POA IsApproved])=1 and zn([POI IsApproved])=1) or [EV MatchStatus]='Verified') and
  ([Phone Verification] ='AutomaticallyVerified' or [Phone Verification] ='ManualyVerified') then 'User Screening issue only'
  ELSEIF [NonVerificationReason]='User Screening Issue' then 'User Screening with other issues'
  
  else [NonVerificationReason]
  end
  ```

- **Not Funded Group (FTD Month)**

  ```
  IF [Funded FTD month]=0 and [IsVL3]=0 then 'No V3'
  elseif [Funded FTD month]=0 and [IsVL3]=1 and [DateTime VL3]<= [DateTime FTD] then 'V3 Before FTD + No FA'
  else 'V3 After FTD + No FA'
  end
  ```

- **Period Ind**

  ```
  if DATEDIFF('month',[Date of Registration],TODAY()-1)= 0.00 then 'This
   Month'
  elseif DATEDIFF('month',[Date of Registration],TODAY()-1)= 1.00 then 'Curr
   Month'
  elseif DATEDIFF('month',[Date of Registration],TODAY()-1)= 2.00 then 'Prev
  Month'
   elseif DATEDIFF('month',[Date of Registration],TODAY()-1)> 2.00 then '5 Month
  Before'
  else 'Other Period'
  END
  ```

- **Period Ind (copy)**

  ```
  if DATEDIFF('month',[Date of Registration],TODAY()-1)= 0.00 then 'Curr
   Month'
  elseif DATEDIFF('month',[Date of Registration],TODAY()-1)= 1.00 then 'Prev
  Month'
   elseif DATEDIFF('month',[Date of Registration],TODAY()-1)> 1.00 then '5 Month
  Before'
  else 'Other Period'
  END
  ```

- **Period Ind _Week**

  ```
  if [Date of Registration] >= dateadd('day',-7,TODAY()) then 'Curr
   Week'
  elseif [Date of Registration] >= dateadd('day',-14,TODAY()) then 'Prev
  Week'
    elseif [Date of Registration] >= dateadd('day',-70,TODAY()) then '8 Weeks
  Before'
  else 'Other Period'
  END
  ```

- **Period Ind VL2**

  ```
  if DATEDIFF('month',[DateTime VL2],TODAY()-1)= 0.00 then 'This
   Month'
  elseif DATEDIFF('month',[DateTime VL2],TODAY()-1)= 1.00 then 'Curr
   Month'
  elseif DATEDIFF('month',[DateTime VL2],TODAY()-1)= 2.00 then 'Prev
  Month'
   elseif DATEDIFF('month',[DateTime VL2],TODAY()-1)> 2.00 then '5 Month
  Before'
  else 'Other Period'
  END
  ```

- **Period Ind_Summary**

  ```
  if DATEDIFF('month',[Date of Registration],TODAY()-1)= 0.00 then 'This
   Month'
  elseif DATEDIFF('month',[Date of Registration],TODAY()-1)= 1.00 then 'Curr
   Month'
  elseif DATEDIFF('month',[Date of Registration],TODAY()-1)= 2.00 then 'Prev
  Month'
   elseif DATEDIFF('month',[Date of Registration],TODAY()-1)> 2.00 then '5 Month
  Before'
  else 'Other Period'
  END
  ```

- **POA Decline Category**

  ```
  IF ISNULL([POA CountDeclines]) THEN "No Declines"
  ELSEIF [POA CountDeclines] = 1 THEN "One Decline"
  ELSEIF [POA CountDeclines] IN (2, 3) THEN "2 to 3 Declines"
  ELSEIF [POA CountDeclines] IN (4, 5) THEN "4 to 5 Declines"
  ELSEIF [POA CountDeclines] > 5 THEN "More than 5 Declines"
  ELSE "Error"
  END
  ```

- **POI Decline Category**

  ```
  IF ISNULL([POI CountDeclines]) THEN "No Declines"
  ELSEIF [POI CountDeclines] = 1 THEN "One Decline"
  ELSEIF [POI CountDeclines] IN (2, 3) THEN "2 to 3 Declines"
  ELSEIF [POI CountDeclines] IN (4, 5) THEN "4 to 5 Declines"
  ELSEIF [POI CountDeclines] > 5 THEN "More than 5 Declines"
  ELSE "Error"
  END
  ```

- **POI/POA STP User %**

  ```
  if [IsVL3] = 1
             and ([EV MatchStatus]='None' or isnull([EV MatchStatus])=TRUE 
                  )
             and [Screening Status]='NoMatch'     
             and [Phone Verification]='AutomaticallyVerified'
             and [DateTime VL2]>DATE("2022-08-14")
  and [POA IsApproved]+[POI IsApproved]=2 and [POA_Manager]+[POI_Manager]=0
   
      then 1 
  end
  ```

- **Regular STP User %**

  ```
  IF [IsVL3] = 1 
           and [EV MatchStatus]='Verified'
           and [Screening Status]='NoMatch'
           and [Phone Verification]='AutomaticallyVerified'
           and [Country ID]=219 
           and [Email Verification]=1
           THEN 1
       ELSEIF  [IsVL3] = 1 
               and [Phone Verification]='AutomaticallyVerified'
               and ifnull([Screening Status],'NoMatch')='NoMatch'
               and [EV MatchStatus]='Verified'
               and [Designated Regulation]='CYSEC'
               and [Country ID]!=219 
               then 1 
      elseif [IsVL3] = 1
             and [EV MatchStatus]='Verified'
             and [Screening Status]='NoMatch'     
             and [Phone Verification]='AutomaticallyVerified'
             and [Country ID]!=219
      then 1  
  
       else 0 END
  ```

- **STP 5 month diff abs**

  ```
  sum (if [Period Ind]='Curr
   Month' then zn([Regular STP User %])+zn([POI/POA STP User %]) else 0 end)/sum(if [Period Ind]='Curr
   Month' then 1 else 0 end)
  -
  
  sum (if [Period Ind]='5 Month
  Before' then [Regular STP User %]+zn([POI/POA STP User %]) else 0 end)/sum(if [Period Ind]='5 Month
  Before' then 1 else 0 end)
  ```

- **STP 5 month diff abs (copy)**

  ```
  sum (if [Period Ind]='Curr
   Month' then 1 else 0 end)/sum(if [Period Ind]='Curr
   Month' then 1 else 0 end)
  -
  
  sum (if [Period Ind]='5 Month
  Before' then [Regular STP User %]+zn([POI/POA STP User %]) else 0 end)/sum(if [Period Ind]='5 Month
  Before' then 1 else 0 end)
  ```

- **STP eToro %**

  ```
  SUM (IF [IsSTP eToro] = 1 THEN 1 ELSE 0 END) / COUNT([CID])
  ```

- **STP POA**

  ```
  if[POA IsResponseAutomatic]=1 and([POA_Manager]=0 or [POA_Manager]=Null) then 1 else 0 end
  ```

- **STP POI**

  ```
  if [POI IsResponseAutomatic]>0 and ([POI_Manager]=0 or [POI_Manager]= NULL) then 1 else 0 end
  ```

- **STP prev month diff abs**

  ```
  sum (if [Period Ind]='Curr
   Month' then zn([Regular STP User %])+zn([POI/POA STP User %]) else 0 end)/sum(if [Period Ind]='Curr
   Month' then 1 else 0 end)
  -
  
  sum (if [Period Ind]='Prev
  Month' then [Regular STP User %]+zn([POI/POA STP User %]) else 0 end)/sum(if [Period Ind]='Prev
  Month' then 1 else 0 end)
  ```

- **SUM Dep.Attempt**

  ```
  sum([DepositAttempt])
  ```

- **SUM FA**

  ```
  sum([FirstAction])
  ```

- **SUM FTD**

  ```
  SUM([Is FTD])
  ```

- **SUM Reg**

  ```
  SUM([IsVL0])
  ```

- **SUM Unverified FTD**

  ```
  SUM(IF [Is FTD] = 1 AND [IsVL3] = 0 THEN 1 ELSE 0 END)
  ```

- **SUM VL1**

  ```
  SUM([IsVL1])
  ```

- **SUM VL2**

  ```
  SUM([IsVL2])
  ```

- **SUM VL3**

  ```
  SUM([IsVL3])
  ```

- **sum([First Deposit Attempt in Reg Month (copy)])/count(CID)**

  ```
  sum([First Deposit Attempt in Reg Month (copy)])/count(CID)
  ```

- **sum([FTD in Reg Month])/sum([First Deposit Attempt in Reg Mon...**

  ```
  sum([FTD in Reg Month])/sum([First Deposit Attempt in Reg Month (copy)])
  ```

- **sum([Is FTD])/count([CID])**

  ```
  sum([Is FTD])/count([CID])
  ```

- **SUM([VL1 in Reg Month])/count([CID])**

  ```
  SUM([VL1 in Reg Month])/count([CID])
  ```

- **SUM([VL2 in Reg Month])/COUNT([CID])**

  ```
  SUM([VL2 in Reg Month])/COUNT([CID])
  ```

- **SUM([VL3&FTD in Reg Month])/count([CID])**

  ```
  SUM([VL3&FTD in Reg Month])/count([CID])
  ```

- **test 1**

  ```
  COUNTD([CID (copy)])/SUM([Total CID per month])
  ```

- **total**

  ```
  /*{FIXED [Period Ind VL2]:sum(if[Period Ind VL2] ='Curr
   Month' then 1 else 0 end)}/ {FIXED [Period Ind VL2]:COUNT([CID (copy)])}*/
  
   if[Period Ind (copy 2)_611082180667727873]='Curr
   Month' then { FIXED [Period Ind (copy 2)_611082180667727873]: sum(1)} else { FIXED [Period Ind (copy 2)_611082180667727873]: sum(0)} end
  ```

- **Total CID per month**

  ```
  { FIXED [Period Ind VL2] : COUNTD([CID (copy)]) }
  ```

- **VB: IsVL3<=72Hrs**

  ```
  IF [IsVL3] = 1
  AND DATEDIFF('hour', [DateTime VL0], [DateTime VL3]) <= 72
  THEN 'Yes' 
  ELSE 'No'
  END
  ```

- **VB: VL3 Categories**

  ```
  IF DATEDIFF('hour', [DateTime VL0], [DateTime VL3]) <= 1
  THEN '<= 1 Hour'
  
  ELSEIF DATEDIFF('hour', [DateTime VL0], [DateTime VL3]) > 1
  AND DATEDIFF('hour', [DateTime VL0], [DateTime VL3]) <= 24
  THEN '<= 24 Hrs'
  
  ELSEIF DATEDIFF('hour', [DateTime VL0], [DateTime VL3]) > 24
  THEN '> 24 Hrs'
  
  ELSE 'Error Check'
  END
  ```

- **Verification Categories**

  ```
  IF     IFNULL([IsVL0],0) = 1 AND IFNULL([IsVL1],0) = 0 AND IFNULL([IsVL2],0) = 0 AND IFNULL([IsVL3],0) = 0  AND IFNULL([Is FTD],0) = 0 THEN "Reg Only"
  ELSEIF IFNULL([IsVL0],0) = 1 AND IFNULL([IsVL1],0) = 1 AND IFNULL([IsVL2],0) = 0 AND IFNULL([IsVL3],0) = 0  AND IFNULL([Is FTD],0) = 0 THEN "In VL1"
  ELSEIF IFNULL([IsVL0],0) = 1 AND IFNULL([IsVL1],0) = 1 AND IFNULL([IsVL2],0) = 1 AND IFNULL([IsVL3],0) = 0  AND IFNULL([Is FTD],0) = 0 THEN "In VL2"
  ELSEIF IFNULL([IsVL0],0) = 1 AND IFNULL([IsVL1],0) = 1 AND IFNULL([IsVL2],0) = 1 AND IFNULL([IsVL3],0) = 1  AND IFNULL([Is FTD],0) = 0 THEN "In VL3"
  ELSEIF IFNULL([IsVL0],0) = 1 AND IFNULL([IsVL1],0) = 1 AND IFNULL([IsVL2],0) = 1 AND IFNULL([IsVL3],0) = 1  AND IFNULL([Is FTD],0) = 1 THEN "Verified FTD"
  ELSEIF IFNULL([IsVL0],0) = 1 AND IFNULL([IsVL1],0) = 1 AND IFNULL([IsVL2],0) = 1 AND IFNULL([IsVL3],0) = 0  AND IFNULL([Is FTD],0) = 1 THEN "Unverified FTD"
  ELSE "Error In Flow"
  END
  ```

- **VL1 in Reg Month**

  ```
  if MONTH([Date of Registration])=MONTH([DateTime VL1]) then 1 else 0 end
  ```

- **VL2 in Reg Month**

  ```
  if MONTH([Date of Registration])=MONTH([DateTime VL2]) then 1 else 0 end
  ```

- **VL3 from reg  5 month diff abs**

  ```
  sum (if [Period Ind]='Curr
   Month' then [VL3 in Reg Month] else 0 end)/sum(if [Period Ind]='Curr
   Month' then 1 else 0 end)
  -
  
  sum (if [Period Ind]='5 Month
  Before' then [VL3 in Reg Month] else 0 end)/sum(if [Period Ind]='5 Month
  Before' then 1 else 0 end)
  ```

- **VL3 from reg prev month diff abs **

  ```
  sum (if [Period Ind]='Curr
   Month' then [VL3 in Reg Month] else 0 end)/sum(if [Period Ind]='Curr
   Month' then 1 else 0 end)
  -
  
  sum (if [Period Ind]='Prev
  Month' then [VL3 in Reg Month] else 0 end)/sum(if [Period Ind]='Prev
  Month' then 1 else 0 end)
  ```

- **VL3 in Reg Month**

  ```
  if MONTH([Date of Registration])=MONTH([DateTime VL3]) then 1 else 0 end
  ```

- **VL3&FTD in Reg Month**

  ```
  if MONTH([Date of Registration])=MONTH([DateTime VL3]) 
  and MONTH([DateTime FTD])=MONTH([DateTime VL3]) 
  and year([DateTime FTD])=year([DateTime VL3]) 
    then 1 else 0 end
  ```

- **WoW eToro STP % Change**

  ```
  COUNT(IF WEEK([DateTime VL3]) = WEEK(TODAY()) AND [IsSTP eToro] = 1 THEN CID END)
  /
  COUNT(IF WEEK([DateTime VL3]) = WEEK(DATEADD('week', -1, TODAY())) AND [IsSTP eToro] = 1 THEN CID END)
  -1
  ```

### Workbook: Professional Customers Report

#### Datasource: Applications

- **EmptyCells**

  ```
  IFNULL(
  LOOKUP(COUNT(CID),0),PREVIOUS_VALUE(0)
  )
  ```

- **MonthFilter**

  ```
  [YearMonth] = [Month]
  ```

#### Datasource: Approved Applications

- **# Customers**

  ```
  COUNT([Real CID])
  ```

- **MonthFilter**

  ```
  [YearMonth] = [Month] and DATETRUNC('month',[From Date]) = [Date]
  ```

#### Datasource: Pending Applications

- **CID**

  ```
  [RealCID]
  ```

- **Days Since Application**

  ```
  If([DaysSinceApplication])<30 THEN '0-30 Days'
      ELSEIF([DaysSinceApplication])<60 THEN '30-60 Days'
          ELSEIF([DaysSinceApplication])<120 THEN '60-120 Days'
              ELSE '>120' END
  ```

- **MonthFilter**

  ```
  [YearMonth] = [Month]
  ```

- **NumOfCID**

  ```
  zn(COUNT([RealCID]))
  ```

#### Datasource: Professional Customer

- **# Customers**

  ```
  COUNT([RealCID])
  ```

- **Active Open Customers**

  ```
  SUM([IsActive])
  ```

- **CID**

  ```
  [RealCID]
  ```

- **MonthFilter**

  ```
  [YearMonth] = [Month]
  ```

### Workbook: Professional Customers: Pending Applications 

#### Datasource: Applications

- **EmptyCells**

  ```
  IFNULL(
  LOOKUP(COUNT(CID),0),PREVIOUS_VALUE(0)
  )
  ```

- **MonthFilter**

  ```
  [YearMonth] = [Month]
  ```

#### Datasource: Approved Applications

- **# Customers**

  ```
  COUNT([Real CID])
  ```

- **MonthFilter**

  ```
  [YearMonth] = [Month] and DATETRUNC('month',[From Date]) = [Date]
  ```

#### Datasource: Pending Applications

- **CID**

  ```
  [RealCID]
  ```

- **Days Since Application**

  ```
  If([DaysSinceApplication])<30 THEN '0-30 Days'
      ELSEIF([DaysSinceApplication])<60 THEN '30-60 Days'
          ELSEIF([DaysSinceApplication])<120 THEN '60-120 Days'
              ELSE '>120' END
  ```

- **info**

  ```
  ""
  ```

- **MonthFilter**

  ```
  [YearMonth] = [Month]
  ```

- **NumOfCID**

  ```
  zn(COUNT([RealCID]))
  ```

#### Datasource: Professional Customer

- **# Customers**

  ```
  COUNT([RealCID])
  ```

- **Active Open Customers**

  ```
  SUM([IsActive])
  ```

- **CID**

  ```
  [RealCID]
  ```

- **MonthFilter**

  ```
  [YearMonth] = [Month]
  ```

#### Datasource: tableau_icons (bi)

- **info**

  ```
  ""
  ```
