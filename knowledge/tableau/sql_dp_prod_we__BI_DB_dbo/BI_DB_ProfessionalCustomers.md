# Tableau Usage — BI_DB_ProfessionalCustomers

> Generated by `tools/tableau/extract_table_metadata.py`. Do not edit by hand.

## Identity

- Full name: `[BI_DB_dbo].[BI_DB_ProfessionalCustomers]`
- Database: `sql_dp_prod_we` (azure_sql_dw)
- Schema: `BI_DB_dbo`
- Connection type: `azure_sql_dw`
- Tableau metadata id: `5433e8aa-0df6-f99b-f201-4abd8c512b40`

## Summary

- Custom SQL queries: **3**
- Downstream workbooks: **3**
- Downstream calculated fields: **26**

## Custom SQL queries referencing this table

### 1. Custom SQL Query

- Tableau id: `3a390f07-c23d-f07b-1be3-f8f54d555d52`

```sql
SELECT pc.Date
        ,pc.RealCID
	,pc.Desk
        ,pc.AM
        ,pc.IsActive
	,dd.MonthName+' '+ CAST(dd.CalendarYear AS VARCHAR(30)) AS YearMonth
	,dc1.Name Country
	,dc1.MarketingRegionManualName Region
  FROM [BI_DB_dbo].[BI_DB_ProfessionalCustomers] pc WITH (NOLOCK)
  INNER JOIN [DWH_dbo].[Dim_Date] dd WITH (NOLOCK)
  ON pc.DateID = dd.DateKey
  INNER JOIN [DWH_dbo].[Dim_Customer] dc WITH (NOLOCK)
  ON pc.RealCID = dc.RealCID
  INNER JOIN [DWH_dbo].[Dim_Country] dc1 WITH (NOLOCK)
  ON dc.CountryID = dc1.CountryID
```

### 2. Custom SQL Query

- Tableau id: `62ec73f3-47cc-3281-df5d-6874318e38e8`

```sql
SELECT pc.Date
        ,pc.RealCID
	,pc.Desk
        ,pc.AM
        ,pc.IsActive
	,dd.MonthName+' '+ CAST(dd.CalendarYear AS VARCHAR(30)) AS YearMonth
	,dc1.Name Country
	,dc1.MarketingRegionManualName Region
	,fm.Revenue_Total Revenue
  FROM [BI_DB_dbo].[BI_DB_ProfessionalCustomers] pc WITH (NOLOCK)
  INNER JOIN [DWH_dbo].[Dim_Date] dd WITH (NOLOCK)
  ON pc.DateID = dd.DateKey
  INNER JOIN [DWH_dbo].[Dim_Customer] dc WITH (NOLOCK)
  ON pc.RealCID = dc.RealCID
  INNER JOIN [DWH_dbo].[Dim_Country] dc1 WITH (NOLOCK)
  ON dc.CountryID = dc1.CountryID
  LEFT JOIN [BI_DB_dbo].[BI_DB_CID_MonthlyPanel_FullData] fm WITH (NOLOCK)
  ON dc.RealCID = fm.CID
  AND pc.Date = fm.ActiveDate
```

### 3. Custom SQL Query

- Tableau id: `b6c6b583-1c41-7eaa-2a89-a8a8fb8bab76`

```sql
SELECT pc.Date
        ,pc.RealCID
	,pc.Desk
        ,pc.AM
        ,pc.IsActive
	,dd.MonthName+' '+ CAST(dd.CalendarYear AS VARCHAR(30)) AS YearMonth
	,dc1.Name Country
	,dc1.MarketingRegionManualName Region
  FROM [BI_DB_dbo].[BI_DB_ProfessionalCustomers] pc WITH (NOLOCK)
  INNER JOIN [DWH_dbo].[Dim_Date] dd WITH (NOLOCK)
  ON pc.DateID = dd.DateKey
  INNER JOIN [DWH_dbo].[Dim_Customer] dc WITH (NOLOCK)
  ON pc.RealCID = dc.RealCID
  INNER JOIN [DWH_dbo].[Dim_Country] dc1 WITH (NOLOCK)
  ON dc.CountryID = dc1.CountryID
```

## Downstream workbooks

_Attribution: 2 via direct table-drag, 3 via custom SQL (workbooks can use both paths)._

| # | Workbook | Project | Owner | Via | Last updated | URL |
|---|---|---|---|---|---|---|
| 1 | Professional Customers Report | Customer Facing | Tom Boksenbojm | custom_sql,direct | 2024-10-06T06:26:04Z | [open](https://reports.etorocorp.com/#/workbooks/ProfessionalCustomersReport) |
| 2 | Professional Customers: Pending Application  | Customer Facing & Support - Old | Tom Boksenbojm | custom_sql | 2024-11-02T10:01:53Z | [open](https://reports.etorocorp.com/#/workbooks/ProfessionalCustomersPendingApplication) |
| 3 | Professional Customers: Pending Applications  | Sandbox | Tom Boksenbojm | custom_sql,direct | 2023-08-31T09:10:08Z | [open](https://reports.etorocorp.com/#/workbooks/ProfessionalCustomersPendingApplications) |

### View URLs (per workbook)

- **Professional Customers Report** (Customer Facing)
  - [Professional Customers](https://reports.etorocorp.com/#/views/ProfessionalCustomersReport/ProfessionalCustomers)
  - [Approved Applications](https://reports.etorocorp.com/#/views/ProfessionalCustomersReport/ApprovedApplications)
  - [Number of Applications](https://reports.etorocorp.com/#/views/ProfessionalCustomersReport/NumberofApplications)
  - [Pending Applications](https://reports.etorocorp.com/#/views/ProfessionalCustomersReport/PendingApplications)
  - [Pending Applications Equity/Total Deposits](https://reports.etorocorp.com/#/views/ProfessionalCustomersReport/PendingApplicationsEquityTotalDeposits)
- **Professional Customers: Pending Application ** (Customer Facing & Support - Old)
  - [Pending Applications ](https://reports.etorocorp.com/#/views/ProfessionalCustomersPendingApplication/PendingApplications)
- **Professional Customers: Pending Applications ** (Sandbox)
  - [Pending Applications ](https://reports.etorocorp.com/#/views/ProfessionalCustomersPendingApplications/PendingApplications)

## Downstream calculated fields (in embedded datasources)

### Workbook: Professional Customers Report

#### Datasource: Applications

- **EmptyCells**

  ```
  IFNULL(
  LOOKUP(COUNT(CID),0),PREVIOUS_VALUE(0)
  )
  ```

- **MonthFilter**

  ```
  [YearMonth] = [Month]
  ```

#### Datasource: Approved Applications

- **# Customers**

  ```
  COUNT([Real CID])
  ```

- **MonthFilter**

  ```
  [YearMonth] = [Month] and DATETRUNC('month',[From Date]) = [Date]
  ```

#### Datasource: Pending Applications

- **CID**

  ```
  [RealCID]
  ```

- **Days Since Application**

  ```
  If([DaysSinceApplication])<30 THEN '0-30 Days'
      ELSEIF([DaysSinceApplication])<60 THEN '30-60 Days'
          ELSEIF([DaysSinceApplication])<120 THEN '60-120 Days'
              ELSE '>120' END
  ```

- **MonthFilter**

  ```
  [YearMonth] = [Month]
  ```

- **NumOfCID**

  ```
  zn(COUNT([RealCID]))
  ```

#### Datasource: Professional Customer

- **# Customers**

  ```
  COUNT([RealCID])
  ```

- **Active Open Customers**

  ```
  SUM([IsActive])
  ```

- **CID**

  ```
  [RealCID]
  ```

- **MonthFilter**

  ```
  [YearMonth] = [Month]
  ```

### Workbook: Professional Customers: Pending Applications 

#### Datasource: Applications

- **EmptyCells**

  ```
  IFNULL(
  LOOKUP(COUNT(CID),0),PREVIOUS_VALUE(0)
  )
  ```

- **MonthFilter**

  ```
  [YearMonth] = [Month]
  ```

#### Datasource: Approved Applications

- **# Customers**

  ```
  COUNT([Real CID])
  ```

- **MonthFilter**

  ```
  [YearMonth] = [Month] and DATETRUNC('month',[From Date]) = [Date]
  ```

#### Datasource: Pending Applications

- **CID**

  ```
  [RealCID]
  ```

- **Days Since Application**

  ```
  If([DaysSinceApplication])<30 THEN '0-30 Days'
      ELSEIF([DaysSinceApplication])<60 THEN '30-60 Days'
          ELSEIF([DaysSinceApplication])<120 THEN '60-120 Days'
              ELSE '>120' END
  ```

- **info**

  ```
  ""
  ```

- **MonthFilter**

  ```
  [YearMonth] = [Month]
  ```

- **NumOfCID**

  ```
  zn(COUNT([RealCID]))
  ```

#### Datasource: Professional Customer

- **# Customers**

  ```
  COUNT([RealCID])
  ```

- **Active Open Customers**

  ```
  SUM([IsActive])
  ```

- **CID**

  ```
  [RealCID]
  ```

- **MonthFilter**

  ```
  [YearMonth] = [Month]
  ```

#### Datasource: tableau_icons (bi)

- **info**

  ```
  ""
  ```
