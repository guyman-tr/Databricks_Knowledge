# Tableau Usage — BI_DB_RevenueForum

> Generated by `tools/tableau/extract_table_metadata.py`. Do not edit by hand.

## Identity

- Full name: `BI_DB_dbo.BI_DB_RevenueForum`
- Database: `sql_dp_prod_we` (azure_sql_dw)
- Schema: `BI_DB_dbo`
- Connection type: `azure_sql_dw`
- Tableau metadata id: `0038c8de-1bfb-05b7-bc2d-25aab61fa7f0`

## Summary

- Custom SQL queries: **2**
- Downstream workbooks: **2**
- Downstream calculated fields: **21**

## Custom SQL queries referencing this table

### 1. Custom SQL Query

- Tableau id: `013767cf-b31f-0eba-b309-b312b036dd1d`

```sql
SELECT * FROM BI_DB_dbo.BI_DB_RevenueForum
```

### 2. Custom SQL Query

- Tableau id: `9ef73572-da5b-7382-2672-d1f2cb61c797`

```sql
SELECT * FROM BI_DB_dbo.BI_DB_RevenueForum
```

## Downstream workbooks

_Attribution: 1 via direct table-drag, 2 via custom SQL (workbooks can use both paths)._

| # | Workbook | Project | Owner | Via | Last updated | URL |
|---|---|---|---|---|---|---|
| 1 | RevenueForum Dashboard | Customer Facing | Adi Meidan | custom_sql,direct | 2026-05-31T10:02:43Z | [open](https://reports.etorocorp.com/#/workbooks/RevenueForumDashboard_17579189706220) |
| 2 | RevenueForum Dashboard | Sandbox | Tom Boksenbojm | custom_sql | 2026-01-05T09:40:53Z | [open](https://reports.etorocorp.com/#/workbooks/RevenueForumDashboard) |

### View URLs (per workbook)

- **RevenueForum Dashboard** (Customer Facing)
  - [KPI](https://reports.etorocorp.com/#/views/RevenueForumDashboard_17579189706220/KPI)
  - [Main KPI](https://reports.etorocorp.com/#/views/RevenueForumDashboard_17579189706220/MainKPI)
  - [Club](https://reports.etorocorp.com/#/views/RevenueForumDashboard_17579189706220/Club)
  - [Revenue By Category](https://reports.etorocorp.com/#/views/RevenueForumDashboard_17579189706220/RevenueByCategory)
  - [Revenue By Asset Type](https://reports.etorocorp.com/#/views/RevenueForumDashboard_17579189706220/RevenueByAssetType)
  - [Avg LTV](https://reports.etorocorp.com/#/views/RevenueForumDashboard_17579189706220/AvgLTV)
- **RevenueForum Dashboard** (Sandbox)
  - [KPI](https://reports.etorocorp.com/#/views/RevenueForumDashboard/KPI)
  - [Main KPI](https://reports.etorocorp.com/#/views/RevenueForumDashboard/MainKPI)
  - [Club](https://reports.etorocorp.com/#/views/RevenueForumDashboard/Club)
  - [Revenue By Category](https://reports.etorocorp.com/#/views/RevenueForumDashboard/RevenueByCategory)
  - [Revenue By Asset Type](https://reports.etorocorp.com/#/views/RevenueForumDashboard/RevenueByAssetType)
  - [Avg LTV](https://reports.etorocorp.com/#/views/RevenueForumDashboard/AvgLTV)

## Downstream calculated fields (in embedded datasources)

### Workbook: RevenueForum Dashboard

#### Datasource: LTV

- **AVG LTV**

  ```
  sum([LTV])/sum([LTV_FTDs])
  ```

- **CPA**

  ```
  sum([Cost])/sum([FTDs])
  ```

- **FirstDayOfMonth**

  ```
  date(str(left([YearMonth],4))+'/'+str(RIGHT([YearMonth],2))+'/'+str(1))
  ```

- **LTV/CPA**

  ```
  [AVG LTV]/[CPA]
  ```

#### Datasource: Revenue Forum

- **ARPU Club**

  ```
  SUM([TotalRevenue])/SUM([ClubMember])
  ```

- **ARPU Funded**

  ```
  SUM([TotalRevenue])/SUM([Funded])
  ```

- **Churn Rate**

  ```
  SUM([Churn])/SUM([Funded])
  ```

- **Club Member**

  ```
  IF [Club]<>'Bronze' THEN 'Club' ELSE 'Bronze' END
  ```

- **CO Ratio**

  ```
  SUM([WithdrawAmount])/SUM([DepositAmount])
  ```

- **Country filter**

  ```
  [Country] =[Country]
  or [Country] = 'All'
  ```

- **CPA ($) **

  ```
  { FIXED [FirstDayOfMonth],[Region]: MAX([Cost])}/([FTD])
  ```

- **Date filter**

  ```
  [MonthYear]=[FirstDayOfMonth]
  ```

- **High Tier Club Member**

  ```
  SUM(IF  [Club] in ('Platinum Plus', 'Diamond' ) THEN [ClubMember] END)
  ```

- **Marketing ($)**

  ```
  { FIXED [FirstDayOfMonth],[Region]: MAX([Cost])}
  ```

- **Net Deposit ($)**

  ```
  SUM([DepositAmount]) - SUM([WithdrawAmount])
  ```

- **Region Filter**

  ```
  [Region]=[Region]
  or [Region] = 'All'
  ```

- **Regulation filter**

  ```
  [Regulation] = [Regulation]
  or [Regulation] = 'All'
  ```

#### Datasource: Revenue Forum Revenue

- **Calculation1**

  ```
  IF [Metric] = 'SDRT' OR [Metric] = 'Dividends' THEN 0 
  ELSE ([Amount]) END
  ```

- **Calculation2**

  ```
  IF MIN([Metric]) = "Grand Total" THEN
      // זה כדי למנוע בעיות חישוב, אבל אם אין לך שורה בשם Total זה לא חובה
      SUM([Amount])
  
  ELSEIF SIZE() = 1 THEN
      // GRAND TOTAL בלבד
      SUM([Amount])
      - SUM(
          IF [Metric] = 'SDRT' OR [Metric] = 'Dividends' THEN [Amount] END
        )
  
  ELSE
      // כל שורה רגילה + Row Total רגיל
      SUM([Amount])
  END
  ```

- **Clubs**

  ```
  IF [Club]<>'Bronze' THEN 'Club' ELSE 'Bronze' END
  ```

- **Grand Total**

  ```
  'Grand Total'
  ```
