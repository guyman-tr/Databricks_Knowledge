# Tableau Usage — BI_DB_DailyCommisionReport

> Generated by `tools/tableau/extract_table_metadata.py`. Do not edit by hand.

## Identity

- Full name: `[BI_DB_dbo].[BI_DB_DailyCommisionReport]`
- Database: `sql_dp_prod_we` (azure_sql_dw)
- Schema: `BI_DB_dbo`
- Connection type: `azure_sql_dw`
- Tableau metadata id: `8337199d-f79c-c6eb-55d1-fa3a0c8ddfb7`

## Summary

- Custom SQL queries: **18**
- Downstream workbooks: **17**
- Downstream calculated fields: **22**

## Custom SQL queries referencing this table

### 1. Custom SQL Query

- Tableau id: `0c5713bd-ef50-2e35-c291-582814875321`

```sql
SELECT dc.GCID,dc.RealCID AS CID
      ,sub.AccountManager
	  ,CASE WHEN dc.GCID LIKE '%[0123]' THEN 'Test' ELSE 'Control' END AS Segment
	  ,c.Last_contact
      ,CAST(dc.FirstDepositDate AS DATE) AS FirstDepositDate
	  ,CAST(dc.RegisteredReal AS DATE) AS RegDate
	  ,dpl.Name AS Club
	  ,dc1.Name AS Country
	  ,b.Crypto_revenue_2023
	  ,b.Revenue_2023
	  ,sub.ACC_CountDeposits
	  ,sub.Active_Month
	  ,sub.LastPosOpenDate
	  ,sub.ACC_Revenue_Total
	  ,sub.ACC_Revenue_Crypto
	  ,sub.ACC_TotalDeposits
	  ,sub.EOM_Equity AS Equity
	  ,CASE WHEN sub.ACC_Revenue_Total =0 THEN 1 ELSE 0 END AS '0 ACC Revenue'
	  ,CASE WHEN sub.ACC_Revenue_Crypto =0 THEN 1 ELSE 0 END AS '0 ACC Crypto Revenue'
FROM DWH_dbo.Dim_Customer dc WITH (NOLOCK)
INNER JOIN 
(SELECT bddcr.CountryID
FROM BI_DB_dbo.BI_DB_DailyCommisionReport bddcr WITH (NOLOCK)
WHERE bddcr.DateID>20230101
GROUP BY bddcr.CountryID
HAVING SUM(CASE WHEN InstrumentTypeID = 10 THEN bddcr.FullCommissions +bddcr.RollOverFee ELSE 0 END)>0
)a ON dc.CountryID=a.CountryID
LEFT JOIN 
(SELECT bddcr.RealCID
       ,SUM(CASE WHEN InstrumentTypeID = 10 THEN bddcr.FullCommissions +bddcr.RollOverFee ELSE 0 END) AS Crypto_revenue_2023
	   ,SUM( bddcr.FullCommissions +bddcr.RollOverFee ) AS Revenue_2023
FROM BI_DB_dbo.BI_DB_DailyCommisionReport bddcr WITH (NOLOCK)
WHERE bddcr.DateID>20230101
GROUP BY bddcr.RealCID
HAVING SUM(CASE WHEN InstrumentTypeID = 10 THEN bddcr.FullCommissions +bddcr.RollOverFee ELSE 0 END)>0
) b ON dc.RealCID=b.RealCID
LEFT JOIN (
SELECT  bdcmpfd.CID
       ,bdcmpfd.AccountManager
       ,bdcmpfd.Active_Month
	   ,bdcmpfd.ActiveDate
	   ,bdcmpfd.LastPosOpenDate
	   ,bdcmpfd.ACC_Revenue_Total
	   ,bdcmpfd.ACC_Revenue_Real_Crypto+bdcmpfd.ACC_Revenue_CFD_Crypto AS ACC_Revenue_Crypto
	   ,bdcmpfd.EOM_Equity
	   ,bdcmpfd.ACC_TotalDeposits
	   ,bdcmpfd.ACC_CountDeposits
FROM BI_DB_dbo.BI_DB_CID_MonthlyPanel_FullData bdcmpfd) sub ON dc.RealCID=sub.CID AND sub.ActiveDate=DATEADD(Month,DATEDIFF(Month,0,GETDATE ()-1),0)
LEFT JOIN (
SELECT  bduts.CID
       ,MAX(CAST(bduts.CreatedDate AS DATE)) AS Last_contact
FROM BI_DB_dbo.BI_DB_UsageTracking_SF bduts WITH (NOLOCK)
WHERE bduts.ActionName IN ('Phone_Call_Succeed__c','Completed_Contact_Email__c') AND bduts.CreatedDate>='20231201' 
GROUP BY bduts.CID)c ON c.CID=dc.RealCID
INNER JOIN DWH_dbo.Dim_Country dc1 WITH (NOLOCK) ON dc.CountryID = dc1.CountryID
INNER JOIN DWH_dbo.Dim_PlayerLevel dpl WITH (NOLOCK) ON dc.PlayerLevelID = dpl.PlayerLevelID
WHERE  (dc.GCID LIKE '%[0123]' OR  dc.GCID LIKE '%[456789]')
AND dc.IsValidCustomer=1 
AND dc.RegisteredReal>='20211101' 
AND dc.RegisteredReal<'20231101' 
--AND dc.VerificationLevelID=3
--AND (dc.FirstDepositDate ='1900-01-01 00:00:00.000' OR dc.FirstDepositDate>='20231201')
AND dc.FirstDepositDate>='20231201'
```

### 2. Custom SQL Query

- Tableau id: `3973c3ca-cb4a-ae98-579a-ee271b3836d0`

```sql
--- yesterday

SELECT 
	'Yesterday' AS [Period],
    RealCID,
	UserName,
	Region,
	Manager,
	Regulation,
	Mifid,
	InstrumentType, -- Added here
    Metric, 
    Amount
FROM 
(
    SELECT 
        RealCID,
		Filtered.UserName,
		Filtered.Region,
		Filtered.Manager,
		Filtered.Regulation,
		Filtered.Mifid,
		Filtered.InstrumentType, -- Added here
        SUM(Commissions) AS Commissions,
        SUM(FullCommissions) AS FullCommissions,
        SUM(RollOverFee) AS RollOverFee,
        SUM(TicketFee) AS TicketFee,
        SUM(TicketFeeByPercent) AS TicketFeeByPercent,
        SUM(AdminFee) AS AdminFee,
        SUM(SpotAdjustFee) AS SpotAdjustFee,
        SUM(VolumeOnOpen) AS VolumeOnOpen,
        SUM(VolumeOnClose) AS VolumeOnClose,
		SUM(ISNULL(FullCommissions,0)) + SUM(ISNULL(RollOverFee,0)) + SUM(ISNULL(TicketFee,0)) + SUM(ISNULL(TicketFeeByPercent,0)) + SUM(ISNULL(AdminFee,0)) + SUM(ISNULL(SpotAdjustFee,0)) AS TotalTradingRevenue
    FROM 
    (
        SELECT *
        FROM BI_DB_dbo.BI_DB_DailyCommisionReport
        WHERE 
            IsValidCustomer = 1
            AND DateID = CAST(FORMAT(CAST(getdate()-1 AS DATE),'yyyyMMdd') as INT)
            AND RealCID IN (
                SELECT TOP 20 RealCID
                FROM BI_DB_dbo.BI_DB_DailyCommisionReport
                WHERE DateID = CAST(FORMAT(CAST(getdate()-1 AS DATE),'yyyyMMdd') as INT) 
                GROUP BY RealCID
                ORDER BY SUM(ISNULL(FullCommissions,0) + ISNULL(RollOverFee,0) + ISNULL(TicketFee,0) + ISNULL(TicketFeeByPercent,0) + ISNULL(AdminFee,0) + ISNULL(SpotAdjustFee,0)) DESC
            )
    ) AS Filtered
    GROUP BY RealCID,		
				UserName,
				Region,
				Manager,
				Regulation,
				Mifid,
				InstrumentType -- Added here
) AS Aggregated
UNPIVOT
(
    Amount FOR Metric IN (
        Commissions,
        FullCommissions,
        RollOverFee,
        TicketFee,
        TicketFeeByPercent,
        AdminFee,
        SpotAdjustFee,
		VolumeOnOpen,
		VolumeOnClose,
		TotalTradingRevenue
    )
) AS unpvt

--- This Month

UNION all 

SELECT 
	'CurrentMonth' AS [Period],
    RealCID,
	UserName,
	Region,
	Manager,
	Regulation,
	Mifid,
	InstrumentType, -- Added here
    Metric, 
    Amount
FROM 
(
    SELECT 
        RealCID,
		Filtered.UserName,
		Filtered.Region,
		Filtered.Manager,
		Filtered.Regulation,
		Filtered.Mifid,
		Filtered.InstrumentType, -- Added here
        SUM(Commissions) AS Commissions,
        SUM(FullCommissions) AS FullCommissions,
        SUM(RollOverFee) AS RollOverFee,
        SUM(TicketFee) AS TicketFee,
        SUM(TicketFeeByPercent) AS TicketFeeByPercent,
        SUM(AdminFee) AS AdminFee,
        SUM(SpotAdjustFee) AS SpotAdjustFee,
        SUM(VolumeOnOpen) AS VolumeOnOpen,
        SUM(VolumeOnClose) AS VolumeOnClose,
		SUM(ISNULL(FullCommissions,0)) + SUM(ISNULL(RollOverFee,0)) + SUM(ISNULL(TicketFee,0)) + SUM(ISNULL(TicketFeeByPercent,0)) + SUM(ISNULL(AdminFee,0)) + SUM(ISNULL(SpotAdjustFee,0))  AS TotalTradingRevenue
    FROM 
    (
        SELECT *
        FROM BI_DB_dbo.BI_DB_DailyCommisionReport
        WHERE 
            IsValidCustomer = 1
            AND DateID BETWEEN CAST(FORMAT(CAST(DATEADD(month, DATEDIFF(month, 0, getdate()-1), 0) AS DATE),'yyyyMMdd') as INT) AND CAST(FORMAT(CAST(getdate()-1 AS DATE),'yyyyMMdd') as INT) 
            AND RealCID IN (
                SELECT TOP 20 RealCID
                FROM BI_DB_dbo.BI_DB_DailyCommisionReport
                WHERE DateID BETWEEN CAST(FORMAT(CAST(DATEADD(month, DATEDIFF(month, 0, getdate()-1), 0) AS DATE),'yyyyMMdd') as INT) AND CAST(FORMAT(CAST(getdate()-1 AS DATE),'yyyyMMdd') as INT) 
                GROUP BY RealCID
                ORDER BY SUM(ISNULL(FullCommissions,0) + ISNULL(RollOverFee,0) + ISNULL(TicketFee,0) + ISNULL(TicketFeeByPercent,0) + ISNULL(AdminFee,0) + ISNULL(SpotAdjustFee,0)) DESC
            )
    ) AS Filtered
    GROUP BY RealCID,		
				UserName,
				Region,
				Manager,
				Regulation,
				Mifid,
				InstrumentType -- Added here
) AS Aggregated
UNPIVOT
(
    Amount FOR Metric IN (
        Commissions,
        FullCommissions,
        RollOverFee,
        TicketFee,
        TicketFeeByPercent,
        AdminFee,
        SpotAdjustFee,
		VolumeOnOpen,
		VolumeOnClose,
		TotalTradingRevenue
    )
) AS unpvt

--- Last Month

UNION all 

SELECT 
	'PreviousMonthSameTime' AS [Period],
    RealCID,
	UserName,
	Region,
	Manager,
	Regulation,
	Mifid,
	InstrumentType, -- Added here
    Metric, 
    Amount
FROM 
(
    SELECT 
        RealCID,
		Filtered.UserName,
		Filtered.Region,
		Filtered.Manager,
		Filtered.Regulation,
		Filtered.Mifid,
		Filtered.InstrumentType, -- Added here
        SUM(Commissions) AS Commissions,
        SUM(FullCommissions) AS FullCommissions,
        SUM(RollOverFee) AS RollOverFee,
        SUM(TicketFee) AS TicketFee,
        SUM(TicketFeeByPercent) AS TicketFeeByPercent,
        SUM(AdminFee) AS AdminFee,
        SUM(SpotAdjustFee) AS SpotAdjustFee,
        SUM(VolumeOnOpen) AS VolumeOnOpen,
        SUM(VolumeOnClose) AS VolumeOnClose,
		SUM(ISNULL(FullCommissions,0)) + SUM(ISNULL(RollOverFee,0)) + SUM(ISNULL(TicketFee,0)) + SUM(ISNULL(TicketFeeByPercent,0)) + SUM(ISNULL(AdminFee,0)) + SUM(ISNULL(SpotAdjustFee,0))  AS TotalTradingRevenue
    FROM 
    (
        SELECT *
        FROM BI_DB_dbo.BI_DB_DailyCommisionReport
        WHERE 
            IsValidCustomer = 1
            AND DateID BETWEEN CAST(FORMAT(CAST( DATEADD(month, DATEDIFF(month, 0, getdate()-1) - 1, 0) AS DATE),'yyyyMMdd') as INT) AND CAST(FORMAT(CAST(dateadd(MONTH,-1,getdate()-1)  AS DATE),'yyyyMMdd') as INT) 
            AND RealCID IN (
                SELECT TOP 20 RealCID
                FROM BI_DB_dbo.BI_DB_DailyCommisionReport
                WHERE DateID BETWEEN CAST(FORMAT(CAST( DATEADD(month, DATEDIFF(month, 0, getdate()-1) - 1, 0) AS DATE),'yyyyMMdd') as INT) AND CAST(FORMAT(CAST(dateadd(MONTH,-1,getdate()-1)  AS DATE),'yyyyMMdd') as INT)
                GROUP BY RealCID
                ORDER BY SUM(ISNULL(FullCommissions,0) + ISNULL(RollOverFee,0) + ISNULL(TicketFee,0) + ISNULL(TicketFeeByPercent,0) + ISNULL(AdminFee,0) + ISNULL(SpotAdjustFee,0)) DESC
            )
    ) AS Filtered
    GROUP BY RealCID,		
				UserName,
				Region,
				Manager,
				Regulation,
				Mifid,
				InstrumentType -- Added here
) AS Aggregated
UNPIVOT
(
    Amount FOR Metric IN (
        Commissions,
        FullCommissions,
        RollOverFee,
        TicketFee,
        TicketFeeByPercent,
        AdminFee,
        SpotAdjustFee,
		VolumeOnOpen,
		VolumeOnClose,
		TotalTradingRevenue
    )
) AS unpvt

--- Current Year

UNION all 

SELECT 
	'CurrentYear' AS [Period],
    RealCID,
	UserName,
	Region,
	Manager,
	Regulation,
	Mifid,
	InstrumentType, -- Added here
    Metric, 
    Amount
FROM 
(
    SELECT 
        RealCID,
		Filtered.UserName,
		Filtered.Region,
		Filtered.Manager,
		Filtered.Regulation,
		Filtered.Mifid,
		Filtered.InstrumentType, -- Added here
        sum(Commissions) AS Commissions,
        sum(FullCommissions) AS FullCommissions,
        sum(RollOverFee) AS RollOverFee,
        sum(TicketFee) AS TicketFee,
        sum(TicketFeeByPercent) AS TicketFeeByPercent,
        sum(AdminFee) AS AdminFee,
        SUM(SpotAdjustFee) AS SpotAdjustFee,
        SUM(VolumeOnOpen) AS VolumeOnOpen,
        SUM(VolumeOnClose) AS VolumeOnClose,
		SUM(ISNULL(FullCommissions,0)) + SUM(ISNULL(RollOverFee,0)) + SUM(ISNULL(TicketFee,0)) + SUM(ISNULL(TicketFeeByPercent,0)) + SUM(ISNULL(AdminFee,0)) + SUM(ISNULL(SpotAdjustFee,0))  AS TotalTradingRevenue
    FROM 
    (
        SELECT ia.*
        FROM BI_DB_dbo.BI_DB_DailyCommisionReport ia 
        WHERE 
            IsValidCustomer = 1
            AND DateID BETWEEN CAST(FORMAT(CAST(DATEADD(yy, DATEDIFF(yy, 0, getdate()-1), 0) AS DATE),'yyyyMMdd') as INT)  AND CAST(FORMAT(CAST(getdate()-1 AS DATE),'yyyyMMdd') as INT) 
			AND RealCID IN (
                SELECT TOP 20 RealCID
                FROM BI_DB_dbo.BI_DB_DailyCommisionReport ia
				WHERE ia.DateID between CAST(FORMAT(CAST(DATEADD(yy, DATEDIFF(yy, 0, getdate()-1), 0) AS DATE),'yyyyMMdd') as INT)  AND CAST(FORMAT(CAST(getdate()-1 AS DATE),'yyyyMMdd') as INT) 
                GROUP BY RealCID
                ORDER BY SUM(ISNULL(FullCommissions,0) + ISNULL(RollOverFee,0) + ISNULL(TicketFee,0) + ISNULL(TicketFeeByPercent,0) + ISNULL(AdminFee,0) + ISNULL(SpotAdjustFee,0)) DESC
            )
    ) AS Filtered
    GROUP BY RealCID,		
				UserName,
				Region,
				Manager,
				Regulation,
				Mifid,
				InstrumentType -- Added here
) AS Aggregated
UNPIVOT
(
    Amount FOR Metric IN (
        Commissions,
        FullCommissions,
        RollOverFee,
        TicketFee,
        TicketFeeByPercent,
        AdminFee,
        SpotAdjustFee,
		VolumeOnOpen,
		VolumeOnClose,
		TotalTradingRevenue
    )
) AS unpvt

--- Previous Year

UNION all 

SELECT 
	'PreviousYearSameTime' AS [Period],
    RealCID,
	UserName,
	Region,
	Manager,
	Regulation,
	Mifid,
	InstrumentType, -- Added here
    Metric, 
    Amount
FROM 
(
    SELECT 
        RealCID,
		Filtered.UserName,
		Filtered.Region,
		Filtered.Manager,
		Filtered.Regulation,
		Filtered.Mifid,
		Filtered.InstrumentType, -- Added here
        sum(Commissions) AS Commissions,
        sum(FullCommissions) AS FullCommissions,
        sum(RollOverFee) AS RollOverFee,
        sum(TicketFee) AS TicketFee,
        sum(TicketFeeByPercent) AS TicketFeeByPercent,
        sum(AdminFee) AS AdminFee,
        SUM(SpotAdjustFee) AS SpotAdjustFee,
        SUM(VolumeOnOpen) AS VolumeOnOpen,
        SUM(VolumeOnClose) AS VolumeOnClose,
		SUM(ISNULL(FullCommissions,0)) + SUM(ISNULL(RollOverFee,0)) + SUM(ISNULL(TicketFee,0)) + SUM(ISNULL(TicketFeeByPercent,0)) + SUM(ISNULL(AdminFee,0)) + SUM(ISNULL(SpotAdjustFee,0))  AS TotalTradingRevenue
    FROM 
    (
        SELECT ia.*
        FROM BI_DB_dbo.BI_DB_DailyCommisionReport ia 
        WHERE 
            IsValidCustomer = 1
            AND DateID BETWEEN CAST(FORMAT(CAST(DATEADD(yy, DATEDIFF(yy, 0, GETDATE()) - 1, 0) AS DATE),'yyyyMMdd') as INT) AND CAST(FORMAT(CAST(dateadd(YEAR,-1,getdate()-1) AS DATE),'yyyyMMdd') as INT) 
			AND RealCID IN (
                SELECT TOP 20 RealCID
                FROM BI_DB_dbo.BI_DB_DailyCommisionReport ia
				WHERE ia.DateID between CAST(FORMAT(CAST(DATEADD(yy, DATEDIFF(yy, 0, GETDATE()) - 1, 0) AS DATE),'yyyyMMdd') as INT) AND CAST(FORMAT(CAST(dateadd(YEAR,-1,getdate()-1) AS DATE),'yyyyMMdd') as INT) 
                GROUP BY RealCID
                ORDER BY SUM(ISNULL(FullCommissions,0) + ISNULL(RollOverFee,0) + ISNULL(TicketFee,0) + ISNULL(TicketFeeByPercent,0) + ISNULL(AdminFee,0) + ISNULL(SpotAdjustFee,0)) DESC
            )
    ) AS Filtered
    GROUP BY RealCID,		
				UserName,
				Region,
				Manager,
				Regulation,
				Mifid,
				InstrumentType -- Added here
) AS Aggregated
UNPIVOT
(
    Amount FOR Metric IN (
        Commissions,
        FullCommissions,
        RollOverFee,
        TicketFee,
        TicketFeeByPercent,
        AdminFee,
        SpotAdjustFee,
		VolumeOnOpen,
		VolumeOnClose,
		TotalTradingRevenue
    )
) AS unpvt

--- Current Week

UNION all 

SELECT 
	'CurrentWeek' AS [Period],
    RealCID,
	UserName,
	Region,
	Manager,
	Regulation,
	Mifid,
	InstrumentType, -- Added here
    Metric, 
    Amount
FROM 
(
    SELECT 
        RealCID,
		Filtered.UserName,
		Filtered.Region,
		Filtered.Manager,
		Filtered.Regulation,
		Filtered.Mifid,
		Filtered.InstrumentType, -- Added here
        SUM(Commissions) AS Commissions,
        SUM(FullCommissions) AS FullCommissions,
        SUM(RollOverFee) AS RollOverFee,
        SUM(TicketFee) AS TicketFee,
        SUM(TicketFeeByPercent) AS TicketFeeByPercent,
        SUM(AdminFee) AS AdminFee,
        SUM(SpotAdjustFee) AS SpotAdjustFee,
        SUM(VolumeOnOpen) AS VolumeOnOpen,
        SUM(VolumeOnClose) AS VolumeOnClose,
		SUM(ISNULL(FullCommissions,0)) + SUM(ISNULL(RollOverFee,0)) + SUM(ISNULL(TicketFee,0)) + SUM(ISNULL(TicketFeeByPercent,0)) + SUM(ISNULL(AdminFee,0)) + SUM(ISNULL(SpotAdjustFee,0))  AS TotalTradingRevenue
    FROM 
    (
        SELECT *
        FROM BI_DB_dbo.BI_DB_DailyCommisionReport
        WHERE 
            IsValidCustomer = 1
            AND DateID BETWEEN CAST(FORMAT(CAST(DATEADD(DAY, 1 - DATEPART(WEEKDAY, getdate()-1), getdate()-1) AS DATE),'yyyyMMdd') as INT)  AND CAST(FORMAT(CAST(getdate()-1 AS DATE),'yyyyMMdd') as INT) 
            AND RealCID IN (
                SELECT TOP 20 RealCID
                FROM BI_DB_dbo.BI_DB_DailyCommisionReport
                WHERE DateID BETWEEN CAST(FORMAT(CAST(DATEADD(DAY, 1 - DATEPART(WEEKDAY, getdate()-1), getdate()-1) AS DATE),'yyyyMMdd') as INT)  AND CAST(FORMAT(CAST(getdate()-1 AS DATE),'yyyyMMdd') as INT) 
                GROUP BY RealCID
                ORDER BY SUM(ISNULL(FullCommissions,0) + ISNULL(RollOverFee,0) + ISNULL(TicketFee,0) + ISNULL(TicketFeeByPercent,0) + ISNULL(AdminFee,0) + ISNULL(SpotAdjustFee,0)) DESC
            )
    ) AS Filtered
    GROUP BY RealCID,		
				UserName,
				Region,
				Manager,
				Regulation,
				Mifid,
				InstrumentType -- Added here
) AS Aggregated
UNPIVOT
(
    Amount FOR Metric IN (
        Commissions,
        FullCommissions,
        RollOverFee,
        TicketFee,
        TicketFeeByPercent,
        AdminFee,
        SpotAdjustFee,
		VolumeOnOpen,
		VolumeOnClose,
		TotalTradingRevenue
    )
) AS unpvt

--- Previous Week

UNION all 

SELECT 
	'PreviousWeekSameTime' AS [Period],
    RealCID,
	UserName,
	Region,
	Manager,
	Regulation,
	Mifid,
	InstrumentType, -- Added here
    Metric, 
    Amount
FROM 
(
    SELECT 
        RealCID,
		Filtered.UserName,
		Filtered.Region,
		Filtered.Manager,
		Filtered.Regulation,
		Filtered.Mifid,
		Filtered.InstrumentType, -- Added here
        SUM(Commissions) AS Commissions,
        SUM(FullCommissions) AS FullCommissions,
        SUM(RollOverFee) AS RollOverFee,
        SUM(TicketFee) AS TicketFee,
        SUM(TicketFeeByPercent) AS TicketFeeByPercent,
        SUM(AdminFee) AS AdminFee,
        SUM(SpotAdjustFee) AS SpotAdjustFee,
        SUM(VolumeOnOpen) AS VolumeOnOpen,
        SUM(VolumeOnClose) AS VolumeOnClose,
		SUM(ISNULL(FullCommissions,0)) + SUM(ISNULL(RollOverFee,0)) + SUM(ISNULL(TicketFee,0)) + SUM(ISNULL(TicketFeeByPercent,0)) + SUM(ISNULL(AdminFee,0)) + SUM(ISNULL(SpotAdjustFee,0))  AS TotalTradingRevenue
    FROM 
    (
        SELECT *
        FROM BI_DB_dbo.BI_DB_DailyCommisionReport
        WHERE 
            IsValidCustomer = 1
            AND DateID BETWEEN CAST(FORMAT(CAST(dateadd (WEEK, -1, DATEADD(DAY, 1 - DATEPART(WEEKDAY, getdate()-1), getdate()-1)) AS DATE),'yyyyMMdd') as INT)  AND CAST(FORMAT(CAST(dateadd(WEEK, -1,getdate()-1) AS DATE),'yyyyMMdd') as INT) 
            AND RealCID IN (
                SELECT TOP 20 RealCID
                FROM BI_DB_dbo.BI_DB_DailyCommisionReport
                WHERE DateID BETWEEN  CAST(FORMAT(CAST(dateadd (WEEK, -1, DATEADD(DAY, 1 - DATEPART(WEEKDAY, getdate()-1), getdate()-1)) AS DATE),'yyyyMMdd') as INT)  AND CAST(FORMAT(CAST(dateadd(WEEK, -1,getdate()-1) AS DATE),'yyyyMMdd') as INT)
                GROUP BY RealCID
                ORDER BY SUM(ISNULL(FullCommissions,0) + ISNULL(RollOverFee,0) + ISNULL(TicketFee,0) + ISNULL(TicketFeeByPercent,0) + ISNULL(AdminFee,0) + ISNULL(SpotAdjustFee,0)) DESC
            )
    ) AS Filtered
    GROUP BY RealCID,		
				UserName,
				Region,
				Manager,
				Regulation,
				Mifid,
				InstrumentType -- Added here
) AS Aggregated
UNPIVOT
(
    Amount FOR Metric IN (
        Commissions,
        FullCommissions,
        RollOverFee,
        TicketFee,
        TicketFeeByPercent,
        AdminFee,
        SpotAdjustFee,
		VolumeOnOpen,
		VolumeOnClose,
		TotalTradingRevenue
    )
) AS unpvt

--- Current Quarter

UNION ALL 

SELECT 
	'CurrentQuarter' AS [Period],
    RealCID,
	UserName,
	Region,
	Manager,
	Regulation,
	Mifid,
	InstrumentType, -- Added here
    Metric, 
    Amount
FROM 
(
    SELECT 
        RealCID,
		Filtered.UserName,
		Filtered.Region,
		Filtered.Manager,
		Filtered.Regulation,
		Filtered.Mifid,
		Filtered.InstrumentType, -- Added here
        SUM(Commissions) AS Commissions,
        SUM(FullCommissions) AS FullCommissions,
        SUM(RollOverFee) AS RollOverFee,
        SUM(TicketFee) AS TicketFee,
        SUM(TicketFeeByPercent) AS TicketFeeByPercent,
        SUM(AdminFee) AS AdminFee,
        SUM(SpotAdjustFee) AS SpotAdjustFee,
        SUM(VolumeOnOpen) AS VolumeOnOpen,
        SUM(VolumeOnClose) AS VolumeOnClose,
		SUM(ISNULL(FullCommissions,0)) + SUM(ISNULL(RollOverFee,0)) + SUM(ISNULL(TicketFee,0)) + SUM(ISNULL(TicketFeeByPercent,0)) + SUM(ISNULL(AdminFee,0)) + SUM(ISNULL(SpotAdjustFee,0))  AS TotalTradingRevenue
    FROM 
    (
        SELECT *
        FROM BI_DB_dbo.BI_DB_DailyCommisionReport
        WHERE 
            IsValidCustomer = 1
            AND DateID BETWEEN CAST(FORMAT(CAST( DATEADD(qq, DATEDIFF(qq, 0, getdate()-1), 0) AS DATE),'yyyyMMdd') as INT)  AND CAST(FORMAT(CAST(getdate()-1 AS DATE),'yyyyMMdd') as INT) 
            AND RealCID IN (
                SELECT TOP 20 RealCID
                FROM BI_DB_dbo.BI_DB_DailyCommisionReport
                WHERE DateID BETWEEN  CAST(FORMAT(CAST( DATEADD(qq, DATEDIFF(qq, 0, getdate()-1), 0) AS DATE),'yyyyMMdd') as INT)  AND CAST(FORMAT(CAST(getdate()-1 AS DATE),'yyyyMMdd') as INT) 
                GROUP BY RealCID
                ORDER BY SUM(ISNULL(FullCommissions,0) + ISNULL(RollOverFee,0) + ISNULL(TicketFee,0) + ISNULL(TicketFeeByPercent,0) + ISNULL(AdminFee,0) + ISNULL(SpotAdjustFee,0)) DESC
            )
    ) AS Filtered
    GROUP BY RealCID,		
				UserName,
				Region,
				Manager,
				Regulation,
				Mifid,
				InstrumentType -- Added here
) AS Aggregated
UNPIVOT
(
    Amount FOR Metric IN (
        Commissions,
        FullCommissions,
        RollOverFee,
        TicketFee,
        TicketFeeByPercent,
        AdminFee,
        SpotAdjustFee,
		VolumeOnOpen,
		VolumeOnClose,
		TotalTradingRevenue
    )
) AS unpvt
```

### 3. Custom SQL Query

- Tableau id: `39af1560-80f2-28c2-3c24-4ff0466ec5f2`

```sql
SELECT [InstrumentType]					
      ,[Region]					
      , dsap.ShortName AS  [US_StateShortname]
	  , dsap.Name AS  [US_StateName]
	  , dcr.FullDate
	  --, dateadd(quarter, datediff(quarter, 0, dcr.FullDate) + 1, -1) quarter_end_date				
	  , SUM(dcr.Commissions) Commissions 				
	,SUM([CommissionOnOpen]) [CommissionOnOpen]				
	,SUM([CommissionOnCloseAdjustment]) [CommissionOnCloseAdjustment]				
	,SUM([CommissionOnClose]) [CommissionOnClose]				
	,SUM([RealizedCommission]) [RealizedCommission]				
	,SUM(UnrealizedCommissionChange) UnrealizedCommissionChange				
  FROM [BI_DB_dbo].[BI_DB_DailyCommisionReport] dcr 					
  JOIN DWH_dbo.Dim_Customer dc					
		ON dcr.RealCID = dc.RealCID--	AND dc.IsCreditReportValidCB=1 AND dc.DesignatedRegulationID IN (6,7,8) AND dc.RegulationID IN (6,7,8) AND dc.CountryID=219 AND dc.IsValidCustomer=1		
 JOIN DWH_dbo.Dim_State_and_Province dsap					
		ON dc.RegionID = dsap.RegionByIP_ID			
  WHERE [DateID] >= 20220101 AND dcr.Region='USA'		
  AND InstrumentTypeID=10					
  AND dcr.[RegulationID] IN (6,7,8) AND dcr.[CountryID]=219					
  AND dcr.[IsValidCustomer]=1 AND dcr.IsCreditReportValidCB=1									
  GROUP BY  [InstrumentType]					
      ,[Region]					
      ,dsap.ShortName			
	  , dsap.Name
	 , dcr.FullDate
```

### 4. Custom SQL Query

- Tableau id: `4f099c2a-c229-ef97-9231-1c7d9a131a98`

```sql
SELECT  dc.GCID
      ,CAST(dc.FirstDepositDate AS DATE) AS FirstDepositDate
	  ,CAST(dc.RegisteredReal AS DATE) AS RegDate
	  ,dc1.Name AS Country
	  ,ISNULL(b.Crypto_revenue_2023,0) AS Crypto_revenue_2023
	  ,ISNULL(b.Revenue_2023,0) AS Revenue_2023
FROM DWH_dbo.Dim_Customer dc WITH (NOLOCK)
INNER JOIN 
(SELECT bddcr.CountryID
FROM BI_DB_dbo.BI_DB_DailyCommisionReport bddcr WITH (NOLOCK)
WHERE bddcr.DateID>20230101
GROUP BY bddcr.CountryID
HAVING SUM(CASE WHEN InstrumentTypeID = 10 THEN bddcr.FullCommissions +bddcr.RollOverFee ELSE 0 END)>0
)a ON dc.CountryID=a.CountryID
LEFT JOIN 
(SELECT bddcr.RealCID
       ,SUM(CASE WHEN InstrumentTypeID = 10 THEN bddcr.FullCommissions +bddcr.RollOverFee ELSE 0 END) AS Crypto_revenue_2023
	   ,SUM( bddcr.FullCommissions +bddcr.RollOverFee ) AS Revenue_2023
FROM BI_DB_dbo.BI_DB_DailyCommisionReport bddcr WITH (NOLOCK)
WHERE bddcr.DateID>20230101
GROUP BY bddcr.RealCID
HAVING SUM(CASE WHEN InstrumentTypeID = 10 THEN bddcr.FullCommissions +bddcr.RollOverFee ELSE 0 END)>0
) b ON dc.RealCID=b.RealCID
INNER JOIN DWH_dbo.Dim_Country dc1 ON dc.CountryID = dc1.CountryID
WHERE  dc.GCID LIKE '%[013]'
AND dc.IsValidCustomer=1 
AND dc.RegisteredReal>='20211101' 
AND dc.RegisteredReal<'20231101' 
AND dc.VerificationLevelID=3
```

### 5. Custom SQL Query

- Tableau id: `5eae625f-d855-3151-93b3-f7fb24dc0e44`

```sql
SELECT a.FullDate 
, a.Regulation
, (CASE WHEN a.InstrumentType = 'Stocks' AND a.IsSettled = 1 THEN 'Real Stocks' 
WHEN a.InstrumentType = 'Stocks' AND a.IsSettled = 0 THEN 'CFD Stocks'
ELSE a.InstrumentType END) AS InstrumentType
, SUM(VolumeOnOpen) AS VolumeOnOpen
, SUM(VolumeOnClose) AS VolumeOnClose
, SUM(VolumeOnOpen) + SUM(VolumeOnClose) AS TotalVolume
, SUM(a.FullCommissions) AS FullCommissions
, SUM(a.Commissions) AS Commissions
, SUM(a.FullCommissionOnCloseAdjustment) AS FullCommissionOnCloseAdjustment
, SUM(a.CommissionOnCloseAdjustment) AS CommissionOnCloseAdjustment
FROM BI_DB_dbo.BI_DB_DailyCommisionReport a
WHERE DateID BETWEEN CAST(FORMAT(CAST(<[Parameters].[Parameter 2]> AS DATE),'yyyyMMdd') as INT)
AND CAST(FORMAT(CAST(<[Parameters].[Parameter 3]> AS DATE),'yyyyMMdd') as INT) 
AND a.Regulation = 'FCA'
AND a.InstrumentType <> 'N/A'
GROUP BY FullDate, a.Regulation
, (CASE WHEN a.InstrumentType = 'Stocks' AND a.IsSettled = 1 THEN 'Real Stocks' 
WHEN a.InstrumentType = 'Stocks' AND a.IsSettled = 0 THEN 'CFD Stocks'
ELSE a.InstrumentType END)
```

### 6. Custom SQL Query

- Tableau id: `687b1612-3cdf-16b8-de8f-2d0048829cf4`

```sql
SELECT bddcr.FullDate
,bddcr.InstrumentID
,bddcr.Instrument
,bddcr.[InstrumentTypeID]
,bddcr.[InstrumentType]
,di.InstrumentDisplayName
,di.ReceivedOnPriceServer
,bddcr.FullCommissions
,bddcr.RollOverFee
FROM BI_DB.dbo.BI_DB_DailyCommisionReport bddcr
INNER JOIN DWH.dbo.Dim_Instrument di 
ON bddcr.InstrumentID = di.InstrumentID
WHERE bddcr.DateID >=20220101
and bddcr.InstrumentTypeID in (4,2,1)
GROUP BY 
bddcr.FullDate
,bddcr.InstrumentID
,bddcr.[Instrument]
,bddcr.[InstrumentTypeID]
,bddcr.[InstrumentType]
,di.InstrumentDisplayName
,di.ReceivedOnPriceServer
,bddcr.FullCommissions
,RollOverFee
```

### 7. Custom SQL Query

- Tableau id: `6aff3d8e-0f2a-647d-f657-d8e44f98282c`

```sql
SELECT cmf.Country,
	  EOMONTH(cmf.DepositDate) AS [Month],
	  SUM(isnull(a1.Revenue,0)) Revenue

	  FROM 
	  
	  (SELECT cmf.CID,cmf.DepositDate,cmf.Country
	  FROM BI_DB.dbo.BI_DB_Money_In_New_Management_Dashboard cmf
	  WHERE cmf.IsFTD=1 AND cmf.DepositMethod='MoneyBookers' AND cmf.PaymentStatusID=2 
	  AND cmf.Country IN   ('Thailand','Morocco','Egypt','Ukraine' ,'Vietnam','Brazil','Argentina','Philippines','United Arab Emirates')
		)  cmf

left JOIN (SELECT bddcr.FullDate,
			bddcr.RealCID,
			bddcr.FullCommissions+bddcr.RollOverFee AS 'Revenue' ,
			bddcr.Country
           FROM BI_DB.dbo.BI_DB_DailyCommisionReport bddcr
		   WHERE bddcr.DateID>=20220701 AND /*bddcr.CountryID IN (9,28,63,138,162,202,216,217,226)*/bddcr.Country IN   ('Thailand','Morocco','Egypt','Ukraine' ,'Vietnam','Brazil','Argentina','Philippines','United Arab Emirates'))AS a1  
			ON cmf.CID = a1.RealCID 
			   AND a1.FullDate>=dateadd(dd,30,cmf.DepositDate) 
GROUP BY cmf.Country,
	  EOMONTH(cmf.DepositDate)
```

### 8. Custom SQL Query

- Tableau id: `77cbec24-4f12-84df-4349-fd5fc650a444`

```sql
SELECT  *
FROM BI_DB..BI_DB_DailyCommisionReport dcr
WHERE dcr.Region IN ('China','Other Asia')
AND dcr.DateID>=20200101
```

### 9. Custom SQL Query

- Tableau id: `a0c2efa9-cb4a-3261-f2b4-b74cb5697d6d`

```sql
--- yesterday

SELECT 
	'Yesterday' AS [Period],
    RealCID,
	UserName,
	Region,
	Manager,
	Regulation,
	Mifid,
    Metric,
    Amount
FROM 
(
    SELECT 
        RealCID,
		Filtered.UserName,
		Filtered.Region,
		Filtered.Manager,
		Filtered.Regulation,
		Filtered.Mifid,
        SUM(Commissions) AS Commissions,
        SUM(FullCommissions) AS FullCommissions,
        SUM(RollOverFee) AS RollOverFee,
        SUM(TicketFee) AS TicketFee,
        SUM(TicketFeeByPercent) AS TicketFeeByPercent,
        SUM(AdminFee) AS AdminFee,
        SUM(SpotAdjustFee) AS SpotAdjustFee,
        SUM(VolumeOnOpen) AS VolumeOnOpen,
        SUM(VolumeOnClose) AS VolumeOnClose,
		SUM(ISNULL(FullCommissions,0)) + SUM(ISNULL(RollOverFee,0)) + SUM(ISNULL(TicketFee,0)) + SUM(ISNULL(TicketFeeByPercent,0)) + SUM(ISNULL(AdminFee,0)) + SUM(ISNULL(SpotAdjustFee,0)) AS TotalTradingRevenue
    FROM 
    (
        SELECT *
        FROM BI_DB_dbo.BI_DB_DailyCommisionReport
        WHERE 
            IsValidCustomer = 1
            AND DateID = CAST(FORMAT(CAST(getdate()-1 AS DATE),'yyyyMMdd') as INT)
            AND RealCID IN (
                SELECT TOP 20 RealCID
                FROM BI_DB_dbo.BI_DB_DailyCommisionReport
                WHERE DateID = CAST(FORMAT(CAST(getdate()-1 AS DATE),'yyyyMMdd') as INT) 
                GROUP BY RealCID
                ORDER BY SUM(ISNULL(FullCommissions,0) + ISNULL(RollOverFee,0) + ISNULL(TicketFee,0) + ISNULL(TicketFeeByPercent,0) + ISNULL(AdminFee,0) + ISNULL(SpotAdjustFee,0)) DESC
            )
    ) AS Filtered
    GROUP BY RealCID,		
				UserName,
				Region,
				Manager,
				Regulation,
				Mifid
) AS Aggregated
UNPIVOT
(
    Amount FOR Metric IN (
        Commissions,
        FullCommissions,
        RollOverFee,
        TicketFee,
        TicketFeeByPercent,
        AdminFee,
        SpotAdjustFee,
		VolumeOnOpen,
		VolumeOnClose,
		TotalTradingRevenue
    )
) AS unpvt

--- This Month

UNION all 

-- DECLARE getdate()-1 DATE = getdate()-1

SELECT 
	'CurrentMonth' AS [Period],
    RealCID,
	UserName,
	Region,
	Manager,
	Regulation,
	Mifid,
    Metric,
    Amount
FROM 
(
    SELECT 
        RealCID,
		Filtered.UserName,
		Filtered.Region,
		Filtered.Manager,
		Filtered.Regulation,
		Filtered.Mifid,
        SUM(Commissions) AS Commissions,
        SUM(FullCommissions) AS FullCommissions,
        SUM(RollOverFee) AS RollOverFee,
        SUM(TicketFee) AS TicketFee,
        SUM(TicketFeeByPercent) AS TicketFeeByPercent,
        SUM(AdminFee) AS AdminFee,
        SUM(SpotAdjustFee) AS SpotAdjustFee,
        SUM(VolumeOnOpen) AS VolumeOnOpen,
        SUM(VolumeOnClose) AS VolumeOnClose,
		SUM(ISNULL(FullCommissions,0)) + SUM(ISNULL(RollOverFee,0)) + SUM(ISNULL(TicketFee,0)) + SUM(ISNULL(TicketFeeByPercent,0)) + SUM(ISNULL(AdminFee,0)) + SUM(ISNULL(SpotAdjustFee,0))  AS TotalTradingRevenue
    FROM 
    (
        SELECT *
        FROM BI_DB_dbo.BI_DB_DailyCommisionReport
        WHERE 
            IsValidCustomer = 1
            AND DateID BETWEEN CAST(FORMAT(CAST(DATEADD(month, DATEDIFF(month, 0, getdate()-1), 0) AS DATE),'yyyyMMdd') as INT) AND CAST(FORMAT(CAST(getdate()-1 AS DATE),'yyyyMMdd') as INT) 
            AND RealCID IN (
                SELECT TOP 20 RealCID
                FROM BI_DB_dbo.BI_DB_DailyCommisionReport
                WHERE DateID BETWEEN CAST(FORMAT(CAST(DATEADD(month, DATEDIFF(month, 0, getdate()-1), 0) AS DATE),'yyyyMMdd') as INT) AND CAST(FORMAT(CAST(getdate()-1 AS DATE),'yyyyMMdd') as INT) 
                GROUP BY RealCID
                ORDER BY SUM(ISNULL(FullCommissions,0) + ISNULL(RollOverFee,0) + ISNULL(TicketFee,0) + ISNULL(TicketFeeByPercent,0) + ISNULL(AdminFee,0) + ISNULL(SpotAdjustFee,0)) DESC
            )
    ) AS Filtered
    GROUP BY RealCID,		
				UserName,
				Region,
				Manager,
				Regulation,
				Mifid
) AS Aggregated
UNPIVOT
(
    Amount FOR Metric IN (
        Commissions,
        FullCommissions,
        RollOverFee,
        TicketFee,
        TicketFeeByPercent,
        AdminFee,
        SpotAdjustFee,
		VolumeOnOpen,
		VolumeOnClose,
		TotalTradingRevenue
    )
) AS unpvt

--- Last Month

UNION all 


SELECT 
	'PreviousMonthSameTime' AS [Period],
    RealCID,
	UserName,
	Region,
	Manager,
	Regulation,
	Mifid,
    Metric,
    Amount
FROM 
(
    SELECT 
        RealCID,
		Filtered.UserName,
		Filtered.Region,
		Filtered.Manager,
		Filtered.Regulation,
		Filtered.Mifid,
        SUM(Commissions) AS Commissions,
        SUM(FullCommissions) AS FullCommissions,
        SUM(RollOverFee) AS RollOverFee,
        SUM(TicketFee) AS TicketFee,
        SUM(TicketFeeByPercent) AS TicketFeeByPercent,
        SUM(AdminFee) AS AdminFee,
        SUM(SpotAdjustFee) AS SpotAdjustFee,
        SUM(VolumeOnOpen) AS VolumeOnOpen,
        SUM(VolumeOnClose) AS VolumeOnClose,
		SUM(ISNULL(FullCommissions,0)) + SUM(ISNULL(RollOverFee,0)) + SUM(ISNULL(TicketFee,0)) + SUM(ISNULL(TicketFeeByPercent,0)) + SUM(ISNULL(AdminFee,0)) + SUM(ISNULL(SpotAdjustFee,0))  AS TotalTradingRevenue
    FROM 
    (
        SELECT *
        FROM BI_DB_dbo.BI_DB_DailyCommisionReport
        WHERE 
            IsValidCustomer = 1
            AND DateID BETWEEN CAST(FORMAT(CAST( DATEADD(month, DATEDIFF(month, 0, getdate()-1) - 1, 0) AS DATE),'yyyyMMdd') as INT) AND CAST(FORMAT(CAST(dateadd(MONTH,-1,getdate()-1)  AS DATE),'yyyyMMdd') as INT) 
            AND RealCID IN (
                SELECT TOP 20 RealCID
                FROM BI_DB_dbo.BI_DB_DailyCommisionReport
                WHERE DateID BETWEEN CAST(FORMAT(CAST( DATEADD(month, DATEDIFF(month, 0, getdate()-1) - 1, 0) AS DATE),'yyyyMMdd') as INT) AND CAST(FORMAT(CAST(dateadd(MONTH,-1,getdate()-1)  AS DATE),'yyyyMMdd') as INT)
                GROUP BY RealCID
                ORDER BY SUM(ISNULL(FullCommissions,0) + ISNULL(RollOverFee,0) + ISNULL(TicketFee,0) + ISNULL(TicketFeeByPercent,0) + ISNULL(AdminFee,0) + ISNULL(SpotAdjustFee,0)) DESC
            )
    ) AS Filtered
    GROUP BY RealCID,		
				UserName,
				Region,
				Manager,
				Regulation,
				Mifid
) AS Aggregated
UNPIVOT
(
    Amount FOR Metric IN (
        Commissions,
        FullCommissions,
        RollOverFee,
        TicketFee,
        TicketFeeByPercent,
        AdminFee,
        SpotAdjustFee,
		VolumeOnOpen,
		VolumeOnClose,
		TotalTradingRevenue
    )
) AS unpvt

--- same day 8 weeks 


UNION all 

--DECLARE getdate()-1 DATE = getdate()-1

SELECT 
	'CurrentYear' AS [Period],
    RealCID,
	UserName,
	Region,
	Manager,
	Regulation,
	Mifid,
    Metric,
    Amount
FROM 
(
    SELECT 
        RealCID,
		Filtered.UserName,
		Filtered.Region,
		Filtered.Manager,
		Filtered.Regulation,
		Filtered.Mifid,
        sum(Commissions) AS Commissions,
        sum(FullCommissions) AS FullCommissions,
        sum(RollOverFee) AS RollOverFee,
        sum(TicketFee) AS TicketFee,
        sum(TicketFeeByPercent) AS TicketFeeByPercent,
        sum(AdminFee) AS AdminFee,
        SUM(SpotAdjustFee) AS SpotAdjustFee,
        SUM(VolumeOnOpen) AS VolumeOnOpen,
        SUM(VolumeOnClose) AS VolumeOnClose,
		SUM(ISNULL(FullCommissions,0)) + SUM(ISNULL(RollOverFee,0)) + SUM(ISNULL(TicketFee,0)) + SUM(ISNULL(TicketFeeByPercent,0)) + SUM(ISNULL(AdminFee,0)) + SUM(ISNULL(SpotAdjustFee,0))  AS TotalTradingRevenue
    FROM 
    (
        SELECT ia.*
        FROM BI_DB_dbo.BI_DB_DailyCommisionReport ia -- select @@DATEFIRST
        WHERE 
            IsValidCustomer = 1
            AND DateID BETWEEN CAST(FORMAT(CAST(DATEADD(yy, DATEDIFF(yy, 0, getdate()-1), 0) AS DATE),'yyyyMMdd') as INT)  AND CAST(FORMAT(CAST(getdate()-1 AS DATE),'yyyyMMdd') as INT) 
			AND RealCID IN (
                SELECT TOP 20 RealCID
                FROM BI_DB_dbo.BI_DB_DailyCommisionReport ia
				WHERE ia.DateID between CAST(FORMAT(CAST(DATEADD(yy, DATEDIFF(yy, 0, getdate()-1), 0) AS DATE),'yyyyMMdd') as INT)  AND CAST(FORMAT(CAST(getdate()-1 AS DATE),'yyyyMMdd') as INT) 
                GROUP BY RealCID
                ORDER BY SUM(ISNULL(FullCommissions,0) + ISNULL(RollOverFee,0) + ISNULL(TicketFee,0) + ISNULL(TicketFeeByPercent,0) + ISNULL(AdminFee,0) + ISNULL(SpotAdjustFee,0)) DESC
            )
    ) AS Filtered
    GROUP BY RealCID,		
				UserName,
				Region,
				Manager,
				Regulation,
				Mifid
) AS Aggregated
UNPIVOT
(
    Amount FOR Metric IN (
        Commissions,
        FullCommissions,
        RollOverFee,
        TicketFee,
        TicketFeeByPercent,
        AdminFee,
        SpotAdjustFee,
		VolumeOnOpen,
		VolumeOnClose,
		TotalTradingRevenue
    )
) AS unpvt

UNION all 

SELECT 
	'PreviousYearSameTime' AS [Period],
    RealCID,
	UserName,
	Region,
	Manager,
	Regulation,
	Mifid,
    Metric,
    Amount
FROM 
(
    SELECT 
        RealCID,
		Filtered.UserName,
		Filtered.Region,
		Filtered.Manager,
		Filtered.Regulation,
		Filtered.Mifid,
        sum(Commissions) AS Commissions,
        sum(FullCommissions) AS FullCommissions,
        sum(RollOverFee) AS RollOverFee,
        sum(TicketFee) AS TicketFee,
        sum(TicketFeeByPercent) AS TicketFeeByPercent,
        sum(AdminFee) AS AdminFee,
        SUM(SpotAdjustFee) AS SpotAdjustFee,
        SUM(VolumeOnOpen) AS VolumeOnOpen,
        SUM(VolumeOnClose) AS VolumeOnClose,
		SUM(ISNULL(FullCommissions,0)) + SUM(ISNULL(RollOverFee,0)) + SUM(ISNULL(TicketFee,0)) + SUM(ISNULL(TicketFeeByPercent,0)) + SUM(ISNULL(AdminFee,0)) + SUM(ISNULL(SpotAdjustFee,0))  AS TotalTradingRevenue
    FROM 
    (
        SELECT ia.*
        FROM BI_DB_dbo.BI_DB_DailyCommisionReport ia -- select @@DATEFIRST
        WHERE 
            IsValidCustomer = 1
            AND DateID BETWEEN CAST(FORMAT(CAST(DATEADD(yy, DATEDIFF(yy, 0, GETDATE()) - 1, 0) AS DATE),'yyyyMMdd') as INT) AND CAST(FORMAT(CAST(dateadd(YEAR,-1,getdate()-1) AS DATE),'yyyyMMdd') as INT) 
			AND RealCID IN (
                SELECT TOP 20 RealCID
                FROM BI_DB_dbo.BI_DB_DailyCommisionReport ia
				WHERE ia.DateID between CAST(FORMAT(CAST(DATEADD(yy, DATEDIFF(yy, 0, GETDATE()) - 1, 0) AS DATE),'yyyyMMdd') as INT) AND CAST(FORMAT(CAST(dateadd(YEAR,-1,getdate()-1) AS DATE),'yyyyMMdd') as INT) 
                GROUP BY RealCID
                ORDER BY SUM(ISNULL(FullCommissions,0) + ISNULL(RollOverFee,0) + ISNULL(TicketFee,0) + ISNULL(TicketFeeByPercent,0) + ISNULL(AdminFee,0) + ISNULL(SpotAdjustFee,0)) DESC
            )
    ) AS Filtered
    GROUP BY RealCID,		
				UserName,
				Region,
				Manager,
				Regulation,
				Mifid
) AS Aggregated
UNPIVOT
(
    Amount FOR Metric IN (
        Commissions,
        FullCommissions,
        RollOverFee,
        TicketFee,
        TicketFeeByPercent,
        AdminFee,
        SpotAdjustFee,
		VolumeOnOpen,
		VolumeOnClose,
		TotalTradingRevenue
    )
) AS unpvt

union all 


SELECT 
	'CurrentWeek' AS [Period],
    RealCID,
	UserName,
	Region,
	Manager,
	Regulation,
	Mifid,
    Metric,
    Amount
FROM 
(
    SELECT 
        RealCID,
		Filtered.UserName,
		Filtered.Region,
		Filtered.Manager,
		Filtered.Regulation,
		Filtered.Mifid,
        SUM(Commissions) AS Commissions,
        SUM(FullCommissions) AS FullCommissions,
        SUM(RollOverFee) AS RollOverFee,
        SUM(TicketFee) AS TicketFee,
        SUM(TicketFeeByPercent) AS TicketFeeByPercent,
        SUM(AdminFee) AS AdminFee,
        SUM(SpotAdjustFee) AS SpotAdjustFee,
        SUM(VolumeOnOpen) AS VolumeOnOpen,
        SUM(VolumeOnClose) AS VolumeOnClose,
		SUM(ISNULL(FullCommissions,0)) + SUM(ISNULL(RollOverFee,0)) + SUM(ISNULL(TicketFee,0)) + SUM(ISNULL(TicketFeeByPercent,0)) + SUM(ISNULL(AdminFee,0)) + SUM(ISNULL(SpotAdjustFee,0))  AS TotalTradingRevenue
    FROM 
    (
        SELECT *
        FROM BI_DB_dbo.BI_DB_DailyCommisionReport
        WHERE 
            IsValidCustomer = 1
            AND DateID BETWEEN CAST(FORMAT(CAST(DATEADD(DAY, 1 - DATEPART(WEEKDAY, getdate()-1), getdate()-1) AS DATE),'yyyyMMdd') as INT)  AND CAST(FORMAT(CAST(getdate()-1 AS DATE),'yyyyMMdd') as INT) 
            AND RealCID IN (
                SELECT TOP 20 RealCID
                FROM BI_DB_dbo.BI_DB_DailyCommisionReport
                WHERE DateID BETWEEN CAST(FORMAT(CAST(DATEADD(DAY, 1 - DATEPART(WEEKDAY, getdate()-1), getdate()-1) AS DATE),'yyyyMMdd') as INT)  AND CAST(FORMAT(CAST(getdate()-1 AS DATE),'yyyyMMdd') as INT) 
                GROUP BY RealCID
                ORDER BY SUM(ISNULL(FullCommissions,0) + ISNULL(RollOverFee,0) + ISNULL(TicketFee,0) + ISNULL(TicketFeeByPercent,0) + ISNULL(AdminFee,0) + ISNULL(SpotAdjustFee,0)) DESC
            )
    ) AS Filtered
    GROUP BY RealCID,		
				UserName,
				Region,
				Manager,
				Regulation,
				Mifid
) AS Aggregated
UNPIVOT
(
    Amount FOR Metric IN (
        Commissions,
        FullCommissions,
        RollOverFee,
        TicketFee,
        TicketFeeByPercent,
        AdminFee,
        SpotAdjustFee,
		VolumeOnOpen,
		VolumeOnClose,
		TotalTradingRevenue
    )
) AS unpvt

union all 



-- DECLARE getdate()-1 DATE = getdate()-1

SELECT 
	'PreviousWeekSameTime' AS [Period],
    RealCID,
	UserName,
	Region,
	Manager,
	Regulation,
	Mifid,
    Metric,
    Amount
FROM 
(
    SELECT 
        RealCID,
		Filtered.UserName,
		Filtered.Region,
		Filtered.Manager,
		Filtered.Regulation,
		Filtered.Mifid,
        SUM(Commissions) AS Commissions,
        SUM(FullCommissions) AS FullCommissions,
        SUM(RollOverFee) AS RollOverFee,
        SUM(TicketFee) AS TicketFee,
        SUM(TicketFeeByPercent) AS TicketFeeByPercent,
        SUM(AdminFee) AS AdminFee,
        SUM(SpotAdjustFee) AS SpotAdjustFee,
        SUM(VolumeOnOpen) AS VolumeOnOpen,
        SUM(VolumeOnClose) AS VolumeOnClose,
		SUM(ISNULL(FullCommissions,0)) + SUM(ISNULL(RollOverFee,0)) + SUM(ISNULL(TicketFee,0)) + SUM(ISNULL(TicketFeeByPercent,0)) + SUM(ISNULL(AdminFee,0)) + SUM(ISNULL(SpotAdjustFee,0))  AS TotalTradingRevenue
    FROM 
    (
        SELECT *
        FROM BI_DB_dbo.BI_DB_DailyCommisionReport
        WHERE 
            IsValidCustomer = 1
            AND DateID BETWEEN CAST(FORMAT(CAST(dateadd (WEEK, -1, DATEADD(DAY, 1 - DATEPART(WEEKDAY, getdate()-1), getdate()-1)) AS DATE),'yyyyMMdd') as INT)  AND CAST(FORMAT(CAST(dateadd(WEEK, -1,getdate()-1) AS DATE),'yyyyMMdd') as INT) 
            AND RealCID IN (
                SELECT TOP 20 RealCID
                FROM BI_DB_dbo.BI_DB_DailyCommisionReport
                WHERE DateID BETWEEN  CAST(FORMAT(CAST(dateadd (WEEK, -1, DATEADD(DAY, 1 - DATEPART(WEEKDAY, getdate()-1), getdate()-1)) AS DATE),'yyyyMMdd') as INT)  AND CAST(FORMAT(CAST(dateadd(WEEK, -1,getdate()-1) AS DATE),'yyyyMMdd') as INT)
                GROUP BY RealCID
                ORDER BY SUM(ISNULL(FullCommissions,0) + ISNULL(RollOverFee,0) + ISNULL(TicketFee,0) + ISNULL(TicketFeeByPercent,0) + ISNULL(AdminFee,0) + ISNULL(SpotAdjustFee,0)) DESC
            )
    ) AS Filtered
    GROUP BY RealCID,		
				UserName,
				Region,
				Manager,
				Regulation,
				Mifid
) AS Aggregated
UNPIVOT
(
    Amount FOR Metric IN (
        Commissions,
        FullCommissions,
        RollOverFee,
        TicketFee,
        TicketFeeByPercent,
        AdminFee,
        SpotAdjustFee,
		VolumeOnOpen,
		VolumeOnClose,
		TotalTradingRevenue
    )
) AS unpvt

UNION ALL 



-- DECLARE getdate()-1 DATE = getdate()-1

SELECT 
	'CurrentQuarter' AS [Period],
    RealCID,
	UserName,
	Region,
	Manager,
	Regulation,
	Mifid,
    Metric,
    Amount
FROM 
(
    SELECT 
        RealCID,
		Filtered.UserName,
		Filtered.Region,
		Filtered.Manager,
		Filtered.Regulation,
		Filtered.Mifid,
        SUM(Commissions) AS Commissions,
        SUM(FullCommissions) AS FullCommissions,
        SUM(RollOverFee) AS RollOverFee,
        SUM(TicketFee) AS TicketFee,
        SUM(TicketFeeByPercent) AS TicketFeeByPercent,
        SUM(AdminFee) AS AdminFee,
        SUM(SpotAdjustFee) AS SpotAdjustFee,
        SUM(VolumeOnOpen) AS VolumeOnOpen,
        SUM(VolumeOnClose) AS VolumeOnClose,
		SUM(ISNULL(FullCommissions,0)) + SUM(ISNULL(RollOverFee,0)) + SUM(ISNULL(TicketFee,0)) + SUM(ISNULL(TicketFeeByPercent,0)) + SUM(ISNULL(AdminFee,0)) + SUM(ISNULL(SpotAdjustFee,0))  AS TotalTradingRevenue
    FROM 
    (
        SELECT *
        FROM BI_DB_dbo.BI_DB_DailyCommisionReport
        WHERE 
            IsValidCustomer = 1
            AND DateID BETWEEN CAST(FORMAT(CAST( DATEADD(qq, DATEDIFF(qq, 0, getdate()-1), 0) AS DATE),'yyyyMMdd') as INT)  AND CAST(FORMAT(CAST(getdate()-1 AS DATE),'yyyyMMdd') as INT) 
            AND RealCID IN (
                SELECT TOP 20 RealCID
                FROM BI_DB_dbo.BI_DB_DailyCommisionReport
                WHERE DateID BETWEEN  CAST(FORMAT(CAST( DATEADD(qq, DATEDIFF(qq, 0, getdate()-1), 0) AS DATE),'yyyyMMdd') as INT)  AND CAST(FORMAT(CAST(getdate()-1 AS DATE),'yyyyMMdd') as INT) 
                GROUP BY RealCID
                ORDER BY SUM(ISNULL(FullCommissions,0) + ISNULL(RollOverFee,0) + ISNULL(TicketFee,0) + ISNULL(TicketFeeByPercent,0) + ISNULL(AdminFee,0) + ISNULL(SpotAdjustFee,0)) DESC
            )
    ) AS Filtered
    GROUP BY RealCID,		
				UserName,
				Region,
				Manager,
				Regulation,
				Mifid
) AS Aggregated
UNPIVOT
(
    Amount FOR Metric IN (
        Commissions,
        FullCommissions,
        RollOverFee,
        TicketFee,
        TicketFeeByPercent,
        AdminFee,
        SpotAdjustFee,
		VolumeOnOpen,
		VolumeOnClose,
		TotalTradingRevenue
    )
) AS unpvt

--- Last Month
```

### 10. Custom SQL Query

- Tableau id: `a1161762-7b73-d70d-5011-eaec9d58dcab`

```sql
--- yesterday

SELECT 
	'Yesterday' AS [Period],
    RealCID,
	UserName,
	Region,
	Manager,
	Regulation,
	Mifid,
    Metric,
    Amount
FROM 
(
    SELECT 
        RealCID,
		Filtered.UserName,
		Filtered.Region,
		Filtered.Manager,
		Filtered.Regulation,
		Filtered.Mifid,
        SUM(Commissions) AS Commissions,
        SUM(FullCommissions) AS FullCommissions,
        SUM(RollOverFee) AS RollOverFee,
        SUM(TicketFee) AS TicketFee,
        SUM(TicketFeeByPercent) AS TicketFeeByPercent,
        SUM(AdminFee) AS AdminFee,
        SUM(SpotAdjustFee) AS SpotAdjustFee,
        SUM(VolumeOnOpen) AS VolumeOnOpen,
        SUM(VolumeOnClose) AS VolumeOnClose,
		SUM(ISNULL(FullCommissions,0)) + SUM(ISNULL(RollOverFee,0)) + SUM(ISNULL(TicketFee,0)) + SUM(ISNULL(TicketFeeByPercent,0)) + SUM(ISNULL(AdminFee,0)) + SUM(ISNULL(SpotAdjustFee,0)) AS TotalTradingRevenue
    FROM 
    (
        SELECT *
        FROM BI_DB_dbo.BI_DB_DailyCommisionReport
        WHERE 
            IsValidCustomer = 1
            AND DateID = CAST(FORMAT(CAST(getdate()-1 AS DATE),'yyyyMMdd') as INT)
            AND RealCID IN (
                SELECT TOP 20 RealCID
                FROM BI_DB_dbo.BI_DB_DailyCommisionReport
                WHERE DateID = CAST(FORMAT(CAST(getdate()-1 AS DATE),'yyyyMMdd') as INT) 
                GROUP BY RealCID
                ORDER BY SUM(ISNULL(FullCommissions,0) + ISNULL(RollOverFee,0) + ISNULL(TicketFee,0) + ISNULL(TicketFeeByPercent,0) + ISNULL(AdminFee,0) + ISNULL(SpotAdjustFee,0)) DESC
            )
    ) AS Filtered
    GROUP BY RealCID,		
				UserName,
				Region,
				Manager,
				Regulation,
				Mifid
) AS Aggregated
UNPIVOT
(
    Amount FOR Metric IN (
        Commissions,
        FullCommissions,
        RollOverFee,
        TicketFee,
        TicketFeeByPercent,
        AdminFee,
        SpotAdjustFee,
		VolumeOnOpen,
		VolumeOnClose,
		TotalTradingRevenue
    )
) AS unpvt

--- This Month

UNION all 

-- DECLARE getdate()-1 DATE = getdate()-1

SELECT 
	'CurrentMonth' AS [Period],
    RealCID,
	UserName,
	Region,
	Manager,
	Regulation,
	Mifid,
    Metric,
    Amount
FROM 
(
    SELECT 
        RealCID,
		Filtered.UserName,
		Filtered.Region,
		Filtered.Manager,
		Filtered.Regulation,
		Filtered.Mifid,
        SUM(Commissions) AS Commissions,
        SUM(FullCommissions) AS FullCommissions,
        SUM(RollOverFee) AS RollOverFee,
        SUM(TicketFee) AS TicketFee,
        SUM(TicketFeeByPercent) AS TicketFeeByPercent,
        SUM(AdminFee) AS AdminFee,
        SUM(SpotAdjustFee) AS SpotAdjustFee,
        SUM(VolumeOnOpen) AS VolumeOnOpen,
        SUM(VolumeOnClose) AS VolumeOnClose,
		SUM(ISNULL(FullCommissions,0)) + SUM(ISNULL(RollOverFee,0)) + SUM(ISNULL(TicketFee,0)) + SUM(ISNULL(TicketFeeByPercent,0)) + SUM(ISNULL(AdminFee,0)) + SUM(ISNULL(SpotAdjustFee,0))  AS TotalTradingRevenue
    FROM 
    (
        SELECT *
        FROM BI_DB_dbo.BI_DB_DailyCommisionReport
        WHERE 
            IsValidCustomer = 1
            AND DateID BETWEEN CAST(FORMAT(CAST(DATEADD(month, DATEDIFF(month, 0, getdate()-1), 0) AS DATE),'yyyyMMdd') as INT) AND CAST(FORMAT(CAST(getdate()-1 AS DATE),'yyyyMMdd') as INT) 
            AND RealCID IN (
                SELECT TOP 20 RealCID
                FROM BI_DB_dbo.BI_DB_DailyCommisionReport
                WHERE DateID BETWEEN CAST(FORMAT(CAST(DATEADD(month, DATEDIFF(month, 0, getdate()-1), 0) AS DATE),'yyyyMMdd') as INT) AND CAST(FORMAT(CAST(getdate()-1 AS DATE),'yyyyMMdd') as INT) 
                GROUP BY RealCID
                ORDER BY SUM(ISNULL(FullCommissions,0) + ISNULL(RollOverFee,0) + ISNULL(TicketFee,0) + ISNULL(TicketFeeByPercent,0) + ISNULL(AdminFee,0) + ISNULL(SpotAdjustFee,0)) DESC
            )
    ) AS Filtered
    GROUP BY RealCID,		
				UserName,
				Region,
				Manager,
				Regulation,
				Mifid
) AS Aggregated
UNPIVOT
(
    Amount FOR Metric IN (
        Commissions,
        FullCommissions,
        RollOverFee,
        TicketFee,
        TicketFeeByPercent,
        AdminFee,
        SpotAdjustFee,
		VolumeOnOpen,
		VolumeOnClose,
		TotalTradingRevenue
    )
) AS unpvt

--- Last Month

UNION all 


SELECT 
	'PreviousMonthSameTime' AS [Period],
    RealCID,
	UserName,
	Region,
	Manager,
	Regulation,
	Mifid,
    Metric,
    Amount
FROM 
(
    SELECT 
        RealCID,
		Filtered.UserName,
		Filtered.Region,
		Filtered.Manager,
		Filtered.Regulation,
		Filtered.Mifid,
        SUM(Commissions) AS Commissions,
        SUM(FullCommissions) AS FullCommissions,
        SUM(RollOverFee) AS RollOverFee,
        SUM(TicketFee) AS TicketFee,
        SUM(TicketFeeByPercent) AS TicketFeeByPercent,
        SUM(AdminFee) AS AdminFee,
        SUM(SpotAdjustFee) AS SpotAdjustFee,
        SUM(VolumeOnOpen) AS VolumeOnOpen,
        SUM(VolumeOnClose) AS VolumeOnClose,
		SUM(ISNULL(FullCommissions,0)) + SUM(ISNULL(RollOverFee,0)) + SUM(ISNULL(TicketFee,0)) + SUM(ISNULL(TicketFeeByPercent,0)) + SUM(ISNULL(AdminFee,0)) + SUM(ISNULL(SpotAdjustFee,0))  AS TotalTradingRevenue
    FROM 
    (
        SELECT *
        FROM BI_DB_dbo.BI_DB_DailyCommisionReport
        WHERE 
            IsValidCustomer = 1
            AND DateID BETWEEN CAST(FORMAT(CAST( DATEADD(month, DATEDIFF(month, 0, getdate()-1) - 1, 0) AS DATE),'yyyyMMdd') as INT) AND CAST(FORMAT(CAST(dateadd(MONTH,-1,getdate()-1)  AS DATE),'yyyyMMdd') as INT) 
            AND RealCID IN (
                SELECT TOP 20 RealCID
                FROM BI_DB_dbo.BI_DB_DailyCommisionReport
                WHERE DateID BETWEEN CAST(FORMAT(CAST( DATEADD(month, DATEDIFF(month, 0, getdate()-1) - 1, 0) AS DATE),'yyyyMMdd') as INT) AND CAST(FORMAT(CAST(dateadd(MONTH,-1,getdate()-1)  AS DATE),'yyyyMMdd') as INT)
                GROUP BY RealCID
                ORDER BY SUM(ISNULL(FullCommissions,0) + ISNULL(RollOverFee,0) + ISNULL(TicketFee,0) + ISNULL(TicketFeeByPercent,0) + ISNULL(AdminFee,0) + ISNULL(SpotAdjustFee,0)) DESC
            )
    ) AS Filtered
    GROUP BY RealCID,		
				UserName,
				Region,
				Manager,
				Regulation,
				Mifid
) AS Aggregated
UNPIVOT
(
    Amount FOR Metric IN (
        Commissions,
        FullCommissions,
        RollOverFee,
        TicketFee,
        TicketFeeByPercent,
        AdminFee,
        SpotAdjustFee,
		VolumeOnOpen,
		VolumeOnClose,
		TotalTradingRevenue
    )
) AS unpvt

--- same day 8 weeks 


UNION all 

--DECLARE getdate()-1 DATE = getdate()-1

SELECT 
	'CurrentYear' AS [Period],
    RealCID,
	UserName,
	Region,
	Manager,
	Regulation,
	Mifid,
    Metric,
    Amount
FROM 
(
    SELECT 
        RealCID,
		Filtered.UserName,
		Filtered.Region,
		Filtered.Manager,
		Filtered.Regulation,
		Filtered.Mifid,
        sum(Commissions) AS Commissions,
        sum(FullCommissions) AS FullCommissions,
        sum(RollOverFee) AS RollOverFee,
        sum(TicketFee) AS TicketFee,
        sum(TicketFeeByPercent) AS TicketFeeByPercent,
        sum(AdminFee) AS AdminFee,
        SUM(SpotAdjustFee) AS SpotAdjustFee,
        SUM(VolumeOnOpen) AS VolumeOnOpen,
        SUM(VolumeOnClose) AS VolumeOnClose,
		SUM(ISNULL(FullCommissions,0)) + SUM(ISNULL(RollOverFee,0)) + SUM(ISNULL(TicketFee,0)) + SUM(ISNULL(TicketFeeByPercent,0)) + SUM(ISNULL(AdminFee,0)) + SUM(ISNULL(SpotAdjustFee,0))  AS TotalTradingRevenue
    FROM 
    (
        SELECT ia.*
        FROM BI_DB_dbo.BI_DB_DailyCommisionReport ia -- select @@DATEFIRST
        WHERE 
            IsValidCustomer = 1
            AND DateID BETWEEN CAST(FORMAT(CAST(DATEADD(yy, DATEDIFF(yy, 0, getdate()-1), 0) AS DATE),'yyyyMMdd') as INT)  AND CAST(FORMAT(CAST(getdate()-1 AS DATE),'yyyyMMdd') as INT) 
			AND RealCID IN (
                SELECT TOP 20 RealCID
                FROM BI_DB_dbo.BI_DB_DailyCommisionReport ia
				WHERE ia.DateID between CAST(FORMAT(CAST(DATEADD(yy, DATEDIFF(yy, 0, getdate()-1), 0) AS DATE),'yyyyMMdd') as INT)  AND CAST(FORMAT(CAST(getdate()-1 AS DATE),'yyyyMMdd') as INT) 
                GROUP BY RealCID
                ORDER BY SUM(ISNULL(FullCommissions,0) + ISNULL(RollOverFee,0) + ISNULL(TicketFee,0) + ISNULL(TicketFeeByPercent,0) + ISNULL(AdminFee,0) + ISNULL(SpotAdjustFee,0)) DESC
            )
    ) AS Filtered
    GROUP BY RealCID,		
				UserName,
				Region,
				Manager,
				Regulation,
				Mifid
) AS Aggregated
UNPIVOT
(
    Amount FOR Metric IN (
        Commissions,
        FullCommissions,
        RollOverFee,
        TicketFee,
        TicketFeeByPercent,
        AdminFee,
        SpotAdjustFee,
		VolumeOnOpen,
		VolumeOnClose,
		TotalTradingRevenue
    )
) AS unpvt

UNION all 

SELECT 
	'PreviousYearSameTime' AS [Period],
    RealCID,
	UserName,
	Region,
	Manager,
	Regulation,
	Mifid,
    Metric,
    Amount
FROM 
(
    SELECT 
        RealCID,
		Filtered.UserName,
		Filtered.Region,
		Filtered.Manager,
		Filtered.Regulation,
		Filtered.Mifid,
        sum(Commissions) AS Commissions,
        sum(FullCommissions) AS FullCommissions,
        sum(RollOverFee) AS RollOverFee,
        sum(TicketFee) AS TicketFee,
        sum(TicketFeeByPercent) AS TicketFeeByPercent,
        sum(AdminFee) AS AdminFee,
        SUM(SpotAdjustFee) AS SpotAdjustFee,
        SUM(VolumeOnOpen) AS VolumeOnOpen,
        SUM(VolumeOnClose) AS VolumeOnClose,
		SUM(ISNULL(FullCommissions,0)) + SUM(ISNULL(RollOverFee,0)) + SUM(ISNULL(TicketFee,0)) + SUM(ISNULL(TicketFeeByPercent,0)) + SUM(ISNULL(AdminFee,0)) + SUM(ISNULL(SpotAdjustFee,0))  AS TotalTradingRevenue
    FROM 
    (
        SELECT ia.*
        FROM BI_DB_dbo.BI_DB_DailyCommisionReport ia -- select @@DATEFIRST
        WHERE 
            IsValidCustomer = 1
            AND DateID BETWEEN CAST(FORMAT(CAST(DATEADD(yy, DATEDIFF(yy, 0, GETDATE()) - 1, 0) AS DATE),'yyyyMMdd') as INT) AND CAST(FORMAT(CAST(dateadd(YEAR,-1,getdate()-1) AS DATE),'yyyyMMdd') as INT) 
			AND RealCID IN (
                SELECT TOP 20 RealCID
                FROM BI_DB_dbo.BI_DB_DailyCommisionReport ia
				WHERE ia.DateID between CAST(FORMAT(CAST(DATEADD(yy, DATEDIFF(yy, 0, GETDATE()) - 1, 0) AS DATE),'yyyyMMdd') as INT) AND CAST(FORMAT(CAST(dateadd(YEAR,-1,getdate()-1) AS DATE),'yyyyMMdd') as INT) 
                GROUP BY RealCID
                ORDER BY SUM(ISNULL(FullCommissions,0) + ISNULL(RollOverFee,0) + ISNULL(TicketFee,0) + ISNULL(TicketFeeByPercent,0) + ISNULL(AdminFee,0) + ISNULL(SpotAdjustFee,0)) DESC
            )
    ) AS Filtered
    GROUP BY RealCID,		
				UserName,
				Region,
				Manager,
				Regulation,
				Mifid
) AS Aggregated
UNPIVOT
(
    Amount FOR Metric IN (
        Commissions,
        FullCommissions,
        RollOverFee,
        TicketFee,
        TicketFeeByPercent,
        AdminFee,
        SpotAdjustFee,
		VolumeOnOpen,
		VolumeOnClose,
		TotalTradingRevenue
    )
) AS unpvt

union all 


SELECT 
	'CurrentWeek' AS [Period],
    RealCID,
	UserName,
	Region,
	Manager,
	Regulation,
	Mifid,
    Metric,
    Amount
FROM 
(
    SELECT 
        RealCID,
		Filtered.UserName,
		Filtered.Region,
		Filtered.Manager,
		Filtered.Regulation,
		Filtered.Mifid,
        SUM(Commissions) AS Commissions,
        SUM(FullCommissions) AS FullCommissions,
        SUM(RollOverFee) AS RollOverFee,
        SUM(TicketFee) AS TicketFee,
        SUM(TicketFeeByPercent) AS TicketFeeByPercent,
        SUM(AdminFee) AS AdminFee,
        SUM(SpotAdjustFee) AS SpotAdjustFee,
        SUM(VolumeOnOpen) AS VolumeOnOpen,
        SUM(VolumeOnClose) AS VolumeOnClose,
		SUM(ISNULL(FullCommissions,0)) + SUM(ISNULL(RollOverFee,0)) + SUM(ISNULL(TicketFee,0)) + SUM(ISNULL(TicketFeeByPercent,0)) + SUM(ISNULL(AdminFee,0)) + SUM(ISNULL(SpotAdjustFee,0))  AS TotalTradingRevenue
    FROM 
    (
        SELECT *
        FROM BI_DB_dbo.BI_DB_DailyCommisionReport
        WHERE 
            IsValidCustomer = 1
            AND DateID BETWEEN CAST(FORMAT(CAST(DATEADD(DAY, 1 - DATEPART(WEEKDAY, getdate()-1), getdate()-1) AS DATE),'yyyyMMdd') as INT)  AND CAST(FORMAT(CAST(getdate()-1 AS DATE),'yyyyMMdd') as INT) 
            AND RealCID IN (
                SELECT TOP 20 RealCID
                FROM BI_DB_dbo.BI_DB_DailyCommisionReport
                WHERE DateID BETWEEN CAST(FORMAT(CAST(DATEADD(DAY, 1 - DATEPART(WEEKDAY, getdate()-1), getdate()-1) AS DATE),'yyyyMMdd') as INT)  AND CAST(FORMAT(CAST(getdate()-1 AS DATE),'yyyyMMdd') as INT) 
                GROUP BY RealCID
                ORDER BY SUM(ISNULL(FullCommissions,0) + ISNULL(RollOverFee,0) + ISNULL(TicketFee,0) + ISNULL(TicketFeeByPercent,0) + ISNULL(AdminFee,0) + ISNULL(SpotAdjustFee,0)) DESC
            )
    ) AS Filtered
    GROUP BY RealCID,		
				UserName,
				Region,
				Manager,
				Regulation,
				Mifid
) AS Aggregated
UNPIVOT
(
    Amount FOR Metric IN (
        Commissions,
        FullCommissions,
        RollOverFee,
        TicketFee,
        TicketFeeByPercent,
        AdminFee,
        SpotAdjustFee,
		VolumeOnOpen,
		VolumeOnClose,
		TotalTradingRevenue
    )
) AS unpvt

union all 



-- DECLARE getdate()-1 DATE = getdate()-1

SELECT 
	'PreviousWeekSameTime' AS [Period],
    RealCID,
	UserName,
	Region,
	Manager,
	Regulation,
	Mifid,
    Metric,
    Amount
FROM 
(
    SELECT 
        RealCID,
		Filtered.UserName,
		Filtered.Region,
		Filtered.Manager,
		Filtered.Regulation,
		Filtered.Mifid,
        SUM(Commissions) AS Commissions,
        SUM(FullCommissions) AS FullCommissions,
        SUM(RollOverFee) AS RollOverFee,
        SUM(TicketFee) AS TicketFee,
        SUM(TicketFeeByPercent) AS TicketFeeByPercent,
        SUM(AdminFee) AS AdminFee,
        SUM(SpotAdjustFee) AS SpotAdjustFee,
        SUM(VolumeOnOpen) AS VolumeOnOpen,
        SUM(VolumeOnClose) AS VolumeOnClose,
		SUM(ISNULL(FullCommissions,0)) + SUM(ISNULL(RollOverFee,0)) + SUM(ISNULL(TicketFee,0)) + SUM(ISNULL(TicketFeeByPercent,0)) + SUM(ISNULL(AdminFee,0)) + SUM(ISNULL(SpotAdjustFee,0))  AS TotalTradingRevenue
    FROM 
    (
        SELECT *
        FROM BI_DB_dbo.BI_DB_DailyCommisionReport
        WHERE 
            IsValidCustomer = 1
            AND DateID BETWEEN CAST(FORMAT(CAST(dateadd (WEEK, -1, DATEADD(DAY, 1 - DATEPART(WEEKDAY, getdate()-1), getdate()-1)) AS DATE),'yyyyMMdd') as INT)  AND CAST(FORMAT(CAST(dateadd(WEEK, -1,getdate()-1) AS DATE),'yyyyMMdd') as INT) 
            AND RealCID IN (
                SELECT TOP 20 RealCID
                FROM BI_DB_dbo.BI_DB_DailyCommisionReport
                WHERE DateID BETWEEN  CAST(FORMAT(CAST(dateadd (WEEK, -1, DATEADD(DAY, 1 - DATEPART(WEEKDAY, getdate()-1), getdate()-1)) AS DATE),'yyyyMMdd') as INT)  AND CAST(FORMAT(CAST(dateadd(WEEK, -1,getdate()-1) AS DATE),'yyyyMMdd') as INT)
                GROUP BY RealCID
                ORDER BY SUM(ISNULL(FullCommissions,0) + ISNULL(RollOverFee,0) + ISNULL(TicketFee,0) + ISNULL(TicketFeeByPercent,0) + ISNULL(AdminFee,0) + ISNULL(SpotAdjustFee,0)) DESC
            )
    ) AS Filtered
    GROUP BY RealCID,		
				UserName,
				Region,
				Manager,
				Regulation,
				Mifid
) AS Aggregated
UNPIVOT
(
    Amount FOR Metric IN (
        Commissions,
        FullCommissions,
        RollOverFee,
        TicketFee,
        TicketFeeByPercent,
        AdminFee,
        SpotAdjustFee,
		VolumeOnOpen,
		VolumeOnClose,
		TotalTradingRevenue
    )
) AS unpvt

UNION ALL 



-- DECLARE getdate()-1 DATE = getdate()-1

SELECT 
	'CurrentQuarter' AS [Period],
    RealCID,
	UserName,
	Region,
	Manager,
	Regulation,
	Mifid,
    Metric,
    Amount
FROM 
(
    SELECT 
        RealCID,
		Filtered.UserName,
		Filtered.Region,
		Filtered.Manager,
		Filtered.Regulation,
		Filtered.Mifid,
        SUM(Commissions) AS Commissions,
        SUM(FullCommissions) AS FullCommissions,
        SUM(RollOverFee) AS RollOverFee,
        SUM(TicketFee) AS TicketFee,
        SUM(TicketFeeByPercent) AS TicketFeeByPercent,
        SUM(AdminFee) AS AdminFee,
        SUM(SpotAdjustFee) AS SpotAdjustFee,
        SUM(VolumeOnOpen) AS VolumeOnOpen,
        SUM(VolumeOnClose) AS VolumeOnClose,
		SUM(ISNULL(FullCommissions,0)) + SUM(ISNULL(RollOverFee,0)) + SUM(ISNULL(TicketFee,0)) + SUM(ISNULL(TicketFeeByPercent,0)) + SUM(ISNULL(AdminFee,0)) + SUM(ISNULL(SpotAdjustFee,0))  AS TotalTradingRevenue
    FROM 
    (
        SELECT *
        FROM BI_DB_dbo.BI_DB_DailyCommisionReport
        WHERE 
            IsValidCustomer = 1
            AND DateID BETWEEN CAST(FORMAT(CAST( DATEADD(qq, DATEDIFF(qq, 0, getdate()-1), 0) AS DATE),'yyyyMMdd') as INT)  AND CAST(FORMAT(CAST(getdate()-1 AS DATE),'yyyyMMdd') as INT) 
            AND RealCID IN (
                SELECT TOP 20 RealCID
                FROM BI_DB_dbo.BI_DB_DailyCommisionReport
                WHERE DateID BETWEEN  CAST(FORMAT(CAST( DATEADD(qq, DATEDIFF(qq, 0, getdate()-1), 0) AS DATE),'yyyyMMdd') as INT)  AND CAST(FORMAT(CAST(getdate()-1 AS DATE),'yyyyMMdd') as INT) 
                GROUP BY RealCID
                ORDER BY SUM(ISNULL(FullCommissions,0) + ISNULL(RollOverFee,0) + ISNULL(TicketFee,0) + ISNULL(TicketFeeByPercent,0) + ISNULL(AdminFee,0) + ISNULL(SpotAdjustFee,0)) DESC
            )
    ) AS Filtered
    GROUP BY RealCID,		
				UserName,
				Region,
				Manager,
				Regulation,
				Mifid
) AS Aggregated
UNPIVOT
(
    Amount FOR Metric IN (
        Commissions,
        FullCommissions,
        RollOverFee,
        TicketFee,
        TicketFeeByPercent,
        AdminFee,
        SpotAdjustFee,
		VolumeOnOpen,
		VolumeOnClose,
		TotalTradingRevenue
    )
) AS unpvt

--- Last Month
```

### 11. Custom SQL Query

- Tableau id: `c53722b2-ad08-b4d9-2bfc-5f95078aaaa5`

```sql
SELECT a.FullDate AS MonthEnding
, a.Regulation
, a.Region
, (CASE WHEN a.InstrumentType = 'Stocks' AND a.IsSettled = 1 THEN 'Real Stocks' 
WHEN a.InstrumentType = 'Stocks' AND a.IsSettled = 0 THEN 'CFD Stocks'
ELSE a.InstrumentType END) AS InstrumentType
, SUM(VolumeOnOpen) AS VolumeOnOpen
, SUM(VolumeOnClose) AS VolumeOnClose
, SUM(VolumeOnOpen) + SUM(VolumeOnClose) AS TotalVolume
, SUM(a.FullCommissions) AS FullCommissions
, SUM(a.Commissions) AS Commissions
, SUM(a.FullCommissionOnCloseAdjustment) AS FullCommissionOnCloseAdjustment
, SUM(a.CommissionOnCloseAdjustment) AS CommissionOnCloseAdjustment
FROM BI_DB_dbo.BI_DB_DailyCommisionReport a
WHERE DateID BETWEEN CAST(FORMAT(CAST(<[Parameters].[Parameter 2]> AS DATE),'yyyyMMdd') as INT) 
AND CAST(FORMAT(CAST(<[Parameters].[Parameter 3]> AS DATE),'yyyyMMdd') as INT) AND a.Regulation = 'FCA'
AND a.InstrumentType <> 'N/A'
GROUP BY a.FullDate, a.Regulation, a.Region
, (CASE WHEN a.InstrumentType = 'Stocks' AND a.IsSettled = 1 THEN 'Real Stocks' 
WHEN a.InstrumentType = 'Stocks' AND a.IsSettled = 0 THEN 'CFD Stocks'
ELSE a.InstrumentType END)
```

### 12. Custom SQL Query

- Tableau id: `c9be1d5d-5d53-5b92-ab93-e7a8452ff30a`

```sql
--- yesterday

SELECT 
	'Yesterday' AS [Period],
    RealCID,
	UserName,
	Region,
	Manager,
	Regulation,
	Mifid,
	InstrumentType, -- Added here
    Metric, 
    Amount
FROM 
(
    SELECT 
        RealCID,
		Filtered.UserName,
		Filtered.Region,
		Filtered.Manager,
		Filtered.Regulation,
		Filtered.Mifid,
		Filtered.InstrumentType, -- Added here
        SUM(Commissions) AS Commissions,
        SUM(FullCommissions) AS FullCommissions,
        SUM(RollOverFee) AS RollOverFee,
        SUM(TicketFee) AS TicketFee,
        SUM(TicketFeeByPercent) AS TicketFeeByPercent,
        SUM(AdminFee) AS AdminFee,
        SUM(SpotAdjustFee) AS SpotAdjustFee,
        SUM(VolumeOnOpen) AS VolumeOnOpen,
        SUM(VolumeOnClose) AS VolumeOnClose,
		SUM(ISNULL(FullCommissions,0)) + SUM(ISNULL(RollOverFee,0)) + SUM(ISNULL(TicketFee,0)) + SUM(ISNULL(TicketFeeByPercent,0)) + SUM(ISNULL(AdminFee,0)) + SUM(ISNULL(SpotAdjustFee,0)) AS TotalTradingRevenue
    FROM 
    (
        SELECT *
        FROM BI_DB_dbo.BI_DB_DailyCommisionReport
        WHERE 
            IsValidCustomer = 1
            AND DateID = CAST(FORMAT(CAST(getdate()-1 AS DATE),'yyyyMMdd') as INT)
            AND RealCID IN (
                SELECT TOP 20 RealCID
                FROM BI_DB_dbo.BI_DB_DailyCommisionReport
                WHERE DateID = CAST(FORMAT(CAST(getdate()-1 AS DATE),'yyyyMMdd') as INT) 
                GROUP BY RealCID
                ORDER BY SUM(ISNULL(FullCommissions,0) + ISNULL(RollOverFee,0) + ISNULL(TicketFee,0) + ISNULL(TicketFeeByPercent,0) + ISNULL(AdminFee,0) + ISNULL(SpotAdjustFee,0)) DESC
            )
    ) AS Filtered
    GROUP BY RealCID,		
				UserName,
				Region,
				Manager,
				Regulation,
				Mifid,
				InstrumentType -- Added here
) AS Aggregated
UNPIVOT
(
    Amount FOR Metric IN (
        Commissions,
        FullCommissions,
        RollOverFee,
        TicketFee,
        TicketFeeByPercent,
        AdminFee,
        SpotAdjustFee,
		VolumeOnOpen,
		VolumeOnClose,
		TotalTradingRevenue
    )
) AS unpvt

--- This Month

UNION all 

SELECT 
	'CurrentMonth' AS [Period],
    RealCID,
	UserName,
	Region,
	Manager,
	Regulation,
	Mifid,
	InstrumentType, -- Added here
    Metric, 
    Amount
FROM 
(
    SELECT 
        RealCID,
		Filtered.UserName,
		Filtered.Region,
		Filtered.Manager,
		Filtered.Regulation,
		Filtered.Mifid,
		Filtered.InstrumentType, -- Added here
        SUM(Commissions) AS Commissions,
        SUM(FullCommissions) AS FullCommissions,
        SUM(RollOverFee) AS RollOverFee,
        SUM(TicketFee) AS TicketFee,
        SUM(TicketFeeByPercent) AS TicketFeeByPercent,
        SUM(AdminFee) AS AdminFee,
        SUM(SpotAdjustFee) AS SpotAdjustFee,
        SUM(VolumeOnOpen) AS VolumeOnOpen,
        SUM(VolumeOnClose) AS VolumeOnClose,
		SUM(ISNULL(FullCommissions,0)) + SUM(ISNULL(RollOverFee,0)) + SUM(ISNULL(TicketFee,0)) + SUM(ISNULL(TicketFeeByPercent,0)) + SUM(ISNULL(AdminFee,0)) + SUM(ISNULL(SpotAdjustFee,0))  AS TotalTradingRevenue
    FROM 
    (
        SELECT *
        FROM BI_DB_dbo.BI_DB_DailyCommisionReport
        WHERE 
            IsValidCustomer = 1
            AND DateID BETWEEN CAST(FORMAT(CAST(DATEADD(month, DATEDIFF(month, 0, getdate()-1), 0) AS DATE),'yyyyMMdd') as INT) AND CAST(FORMAT(CAST(getdate()-1 AS DATE),'yyyyMMdd') as INT) 
            AND RealCID IN (
                SELECT TOP 20 RealCID
                FROM BI_DB_dbo.BI_DB_DailyCommisionReport
                WHERE DateID BETWEEN CAST(FORMAT(CAST(DATEADD(month, DATEDIFF(month, 0, getdate()-1), 0) AS DATE),'yyyyMMdd') as INT) AND CAST(FORMAT(CAST(getdate()-1 AS DATE),'yyyyMMdd') as INT) 
                GROUP BY RealCID
                ORDER BY SUM(ISNULL(FullCommissions,0) + ISNULL(RollOverFee,0) + ISNULL(TicketFee,0) + ISNULL(TicketFeeByPercent,0) + ISNULL(AdminFee,0) + ISNULL(SpotAdjustFee,0)) DESC
            )
    ) AS Filtered
    GROUP BY RealCID,		
				UserName,
				Region,
				Manager,
				Regulation,
				Mifid,
				InstrumentType -- Added here
) AS Aggregated
UNPIVOT
(
    Amount FOR Metric IN (
        Commissions,
        FullCommissions,
        RollOverFee,
        TicketFee,
        TicketFeeByPercent,
        AdminFee,
        SpotAdjustFee,
		VolumeOnOpen,
		VolumeOnClose,
		TotalTradingRevenue
    )
) AS unpvt

--- Last Month

UNION all 

SELECT 
	'PreviousMonthSameTime' AS [Period],
    RealCID,
	UserName,
	Region,
	Manager,
	Regulation,
	Mifid,
	InstrumentType, -- Added here
    Metric, 
    Amount
FROM 
(
    SELECT 
        RealCID,
		Filtered.UserName,
		Filtered.Region,
		Filtered.Manager,
		Filtered.Regulation,
		Filtered.Mifid,
		Filtered.InstrumentType, -- Added here
        SUM(Commissions) AS Commissions,
        SUM(FullCommissions) AS FullCommissions,
        SUM(RollOverFee) AS RollOverFee,
        SUM(TicketFee) AS TicketFee,
        SUM(TicketFeeByPercent) AS TicketFeeByPercent,
        SUM(AdminFee) AS AdminFee,
        SUM(SpotAdjustFee) AS SpotAdjustFee,
        SUM(VolumeOnOpen) AS VolumeOnOpen,
        SUM(VolumeOnClose) AS VolumeOnClose,
		SUM(ISNULL(FullCommissions,0)) + SUM(ISNULL(RollOverFee,0)) + SUM(ISNULL(TicketFee,0)) + SUM(ISNULL(TicketFeeByPercent,0)) + SUM(ISNULL(AdminFee,0)) + SUM(ISNULL(SpotAdjustFee,0))  AS TotalTradingRevenue
    FROM 
    (
        SELECT *
        FROM BI_DB_dbo.BI_DB_DailyCommisionReport
        WHERE 
            IsValidCustomer = 1
            AND DateID BETWEEN CAST(FORMAT(CAST( DATEADD(month, DATEDIFF(month, 0, getdate()-1) - 1, 0) AS DATE),'yyyyMMdd') as INT) AND CAST(FORMAT(CAST(dateadd(MONTH,-1,getdate()-1)  AS DATE),'yyyyMMdd') as INT) 
            AND RealCID IN (
                SELECT TOP 20 RealCID
                FROM BI_DB_dbo.BI_DB_DailyCommisionReport
                WHERE DateID BETWEEN CAST(FORMAT(CAST( DATEADD(month, DATEDIFF(month, 0, getdate()-1) - 1, 0) AS DATE),'yyyyMMdd') as INT) AND CAST(FORMAT(CAST(dateadd(MONTH,-1,getdate()-1)  AS DATE),'yyyyMMdd') as INT)
                GROUP BY RealCID
                ORDER BY SUM(ISNULL(FullCommissions,0) + ISNULL(RollOverFee,0) + ISNULL(TicketFee,0) + ISNULL(TicketFeeByPercent,0) + ISNULL(AdminFee,0) + ISNULL(SpotAdjustFee,0)) DESC
            )
    ) AS Filtered
    GROUP BY RealCID,		
				UserName,
				Region,
				Manager,
				Regulation,
				Mifid,
				InstrumentType -- Added here
) AS Aggregated
UNPIVOT
(
    Amount FOR Metric IN (
        Commissions,
        FullCommissions,
        RollOverFee,
        TicketFee,
        TicketFeeByPercent,
        AdminFee,
        SpotAdjustFee,
		VolumeOnOpen,
		VolumeOnClose,
		TotalTradingRevenue
    )
) AS unpvt

--- Current Year

UNION all 

SELECT 
	'CurrentYear' AS [Period],
    RealCID,
	UserName,
	Region,
	Manager,
	Regulation,
	Mifid,
	InstrumentType, -- Added here
    Metric, 
    Amount
FROM 
(
    SELECT 
        RealCID,
		Filtered.UserName,
		Filtered.Region,
		Filtered.Manager,
		Filtered.Regulation,
		Filtered.Mifid,
		Filtered.InstrumentType, -- Added here
        sum(Commissions) AS Commissions,
        sum(FullCommissions) AS FullCommissions,
        sum(RollOverFee) AS RollOverFee,
        sum(TicketFee) AS TicketFee,
        sum(TicketFeeByPercent) AS TicketFeeByPercent,
        sum(AdminFee) AS AdminFee,
        SUM(SpotAdjustFee) AS SpotAdjustFee,
        SUM(VolumeOnOpen) AS VolumeOnOpen,
        SUM(VolumeOnClose) AS VolumeOnClose,
		SUM(ISNULL(FullCommissions,0)) + SUM(ISNULL(RollOverFee,0)) + SUM(ISNULL(TicketFee,0)) + SUM(ISNULL(TicketFeeByPercent,0)) + SUM(ISNULL(AdminFee,0)) + SUM(ISNULL(SpotAdjustFee,0))  AS TotalTradingRevenue
    FROM 
    (
        SELECT ia.*
        FROM BI_DB_dbo.BI_DB_DailyCommisionReport ia 
        WHERE 
            IsValidCustomer = 1
            AND DateID BETWEEN CAST(FORMAT(CAST(DATEADD(yy, DATEDIFF(yy, 0, getdate()-1), 0) AS DATE),'yyyyMMdd') as INT)  AND CAST(FORMAT(CAST(getdate()-1 AS DATE),'yyyyMMdd') as INT) 
			AND RealCID IN (
                SELECT TOP 20 RealCID
                FROM BI_DB_dbo.BI_DB_DailyCommisionReport ia
				WHERE ia.DateID between CAST(FORMAT(CAST(DATEADD(yy, DATEDIFF(yy, 0, getdate()-1), 0) AS DATE),'yyyyMMdd') as INT)  AND CAST(FORMAT(CAST(getdate()-1 AS DATE),'yyyyMMdd') as INT) 
                GROUP BY RealCID
                ORDER BY SUM(ISNULL(FullCommissions,0) + ISNULL(RollOverFee,0) + ISNULL(TicketFee,0) + ISNULL(TicketFeeByPercent,0) + ISNULL(AdminFee,0) + ISNULL(SpotAdjustFee,0)) DESC
            )
    ) AS Filtered
    GROUP BY RealCID,		
				UserName,
				Region,
				Manager,
				Regulation,
				Mifid,
				InstrumentType -- Added here
) AS Aggregated
UNPIVOT
(
    Amount FOR Metric IN (
        Commissions,
        FullCommissions,
        RollOverFee,
        TicketFee,
        TicketFeeByPercent,
        AdminFee,
        SpotAdjustFee,
		VolumeOnOpen,
		VolumeOnClose,
		TotalTradingRevenue
    )
) AS unpvt

--- Previous Year

UNION all 

SELECT 
	'PreviousYearSameTime' AS [Period],
    RealCID,
	UserName,
	Region,
	Manager,
	Regulation,
	Mifid,
	InstrumentType, -- Added here
    Metric, 
    Amount
FROM 
(
    SELECT 
        RealCID,
		Filtered.UserName,
		Filtered.Region,
		Filtered.Manager,
		Filtered.Regulation,
		Filtered.Mifid,
		Filtered.InstrumentType, -- Added here
        sum(Commissions) AS Commissions,
        sum(FullCommissions) AS FullCommissions,
        sum(RollOverFee) AS RollOverFee,
        sum(TicketFee) AS TicketFee,
        sum(TicketFeeByPercent) AS TicketFeeByPercent,
        sum(AdminFee) AS AdminFee,
        SUM(SpotAdjustFee) AS SpotAdjustFee,
        SUM(VolumeOnOpen) AS VolumeOnOpen,
        SUM(VolumeOnClose) AS VolumeOnClose,
		SUM(ISNULL(FullCommissions,0)) + SUM(ISNULL(RollOverFee,0)) + SUM(ISNULL(TicketFee,0)) + SUM(ISNULL(TicketFeeByPercent,0)) + SUM(ISNULL(AdminFee,0)) + SUM(ISNULL(SpotAdjustFee,0))  AS TotalTradingRevenue
    FROM 
    (
        SELECT ia.*
        FROM BI_DB_dbo.BI_DB_DailyCommisionReport ia 
        WHERE 
            IsValidCustomer = 1
            AND DateID BETWEEN CAST(FORMAT(CAST(DATEADD(yy, DATEDIFF(yy, 0, GETDATE()) - 1, 0) AS DATE),'yyyyMMdd') as INT) AND CAST(FORMAT(CAST(dateadd(YEAR,-1,getdate()-1) AS DATE),'yyyyMMdd') as INT) 
			AND RealCID IN (
                SELECT TOP 20 RealCID
                FROM BI_DB_dbo.BI_DB_DailyCommisionReport ia
				WHERE ia.DateID between CAST(FORMAT(CAST(DATEADD(yy, DATEDIFF(yy, 0, GETDATE()) - 1, 0) AS DATE),'yyyyMMdd') as INT) AND CAST(FORMAT(CAST(dateadd(YEAR,-1,getdate()-1) AS DATE),'yyyyMMdd') as INT) 
                GROUP BY RealCID
                ORDER BY SUM(ISNULL(FullCommissions,0) + ISNULL(RollOverFee,0) + ISNULL(TicketFee,0) + ISNULL(TicketFeeByPercent,0) + ISNULL(AdminFee,0) + ISNULL(SpotAdjustFee,0)) DESC
            )
    ) AS Filtered
    GROUP BY RealCID,		
				UserName,
				Region,
				Manager,
				Regulation,
				Mifid,
				InstrumentType -- Added here
) AS Aggregated
UNPIVOT
(
    Amount FOR Metric IN (
        Commissions,
        FullCommissions,
        RollOverFee,
        TicketFee,
        TicketFeeByPercent,
        AdminFee,
        SpotAdjustFee,
		VolumeOnOpen,
		VolumeOnClose,
		TotalTradingRevenue
    )
) AS unpvt

--- Current Week

UNION all 

SELECT 
	'CurrentWeek' AS [Period],
    RealCID,
	UserName,
	Region,
	Manager,
	Regulation,
	Mifid,
	InstrumentType, -- Added here
    Metric, 
    Amount
FROM 
(
    SELECT 
        RealCID,
		Filtered.UserName,
		Filtered.Region,
		Filtered.Manager,
		Filtered.Regulation,
		Filtered.Mifid,
		Filtered.InstrumentType, -- Added here
        SUM(Commissions) AS Commissions,
        SUM(FullCommissions) AS FullCommissions,
        SUM(RollOverFee) AS RollOverFee,
        SUM(TicketFee) AS TicketFee,
        SUM(TicketFeeByPercent) AS TicketFeeByPercent,
        SUM(AdminFee) AS AdminFee,
        SUM(SpotAdjustFee) AS SpotAdjustFee,
        SUM(VolumeOnOpen) AS VolumeOnOpen,
        SUM(VolumeOnClose) AS VolumeOnClose,
		SUM(ISNULL(FullCommissions,0)) + SUM(ISNULL(RollOverFee,0)) + SUM(ISNULL(TicketFee,0)) + SUM(ISNULL(TicketFeeByPercent,0)) + SUM(ISNULL(AdminFee,0)) + SUM(ISNULL(SpotAdjustFee,0))  AS TotalTradingRevenue
    FROM 
    (
        SELECT *
        FROM BI_DB_dbo.BI_DB_DailyCommisionReport
        WHERE 
            IsValidCustomer = 1
            AND DateID BETWEEN CAST(FORMAT(CAST(DATEADD(DAY, 1 - DATEPART(WEEKDAY, getdate()-1), getdate()-1) AS DATE),'yyyyMMdd') as INT)  AND CAST(FORMAT(CAST(getdate()-1 AS DATE),'yyyyMMdd') as INT) 
            AND RealCID IN (
                SELECT TOP 20 RealCID
                FROM BI_DB_dbo.BI_DB_DailyCommisionReport
                WHERE DateID BETWEEN CAST(FORMAT(CAST(DATEADD(DAY, 1 - DATEPART(WEEKDAY, getdate()-1), getdate()-1) AS DATE),'yyyyMMdd') as INT)  AND CAST(FORMAT(CAST(getdate()-1 AS DATE),'yyyyMMdd') as INT) 
                GROUP BY RealCID
                ORDER BY SUM(ISNULL(FullCommissions,0) + ISNULL(RollOverFee,0) + ISNULL(TicketFee,0) + ISNULL(TicketFeeByPercent,0) + ISNULL(AdminFee,0) + ISNULL(SpotAdjustFee,0)) DESC
            )
    ) AS Filtered
    GROUP BY RealCID,		
				UserName,
				Region,
				Manager,
				Regulation,
				Mifid,
				InstrumentType -- Added here
) AS Aggregated
UNPIVOT
(
    Amount FOR Metric IN (
        Commissions,
        FullCommissions,
        RollOverFee,
        TicketFee,
        TicketFeeByPercent,
        AdminFee,
        SpotAdjustFee,
		VolumeOnOpen,
		VolumeOnClose,
		TotalTradingRevenue
    )
) AS unpvt

--- Previous Week

UNION all 

SELECT 
	'PreviousWeekSameTime' AS [Period],
    RealCID,
	UserName,
	Region,
	Manager,
	Regulation,
	Mifid,
	InstrumentType, -- Added here
    Metric, 
    Amount
FROM 
(
    SELECT 
        RealCID,
		Filtered.UserName,
		Filtered.Region,
		Filtered.Manager,
		Filtered.Regulation,
		Filtered.Mifid,
		Filtered.InstrumentType, -- Added here
        SUM(Commissions) AS Commissions,
        SUM(FullCommissions) AS FullCommissions,
        SUM(RollOverFee) AS RollOverFee,
        SUM(TicketFee) AS TicketFee,
        SUM(TicketFeeByPercent) AS TicketFeeByPercent,
        SUM(AdminFee) AS AdminFee,
        SUM(SpotAdjustFee) AS SpotAdjustFee,
        SUM(VolumeOnOpen) AS VolumeOnOpen,
        SUM(VolumeOnClose) AS VolumeOnClose,
		SUM(ISNULL(FullCommissions,0)) + SUM(ISNULL(RollOverFee,0)) + SUM(ISNULL(TicketFee,0)) + SUM(ISNULL(TicketFeeByPercent,0)) + SUM(ISNULL(AdminFee,0)) + SUM(ISNULL(SpotAdjustFee,0))  AS TotalTradingRevenue
    FROM 
    (
        SELECT *
        FROM BI_DB_dbo.BI_DB_DailyCommisionReport
        WHERE 
            IsValidCustomer = 1
            AND DateID BETWEEN CAST(FORMAT(CAST(dateadd (WEEK, -1, DATEADD(DAY, 1 - DATEPART(WEEKDAY, getdate()-1), getdate()-1)) AS DATE),'yyyyMMdd') as INT)  AND CAST(FORMAT(CAST(dateadd(WEEK, -1,getdate()-1) AS DATE),'yyyyMMdd') as INT) 
            AND RealCID IN (
                SELECT TOP 20 RealCID
                FROM BI_DB_dbo.BI_DB_DailyCommisionReport
                WHERE DateID BETWEEN  CAST(FORMAT(CAST(dateadd (WEEK, -1, DATEADD(DAY, 1 - DATEPART(WEEKDAY, getdate()-1), getdate()-1)) AS DATE),'yyyyMMdd') as INT)  AND CAST(FORMAT(CAST(dateadd(WEEK, -1,getdate()-1) AS DATE),'yyyyMMdd') as INT)
                GROUP BY RealCID
                ORDER BY SUM(ISNULL(FullCommissions,0) + ISNULL(RollOverFee,0) + ISNULL(TicketFee,0) + ISNULL(TicketFeeByPercent,0) + ISNULL(AdminFee,0) + ISNULL(SpotAdjustFee,0)) DESC
            )
    ) AS Filtered
    GROUP BY RealCID,		
				UserName,
				Region,
				Manager,
				Regulation,
				Mifid,
				InstrumentType -- Added here
) AS Aggregated
UNPIVOT
(
    Amount FOR Metric IN (
        Commissions,
        FullCommissions,
        RollOverFee,
        TicketFee,
        TicketFeeByPercent,
        AdminFee,
        SpotAdjustFee,
		VolumeOnOpen,
		VolumeOnClose,
		TotalTradingRevenue
    )
) AS unpvt

--- Current Quarter

UNION ALL 

SELECT 
	'CurrentQuarter' AS [Period],
    RealCID,
	UserName,
	Region,
	Manager,
	Regulation,
	Mifid,
	InstrumentType, -- Added here
    Metric, 
    Amount
FROM 
(
    SELECT 
        RealCID,
		Filtered.UserName,
		Filtered.Region,
		Filtered.Manager,
		Filtered.Regulation,
		Filtered.Mifid,
		Filtered.InstrumentType, -- Added here
        SUM(Commissions) AS Commissions,
        SUM(FullCommissions) AS FullCommissions,
        SUM(RollOverFee) AS RollOverFee,
        SUM(TicketFee) AS TicketFee,
        SUM(TicketFeeByPercent) AS TicketFeeByPercent,
        SUM(AdminFee) AS AdminFee,
        SUM(SpotAdjustFee) AS SpotAdjustFee,
        SUM(VolumeOnOpen) AS VolumeOnOpen,
        SUM(VolumeOnClose) AS VolumeOnClose,
		SUM(ISNULL(FullCommissions,0)) + SUM(ISNULL(RollOverFee,0)) + SUM(ISNULL(TicketFee,0)) + SUM(ISNULL(TicketFeeByPercent,0)) + SUM(ISNULL(AdminFee,0)) + SUM(ISNULL(SpotAdjustFee,0))  AS TotalTradingRevenue
    FROM 
    (
        SELECT *
        FROM BI_DB_dbo.BI_DB_DailyCommisionReport
        WHERE 
            IsValidCustomer = 1
            AND DateID BETWEEN CAST(FORMAT(CAST( DATEADD(qq, DATEDIFF(qq, 0, getdate()-1), 0) AS DATE),'yyyyMMdd') as INT)  AND CAST(FORMAT(CAST(getdate()-1 AS DATE),'yyyyMMdd') as INT) 
            AND RealCID IN (
                SELECT TOP 20 RealCID
                FROM BI_DB_dbo.BI_DB_DailyCommisionReport
                WHERE DateID BETWEEN  CAST(FORMAT(CAST( DATEADD(qq, DATEDIFF(qq, 0, getdate()-1), 0) AS DATE),'yyyyMMdd') as INT)  AND CAST(FORMAT(CAST(getdate()-1 AS DATE),'yyyyMMdd') as INT) 
                GROUP BY RealCID
                ORDER BY SUM(ISNULL(FullCommissions,0) + ISNULL(RollOverFee,0) + ISNULL(TicketFee,0) + ISNULL(TicketFeeByPercent,0) + ISNULL(AdminFee,0) + ISNULL(SpotAdjustFee,0)) DESC
            )
    ) AS Filtered
    GROUP BY RealCID,		
				UserName,
				Region,
				Manager,
				Regulation,
				Mifid,
				InstrumentType -- Added here
) AS Aggregated
UNPIVOT
(
    Amount FOR Metric IN (
        Commissions,
        FullCommissions,
        RollOverFee,
        TicketFee,
        TicketFeeByPercent,
        AdminFee,
        SpotAdjustFee,
		VolumeOnOpen,
		VolumeOnClose,
		TotalTradingRevenue
    )
) AS unpvt
```

### 13. Custom SQL Query

- Tableau id: `d2113013-36ea-ed17-0ce5-5d3145852728`

```sql
--- yesterday

SELECT 
	'Yesterday' AS [Period],
    RealCID,
	UserName,
	Region,
	Manager,
	Regulation,
	Mifid,
    Metric,
    Amount
FROM 
(
    SELECT 
        RealCID,
		Filtered.UserName,
		Filtered.Region,
		Filtered.Manager,
		Filtered.Regulation,
		Filtered.Mifid,
        SUM(Commissions) AS Commissions,
        SUM(FullCommissions) AS FullCommissions,
        SUM(RollOverFee) AS RollOverFee,
        SUM(TicketFee) AS TicketFee,
        SUM(TicketFeeByPercent) AS TicketFeeByPercent,
        SUM(AdminFee) AS AdminFee,
        SUM(SpotAdjustFee) AS SpotAdjustFee,
        SUM(VolumeOnOpen) AS VolumeOnOpen,
        SUM(VolumeOnClose) AS VolumeOnClose,
		SUM(ISNULL(FullCommissions,0)) + SUM(ISNULL(RollOverFee,0)) + SUM(ISNULL(TicketFee,0)) + SUM(ISNULL(TicketFeeByPercent,0)) + SUM(ISNULL(AdminFee,0)) + SUM(ISNULL(SpotAdjustFee,0)) AS TotalTradingRevenue
    FROM 
    (
        SELECT *
        FROM BI_DB_dbo.BI_DB_DailyCommisionReport
        WHERE 
            IsValidCustomer = 1
            AND DateID = CAST(FORMAT(CAST(getdate()-1 AS DATE),'yyyyMMdd') as INT)
            AND RealCID IN (
                SELECT TOP 20 RealCID
                FROM BI_DB_dbo.BI_DB_DailyCommisionReport
                WHERE DateID = CAST(FORMAT(CAST(getdate()-1 AS DATE),'yyyyMMdd') as INT) 
                GROUP BY RealCID
                ORDER BY SUM(ISNULL(FullCommissions,0) + ISNULL(RollOverFee,0) + ISNULL(TicketFee,0) + ISNULL(TicketFeeByPercent,0) + ISNULL(AdminFee,0) + ISNULL(SpotAdjustFee,0)) DESC
            )
    ) AS Filtered
    GROUP BY RealCID,		
				UserName,
				Region,
				Manager,
				Regulation,
				Mifid
) AS Aggregated
UNPIVOT
(
    Amount FOR Metric IN (
        Commissions,
        FullCommissions,
        RollOverFee,
        TicketFee,
        TicketFeeByPercent,
        AdminFee,
        SpotAdjustFee,
		VolumeOnOpen,
		VolumeOnClose,
		TotalTradingRevenue
    )
) AS unpvt

--- This Month

UNION all 

-- DECLARE getdate()-1 DATE = getdate()-1

SELECT 
	'CurrentMonth' AS [Period],
    RealCID,
	UserName,
	Region,
	Manager,
	Regulation,
	Mifid,
    Metric,
    Amount
FROM 
(
    SELECT 
        RealCID,
		Filtered.UserName,
		Filtered.Region,
		Filtered.Manager,
		Filtered.Regulation,
		Filtered.Mifid,
        SUM(Commissions) AS Commissions,
        SUM(FullCommissions) AS FullCommissions,
        SUM(RollOverFee) AS RollOverFee,
        SUM(TicketFee) AS TicketFee,
        SUM(TicketFeeByPercent) AS TicketFeeByPercent,
        SUM(AdminFee) AS AdminFee,
        SUM(SpotAdjustFee) AS SpotAdjustFee,
        SUM(VolumeOnOpen) AS VolumeOnOpen,
        SUM(VolumeOnClose) AS VolumeOnClose,
		SUM(ISNULL(FullCommissions,0)) + SUM(ISNULL(RollOverFee,0)) + SUM(ISNULL(TicketFee,0)) + SUM(ISNULL(TicketFeeByPercent,0)) + SUM(ISNULL(AdminFee,0)) + SUM(ISNULL(SpotAdjustFee,0))  AS TotalTradingRevenue
    FROM 
    (
        SELECT *
        FROM BI_DB_dbo.BI_DB_DailyCommisionReport
        WHERE 
            IsValidCustomer = 1
            AND DateID BETWEEN CAST(FORMAT(CAST(DATEADD(month, DATEDIFF(month, 0, getdate()-1), 0) AS DATE),'yyyyMMdd') as INT) AND CAST(FORMAT(CAST(getdate()-1 AS DATE),'yyyyMMdd') as INT) 
            AND RealCID IN (
                SELECT TOP 20 RealCID
                FROM BI_DB_dbo.BI_DB_DailyCommisionReport
                WHERE DateID BETWEEN CAST(FORMAT(CAST(DATEADD(month, DATEDIFF(month, 0, getdate()-1), 0) AS DATE),'yyyyMMdd') as INT) AND CAST(FORMAT(CAST(getdate()-1 AS DATE),'yyyyMMdd') as INT) 
                GROUP BY RealCID
                ORDER BY SUM(ISNULL(FullCommissions,0) + ISNULL(RollOverFee,0) + ISNULL(TicketFee,0) + ISNULL(TicketFeeByPercent,0) + ISNULL(AdminFee,0) + ISNULL(SpotAdjustFee,0)) DESC
            )
    ) AS Filtered
    GROUP BY RealCID,		
				UserName,
				Region,
				Manager,
				Regulation,
				Mifid
) AS Aggregated
UNPIVOT
(
    Amount FOR Metric IN (
        Commissions,
        FullCommissions,
        RollOverFee,
        TicketFee,
        TicketFeeByPercent,
        AdminFee,
        SpotAdjustFee,
		VolumeOnOpen,
		VolumeOnClose,
		TotalTradingRevenue
    )
) AS unpvt

--- Last Month

UNION all 


SELECT 
	'PreviousMonthSameTime' AS [Period],
    RealCID,
	UserName,
	Region,
	Manager,
	Regulation,
	Mifid,
    Metric,
    Amount
FROM 
(
    SELECT 
        RealCID,
		Filtered.UserName,
		Filtered.Region,
		Filtered.Manager,
		Filtered.Regulation,
		Filtered.Mifid,
        SUM(Commissions) AS Commissions,
        SUM(FullCommissions) AS FullCommissions,
        SUM(RollOverFee) AS RollOverFee,
        SUM(TicketFee) AS TicketFee,
        SUM(TicketFeeByPercent) AS TicketFeeByPercent,
        SUM(AdminFee) AS AdminFee,
        SUM(SpotAdjustFee) AS SpotAdjustFee,
        SUM(VolumeOnOpen) AS VolumeOnOpen,
        SUM(VolumeOnClose) AS VolumeOnClose,
		SUM(ISNULL(FullCommissions,0)) + SUM(ISNULL(RollOverFee,0)) + SUM(ISNULL(TicketFee,0)) + SUM(ISNULL(TicketFeeByPercent,0)) + SUM(ISNULL(AdminFee,0)) + SUM(ISNULL(SpotAdjustFee,0))  AS TotalTradingRevenue
    FROM 
    (
        SELECT *
        FROM BI_DB_dbo.BI_DB_DailyCommisionReport
        WHERE 
            IsValidCustomer = 1
            AND DateID BETWEEN CAST(FORMAT(CAST( DATEADD(month, DATEDIFF(month, 0, getdate()-1) - 1, 0) AS DATE),'yyyyMMdd') as INT) AND CAST(FORMAT(CAST(dateadd(MONTH,-1,getdate()-1)  AS DATE),'yyyyMMdd') as INT) 
            AND RealCID IN (
                SELECT TOP 20 RealCID
                FROM BI_DB_dbo.BI_DB_DailyCommisionReport
                WHERE DateID BETWEEN CAST(FORMAT(CAST( DATEADD(month, DATEDIFF(month, 0, getdate()-1) - 1, 0) AS DATE),'yyyyMMdd') as INT) AND CAST(FORMAT(CAST(dateadd(MONTH,-1,getdate()-1)  AS DATE),'yyyyMMdd') as INT)
                GROUP BY RealCID
                ORDER BY SUM(ISNULL(FullCommissions,0) + ISNULL(RollOverFee,0) + ISNULL(TicketFee,0) + ISNULL(TicketFeeByPercent,0) + ISNULL(AdminFee,0) + ISNULL(SpotAdjustFee,0)) DESC
            )
    ) AS Filtered
    GROUP BY RealCID,		
				UserName,
				Region,
				Manager,
				Regulation,
				Mifid
) AS Aggregated
UNPIVOT
(
    Amount FOR Metric IN (
        Commissions,
        FullCommissions,
        RollOverFee,
        TicketFee,
        TicketFeeByPercent,
        AdminFee,
        SpotAdjustFee,
		VolumeOnOpen,
		VolumeOnClose,
		TotalTradingRevenue
    )
) AS unpvt

--- same day 8 weeks 


UNION all 

--DECLARE getdate()-1 DATE = getdate()-1

SELECT 
	'CurrentYear' AS [Period],
    RealCID,
	UserName,
	Region,
	Manager,
	Regulation,
	Mifid,
    Metric,
    Amount
FROM 
(
    SELECT 
        RealCID,
		Filtered.UserName,
		Filtered.Region,
		Filtered.Manager,
		Filtered.Regulation,
		Filtered.Mifid,
        sum(Commissions) AS Commissions,
        sum(FullCommissions) AS FullCommissions,
        sum(RollOverFee) AS RollOverFee,
        sum(TicketFee) AS TicketFee,
        sum(TicketFeeByPercent) AS TicketFeeByPercent,
        sum(AdminFee) AS AdminFee,
        SUM(SpotAdjustFee) AS SpotAdjustFee,
        SUM(VolumeOnOpen) AS VolumeOnOpen,
        SUM(VolumeOnClose) AS VolumeOnClose,
		SUM(ISNULL(FullCommissions,0)) + SUM(ISNULL(RollOverFee,0)) + SUM(ISNULL(TicketFee,0)) + SUM(ISNULL(TicketFeeByPercent,0)) + SUM(ISNULL(AdminFee,0)) + SUM(ISNULL(SpotAdjustFee,0))  AS TotalTradingRevenue
    FROM 
    (
        SELECT ia.*
        FROM BI_DB_dbo.BI_DB_DailyCommisionReport ia -- select @@DATEFIRST
        WHERE 
            IsValidCustomer = 1
            AND DateID BETWEEN CAST(FORMAT(CAST(DATEADD(yy, DATEDIFF(yy, 0, getdate()-1), 0) AS DATE),'yyyyMMdd') as INT)  AND CAST(FORMAT(CAST(getdate()-1 AS DATE),'yyyyMMdd') as INT) 
			AND RealCID IN (
                SELECT TOP 20 RealCID
                FROM BI_DB_dbo.BI_DB_DailyCommisionReport ia
				WHERE ia.DateID between CAST(FORMAT(CAST(DATEADD(yy, DATEDIFF(yy, 0, getdate()-1), 0) AS DATE),'yyyyMMdd') as INT)  AND CAST(FORMAT(CAST(getdate()-1 AS DATE),'yyyyMMdd') as INT) 
                GROUP BY RealCID
                ORDER BY SUM(ISNULL(FullCommissions,0) + ISNULL(RollOverFee,0) + ISNULL(TicketFee,0) + ISNULL(TicketFeeByPercent,0) + ISNULL(AdminFee,0) + ISNULL(SpotAdjustFee,0)) DESC
            )
    ) AS Filtered
    GROUP BY RealCID,		
				UserName,
				Region,
				Manager,
				Regulation,
				Mifid
) AS Aggregated
UNPIVOT
(
    Amount FOR Metric IN (
        Commissions,
        FullCommissions,
        RollOverFee,
        TicketFee,
        TicketFeeByPercent,
        AdminFee,
        SpotAdjustFee,
		VolumeOnOpen,
		VolumeOnClose,
		TotalTradingRevenue
    )
) AS unpvt

UNION all 

SELECT 
	'PreviousYearSameTime' AS [Period],
    RealCID,
	UserName,
	Region,
	Manager,
	Regulation,
	Mifid,
    Metric,
    Amount
FROM 
(
    SELECT 
        RealCID,
		Filtered.UserName,
		Filtered.Region,
		Filtered.Manager,
		Filtered.Regulation,
		Filtered.Mifid,
        sum(Commissions) AS Commissions,
        sum(FullCommissions) AS FullCommissions,
        sum(RollOverFee) AS RollOverFee,
        sum(TicketFee) AS TicketFee,
        sum(TicketFeeByPercent) AS TicketFeeByPercent,
        sum(AdminFee) AS AdminFee,
        SUM(SpotAdjustFee) AS SpotAdjustFee,
        SUM(VolumeOnOpen) AS VolumeOnOpen,
        SUM(VolumeOnClose) AS VolumeOnClose,
		SUM(ISNULL(FullCommissions,0)) + SUM(ISNULL(RollOverFee,0)) + SUM(ISNULL(TicketFee,0)) + SUM(ISNULL(TicketFeeByPercent,0)) + SUM(ISNULL(AdminFee,0)) + SUM(ISNULL(SpotAdjustFee,0))  AS TotalTradingRevenue
    FROM 
    (
        SELECT ia.*
        FROM BI_DB_dbo.BI_DB_DailyCommisionReport ia -- select @@DATEFIRST
        WHERE 
            IsValidCustomer = 1
            AND DateID BETWEEN CAST(FORMAT(CAST(DATEADD(yy, DATEDIFF(yy, 0, GETDATE()) - 1, 0) AS DATE),'yyyyMMdd') as INT) AND CAST(FORMAT(CAST(dateadd(YEAR,-1,getdate()-1) AS DATE),'yyyyMMdd') as INT) 
			AND RealCID IN (
                SELECT TOP 20 RealCID
                FROM BI_DB_dbo.BI_DB_DailyCommisionReport ia
				WHERE ia.DateID between CAST(FORMAT(CAST(DATEADD(yy, DATEDIFF(yy, 0, GETDATE()) - 1, 0) AS DATE),'yyyyMMdd') as INT) AND CAST(FORMAT(CAST(dateadd(YEAR,-1,getdate()-1) AS DATE),'yyyyMMdd') as INT) 
                GROUP BY RealCID
                ORDER BY SUM(ISNULL(FullCommissions,0) + ISNULL(RollOverFee,0) + ISNULL(TicketFee,0) + ISNULL(TicketFeeByPercent,0) + ISNULL(AdminFee,0) + ISNULL(SpotAdjustFee,0)) DESC
            )
    ) AS Filtered
    GROUP BY RealCID,		
				UserName,
				Region,
				Manager,
				Regulation,
				Mifid
) AS Aggregated
UNPIVOT
(
    Amount FOR Metric IN (
        Commissions,
        FullCommissions,
        RollOverFee,
        TicketFee,
        TicketFeeByPercent,
        AdminFee,
        SpotAdjustFee,
		VolumeOnOpen,
		VolumeOnClose,
		TotalTradingRevenue
    )
) AS unpvt

union all 


SELECT 
	'CurrentWeek' AS [Period],
    RealCID,
	UserName,
	Region,
	Manager,
	Regulation,
	Mifid,
    Metric,
    Amount
FROM 
(
    SELECT 
        RealCID,
		Filtered.UserName,
		Filtered.Region,
		Filtered.Manager,
		Filtered.Regulation,
		Filtered.Mifid,
        SUM(Commissions) AS Commissions,
        SUM(FullCommissions) AS FullCommissions,
        SUM(RollOverFee) AS RollOverFee,
        SUM(TicketFee) AS TicketFee,
        SUM(TicketFeeByPercent) AS TicketFeeByPercent,
        SUM(AdminFee) AS AdminFee,
        SUM(SpotAdjustFee) AS SpotAdjustFee,
        SUM(VolumeOnOpen) AS VolumeOnOpen,
        SUM(VolumeOnClose) AS VolumeOnClose,
		SUM(ISNULL(FullCommissions,0)) + SUM(ISNULL(RollOverFee,0)) + SUM(ISNULL(TicketFee,0)) + SUM(ISNULL(TicketFeeByPercent,0)) + SUM(ISNULL(AdminFee,0)) + SUM(ISNULL(SpotAdjustFee,0))  AS TotalTradingRevenue
    FROM 
    (
        SELECT *
        FROM BI_DB_dbo.BI_DB_DailyCommisionReport
        WHERE 
            IsValidCustomer = 1
            AND DateID BETWEEN CAST(FORMAT(CAST(DATEADD(DAY, 1 - DATEPART(WEEKDAY, getdate()-1), getdate()-1) AS DATE),'yyyyMMdd') as INT)  AND CAST(FORMAT(CAST(getdate()-1 AS DATE),'yyyyMMdd') as INT) 
            AND RealCID IN (
                SELECT TOP 20 RealCID
                FROM BI_DB_dbo.BI_DB_DailyCommisionReport
                WHERE DateID BETWEEN CAST(FORMAT(CAST(DATEADD(DAY, 1 - DATEPART(WEEKDAY, getdate()-1), getdate()-1) AS DATE),'yyyyMMdd') as INT)  AND CAST(FORMAT(CAST(getdate()-1 AS DATE),'yyyyMMdd') as INT) 
                GROUP BY RealCID
                ORDER BY SUM(ISNULL(FullCommissions,0) + ISNULL(RollOverFee,0) + ISNULL(TicketFee,0) + ISNULL(TicketFeeByPercent,0) + ISNULL(AdminFee,0) + ISNULL(SpotAdjustFee,0)) DESC
            )
    ) AS Filtered
    GROUP BY RealCID,		
				UserName,
				Region,
				Manager,
				Regulation,
				Mifid
) AS Aggregated
UNPIVOT
(
    Amount FOR Metric IN (
        Commissions,
        FullCommissions,
        RollOverFee,
        TicketFee,
        TicketFeeByPercent,
        AdminFee,
        SpotAdjustFee,
		VolumeOnOpen,
		VolumeOnClose,
		TotalTradingRevenue
    )
) AS unpvt

union all 



-- DECLARE getdate()-1 DATE = getdate()-1

SELECT 
	'PreviousWeekSameTime' AS [Period],
    RealCID,
	UserName,
	Region,
	Manager,
	Regulation,
	Mifid,
    Metric,
    Amount
FROM 
(
    SELECT 
        RealCID,
		Filtered.UserName,
		Filtered.Region,
		Filtered.Manager,
		Filtered.Regulation,
		Filtered.Mifid,
        SUM(Commissions) AS Commissions,
        SUM(FullCommissions) AS FullCommissions,
        SUM(RollOverFee) AS RollOverFee,
        SUM(TicketFee) AS TicketFee,
        SUM(TicketFeeByPercent) AS TicketFeeByPercent,
        SUM(AdminFee) AS AdminFee,
        SUM(SpotAdjustFee) AS SpotAdjustFee,
        SUM(VolumeOnOpen) AS VolumeOnOpen,
        SUM(VolumeOnClose) AS VolumeOnClose,
		SUM(ISNULL(FullCommissions,0)) + SUM(ISNULL(RollOverFee,0)) + SUM(ISNULL(TicketFee,0)) + SUM(ISNULL(TicketFeeByPercent,0)) + SUM(ISNULL(AdminFee,0)) + SUM(ISNULL(SpotAdjustFee,0))  AS TotalTradingRevenue
    FROM 
    (
        SELECT *
        FROM BI_DB_dbo.BI_DB_DailyCommisionReport
        WHERE 
            IsValidCustomer = 1
            AND DateID BETWEEN CAST(FORMAT(CAST(dateadd (WEEK, -1, DATEADD(DAY, 1 - DATEPART(WEEKDAY, getdate()-1), getdate()-1)) AS DATE),'yyyyMMdd') as INT)  AND CAST(FORMAT(CAST(dateadd(WEEK, -1,getdate()-1) AS DATE),'yyyyMMdd') as INT) 
            AND RealCID IN (
                SELECT TOP 20 RealCID
                FROM BI_DB_dbo.BI_DB_DailyCommisionReport
                WHERE DateID BETWEEN  CAST(FORMAT(CAST(dateadd (WEEK, -1, DATEADD(DAY, 1 - DATEPART(WEEKDAY, getdate()-1), getdate()-1)) AS DATE),'yyyyMMdd') as INT)  AND CAST(FORMAT(CAST(dateadd(WEEK, -1,getdate()-1) AS DATE),'yyyyMMdd') as INT)
                GROUP BY RealCID
                ORDER BY SUM(ISNULL(FullCommissions,0) + ISNULL(RollOverFee,0) + ISNULL(TicketFee,0) + ISNULL(TicketFeeByPercent,0) + ISNULL(AdminFee,0) + ISNULL(SpotAdjustFee,0)) DESC
            )
    ) AS Filtered
    GROUP BY RealCID,		
				UserName,
				Region,
				Manager,
				Regulation,
				Mifid
) AS Aggregated
UNPIVOT
(
    Amount FOR Metric IN (
        Commissions,
        FullCommissions,
        RollOverFee,
        TicketFee,
        TicketFeeByPercent,
        AdminFee,
        SpotAdjustFee,
		VolumeOnOpen,
		VolumeOnClose,
		TotalTradingRevenue
    )
) AS unpvt

UNION ALL 



-- DECLARE getdate()-1 DATE = getdate()-1

SELECT 
	'CurrentQuarter' AS [Period],
    RealCID,
	UserName,
	Region,
	Manager,
	Regulation,
	Mifid,
    Metric,
    Amount
FROM 
(
    SELECT 
        RealCID,
		Filtered.UserName,
		Filtered.Region,
		Filtered.Manager,
		Filtered.Regulation,
		Filtered.Mifid,
        SUM(Commissions) AS Commissions,
        SUM(FullCommissions) AS FullCommissions,
        SUM(RollOverFee) AS RollOverFee,
        SUM(TicketFee) AS TicketFee,
        SUM(TicketFeeByPercent) AS TicketFeeByPercent,
        SUM(AdminFee) AS AdminFee,
        SUM(SpotAdjustFee) AS SpotAdjustFee,
        SUM(VolumeOnOpen) AS VolumeOnOpen,
        SUM(VolumeOnClose) AS VolumeOnClose,
		SUM(ISNULL(FullCommissions,0)) + SUM(ISNULL(RollOverFee,0)) + SUM(ISNULL(TicketFee,0)) + SUM(ISNULL(TicketFeeByPercent,0)) + SUM(ISNULL(AdminFee,0)) + SUM(ISNULL(SpotAdjustFee,0))  AS TotalTradingRevenue
    FROM 
    (
        SELECT *
        FROM BI_DB_dbo.BI_DB_DailyCommisionReport
        WHERE 
            IsValidCustomer = 1
            AND DateID BETWEEN CAST(FORMAT(CAST( DATEADD(qq, DATEDIFF(qq, 0, getdate()-1), 0) AS DATE),'yyyyMMdd') as INT)  AND CAST(FORMAT(CAST(getdate()-1 AS DATE),'yyyyMMdd') as INT) 
            AND RealCID IN (
                SELECT TOP 20 RealCID
                FROM BI_DB_dbo.BI_DB_DailyCommisionReport
                WHERE DateID BETWEEN  CAST(FORMAT(CAST( DATEADD(qq, DATEDIFF(qq, 0, getdate()-1), 0) AS DATE),'yyyyMMdd') as INT)  AND CAST(FORMAT(CAST(getdate()-1 AS DATE),'yyyyMMdd') as INT) 
                GROUP BY RealCID
                ORDER BY SUM(ISNULL(FullCommissions,0) + ISNULL(RollOverFee,0) + ISNULL(TicketFee,0) + ISNULL(TicketFeeByPercent,0) + ISNULL(AdminFee,0) + ISNULL(SpotAdjustFee,0)) DESC
            )
    ) AS Filtered
    GROUP BY RealCID,		
				UserName,
				Region,
				Manager,
				Regulation,
				Mifid
) AS Aggregated
UNPIVOT
(
    Amount FOR Metric IN (
        Commissions,
        FullCommissions,
        RollOverFee,
        TicketFee,
        TicketFeeByPercent,
        AdminFee,
        SpotAdjustFee,
		VolumeOnOpen,
		VolumeOnClose,
		TotalTradingRevenue
    )
) AS unpvt

--- Last Month
```

### 14. Custom SQL Query

- Tableau id: `e6a48e1f-a7e8-db43-c9f2-d6dfd6a61239`

```sql
SELECT ftd.EOM,
	  ftd.FirstDepositFundingType,
	  ftd.Country,
	  COUNT( ftd.CID) AS Clients,
	  SUM(ftd.FirstDepositAmount) 'FTDA',
	  SUM(rev_total.Revenue) 'Revenue'
	  from
	  (
	  SELECT EOMONTH(CAST(bdcd.FirstDepositDate AS date)) 'EOM',
	  bdcd.FirstDepositFundingType,
	  bdcd.FirstDepositAmount,
	  bdcd.CID,
	  bdcd.Country
      FROM BI_DB..BI_DB_CIDFirstDates bdcd
	  WHERE bdcd.FirstDepositDate IS NOT NULL AND CAST(bdcd.FirstDepositDate AS DATE)>=DATEADD(mm,-3,GETDATE()-1)
	  AND bdcd.Country IN ('Thailand','Morocco','Egypt','Ukraine' ,'Vietnam','Brazil','Argentina','Philippines','United Arab Emirates')
	  )AS ftd
	  LEFT JOIN 
	  (
			SELECT EOMONTH(bddcr.FullDate) 'EOM',
			bddcr.RealCID,
			bddcr.Country,
			SUM(bddcr.FullCommissions+bddcr.RollOverFee) AS 'Revenue' 
			
           FROM BI_DB.dbo.BI_DB_DailyCommisionReport bddcr
		   WHERE bddcr.DateID>=CAST(CONVERT(VARCHAR(8), DATEADD(mm,-3,getdate()-1), 112) AS INT)
                 AND bddcr.Country IN ('Thailand','Morocco','Egypt','Ukraine' ,'Vietnam','Brazil','Argentina','Philippines','United Arab Emirates')               
		GROUP BY EOMONTH(bddcr.FullDate),
			bddcr.RealCID,
			bddcr.Country
		)rev_total
		ON ftd.EOM=rev_total.EOM AND rev_total.RealCID=ftd.CID AND ftd.Country=rev_total.Country
		GROUP BY ftd.EOM,
	  ftd.FirstDepositFundingType,
	  ftd.Country
```

### 15. Custom SQL Query

- Tableau id: `e919b626-3c98-4cbd-097e-1cbfe5468aab`

```sql
SELECT
	md.RealCID,
	md.Regulation,
	md.Country,
	md.FullDate,
	md.DateID,
	-- All
	SUM(md.FullCommissions) AS FullCommissions_All,
	SUM(md.Commissions) AS Commissions_All,
	SUM(md.RollOverFee) AS RollOverFee_All,
	-- CFD
	SUM(CASE WHEN md.IsSettled = 0 THEN md.FullCommissions ELSE 0 END) AS FullCommissions_CFD,
	SUM(CASE WHEN md.IsSettled = 0 THEN md.Commissions ELSE 0 END) AS Commissions_CFD,
	SUM(CASE WHEN md.IsSettled = 0 THEN md.RollOverFee ELSE 0 END) AS RollOverFee_CFD,	
	-- CFD crypto
	SUM(CASE WHEN md.IsSettled = 0 AND md.InstrumentType = 'Crypto Currencies' THEN md.FullCommissions ELSE 0 END) AS FullCommissions_CFD_crypto,
	SUM(CASE WHEN md.IsSettled = 0 AND md.InstrumentType = 'Crypto Currencies' THEN md.Commissions ELSE 0 END) AS Commissions_CFD_crypto,
	SUM(CASE WHEN md.IsSettled = 0 AND md.InstrumentType = 'Crypto Currencies' THEN md.RollOverFee ELSE 0 END) AS RollOverFee_CFD_crypto
FROM 
(
SELECT *
FROM BI_DB_dbo.BI_DB_DailyCommisionReport bddcr
WHERE 1=1
	AND bddcr.RegulationID = 13 -- MAS regulation
	AND bddcr.FullDate >= CAST('2025-07-01' AS DATE) -- 1 July 2025 onwards
) md
GROUP BY
	md.RealCID,
	md.Regulation,
	md.Country,
	md.FullDate,
	md.DateID
--ORDER BY md.RealCID, md.FullDate
```

### 16. Custom SQL Query

- Tableau id: `f8c8a056-8ab7-3c17-c683-dc04ddd0a06c`

```sql
SELECT bddcr.FullDate
,di.InstrumentID
,di.Name Instrument
,di.[InstrumentTypeID]
,di.[InstrumentType]
,di.InstrumentDisplayName
,di.ReceivedOnPriceServer
,bddcr.FullCommissions
,bddcr.RollOverFee
FROM BI_DB_dbo.BI_DB_DailyCommisionReport bddcr
INNER JOIN DWH_dbo.Dim_Instrument di 
ON bddcr.InstrumentID = di.InstrumentID
WHERE bddcr.DateID >=20220101
and bddcr.InstrumentTypeID in (4,2,1)
GROUP BY 
bddcr.FullDate
,di.InstrumentID
,di.Name 
,di.[InstrumentTypeID]
,di.[InstrumentType]
,di.InstrumentDisplayName
,di.ReceivedOnPriceServer
,bddcr.FullCommissions
,bddcr.RollOverFee
```

### 17. Custom SQL Query

- Tableau id: `f9d0dff6-fd30-4111-84b8-386fd9ba47e4`

```sql
SELECT dd.FullDate
      ,a.CID
		,a.Islamic
		,b.FullCommissions
		,a.InstrumentID
		,a.InstrumentType
FROM [DWH_dbo].[Dim_Date]  dd
LEFT JOIN (SELECT Date, CID, InstrumentID, InstrumentType, SUM(Islamic) Islamic FROM [Dealing_dbo].[Dealing_Rollover_Assurance] GROUP BY Date,CID, InstrumentID, InstrumentType)a
ON dd.FullDate=a.Date
LEFT JOIN (SELECT FullDate,RealCID, InstrumentID, InstrumentType, SUM(FullCommissions) FullCommissions FROM [BI_DB_dbo].[BI_DB_DailyCommisionReport] GROUP BY FullDate,RealCID, InstrumentID, InstrumentType) b
ON dd.FullDate=b.FullDate AND a.CID=b.RealCID AND a.InstrumentID = b.InstrumentID
WHERE YEAR(dd.FullDate) >= 2022
AND dd.FullDate < CAST(GETDATE() AS DATE)
```

### 18. Custom SQL Query

- Tableau id: `fc319955-88b7-2150-3fcf-943dc0de026b`

```sql
--- yesterday

SELECT 
	'Yesterday' AS [Period],
    RealCID,
	UserName,
	Region,
	Manager,
	Regulation,
	Mifid,
    Metric,
    Amount
FROM 
(
    SELECT 
        RealCID,
		Filtered.UserName,
		Filtered.Region,
		Filtered.Manager,
		Filtered.Regulation,
		Filtered.Mifid,
        SUM(Commissions) AS Commissions,
        SUM(FullCommissions) AS FullCommissions,
        SUM(RollOverFee) AS RollOverFee,
        SUM(TicketFee) AS TicketFee,
        SUM(TicketFeeByPercent) AS TicketFeeByPercent,
        SUM(AdminFee) AS AdminFee,
        SUM(SpotAdjustFee) AS SpotAdjustFee,
        SUM(VolumeOnOpen) AS VolumeOnOpen,
        SUM(VolumeOnClose) AS VolumeOnClose,
		SUM(ISNULL(FullCommissions,0)) + SUM(ISNULL(RollOverFee,0)) + SUM(ISNULL(TicketFee,0)) + SUM(ISNULL(TicketFeeByPercent,0)) + SUM(ISNULL(AdminFee,0)) + SUM(ISNULL(SpotAdjustFee,0)) AS TotalTradingRevenue
    FROM 
    (
        SELECT *
        FROM BI_DB_dbo.BI_DB_DailyCommisionReport
        WHERE 
            IsValidCustomer = 1
            AND DateID = CAST(FORMAT(CAST(getdate()-1 AS DATE),'yyyyMMdd') as INT)
            AND RealCID IN (
                SELECT TOP 20 RealCID
                FROM BI_DB_dbo.BI_DB_DailyCommisionReport
                WHERE DateID = CAST(FORMAT(CAST(getdate()-1 AS DATE),'yyyyMMdd') as INT) 
                GROUP BY RealCID
                ORDER BY SUM(ISNULL(FullCommissions,0) + ISNULL(RollOverFee,0) + ISNULL(TicketFee,0) + ISNULL(TicketFeeByPercent,0) + ISNULL(AdminFee,0) + ISNULL(SpotAdjustFee,0)) DESC
            )
    ) AS Filtered
    GROUP BY RealCID,		
				UserName,
				Region,
				Manager,
				Regulation,
				Mifid
) AS Aggregated
UNPIVOT
(
    Amount FOR Metric IN (
        Commissions,
        FullCommissions,
        RollOverFee,
        TicketFee,
        TicketFeeByPercent,
        AdminFee,
        SpotAdjustFee,
		VolumeOnOpen,
		VolumeOnClose,
		TotalTradingRevenue
    )
) AS unpvt

--- This Month

UNION all 

-- DECLARE getdate()-1 DATE = getdate()-1

SELECT 
	'CurrentMonth' AS [Period],
    RealCID,
	UserName,
	Region,
	Manager,
	Regulation,
	Mifid,
    Metric,
    Amount
FROM 
(
    SELECT 
        RealCID,
		Filtered.UserName,
		Filtered.Region,
		Filtered.Manager,
		Filtered.Regulation,
		Filtered.Mifid,
        SUM(Commissions) AS Commissions,
        SUM(FullCommissions) AS FullCommissions,
        SUM(RollOverFee) AS RollOverFee,
        SUM(TicketFee) AS TicketFee,
        SUM(TicketFeeByPercent) AS TicketFeeByPercent,
        SUM(AdminFee) AS AdminFee,
        SUM(SpotAdjustFee) AS SpotAdjustFee,
        SUM(VolumeOnOpen) AS VolumeOnOpen,
        SUM(VolumeOnClose) AS VolumeOnClose,
		SUM(ISNULL(FullCommissions,0)) + SUM(ISNULL(RollOverFee,0)) + SUM(ISNULL(TicketFee,0)) + SUM(ISNULL(TicketFeeByPercent,0)) + SUM(ISNULL(AdminFee,0)) + SUM(ISNULL(SpotAdjustFee,0))  AS TotalTradingRevenue
    FROM 
    (
        SELECT *
        FROM BI_DB_dbo.BI_DB_DailyCommisionReport
        WHERE 
            IsValidCustomer = 1
            AND DateID BETWEEN CAST(FORMAT(CAST(DATEADD(month, DATEDIFF(month, 0, getdate()-1), 0) AS DATE),'yyyyMMdd') as INT) AND CAST(FORMAT(CAST(getdate()-1 AS DATE),'yyyyMMdd') as INT) 
            AND RealCID IN (
                SELECT TOP 20 RealCID
                FROM BI_DB_dbo.BI_DB_DailyCommisionReport
                WHERE DateID BETWEEN CAST(FORMAT(CAST(DATEADD(month, DATEDIFF(month, 0, getdate()-1), 0) AS DATE),'yyyyMMdd') as INT) AND CAST(FORMAT(CAST(getdate()-1 AS DATE),'yyyyMMdd') as INT) 
                GROUP BY RealCID
                ORDER BY SUM(ISNULL(FullCommissions,0) + ISNULL(RollOverFee,0) + ISNULL(TicketFee,0) + ISNULL(TicketFeeByPercent,0) + ISNULL(AdminFee,0) + ISNULL(SpotAdjustFee,0)) DESC
            )
    ) AS Filtered
    GROUP BY RealCID,		
				UserName,
				Region,
				Manager,
				Regulation,
				Mifid
) AS Aggregated
UNPIVOT
(
    Amount FOR Metric IN (
        Commissions,
        FullCommissions,
        RollOverFee,
        TicketFee,
        TicketFeeByPercent,
        AdminFee,
        SpotAdjustFee,
		VolumeOnOpen,
		VolumeOnClose,
		TotalTradingRevenue
    )
) AS unpvt

--- Last Month

UNION all 


SELECT 
	'PreviousMonthSameTime' AS [Period],
    RealCID,
	UserName,
	Region,
	Manager,
	Regulation,
	Mifid,
    Metric,
    Amount
FROM 
(
    SELECT 
        RealCID,
		Filtered.UserName,
		Filtered.Region,
		Filtered.Manager,
		Filtered.Regulation,
		Filtered.Mifid,
        SUM(Commissions) AS Commissions,
        SUM(FullCommissions) AS FullCommissions,
        SUM(RollOverFee) AS RollOverFee,
        SUM(TicketFee) AS TicketFee,
        SUM(TicketFeeByPercent) AS TicketFeeByPercent,
        SUM(AdminFee) AS AdminFee,
        SUM(SpotAdjustFee) AS SpotAdjustFee,
        SUM(VolumeOnOpen) AS VolumeOnOpen,
        SUM(VolumeOnClose) AS VolumeOnClose,
		SUM(ISNULL(FullCommissions,0)) + SUM(ISNULL(RollOverFee,0)) + SUM(ISNULL(TicketFee,0)) + SUM(ISNULL(TicketFeeByPercent,0)) + SUM(ISNULL(AdminFee,0)) + SUM(ISNULL(SpotAdjustFee,0))  AS TotalTradingRevenue
    FROM 
    (
        SELECT *
        FROM BI_DB_dbo.BI_DB_DailyCommisionReport
        WHERE 
            IsValidCustomer = 1
            AND DateID BETWEEN CAST(FORMAT(CAST( DATEADD(month, DATEDIFF(month, 0, getdate()-1) - 1, 0) AS DATE),'yyyyMMdd') as INT) AND CAST(FORMAT(CAST(dateadd(MONTH,-1,getdate()-1)  AS DATE),'yyyyMMdd') as INT) 
            AND RealCID IN (
                SELECT TOP 20 RealCID
                FROM BI_DB_dbo.BI_DB_DailyCommisionReport
                WHERE DateID BETWEEN CAST(FORMAT(CAST( DATEADD(month, DATEDIFF(month, 0, getdate()-1) - 1, 0) AS DATE),'yyyyMMdd') as INT) AND CAST(FORMAT(CAST(dateadd(MONTH,-1,getdate()-1)  AS DATE),'yyyyMMdd') as INT)
                GROUP BY RealCID
                ORDER BY SUM(ISNULL(FullCommissions,0) + ISNULL(RollOverFee,0) + ISNULL(TicketFee,0) + ISNULL(TicketFeeByPercent,0) + ISNULL(AdminFee,0) + ISNULL(SpotAdjustFee,0)) DESC
            )
    ) AS Filtered
    GROUP BY RealCID,		
				UserName,
				Region,
				Manager,
				Regulation,
				Mifid
) AS Aggregated
UNPIVOT
(
    Amount FOR Metric IN (
        Commissions,
        FullCommissions,
        RollOverFee,
        TicketFee,
        TicketFeeByPercent,
        AdminFee,
        SpotAdjustFee,
		VolumeOnOpen,
		VolumeOnClose,
		TotalTradingRevenue
    )
) AS unpvt

--- same day 8 weeks 


UNION all 

--DECLARE getdate()-1 DATE = getdate()-1

SELECT 
	'CurrentYear' AS [Period],
    RealCID,
	UserName,
	Region,
	Manager,
	Regulation,
	Mifid,
    Metric,
    Amount
FROM 
(
    SELECT 
        RealCID,
		Filtered.UserName,
		Filtered.Region,
		Filtered.Manager,
		Filtered.Regulation,
		Filtered.Mifid,
        sum(Commissions) AS Commissions,
        sum(FullCommissions) AS FullCommissions,
        sum(RollOverFee) AS RollOverFee,
        sum(TicketFee) AS TicketFee,
        sum(TicketFeeByPercent) AS TicketFeeByPercent,
        sum(AdminFee) AS AdminFee,
        SUM(SpotAdjustFee) AS SpotAdjustFee,
        SUM(VolumeOnOpen) AS VolumeOnOpen,
        SUM(VolumeOnClose) AS VolumeOnClose,
		SUM(ISNULL(FullCommissions,0)) + SUM(ISNULL(RollOverFee,0)) + SUM(ISNULL(TicketFee,0)) + SUM(ISNULL(TicketFeeByPercent,0)) + SUM(ISNULL(AdminFee,0)) + SUM(ISNULL(SpotAdjustFee,0))  AS TotalTradingRevenue
    FROM 
    (
        SELECT ia.*
        FROM BI_DB_dbo.BI_DB_DailyCommisionReport ia -- select @@DATEFIRST
        WHERE 
            IsValidCustomer = 1
            AND DateID BETWEEN CAST(FORMAT(CAST(DATEADD(yy, DATEDIFF(yy, 0, getdate()-1), 0) AS DATE),'yyyyMMdd') as INT)  AND CAST(FORMAT(CAST(getdate()-1 AS DATE),'yyyyMMdd') as INT) 
			AND RealCID IN (
                SELECT TOP 20 RealCID
                FROM BI_DB_dbo.BI_DB_DailyCommisionReport ia
				WHERE ia.DateID between CAST(FORMAT(CAST(DATEADD(yy, DATEDIFF(yy, 0, getdate()-1), 0) AS DATE),'yyyyMMdd') as INT)  AND CAST(FORMAT(CAST(getdate()-1 AS DATE),'yyyyMMdd') as INT) 
                GROUP BY RealCID
                ORDER BY SUM(ISNULL(FullCommissions,0) + ISNULL(RollOverFee,0) + ISNULL(TicketFee,0) + ISNULL(TicketFeeByPercent,0) + ISNULL(AdminFee,0) + ISNULL(SpotAdjustFee,0)) DESC
            )
    ) AS Filtered
    GROUP BY RealCID,		
				UserName,
				Region,
				Manager,
				Regulation,
				Mifid
) AS Aggregated
UNPIVOT
(
    Amount FOR Metric IN (
        Commissions,
        FullCommissions,
        RollOverFee,
        TicketFee,
        TicketFeeByPercent,
        AdminFee,
        SpotAdjustFee,
		VolumeOnOpen,
		VolumeOnClose,
		TotalTradingRevenue
    )
) AS unpvt

UNION all 

SELECT 
	'PreviousYearSameTime' AS [Period],
    RealCID,
	UserName,
	Region,
	Manager,
	Regulation,
	Mifid,
    Metric,
    Amount
FROM 
(
    SELECT 
        RealCID,
		Filtered.UserName,
		Filtered.Region,
		Filtered.Manager,
		Filtered.Regulation,
		Filtered.Mifid,
        sum(Commissions) AS Commissions,
        sum(FullCommissions) AS FullCommissions,
        sum(RollOverFee) AS RollOverFee,
        sum(TicketFee) AS TicketFee,
        sum(TicketFeeByPercent) AS TicketFeeByPercent,
        sum(AdminFee) AS AdminFee,
        SUM(SpotAdjustFee) AS SpotAdjustFee,
        SUM(VolumeOnOpen) AS VolumeOnOpen,
        SUM(VolumeOnClose) AS VolumeOnClose,
		SUM(ISNULL(FullCommissions,0)) + SUM(ISNULL(RollOverFee,0)) + SUM(ISNULL(TicketFee,0)) + SUM(ISNULL(TicketFeeByPercent,0)) + SUM(ISNULL(AdminFee,0)) + SUM(ISNULL(SpotAdjustFee,0))  AS TotalTradingRevenue
    FROM 
    (
        SELECT ia.*
        FROM BI_DB_dbo.BI_DB_DailyCommisionReport ia -- select @@DATEFIRST
        WHERE 
            IsValidCustomer = 1
            AND DateID BETWEEN CAST(FORMAT(CAST(DATEADD(yy, DATEDIFF(yy, 0, GETDATE()) - 1, 0) AS DATE),'yyyyMMdd') as INT) AND CAST(FORMAT(CAST(dateadd(YEAR,-1,getdate()-1) AS DATE),'yyyyMMdd') as INT) 
			AND RealCID IN (
                SELECT TOP 20 RealCID
                FROM BI_DB_dbo.BI_DB_DailyCommisionReport ia
				WHERE ia.DateID between CAST(FORMAT(CAST(DATEADD(yy, DATEDIFF(yy, 0, GETDATE()) - 1, 0) AS DATE),'yyyyMMdd') as INT) AND CAST(FORMAT(CAST(dateadd(YEAR,-1,getdate()-1) AS DATE),'yyyyMMdd') as INT) 
                GROUP BY RealCID
                ORDER BY SUM(ISNULL(FullCommissions,0) + ISNULL(RollOverFee,0) + ISNULL(TicketFee,0) + ISNULL(TicketFeeByPercent,0) + ISNULL(AdminFee,0) + ISNULL(SpotAdjustFee,0)) DESC
            )
    ) AS Filtered
    GROUP BY RealCID,		
				UserName,
				Region,
				Manager,
				Regulation,
				Mifid
) AS Aggregated
UNPIVOT
(
    Amount FOR Metric IN (
        Commissions,
        FullCommissions,
        RollOverFee,
        TicketFee,
        TicketFeeByPercent,
        AdminFee,
        SpotAdjustFee,
		VolumeOnOpen,
		VolumeOnClose,
		TotalTradingRevenue
    )
) AS unpvt

union all 


SELECT 
	'CurrentWeek' AS [Period],
    RealCID,
	UserName,
	Region,
	Manager,
	Regulation,
	Mifid,
    Metric,
    Amount
FROM 
(
    SELECT 
        RealCID,
		Filtered.UserName,
		Filtered.Region,
		Filtered.Manager,
		Filtered.Regulation,
		Filtered.Mifid,
        SUM(Commissions) AS Commissions,
        SUM(FullCommissions) AS FullCommissions,
        SUM(RollOverFee) AS RollOverFee,
        SUM(TicketFee) AS TicketFee,
        SUM(TicketFeeByPercent) AS TicketFeeByPercent,
        SUM(AdminFee) AS AdminFee,
        SUM(SpotAdjustFee) AS SpotAdjustFee,
        SUM(VolumeOnOpen) AS VolumeOnOpen,
        SUM(VolumeOnClose) AS VolumeOnClose,
		SUM(ISNULL(FullCommissions,0)) + SUM(ISNULL(RollOverFee,0)) + SUM(ISNULL(TicketFee,0)) + SUM(ISNULL(TicketFeeByPercent,0)) + SUM(ISNULL(AdminFee,0)) + SUM(ISNULL(SpotAdjustFee,0))  AS TotalTradingRevenue
    FROM 
    (
        SELECT *
        FROM BI_DB_dbo.BI_DB_DailyCommisionReport
        WHERE 
            IsValidCustomer = 1
            AND DateID BETWEEN CAST(FORMAT(CAST(DATEADD(DAY, 1 - DATEPART(WEEKDAY, getdate()-1), getdate()-1) AS DATE),'yyyyMMdd') as INT)  AND CAST(FORMAT(CAST(getdate()-1 AS DATE),'yyyyMMdd') as INT) 
            AND RealCID IN (
                SELECT TOP 20 RealCID
                FROM BI_DB_dbo.BI_DB_DailyCommisionReport
                WHERE DateID BETWEEN CAST(FORMAT(CAST(DATEADD(DAY, 1 - DATEPART(WEEKDAY, getdate()-1), getdate()-1) AS DATE),'yyyyMMdd') as INT)  AND CAST(FORMAT(CAST(getdate()-1 AS DATE),'yyyyMMdd') as INT) 
                GROUP BY RealCID
                ORDER BY SUM(ISNULL(FullCommissions,0) + ISNULL(RollOverFee,0) + ISNULL(TicketFee,0) + ISNULL(TicketFeeByPercent,0) + ISNULL(AdminFee,0) + ISNULL(SpotAdjustFee,0)) DESC
            )
    ) AS Filtered
    GROUP BY RealCID,		
				UserName,
				Region,
				Manager,
				Regulation,
				Mifid
) AS Aggregated
UNPIVOT
(
    Amount FOR Metric IN (
        Commissions,
        FullCommissions,
        RollOverFee,
        TicketFee,
        TicketFeeByPercent,
        AdminFee,
        SpotAdjustFee,
		VolumeOnOpen,
		VolumeOnClose,
		TotalTradingRevenue
    )
) AS unpvt

union all 



-- DECLARE getdate()-1 DATE = getdate()-1

SELECT 
	'PreviousWeekSameTime' AS [Period],
    RealCID,
	UserName,
	Region,
	Manager,
	Regulation,
	Mifid,
    Metric,
    Amount
FROM 
(
    SELECT 
        RealCID,
		Filtered.UserName,
		Filtered.Region,
		Filtered.Manager,
		Filtered.Regulation,
		Filtered.Mifid,
        SUM(Commissions) AS Commissions,
        SUM(FullCommissions) AS FullCommissions,
        SUM(RollOverFee) AS RollOverFee,
        SUM(TicketFee) AS TicketFee,
        SUM(TicketFeeByPercent) AS TicketFeeByPercent,
        SUM(AdminFee) AS AdminFee,
        SUM(SpotAdjustFee) AS SpotAdjustFee,
        SUM(VolumeOnOpen) AS VolumeOnOpen,
        SUM(VolumeOnClose) AS VolumeOnClose,
		SUM(ISNULL(FullCommissions,0)) + SUM(ISNULL(RollOverFee,0)) + SUM(ISNULL(TicketFee,0)) + SUM(ISNULL(TicketFeeByPercent,0)) + SUM(ISNULL(AdminFee,0)) + SUM(ISNULL(SpotAdjustFee,0))  AS TotalTradingRevenue
    FROM 
    (
        SELECT *
        FROM BI_DB_dbo.BI_DB_DailyCommisionReport
        WHERE 
            IsValidCustomer = 1
            AND DateID BETWEEN CAST(FORMAT(CAST(dateadd (WEEK, -1, DATEADD(DAY, 1 - DATEPART(WEEKDAY, getdate()-1), getdate()-1)) AS DATE),'yyyyMMdd') as INT)  AND CAST(FORMAT(CAST(dateadd(WEEK, -1,getdate()-1) AS DATE),'yyyyMMdd') as INT) 
            AND RealCID IN (
                SELECT TOP 20 RealCID
                FROM BI_DB_dbo.BI_DB_DailyCommisionReport
                WHERE DateID BETWEEN  CAST(FORMAT(CAST(dateadd (WEEK, -1, DATEADD(DAY, 1 - DATEPART(WEEKDAY, getdate()-1), getdate()-1)) AS DATE),'yyyyMMdd') as INT)  AND CAST(FORMAT(CAST(dateadd(WEEK, -1,getdate()-1) AS DATE),'yyyyMMdd') as INT)
                GROUP BY RealCID
                ORDER BY SUM(ISNULL(FullCommissions,0) + ISNULL(RollOverFee,0) + ISNULL(TicketFee,0) + ISNULL(TicketFeeByPercent,0) + ISNULL(AdminFee,0) + ISNULL(SpotAdjustFee,0)) DESC
            )
    ) AS Filtered
    GROUP BY RealCID,		
				UserName,
				Region,
				Manager,
				Regulation,
				Mifid
) AS Aggregated
UNPIVOT
(
    Amount FOR Metric IN (
        Commissions,
        FullCommissions,
        RollOverFee,
        TicketFee,
        TicketFeeByPercent,
        AdminFee,
        SpotAdjustFee,
		VolumeOnOpen,
		VolumeOnClose,
		TotalTradingRevenue
    )
) AS unpvt

UNION ALL 



-- DECLARE getdate()-1 DATE = getdate()-1

SELECT 
	'CurrentQuarter' AS [Period],
    RealCID,
	UserName,
	Region,
	Manager,
	Regulation,
	Mifid,
    Metric,
    Amount
FROM 
(
    SELECT 
        RealCID,
		Filtered.UserName,
		Filtered.Region,
		Filtered.Manager,
		Filtered.Regulation,
		Filtered.Mifid,
        SUM(Commissions) AS Commissions,
        SUM(FullCommissions) AS FullCommissions,
        SUM(RollOverFee) AS RollOverFee,
        SUM(TicketFee) AS TicketFee,
        SUM(TicketFeeByPercent) AS TicketFeeByPercent,
        SUM(AdminFee) AS AdminFee,
        SUM(SpotAdjustFee) AS SpotAdjustFee,
        SUM(VolumeOnOpen) AS VolumeOnOpen,
        SUM(VolumeOnClose) AS VolumeOnClose,
		SUM(ISNULL(FullCommissions,0)) + SUM(ISNULL(RollOverFee,0)) + SUM(ISNULL(TicketFee,0)) + SUM(ISNULL(TicketFeeByPercent,0)) + SUM(ISNULL(AdminFee,0)) + SUM(ISNULL(SpotAdjustFee,0))  AS TotalTradingRevenue
    FROM 
    (
        SELECT *
        FROM BI_DB_dbo.BI_DB_DailyCommisionReport
        WHERE 
            IsValidCustomer = 1
            AND DateID BETWEEN CAST(FORMAT(CAST( DATEADD(qq, DATEDIFF(qq, 0, getdate()-1), 0) AS DATE),'yyyyMMdd') as INT)  AND CAST(FORMAT(CAST(getdate()-1 AS DATE),'yyyyMMdd') as INT) 
            AND RealCID IN (
                SELECT TOP 20 RealCID
                FROM BI_DB_dbo.BI_DB_DailyCommisionReport
                WHERE DateID BETWEEN  CAST(FORMAT(CAST( DATEADD(qq, DATEDIFF(qq, 0, getdate()-1), 0) AS DATE),'yyyyMMdd') as INT)  AND CAST(FORMAT(CAST(getdate()-1 AS DATE),'yyyyMMdd') as INT) 
                GROUP BY RealCID
                ORDER BY SUM(ISNULL(FullCommissions,0) + ISNULL(RollOverFee,0) + ISNULL(TicketFee,0) + ISNULL(TicketFeeByPercent,0) + ISNULL(AdminFee,0) + ISNULL(SpotAdjustFee,0)) DESC
            )
    ) AS Filtered
    GROUP BY RealCID,		
				UserName,
				Region,
				Manager,
				Regulation,
				Mifid
) AS Aggregated
UNPIVOT
(
    Amount FOR Metric IN (
        Commissions,
        FullCommissions,
        RollOverFee,
        TicketFee,
        TicketFeeByPercent,
        AdminFee,
        SpotAdjustFee,
		VolumeOnOpen,
		VolumeOnClose,
		TotalTradingRevenue
    )
) AS unpvt

--- Last Month
```

## Downstream workbooks

_Attribution: 1 via direct table-drag, 16 via custom SQL (workbooks can use both paths)._

| # | Workbook | Project | Owner | Via | Last updated | URL |
|---|---|---|---|---|---|---|
| 1 | Asia commision Report   | 14-02-2024 | Maor Hacun | custom_sql | 2024-02-14T08:18:29Z | [open](https://reports.etorocorp.com/#/workbooks/AsiacommisionReport) |
| 2 | Money IN: overview  by MOP/Country for Doron | 17.7.24 form sandbox | Adi Meidan | custom_sql | 2024-07-17T10:40:45Z | [open](https://reports.etorocorp.com/#/workbooks/MoneyINoverviewbyMOPCountryforDoron_16614269104770) |
| 3 | Revenue development YTD | 17.7.24 form sandbox | Bar Arian | custom_sql | 2024-07-17T10:40:07Z | [open](https://reports.etorocorp.com/#/workbooks/RevenuedevelopmentYTD) |
| 4 | USA Paypal Monitoring | 17.7.24 form sandbox | Adi Meidan | custom_sql | 2024-07-17T10:40:53Z | [open](https://reports.etorocorp.com/#/workbooks/USAPaypalMonitoring) |
| 5 | Archived: US Crypto Revenue by State | 5-US Finance | Paloma Cui | custom_sql | 2025-11-24T08:03:44Z | [open](https://reports.etorocorp.com/#/workbooks/USBusinessStrategyKPIMonitoring) |
| 6 | Daily Commission New 2025 | Analysts useful reports | Adi Meidan | custom_sql | 2026-03-08T08:23:31Z | [open](https://reports.etorocorp.com/#/workbooks/GuySandboxDailyCommissionoptimized) |
| 7 | Daily Commission New 2025 | Commission Reports | Guy Manova | custom_sql | 2026-05-31T04:40:02Z | [open](https://reports.etorocorp.com/#/workbooks/DailyCommissionNew2025_17613146138560) |
| 8 | Daily Commission New 2025 - ICC | Commission Reports | Guy Manova | custom_sql | 2026-05-31T10:55:04Z | [open](https://reports.etorocorp.com/#/workbooks/DailyCommissionNew2025-ICC) |
| 9 | Investment Office: Monthly Summary | Community | Eti Rozolio | custom_sql | 2025-08-22T11:06:13Z | [open](https://reports.etorocorp.com/#/workbooks/InvestmentOfficeMonthlySummary) |
| 10 | Revenue Forum  | Customer Facing | Tom Boksenbojm | direct | 2024-04-24T09:45:12Z | [open](https://reports.etorocorp.com/#/workbooks/RevenueForum) |
| 11 | Crypto Leads | eTM Marketing & CF Reporting | Jan Iablunovskey | custom_sql | 2024-01-04T16:09:55Z | [open](https://reports.etorocorp.com/#/workbooks/CryptoLeads) |
| 12 | Crypto 1 M leads list | eTM Marketing & CF Reporting Archive | Jan Iablunovskey | custom_sql | 2026-02-04T07:46:33Z | [open](https://reports.etorocorp.com/#/workbooks/Crypto1Mleadslist) |
| 13 | Daily Commission New 2025 | Marketing - Sandbox | Adi Meidan | custom_sql | 2025-10-19T12:36:03Z | [open](https://reports.etorocorp.com/#/workbooks/DailyCommissionNew2025) |
| 14 | Daily Commission New 2025 19.10.25 | Marketing - Sandbox | Guy Manova | custom_sql | 2025-11-03T08:43:04Z | [open](https://reports.etorocorp.com/#/workbooks/DailyCommissionNew202519_10_25) |
| 15 | BISG-00139 CFD Revenue breakdown_v2 | SG Office | Kerrin Kow | custom_sql | 2026-05-12T10:11:06Z | [open](https://reports.etorocorp.com/#/workbooks/BISG-00139CFDRevenuebreakdown_v2) |
| 16 | Revenue Assurance | Trading | Gili Goldbaum | custom_sql | 2023-11-28T12:30:30Z | [open](https://reports.etorocorp.com/#/workbooks/RevenueAssurance) |
| 17 | UK Finance General Dashboard | UK-Controlled - Pre Prod | Adi Meidan | custom_sql | 2025-12-22T09:07:16Z | [open](https://reports.etorocorp.com/#/workbooks/UKFinanceGeneralDashboard) |

### View URLs (per workbook)

- **Asia commision Report  ** (14-02-2024)
  - [Com by Asset Class OverTime](https://reports.etorocorp.com/#/views/AsiacommisionReport/CombyAssetClassOverTime)
- **Money IN: overview  by MOP/Country for Doron** (17.7.24 form sandbox)
  - [Top 5 Countries](https://reports.etorocorp.com/#/views/MoneyINoverviewbyMOPCountryforDoron_16614269104770/Top5Countries)
  - [Country overview](https://reports.etorocorp.com/#/views/MoneyINoverviewbyMOPCountryforDoron_16614269104770/Countryoverview)
  - [Mop overview](https://reports.etorocorp.com/#/views/MoneyINoverviewbyMOPCountryforDoron_16614269104770/Mopoverview)
  - [Payoneer - Money IN](https://reports.etorocorp.com/#/views/MoneyINoverviewbyMOPCountryforDoron_16614269104770/Payoneer-MoneyIN)
  - [Payoneer - Money Out](https://reports.etorocorp.com/#/views/MoneyINoverviewbyMOPCountryforDoron_16614269104770/Payoneer-MoneyOut)
- **Revenue development YTD** (17.7.24 form sandbox)
  - [Copy Revenue Development YTD ](https://reports.etorocorp.com/#/views/RevenuedevelopmentYTD/CopyRevenueDevelopmentYTD)
  - [New CFD Revenue Development YTD](https://reports.etorocorp.com/#/views/RevenuedevelopmentYTD/NewCFDRevenueDevelopmentYTD)
- **USA Paypal Monitoring** (17.7.24 form sandbox)
  - [USA PayPal Monitoring ](https://reports.etorocorp.com/#/views/USAPaypalMonitoring/USAPayPalMonitoring)
- **Archived: US Crypto Revenue by State** (5-US Finance)
  - [Archived: US Crypto Revenue by State](https://reports.etorocorp.com/#/views/USBusinessStrategyKPIMonitoring/USCryptoRevenuebyState)
- **Daily Commission New 2025** (Analysts useful reports)
  - [Instruments and AssetClass](https://reports.etorocorp.com/#/views/GuySandboxDailyCommissionoptimized/InstrumentsandAssetClass)
  - [Volume, Rollovers and NOP](https://reports.etorocorp.com/#/views/GuySandboxDailyCommissionoptimized/VolumeRolloversandNOP)
  - [Clients](https://reports.etorocorp.com/#/views/GuySandboxDailyCommissionoptimized/Clients)
- **Daily Commission New 2025** (Commission Reports)
  - [Instruments and AssetClass new](https://reports.etorocorp.com/#/views/DailyCommissionNew2025_17613146138560/InstrumentsandAssetClassnew)
  - [Volume, Rollovers and NOP](https://reports.etorocorp.com/#/views/DailyCommissionNew2025_17613146138560/VolumeRolloversandNOP)
  - [Clients](https://reports.etorocorp.com/#/views/DailyCommissionNew2025_17613146138560/Clients)
  - [Top Clients](https://reports.etorocorp.com/#/views/DailyCommissionNew2025_17613146138560/TopClients)
- **Daily Commission New 2025 - ICC** (Commission Reports)
  - [Instruments and AssetClass new](https://reports.etorocorp.com/#/views/DailyCommissionNew2025-ICC/InstrumentsandAssetClassnew)
  - [Volume, Rollovers and NOP](https://reports.etorocorp.com/#/views/DailyCommissionNew2025-ICC/VolumeRolloversandNOP)
  - [Clients](https://reports.etorocorp.com/#/views/DailyCommissionNew2025-ICC/Clients)
  - [Top Clients](https://reports.etorocorp.com/#/views/DailyCommissionNew2025-ICC/TopClients)
- **Investment Office: Monthly Summary** (Community)
  - [First Action](https://reports.etorocorp.com/#/views/InvestmentOfficeMonthlySummary/FirstAction)
  - [MCAU Development](https://reports.etorocorp.com/#/views/InvestmentOfficeMonthlySummary/MCAUDevelopment)
  - [Volumes Development YTD](https://reports.etorocorp.com/#/views/InvestmentOfficeMonthlySummary/VolumesDevelopmentYTD)
  - [Avg Holding Duration](https://reports.etorocorp.com/#/views/InvestmentOfficeMonthlySummary/AvgHoldingDuration)
  - [Copy Revenue Development YTD ](https://reports.etorocorp.com/#/views/InvestmentOfficeMonthlySummary/CopyRevenueDevelopmentYTD)
  - [New CFD Revenue Development YTD](https://reports.etorocorp.com/#/views/InvestmentOfficeMonthlySummary/NewCFDRevenueDevelopmentYTD)
  - [Instrument List](https://reports.etorocorp.com/#/views/InvestmentOfficeMonthlySummary/InstrumentList)
  - [NMI by Club ](https://reports.etorocorp.com/#/views/InvestmentOfficeMonthlySummary/NMIbyClub)
- **Revenue Forum ** (Customer Facing)
  - [Revenue Forum Dashboard](https://reports.etorocorp.com/#/views/RevenueForum/RevenueForumDashboard)
- **Crypto Leads** (eTM Marketing & CF Reporting)
  - [Crypto Leads](https://reports.etorocorp.com/#/views/CryptoLeads/CryptoLeads)
- **Crypto 1 M leads list** (eTM Marketing & CF Reporting Archive)
  - [1M Crypto Detailed List](https://reports.etorocorp.com/#/views/Crypto1Mleadslist/1MCryptoDetailedList)
- **Daily Commission New 2025** (Marketing - Sandbox)
  - [Instruments and AssetClass](https://reports.etorocorp.com/#/views/DailyCommissionNew2025/InstrumentsandAssetClass)
  - [Instruments and AssetClass new](https://reports.etorocorp.com/#/views/DailyCommissionNew2025/InstrumentsandAssetClassnew)
  - [Volume, Rollovers and NOP](https://reports.etorocorp.com/#/views/DailyCommissionNew2025/VolumeRolloversandNOP)
  - [Clients](https://reports.etorocorp.com/#/views/DailyCommissionNew2025/Clients)
- **Daily Commission New 2025 19.10.25** (Marketing - Sandbox)
  - [Instruments and AssetClass new](https://reports.etorocorp.com/#/views/DailyCommissionNew202519_10_25/InstrumentsandAssetClassnew)
  - [Volume, Rollovers and NOP](https://reports.etorocorp.com/#/views/DailyCommissionNew202519_10_25/VolumeRolloversandNOP)
  - [Clients](https://reports.etorocorp.com/#/views/DailyCommissionNew202519_10_25/Clients)
- **BISG-00139 CFD Revenue breakdown_v2** (SG Office)
  - [MAS Daily commissions](https://reports.etorocorp.com/#/views/BISG-00139CFDRevenuebreakdown_v2/MASDailycommissions)
  - [MAS manual and CFD crypto trader count](https://reports.etorocorp.com/#/views/BISG-00139CFDRevenuebreakdown_v2/MASmanualandCFDcryptotradercount)
- **Revenue Assurance** (Trading)
  - [Monthly Comission stats](https://reports.etorocorp.com/#/views/RevenueAssurance/MonthlyComissionstats)
  - [Commission Diff](https://reports.etorocorp.com/#/views/RevenueAssurance/CommissionDiff)
  - [Roll over](https://reports.etorocorp.com/#/views/RevenueAssurance/Rollover)
  - [CIDs Exempted from RollOver Fee](https://reports.etorocorp.com/#/views/RevenueAssurance/CIDsExemptedfromRollOverFee)
- **UK Finance General Dashboard** (UK-Controlled - Pre Prod)
  - [FCA Monthly Data](https://reports.etorocorp.com/#/views/UKFinanceGeneralDashboard/FCADatabyRegion)
  - [FCA Output](https://reports.etorocorp.com/#/views/UKFinanceGeneralDashboard/FCAOutput)
  - [Regulation/Residancy Split](https://reports.etorocorp.com/#/views/UKFinanceGeneralDashboard/RegulationResidancySplit)
  - [Active & Funded Users (Monthly/Yearly)](https://reports.etorocorp.com/#/views/UKFinanceGeneralDashboard/ActiveFundedUsersMonthlyYearly)
  - [Unrealized Equity inc Cash (Equity>0)](https://reports.etorocorp.com/#/views/UKFinanceGeneralDashboard/UnrealizedEquityincCashEquity0)
  - [FCA Annual Data](https://reports.etorocorp.com/#/views/UKFinanceGeneralDashboard/FCAAnnualData)
  - [FCA Annual Data Summary](https://reports.etorocorp.com/#/views/UKFinanceGeneralDashboard/FCAAnnualDataSummary)
  - [Revenue_Total Dashbaord](https://reports.etorocorp.com/#/views/UKFinanceGeneralDashboard/Revenue_TotalDashbaord)
  - [FCA Client Volumes - CFD](https://reports.etorocorp.com/#/views/UKFinanceGeneralDashboard/FCAClientVolumes-CFD)
  - [FCA Client Volumes - Real Stocks & ETF](https://reports.etorocorp.com/#/views/UKFinanceGeneralDashboard/FCAClientVolumes-RealStocksETF)

## Downstream calculated fields (in embedded datasources)

### Workbook: Revenue Forum 

#### Datasource: BI_DB_DailyCommisionReport (BI_DB_dbo.BI_DB_DailyCommisionReport) (sql_dp_prod_we)

- **% FullCommission Region**

  ```
  SUM([Full Commissions])/SUM([Fixed Full Commission])
  ```

- **CurrentMonth**

  ```
  DATETRUNC('month',[Full Date])
  ```

- **CurrentMonthName**

  ```
  STR(DATEPART('year',[CurrentMonth]))+'-'+
  IF LEN(STR(DATEPART('month',[CurrentMonth]))) = 1 
      THEN '0'+STR(DATEPART('month',[CurrentMonth]))
          ELSE STR(DATEPART('month',[CurrentMonth])) END
  ```

- **Fixed Full Commission**

  ```
  { FIXED  [Is Settled],[Asset Class ]:SUM([Full Commissions])}
  ```

- **Header: % Region from eToro**

  ```
  '% Region from eToro'+CHAR(10)+ [Calendar Year Month]
  ```

- **Header: Total Period eToro**

  ```
  'Total eToro'+CHAR(10)+ [Calendar Year Month]
  ```

- **Header: Total Period Region**

  ```
  'Total Region'+CHAR(10)+ [Calendar Year Month]
  ```

- **IsCurrentMonth**

  ```
  [CurrentMonthName] = [Year - Month]
  ```

- **IsPreviousMonth**

  ```
  [PreviousMonthName] = [Year - Month]
  ```

- **PreviousMonth**

  ```
  DATEADD('month',1,DATETRUNC('month',[Full Date]))
  ```

- **PreviousMonthName**

  ```
  STR(DATEPART('year',[PreviousMonth]))+'-'+
  IF LEN(STR(DATEPART('month',[PreviousMonth]))) = 1 
      THEN '0'+STR(DATEPART('month',[PreviousMonth]))
          ELSE STR(DATEPART('month',[PreviousMonth])) END
  ```

#### Datasource: BI_DB_DDR_TimeRange_Aggregated_Country_Level_Pivot

- **% Column Value Region**

  ```
  SUM([ColumnValue])/SUM([Fixed Column Value])
  ```

- **CurrentMonth**

  ```
  DATETRUNC('month',[FullDate])
  ```

- **CurrentMonthName**

  ```
  STR(DATEPART('year',[CurrentMonth]))+'-'+
  IF LEN(STR(DATEPART('month',[CurrentMonth]))) = 1 
      THEN '0'+STR(DATEPART('month',[CurrentMonth]))
          ELSE STR(DATEPART('month',[CurrentMonth])) END
  ```

- **Fixed Column Value**

  ```
  { FIXED [Revenue Category] :SUM([ColumnValue])}
  ```

- **Header: % Region from eToro**

  ```
  '% Region from eToro'+CHAR(10)+ [CalendarYearMonth]
  ```

- **Header: Total Period eToro**

  ```
  'Total eToro'+CHAR(10)+ [CalendarYearMonth]
  ```

- **Header: Total Period Region**

  ```
  'Total Region'+CHAR(10)+ [CalendarYearMonth]
  ```

- **IsCurrentMonth**

  ```
  [CurrentMonthName] = [Year - Month]
  ```

- **IsPreviousMonth**

  ```
  [PreviousMonthName] = [Year - Month]
  ```

- **PreviousMonth**

  ```
  DATEADD('month',1,DATETRUNC('month',[FullDate]))
  ```

- **PreviousMonthName**

  ```
  STR(DATEPART('year',[PreviousMonth]))+'-'+
  IF LEN(STR(DATEPART('month',[PreviousMonth]))) = 1 
      THEN '0'+STR(DATEPART('month',[PreviousMonth]))
          ELSE STR(DATEPART('month',[PreviousMonth])) END
  ```
