# Tableau Workbook — eToro's Daily Data Report (New DDR 2025) - LIVE QUERY

> Generated by `tools/tableau/extract_workbook_metadata.py`. Do not edit by hand.

## Identity

- Workbook luid: `b5d58e4d-3644-402e-aaa2-274c0a51221a`
- Project: `DDR's`
- Owner: Guy Manova
- Last updated: 2026-01-11T10:21:02Z
- Custom-SQL retrieval mode: `fallback_scan`

## Summary

- Custom SQL tables: **0**
- Embedded datasources: **2**
- Distinct upstream tables: **0**

## Upstream tables (from workbook.upstreamTables)

_None._

## Custom SQL queries used by this workbook

_No custom SQL tables found for this workbook._
## Embedded datasources (with calculated fields)

### Datasource: Registrations

- Datasource id: `1abb0439-096b-4462-a1f7-b33e0f87bb5b`
- Has extracts: False

#### Calculated fields (1)

- **Registration to FTD Conversion %**

  ```
  SUM([ConvertedRegistrationFTD])/SUM([Registered])
  ```

### Datasource: Main DDR Query

- Datasource id: `58f7e2fa-396a-dcc8-464f-ba7003850d07`
- Has extracts: False

#### Calculated fields (36)

- **% of Full Commission: Commodities**

  ```
  SUM([FullCommissionCommodities])/SUM([FullCommission])
  ```

- **% of Full Commission: Copy**

  ```
  SUM([FullCommissionCopy])/SUM([FullCommission])
  ```

- **% of Full Commission: Crypto**

  ```
  SUM([FullCommissionCrypto])/SUM([FullCommission])
  ```

- **% of Full Commission: Currencies**

  ```
  SUM([FullCommissionCurrencies])/SUM([FullCommission])
  ```

- **% of Full Commission: ETFs**

  ```
  SUM([FullCommissionETF])/SUM([FullCommission])
  ```

- **% of Full Commission: Indices**

  ```
  SUM([FullCommissionIndices])/SUM([FullCommission])
  ```

- **% of Full Commission: Stocks**

  ```
  SUM([FullCommissionStocks])/SUM([FullCommission])
  ```

- **% of Traders with Profit**

  ```
  SUM([TradersWithProfit])/(SUM([TradersWithProfit])+SUM([TradersWithLoss]))
  ```

- **% of Traders with Profit Copy**

  ```
  SUM([TradersWithProfitCopy])/(SUM([TradersWithProfitCopy])+SUM([TradersWithLossCopy]))
  ```

- **% of Traders with Profit Stocks**

  ```
  SUM([TradersWithProfitStocks])/(SUM([TradersWithProfitStocks])+SUM([TradersWithLossStocks]))
  ```

- **Active Accounts (TP, #)**

  ```
  [Portfolio_Only]+[ActiveTraded]
  ```

- **Adjusted Withdraws (GE, $)**

  ```
  [GlobalWithdraw_ExclRedeem]-[CashoutAdjustment]
  ```

- **Adjusted Withdraws / Deposits Ratio (GE, $)**

  ```
  SUM([Adjusted Withdraws (GE, $)])/SUM([GlobalDepositsAmount])
  ```

- **Avg. Deposit Amount (Global)**

  ```
  SUM([GlobalDepositsAmount])/SUM([GlobalDepositsCount])
  ```

- **Avg. Deposit Amount to TP**

  ```
  SUM([Deposits to TP ($ Amount)])/SUM([Deposits to TP (# Count)])
  ```

- **Avg. FTD Amount (Global)**

  ```
  SUM([TotalFTDGlobalAmount])/SUM([TotalFTDGlobalCount])
  ```

- **Avg. New Trade Amount**

  ```
  SUM([InvestmentAmountInNewTradesAmount])/SUM([NewTradesCount])
  ```

- **Avg. TP FTDA**

  ```
  SUM([TP_FTDA])/SUM([TPFirstDeposited])
  ```

- **Avg. TP's FTD Amount**

  ```
  SUM([TP_FTDA])/SUM([TPFirstDeposited])
  ```

- **Avg. Withdraw Amount (Global)**

  ```
  SUM([GlobalWithdrawsAmount])/SUM([GlobalWithdrawsCount])
  ```

- **Avg. Withdraw Amount from TP**

  ```
  SUM([Withdraws from TP ($ Amount)])/SUM([Withdraws from TP (# Count)])
  ```

- **Deposits to TP (# Count)**

  ```
  [InternalDepositsTPCount]+[ExternalDepositsTPCount]
  ```

- **Deposits to TP ($ Amount)**

  ```
  [InternalDepositsTPAmount]+[ExternalDepositsTPAmount]
  ```

- **Global Withdraws / Deposits Ratio**

  ```
  SUM([GlobalWithdrawsAmount])/SUM([GlobalDepositsAmount])
  ```

- **MiFID Category**

  ```
  IF [MifidCategorizationID] = 2
  OR [MifidCategorizationID] = 3
  THEN "Professional" ELSE "Retail" END
  ```

- **Net Deposits (GE, $)**

  ```
  [GlobalDepositsAmount]-[GlobalWithdrawsAmount]
  ```

- **Net Deposits to TP**

  ```
  ([InternalDepositsTPAmount] + [ExternalDepositsTPAmount]) - ([ExternalWithdrawTPAmount] + [InternalWithdrawTPAmount])
  ```

- **Net New Trades (TP, #)**

  ```
  [NewTradesCount]-[ClosedTradesCount]
  ```

- **Net New Trades (TP, $)**

  ```
  [InvestmentAmountInNewTradesAmount]-[InvestmentAmountClosedTradesAmount]
  ```

- **PlaceHolder1**

  ```
  ""
  ```

- **PlaceHolder2**

  ```
  ""
  ```

- **PlaceHolder3**

  ```
  ""
  ```

- **PlaceHolder4**

  ```
  ""
  ```

- **Withdraws from TP (# Count)**

  ```
  [ExternalWithdrawTPCount]+[InternalWithdrawTPCount]
  ```

- **Withdraws from TP ($ Amount)**

  ```
  [ExternalWithdrawTPAmount]+[InternalWithdrawTPAmount]
  ```

- **Yesterday's Date**

  ```
  TODAY()-1
  ```
