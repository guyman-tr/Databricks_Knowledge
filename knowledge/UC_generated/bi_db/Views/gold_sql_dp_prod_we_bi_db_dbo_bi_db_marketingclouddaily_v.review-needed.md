# Review-needed sidecar — `gold_sql_dp_prod_we_bi_db_dbo_bi_db_marketingclouddaily_v`

Generated: 2026-05-19
Wiki: `knowledge/UC_generated/bi_db/Views/gold_sql_dp_prod_we_bi_db_dbo_bi_db_marketingclouddaily_v.md`

## UNVERIFIED columns

| Column | Reason |
|--------|--------|
| `AccountId` | transform='runtime_lineage' src='—'.'—'; no upstream wiki match and no source-code expression. NOTE: live UC comment (if any) is intentionally NOT used as a source — the live UC comment is the artifact we are trying to replace, not anchor against. |
| `CID` | transform='runtime_lineage' src='—'.'—'; no upstream wiki match and no source-code expression. NOTE: live UC comment (if any) is intentionally NOT used as a source — the live UC comment is the artifact we are trying to replace, not anchor against. |
| `MobileAppLastLogin` | transform='runtime_lineage' src='—'.'—'; no upstream wiki match and no source-code expression. NOTE: live UC comment (if any) is intentionally NOT used as a source — the live UC comment is the artifact we are trying to replace, not anchor against. |
| `WatchlistLastAddedETF` | transform='runtime_lineage' src='—'.'—'; no upstream wiki match and no source-code expression. NOTE: live UC comment (if any) is intentionally NOT used as a source — the live UC comment is the artifact we are trying to replace, not anchor against. |
| `WatchlistLastAddedCrypto` | transform='runtime_lineage' src='—'.'—'; no upstream wiki match and no source-code expression. NOTE: live UC comment (if any) is intentionally NOT used as a source — the live UC comment is the artifact we are trying to replace, not anchor against. |
| `WatchlistLastAddedStocks` | transform='runtime_lineage' src='—'.'—'; no upstream wiki match and no source-code expression. NOTE: live UC comment (if any) is intentionally NOT used as a source — the live UC comment is the artifact we are trying to replace, not anchor against. |
| `WatchlistLastAddedCommodities` | transform='runtime_lineage' src='—'.'—'; no upstream wiki match and no source-code expression. NOTE: live UC comment (if any) is intentionally NOT used as a source — the live UC comment is the artifact we are trying to replace, not anchor against. |
| `WatchlistLastAddedIndecies` | transform='runtime_lineage' src='—'.'—'; no upstream wiki match and no source-code expression. NOTE: live UC comment (if any) is intentionally NOT used as a source — the live UC comment is the artifact we are trying to replace, not anchor against. |
| `WatchlistLastAddedPI` | transform='runtime_lineage' src='—'.'—'; no upstream wiki match and no source-code expression. NOTE: live UC comment (if any) is intentionally NOT used as a source — the live UC comment is the artifact we are trying to replace, not anchor against. |
| `WatchlistLastAddedCopyPortfolio` | transform='runtime_lineage' src='—'.'—'; no upstream wiki match and no source-code expression. NOTE: live UC comment (if any) is intentionally NOT used as a source — the live UC comment is the artifact we are trying to replace, not anchor against. |
| `WatchlistLastAddedNonPIUser` | transform='runtime_lineage' src='—'.'—'; no upstream wiki match and no source-code expression. NOTE: live UC comment (if any) is intentionally NOT used as a source — the live UC comment is the artifact we are trying to replace, not anchor against. |
| `WatchlistLastDateID` | transform='runtime_lineage' src='—'.'—'; no upstream wiki match and no source-code expression. NOTE: live UC comment (if any) is intentionally NOT used as a source — the live UC comment is the artifact we are trying to replace, not anchor against. |
| `GainThisWeek` | transform='runtime_lineage' src='—'.'—'; no upstream wiki match and no source-code expression. NOTE: live UC comment (if any) is intentionally NOT used as a source — the live UC comment is the artifact we are trying to replace, not anchor against. |
| `GainOneMonthAgo` | transform='runtime_lineage' src='—'.'—'; no upstream wiki match and no source-code expression. NOTE: live UC comment (if any) is intentionally NOT used as a source — the live UC comment is the artifact we are trying to replace, not anchor against. |
| `GainThreeMonthsAgo` | transform='runtime_lineage' src='—'.'—'; no upstream wiki match and no source-code expression. NOTE: live UC comment (if any) is intentionally NOT used as a source — the live UC comment is the artifact we are trying to replace, not anchor against. |
| `GainSixMonthsAgo` | transform='runtime_lineage' src='—'.'—'; no upstream wiki match and no source-code expression. NOTE: live UC comment (if any) is intentionally NOT used as a source — the live UC comment is the artifact we are trying to replace, not anchor against. |
| `GainOneYearAgo` | transform='runtime_lineage' src='—'.'—'; no upstream wiki match and no source-code expression. NOTE: live UC comment (if any) is intentionally NOT used as a source — the live UC comment is the artifact we are trying to replace, not anchor against. |
| `GainLastDate` | transform='runtime_lineage' src='—'.'—'; no upstream wiki match and no source-code expression. NOTE: live UC comment (if any) is intentionally NOT used as a source — the live UC comment is the artifact we are trying to replace, not anchor against. |
| `GainExecutionID` | transform='runtime_lineage' src='—'.'—'; no upstream wiki match and no source-code expression. NOTE: live UC comment (if any) is intentionally NOT used as a source — the live UC comment is the artifact we are trying to replace, not anchor against. |
| `UpdateDate` | transform='runtime_lineage' src='—'.'—'; no upstream wiki match and no source-code expression. NOTE: live UC comment (if any) is intentionally NOT used as a source — the live UC comment is the artifact we are trying to replace, not anchor against. |
| `eMoneyIsInRollout` | transform='runtime_lineage' src='—'.'—'; no upstream wiki match and no source-code expression. NOTE: live UC comment (if any) is intentionally NOT used as a source — the live UC comment is the artifact we are trying to replace, not anchor against. |
| `eMoneyIsInRolloutDate` | transform='runtime_lineage' src='—'.'—'; no upstream wiki match and no source-code expression. NOTE: live UC comment (if any) is intentionally NOT used as a source — the live UC comment is the artifact we are trying to replace, not anchor against. |
| `AirDropRemainder` | transform='runtime_lineage' src='—'.'—'; no upstream wiki match and no source-code expression. NOTE: live UC comment (if any) is intentionally NOT used as a source — the live UC comment is the artifact we are trying to replace, not anchor against. |
| `VerificationLevel3Date` | transform='runtime_lineage' src='—'.'—'; no upstream wiki match and no source-code expression. NOTE: live UC comment (if any) is intentionally NOT used as a source — the live UC comment is the artifact we are trying to replace, not anchor against. |
| `AirdropServeyDate` | transform='runtime_lineage' src='—'.'—'; no upstream wiki match and no source-code expression. NOTE: live UC comment (if any) is intentionally NOT used as a source — the live UC comment is the artifact we are trying to replace, not anchor against. |
| `AirdropPotentialUpdateDate` | transform='runtime_lineage' src='—'.'—'; no upstream wiki match and no source-code expression. NOTE: live UC comment (if any) is intentionally NOT used as a source — the live UC comment is the artifact we are trying to replace, not anchor against. |
| `WalletLastLogin` | transform='runtime_lineage' src='—'.'—'; no upstream wiki match and no source-code expression. NOTE: live UC comment (if any) is intentionally NOT used as a source — the live UC comment is the artifact we are trying to replace, not anchor against. |
| `eMoneyExternalTransferToIBANLastDate` | transform='runtime_lineage' src='—'.'—'; no upstream wiki match and no source-code expression. NOTE: live UC comment (if any) is intentionally NOT used as a source — the live UC comment is the artifact we are trying to replace, not anchor against. |
| `eMoneyDepositToPlatformLastDate` | transform='runtime_lineage' src='—'.'—'; no upstream wiki match and no source-code expression. NOTE: live UC comment (if any) is intentionally NOT used as a source — the live UC comment is the artifact we are trying to replace, not anchor against. |
| `eMoneyWithrawalFromPlatformLastDate` | transform='runtime_lineage' src='—'.'—'; no upstream wiki match and no source-code expression. NOTE: live UC comment (if any) is intentionally NOT used as a source — the live UC comment is the artifact we are trying to replace, not anchor against. |
| `eMoneyEODBalanceAmount` | transform='runtime_lineage' src='—'.'—'; no upstream wiki match and no source-code expression. NOTE: live UC comment (if any) is intentionally NOT used as a source — the live UC comment is the artifact we are trying to replace, not anchor against. |
| `eMoneyEODBalanceDate` | transform='runtime_lineage' src='—'.'—'; no upstream wiki match and no source-code expression. NOTE: live UC comment (if any) is intentionally NOT used as a source — the live UC comment is the artifact we are trying to replace, not anchor against. |
| `eMoneyCardTransactionLastDate` | transform='runtime_lineage' src='—'.'—'; no upstream wiki match and no source-code expression. NOTE: live UC comment (if any) is intentionally NOT used as a source — the live UC comment is the artifact we are trying to replace, not anchor against. |
| `KYCFlowName` | transform='runtime_lineage' src='—'.'—'; no upstream wiki match and no source-code expression. NOTE: live UC comment (if any) is intentionally NOT used as a source — the live UC comment is the artifact we are trying to replace, not anchor against. |
| `KYCLeadScore` | transform='runtime_lineage' src='—'.'—'; no upstream wiki match and no source-code expression. NOTE: live UC comment (if any) is intentionally NOT used as a source — the live UC comment is the artifact we are trying to replace, not anchor against. |
| `AirdropCustomerID` | transform='runtime_lineage' src='—'.'—'; no upstream wiki match and no source-code expression. NOTE: live UC comment (if any) is intentionally NOT used as a source — the live UC comment is the artifact we are trying to replace, not anchor against. |
| `Credit` | transform='runtime_lineage' src='—'.'—'; no upstream wiki match and no source-code expression. NOTE: live UC comment (if any) is intentionally NOT used as a source — the live UC comment is the artifact we are trying to replace, not anchor against. |
| `FirstTimeCopiedDate` | transform='runtime_lineage' src='—'.'—'; no upstream wiki match and no source-code expression. NOTE: live UC comment (if any) is intentionally NOT used as a source — the live UC comment is the artifact we are trying to replace, not anchor against. |
| `PrivacyPolicyID` | transform='runtime_lineage' src='—'.'—'; no upstream wiki match and no source-code expression. NOTE: live UC comment (if any) is intentionally NOT used as a source — the live UC comment is the artifact we are trying to replace, not anchor against. |
| `LSD` | transform='runtime_lineage' src='—'.'—'; no upstream wiki match and no source-code expression. NOTE: live UC comment (if any) is intentionally NOT used as a source — the live UC comment is the artifact we are trying to replace, not anchor against. |
| `LSDDate` | transform='runtime_lineage' src='—'.'—'; no upstream wiki match and no source-code expression. NOTE: live UC comment (if any) is intentionally NOT used as a source — the live UC comment is the artifact we are trying to replace, not anchor against. |
| `TX_Tier_3M` | transform='runtime_lineage' src='—'.'—'; no upstream wiki match and no source-code expression. NOTE: live UC comment (if any) is intentionally NOT used as a source — the live UC comment is the artifact we are trying to replace, not anchor against. |
| `Amount_Tier_3M` | transform='runtime_lineage' src='—'.'—'; no upstream wiki match and no source-code expression. NOTE: live UC comment (if any) is intentionally NOT used as a source — the live UC comment is the artifact we are trying to replace, not anchor against. |
| `TX_Tier_3M_Deposits` | transform='runtime_lineage' src='—'.'—'; no upstream wiki match and no source-code expression. NOTE: live UC comment (if any) is intentionally NOT used as a source — the live UC comment is the artifact we are trying to replace, not anchor against. |
| `TX_Tier_3M_CO` | transform='runtime_lineage' src='—'.'—'; no upstream wiki match and no source-code expression. NOTE: live UC comment (if any) is intentionally NOT used as a source — the live UC comment is the artifact we are trying to replace, not anchor against. |
| `Amount_Tier_3M_CO` | transform='runtime_lineage' src='—'.'—'; no upstream wiki match and no source-code expression. NOTE: live UC comment (if any) is intentionally NOT used as a source — the live UC comment is the artifact we are trying to replace, not anchor against. |
| `Amount_Tier_3M_Deposits` | transform='runtime_lineage' src='—'.'—'; no upstream wiki match and no source-code expression. NOTE: live UC comment (if any) is intentionally NOT used as a source — the live UC comment is the artifact we are trying to replace, not anchor against. |
| `AccountSubProgram` | transform='runtime_lineage' src='—'.'—'; no upstream wiki match and no source-code expression. NOTE: live UC comment (if any) is intentionally NOT used as a source — the live UC comment is the artifact we are trying to replace, not anchor against. |
| `CardCreateDate` | transform='runtime_lineage' src='—'.'—'; no upstream wiki match and no source-code expression. NOTE: live UC comment (if any) is intentionally NOT used as a source — the live UC comment is the artifact we are trying to replace, not anchor against. |
| `KYC_Experience_Level` | transform='runtime_lineage' src='—'.'—'; no upstream wiki match and no source-code expression. NOTE: live UC comment (if any) is intentionally NOT used as a source — the live UC comment is the artifact we are trying to replace, not anchor against. |
| `KYC_Planned_Invested_Amount` | transform='runtime_lineage' src='—'.'—'; no upstream wiki match and no source-code expression. NOTE: live UC comment (if any) is intentionally NOT used as a source — the live UC comment is the artifact we are trying to replace, not anchor against. |
| `KYC_CFD_Level` | transform='runtime_lineage' src='—'.'—'; no upstream wiki match and no source-code expression. NOTE: live UC comment (if any) is intentionally NOT used as a source — the live UC comment is the artifact we are trying to replace, not anchor against. |
| `IOB_Opt_In` | transform='runtime_lineage' src='—'.'—'; no upstream wiki match and no source-code expression. NOTE: live UC comment (if any) is intentionally NOT used as a source — the live UC comment is the artifact we are trying to replace, not anchor against. |
| `IOB_Opt_In_ValidFrom` | transform='runtime_lineage' src='—'.'—'; no upstream wiki match and no source-code expression. NOTE: live UC comment (if any) is intentionally NOT used as a source — the live UC comment is the artifact we are trying to replace, not anchor against. |
| `AmountToClubUpgrade` | transform='runtime_lineage' src='—'.'—'; no upstream wiki match and no source-code expression. NOTE: live UC comment (if any) is intentionally NOT used as a source — the live UC comment is the artifact we are trying to replace, not anchor against. |
| `UnRealizedEquity` | transform='runtime_lineage' src='—'.'—'; no upstream wiki match and no source-code expression. NOTE: live UC comment (if any) is intentionally NOT used as a source — the live UC comment is the artifact we are trying to replace, not anchor against. |
| `MaxEquity_LastYear` | transform='runtime_lineage' src='—'.'—'; no upstream wiki match and no source-code expression. NOTE: live UC comment (if any) is intentionally NOT used as a source — the live UC comment is the artifact we are trying to replace, not anchor against. |
| `MaxEquity_LastWeek` | transform='runtime_lineage' src='—'.'—'; no upstream wiki match and no source-code expression. NOTE: live UC comment (if any) is intentionally NOT used as a source — the live UC comment is the artifact we are trying to replace, not anchor against. |
| `CashoutAmount_LastWeek` | transform='runtime_lineage' src='—'.'—'; no upstream wiki match and no source-code expression. NOTE: live UC comment (if any) is intentionally NOT used as a source — the live UC comment is the artifact we are trying to replace, not anchor against. |
| `CashoutAmount_InProcess` | transform='runtime_lineage' src='—'.'—'; no upstream wiki match and no source-code expression. NOTE: live UC comment (if any) is intentionally NOT used as a source — the live UC comment is the artifact we are trying to replace, not anchor against. |
| `TotalDepositsAmount_LastYear` | transform='runtime_lineage' src='—'.'—'; no upstream wiki match and no source-code expression. NOTE: live UC comment (if any) is intentionally NOT used as a source — the live UC comment is the artifact we are trying to replace, not anchor against. |
| `KYC_PlannedInvestment_Stocks` | transform='runtime_lineage' src='—'.'—'; no upstream wiki match and no source-code expression. NOTE: live UC comment (if any) is intentionally NOT used as a source — the live UC comment is the artifact we are trying to replace, not anchor against. |
| `KYC_PlannedInvestment_Crypto` | transform='runtime_lineage' src='—'.'—'; no upstream wiki match and no source-code expression. NOTE: live UC comment (if any) is intentionally NOT used as a source — the live UC comment is the artifact we are trying to replace, not anchor against. |
| `KYC_PlannedInvestment_FX` | transform='runtime_lineage' src='—'.'—'; no upstream wiki match and no source-code expression. NOTE: live UC comment (if any) is intentionally NOT used as a source — the live UC comment is the artifact we are trying to replace, not anchor against. |
| `Total_KYC_PlannedInvestment_Answers` | transform='runtime_lineage' src='—'.'—'; no upstream wiki match and no source-code expression. NOTE: live UC comment (if any) is intentionally NOT used as a source — the live UC comment is the artifact we are trying to replace, not anchor against. |
| `StocksLendingStatusID` | transform='runtime_lineage' src='—'.'—'; no upstream wiki match and no source-code expression. NOTE: live UC comment (if any) is intentionally NOT used as a source — the live UC comment is the artifact we are trying to replace, not anchor against. |
| `StocksLendingOptInDate` | transform='runtime_lineage' src='—'.'—'; no upstream wiki match and no source-code expression. NOTE: live UC comment (if any) is intentionally NOT used as a source — the live UC comment is the artifact we are trying to replace, not anchor against. |
| `eTM_IBAN_Type` | transform='runtime_lineage' src='—'.'—'; no upstream wiki match and no source-code expression. NOTE: live UC comment (if any) is intentionally NOT used as a source — the live UC comment is the artifact we are trying to replace, not anchor against. |
| `eTM_AccountName_LocalCurrency` | transform='runtime_lineage' src='—'.'—'; no upstream wiki match and no source-code expression. NOTE: live UC comment (if any) is intentionally NOT used as a source — the live UC comment is the artifact we are trying to replace, not anchor against. |
| `Cluster` | transform='runtime_lineage' src='—'.'—'; no upstream wiki match and no source-code expression. NOTE: live UC comment (if any) is intentionally NOT used as a source — the live UC comment is the artifact we are trying to replace, not anchor against. |
| `ClusterDate` | transform='runtime_lineage' src='—'.'—'; no upstream wiki match and no source-code expression. NOTE: live UC comment (if any) is intentionally NOT used as a source — the live UC comment is the artifact we are trying to replace, not anchor against. |
| `PositionOpen_LastDate_ETF` | transform='runtime_lineage' src='—'.'—'; no upstream wiki match and no source-code expression. NOTE: live UC comment (if any) is intentionally NOT used as a source — the live UC comment is the artifact we are trying to replace, not anchor against. |
| `Rewarded_LastMonth_eMoney` | transform='runtime_lineage' src='—'.'—'; no upstream wiki match and no source-code expression. NOTE: live UC comment (if any) is intentionally NOT used as a source — the live UC comment is the artifact we are trying to replace, not anchor against. |
| `Monthly_CardSpent_eMoney` | transform='runtime_lineage' src='—'.'—'; no upstream wiki match and no source-code expression. NOTE: live UC comment (if any) is intentionally NOT used as a source — the live UC comment is the artifact we are trying to replace, not anchor against. |
| `Monthly_RewardedChashBack_eMoney` | transform='runtime_lineage' src='—'.'—'; no upstream wiki match and no source-code expression. NOTE: live UC comment (if any) is intentionally NOT used as a source — the live UC comment is the artifact we are trying to replace, not anchor against. |
| `Monthly_CardEligibleCashBack_eMoney` | transform='runtime_lineage' src='—'.'—'; no upstream wiki match and no source-code expression. NOTE: live UC comment (if any) is intentionally NOT used as a source — the live UC comment is the artifact we are trying to replace, not anchor against. |
| `FirstDepositDateGlobal` | transform='runtime_lineage' src='—'.'—'; no upstream wiki match and no source-code expression. NOTE: live UC comment (if any) is intentionally NOT used as a source — the live UC comment is the artifact we are trying to replace, not anchor against. |
| `DepositsUSD_Global` | transform='runtime_lineage' src='—'.'—'; no upstream wiki match and no source-code expression. NOTE: live UC comment (if any) is intentionally NOT used as a source — the live UC comment is the artifact we are trying to replace, not anchor against. |
| `BalanceGlobal` | transform='runtime_lineage' src='—'.'—'; no upstream wiki match and no source-code expression. NOTE: live UC comment (if any) is intentionally NOT used as a source — the live UC comment is the artifact we are trying to replace, not anchor against. |
| `EquityGlobal` | transform='runtime_lineage' src='—'.'—'; no upstream wiki match and no source-code expression. NOTE: live UC comment (if any) is intentionally NOT used as a source — the live UC comment is the artifact we are trying to replace, not anchor against. |
| `Total_Deposit_USD` | transform='runtime_lineage' src='—'.'—'; no upstream wiki match and no source-code expression. NOTE: live UC comment (if any) is intentionally NOT used as a source — the live UC comment is the artifact we are trying to replace, not anchor against. |
| `Total_Deposit_EUR` | transform='runtime_lineage' src='—'.'—'; no upstream wiki match and no source-code expression. NOTE: live UC comment (if any) is intentionally NOT used as a source — the live UC comment is the artifact we are trying to replace, not anchor against. |
| `Total_Deposit_GBP` | transform='runtime_lineage' src='—'.'—'; no upstream wiki match and no source-code expression. NOTE: live UC comment (if any) is intentionally NOT used as a source — the live UC comment is the artifact we are trying to replace, not anchor against. |
| `Current_Balance_USD` | transform='runtime_lineage' src='—'.'—'; no upstream wiki match and no source-code expression. NOTE: live UC comment (if any) is intentionally NOT used as a source — the live UC comment is the artifact we are trying to replace, not anchor against. |
| `Current_Balance_EUR` | transform='runtime_lineage' src='—'.'—'; no upstream wiki match and no source-code expression. NOTE: live UC comment (if any) is intentionally NOT used as a source — the live UC comment is the artifact we are trying to replace, not anchor against. |
| `Current_Balance_GBP` | transform='runtime_lineage' src='—'.'—'; no upstream wiki match and no source-code expression. NOTE: live UC comment (if any) is intentionally NOT used as a source — the live UC comment is the artifact we are trying to replace, not anchor against. |
| `LastTransactionDate` | transform='runtime_lineage' src='—'.'—'; no upstream wiki match and no source-code expression. NOTE: live UC comment (if any) is intentionally NOT used as a source — the live UC comment is the artifact we are trying to replace, not anchor against. |
| `AcceptedTnCs_Date` | transform='runtime_lineage' src='—'.'—'; no upstream wiki match and no source-code expression. NOTE: live UC comment (if any) is intentionally NOT used as a source — the live UC comment is the artifact we are trying to replace, not anchor against. |
| `DIY_PortfolioCreatedDate` | transform='runtime_lineage' src='—'.'—'; no upstream wiki match and no source-code expression. NOTE: live UC comment (if any) is intentionally NOT used as a source — the live UC comment is the artifact we are trying to replace, not anchor against. |
| `DIY_FirstDepositDate` | transform='runtime_lineage' src='—'.'—'; no upstream wiki match and no source-code expression. NOTE: live UC comment (if any) is intentionally NOT used as a source — the live UC comment is the artifact we are trying to replace, not anchor against. |
| `Cash_PortfolioCreatedDate` | transform='runtime_lineage' src='—'.'—'; no upstream wiki match and no source-code expression. NOTE: live UC comment (if any) is intentionally NOT used as a source — the live UC comment is the artifact we are trying to replace, not anchor against. |
| `Cash_FirstDepositDate` | transform='runtime_lineage' src='—'.'—'; no upstream wiki match and no source-code expression. NOTE: live UC comment (if any) is intentionally NOT used as a source — the live UC comment is the artifact we are trying to replace, not anchor against. |
| `Managed_PortfolioCreatedDate` | transform='runtime_lineage' src='—'.'—'; no upstream wiki match and no source-code expression. NOTE: live UC comment (if any) is intentionally NOT used as a source — the live UC comment is the artifact we are trying to replace, not anchor against. |
| `Managed_FirstDepositDate` | transform='runtime_lineage' src='—'.'—'; no upstream wiki match and no source-code expression. NOTE: live UC comment (if any) is intentionally NOT used as a source — the live UC comment is the artifact we are trying to replace, not anchor against. |
| `IsDefunded_across_all_portfolios` | transform='runtime_lineage' src='—'.'—'; no upstream wiki match and no source-code expression. NOTE: live UC comment (if any) is intentionally NOT used as a source — the live UC comment is the artifact we are trying to replace, not anchor against. |
| `RAF_Inviter` | transform='runtime_lineage' src='—'.'—'; no upstream wiki match and no source-code expression. NOTE: live UC comment (if any) is intentionally NOT used as a source — the live UC comment is the artifact we are trying to replace, not anchor against. |
| `RAF_LastCashoutDate` | transform='runtime_lineage' src='—'.'—'; no upstream wiki match and no source-code expression. NOTE: live UC comment (if any) is intentionally NOT used as a source — the live UC comment is the artifact we are trying to replace, not anchor against. |
| `etr_y` | transform='runtime_lineage' src='—'.'—'; no upstream wiki match and no source-code expression. NOTE: live UC comment (if any) is intentionally NOT used as a source — the live UC comment is the artifact we are trying to replace, not anchor against. |
| `etr_ym` | transform='runtime_lineage' src='—'.'—'; no upstream wiki match and no source-code expression. NOTE: live UC comment (if any) is intentionally NOT used as a source — the live UC comment is the artifact we are trying to replace, not anchor against. |
| `etr_ymd` | transform='runtime_lineage' src='—'.'—'; no upstream wiki match and no source-code expression. NOTE: live UC comment (if any) is intentionally NOT used as a source — the live UC comment is the artifact we are trying to replace, not anchor against. |

## Tier 4 candidates

_None._

## Cross-check mismatches

_None._

## Open questions

_None._

## Reviewer Corrections

> Domain-expert overrides applied as **Tier 5** on the next regeneration.
> Tier 5 is absolute — it overrides every other tier including Tier 1.

| Column | Correction | Current (wrong) | Reason | Reviewer |
|--------|------------|-----------------|--------|----------|
| _(none)_ | | | | |
