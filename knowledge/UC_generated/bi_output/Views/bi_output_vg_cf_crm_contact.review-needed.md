# Review-needed sidecar — `bi_output_vg_cf_crm_contact`

Generated: 2026-06-19
Wiki: `knowledge/UC_generated/bi_output/Views/bi_output_vg_cf_crm_contact.md`

## UNVERIFIED columns

| Column | Reason |
|--------|--------|
| `Id_Source` | transform='unknown' src='—'.'—'; no upstream wiki match and no source-code expression. NOTE: live UC comment (if any) is intentionally NOT used as a source — the live UC comment is the artifact we are trying to replace, not anchor against. |
| `Id` | transform='unknown' src='—'.'—'; no upstream wiki match and no source-code expression. NOTE: live UC comment (if any) is intentionally NOT used as a source — the live UC comment is the artifact we are trying to replace, not anchor against. |
| `CreatedDateTime` | transform='unknown' src='—'.'—'; no upstream wiki match and no source-code expression. NOTE: live UC comment (if any) is intentionally NOT used as a source — the live UC comment is the artifact we are trying to replace, not anchor against. |
| `Task_Id` | transform='unknown' src='—'.'—'; no upstream wiki match and no source-code expression. NOTE: live UC comment (if any) is intentionally NOT used as a source — the live UC comment is the artifact we are trying to replace, not anchor against. |
| `CustEng_Id` | transform='unknown' src='—'.'—'; no upstream wiki match and no source-code expression. NOTE: live UC comment (if any) is intentionally NOT used as a source — the live UC comment is the artifact we are trying to replace, not anchor against. |
| `Email_Id` | transform='unknown' src='—'.'—'; no upstream wiki match and no source-code expression. NOTE: live UC comment (if any) is intentionally NOT used as a source — the live UC comment is the artifact we are trying to replace, not anchor against. |
| `Case_Id` | transform='unknown' src='—'.'—'; no upstream wiki match and no source-code expression. NOTE: live UC comment (if any) is intentionally NOT used as a source — the live UC comment is the artifact we are trying to replace, not anchor against. |
| `Case_Number` | transform='unknown' src='—'.'—'; no upstream wiki match and no source-code expression. NOTE: live UC comment (if any) is intentionally NOT used as a source — the live UC comment is the artifact we are trying to replace, not anchor against. |
| `CID` | transform='unknown' src='—'.'—'; no upstream wiki match and no source-code expression. NOTE: live UC comment (if any) is intentionally NOT used as a source — the live UC comment is the artifact we are trying to replace, not anchor against. |
| `Task_CreatedDate` | transform='unknown' src='—'.'—'; no upstream wiki match and no source-code expression. NOTE: live UC comment (if any) is intentionally NOT used as a source — the live UC comment is the artifact we are trying to replace, not anchor against. |
| `Task_LastModifiedDate` | transform='unknown' src='—'.'—'; no upstream wiki match and no source-code expression. NOTE: live UC comment (if any) is intentionally NOT used as a source — the live UC comment is the artifact we are trying to replace, not anchor against. |
| `CustEng_CreatedDate` | transform='unknown' src='—'.'—'; no upstream wiki match and no source-code expression. NOTE: live UC comment (if any) is intentionally NOT used as a source — the live UC comment is the artifact we are trying to replace, not anchor against. |
| `CustEng_LastModifiedDate` | transform='unknown' src='—'.'—'; no upstream wiki match and no source-code expression. NOTE: live UC comment (if any) is intentionally NOT used as a source — the live UC comment is the artifact we are trying to replace, not anchor against. |
| `Email_CreatedDate` | transform='unknown' src='—'.'—'; no upstream wiki match and no source-code expression. NOTE: live UC comment (if any) is intentionally NOT used as a source — the live UC comment is the artifact we are trying to replace, not anchor against. |
| `Email_LastModifiedDate` | transform='unknown' src='—'.'—'; no upstream wiki match and no source-code expression. NOTE: live UC comment (if any) is intentionally NOT used as a source — the live UC comment is the artifact we are trying to replace, not anchor against. |
| `Task_Subject` | transform='unknown' src='—'.'—'; no upstream wiki match and no source-code expression. NOTE: live UC comment (if any) is intentionally NOT used as a source — the live UC comment is the artifact we are trying to replace, not anchor against. |
| `Task_Subtype` | transform='unknown' src='—'.'—'; no upstream wiki match and no source-code expression. NOTE: live UC comment (if any) is intentionally NOT used as a source — the live UC comment is the artifact we are trying to replace, not anchor against. |
| `Task_Status` | transform='unknown' src='—'.'—'; no upstream wiki match and no source-code expression. NOTE: live UC comment (if any) is intentionally NOT used as a source — the live UC comment is the artifact we are trying to replace, not anchor against. |
| `CustEng_CallSummary` | transform='unknown' src='—'.'—'; no upstream wiki match and no source-code expression. NOTE: live UC comment (if any) is intentionally NOT used as a source — the live UC comment is the artifact we are trying to replace, not anchor against. |
| `CustEng_ZoomCall` | transform='unknown' src='—'.'—'; no upstream wiki match and no source-code expression. NOTE: live UC comment (if any) is intentionally NOT used as a source — the live UC comment is the artifact we are trying to replace, not anchor against. |
| `OwnerId` | transform='unknown' src='—'.'—'; no upstream wiki match and no source-code expression. NOTE: live UC comment (if any) is intentionally NOT used as a source — the live UC comment is the artifact we are trying to replace, not anchor against. |
| `Owner` | transform='unknown' src='—'.'—'; no upstream wiki match and no source-code expression. NOTE: live UC comment (if any) is intentionally NOT used as a source — the live UC comment is the artifact we are trying to replace, not anchor against. |
| `AccountManagerId` | transform='unknown' src='—'.'—'; no upstream wiki match and no source-code expression. NOTE: live UC comment (if any) is intentionally NOT used as a source — the live UC comment is the artifact we are trying to replace, not anchor against. |
| `AccountManager` | transform='unknown' src='—'.'—'; no upstream wiki match and no source-code expression. NOTE: live UC comment (if any) is intentionally NOT used as a source — the live UC comment is the artifact we are trying to replace, not anchor against. |
| `CallDurationInSeconds` | transform='unknown' src='—'.'—'; no upstream wiki match and no source-code expression. NOTE: live UC comment (if any) is intentionally NOT used as a source — the live UC comment is the artifact we are trying to replace, not anchor against. |
| `Vonage_CallDuration` | transform='unknown' src='—'.'—'; no upstream wiki match and no source-code expression. NOTE: live UC comment (if any) is intentionally NOT used as a source — the live UC comment is the artifact we are trying to replace, not anchor against. |
| `Task_CallDuration` | transform='unknown' src='—'.'—'; no upstream wiki match and no source-code expression. NOTE: live UC comment (if any) is intentionally NOT used as a source — the live UC comment is the artifact we are trying to replace, not anchor against. |
| `CustEng_CallDuration` | transform='unknown' src='—'.'—'; no upstream wiki match and no source-code expression. NOTE: live UC comment (if any) is intentionally NOT used as a source — the live UC comment is the artifact we are trying to replace, not anchor against. |
| `ContactDirection` | transform='unknown' src='—'.'—'; no upstream wiki match and no source-code expression. NOTE: live UC comment (if any) is intentionally NOT used as a source — the live UC comment is the artifact we are trying to replace, not anchor against. |
| `ContactType` | transform='unknown' src='—'.'—'; no upstream wiki match and no source-code expression. NOTE: live UC comment (if any) is intentionally NOT used as a source — the live UC comment is the artifact we are trying to replace, not anchor against. |
| `ContactType_Group` | transform='unknown' src='—'.'—'; no upstream wiki match and no source-code expression. NOTE: live UC comment (if any) is intentionally NOT used as a source — the live UC comment is the artifact we are trying to replace, not anchor against. |
| `CF_Terminology` | transform='unknown' src='—'.'—'; no upstream wiki match and no source-code expression. NOTE: live UC comment (if any) is intentionally NOT used as a source — the live UC comment is the artifact we are trying to replace, not anchor against. |

## Tier 4 candidates

_None._

## Cross-check mismatches

_None._

## Open questions

_None._

## Reviewer Corrections

> Domain-expert overrides applied as **Tier 5** on the next regeneration.
> Tier 5 is absolute — it overrides every other tier including Tier 1.

| Column | Correction | Current (wrong) | Reason | Reviewer |
|--------|------------|-----------------|--------|----------|
| _(none)_ | | | | |
