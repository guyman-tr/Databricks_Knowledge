# Review-needed sidecar — `vg_fact_billingdeposit_for_genie`

Generated: 2026-06-19
Wiki: `knowledge/UC_generated/bi_output/Views/vg_fact_billingdeposit_for_genie.md`

## UNVERIFIED columns

| Column | Reason |
|--------|--------|
| `CID` | transform='unknown' src='—'.'—'; no upstream wiki match and no source-code expression. NOTE: live UC comment (if any) is intentionally NOT used as a source — the live UC comment is the artifact we are trying to replace, not anchor against. |
| `CurrencyID` | transform='unknown' src='—'.'—'; no upstream wiki match and no source-code expression. NOTE: live UC comment (if any) is intentionally NOT used as a source — the live UC comment is the artifact we are trying to replace, not anchor against. |
| `Commission` | transform='unknown' src='—'.'—'; no upstream wiki match and no source-code expression. NOTE: live UC comment (if any) is intentionally NOT used as a source — the live UC comment is the artifact we are trying to replace, not anchor against. |
| `Approved` | transform='unknown' src='—'.'—'; no upstream wiki match and no source-code expression. NOTE: live UC comment (if any) is intentionally NOT used as a source — the live UC comment is the artifact we are trying to replace, not anchor against. |
| `ModificationDate` | transform='unknown' src='—'.'—'; no upstream wiki match and no source-code expression. NOTE: live UC comment (if any) is intentionally NOT used as a source — the live UC comment is the artifact we are trying to replace, not anchor against. |
| `ModificationDateID` | transform='unknown' src='—'.'—'; no upstream wiki match and no source-code expression. NOTE: live UC comment (if any) is intentionally NOT used as a source — the live UC comment is the artifact we are trying to replace, not anchor against. |
| `FundingID` | transform='unknown' src='—'.'—'; no upstream wiki match and no source-code expression. NOTE: live UC comment (if any) is intentionally NOT used as a source — the live UC comment is the artifact we are trying to replace, not anchor against. |
| `ExchangeRate` | transform='unknown' src='—'.'—'; no upstream wiki match and no source-code expression. NOTE: live UC comment (if any) is intentionally NOT used as a source — the live UC comment is the artifact we are trying to replace, not anchor against. |
| `DepositID` | transform='unknown' src='—'.'—'; no upstream wiki match and no source-code expression. NOTE: live UC comment (if any) is intentionally NOT used as a source — the live UC comment is the artifact we are trying to replace, not anchor against. |
| `ProcessorValueDate` | transform='unknown' src='—'.'—'; no upstream wiki match and no source-code expression. NOTE: live UC comment (if any) is intentionally NOT used as a source — the live UC comment is the artifact we are trying to replace, not anchor against. |
| `DepotID` | transform='unknown' src='—'.'—'; no upstream wiki match and no source-code expression. NOTE: live UC comment (if any) is intentionally NOT used as a source — the live UC comment is the artifact we are trying to replace, not anchor against. |
| `SecuredCardDataAsString` | transform='unknown' src='—'.'—'; no upstream wiki match and no source-code expression. NOTE: live UC comment (if any) is intentionally NOT used as a source — the live UC comment is the artifact we are trying to replace, not anchor against. |
| `BinCodeAsString` | transform='unknown' src='—'.'—'; no upstream wiki match and no source-code expression. NOTE: live UC comment (if any) is intentionally NOT used as a source — the live UC comment is the artifact we are trying to replace, not anchor against. |
| `BinCountryIDAsInteger` | transform='unknown' src='—'.'—'; no upstream wiki match and no source-code expression. NOTE: live UC comment (if any) is intentionally NOT used as a source — the live UC comment is the artifact we are trying to replace, not anchor against. |
| `CardTypeIDAsInteger` | transform='unknown' src='—'.'—'; no upstream wiki match and no source-code expression. NOTE: live UC comment (if any) is intentionally NOT used as a source — the live UC comment is the artifact we are trying to replace, not anchor against. |
| `PaymentStatusID` | transform='unknown' src='—'.'—'; no upstream wiki match and no source-code expression. NOTE: live UC comment (if any) is intentionally NOT used as a source — the live UC comment is the artifact we are trying to replace, not anchor against. |
| `ManagerID` | transform='unknown' src='—'.'—'; no upstream wiki match and no source-code expression. NOTE: live UC comment (if any) is intentionally NOT used as a source — the live UC comment is the artifact we are trying to replace, not anchor against. |
| `RiskManagementStatusID` | transform='unknown' src='—'.'—'; no upstream wiki match and no source-code expression. NOTE: live UC comment (if any) is intentionally NOT used as a source — the live UC comment is the artifact we are trying to replace, not anchor against. |
| `Amount` | transform='unknown' src='—'.'—'; no upstream wiki match and no source-code expression. NOTE: live UC comment (if any) is intentionally NOT used as a source — the live UC comment is the artifact we are trying to replace, not anchor against. |
| `PaymentDate` | transform='unknown' src='—'.'—'; no upstream wiki match and no source-code expression. NOTE: live UC comment (if any) is intentionally NOT used as a source — the live UC comment is the artifact we are trying to replace, not anchor against. |
| `IPAddress` | transform='unknown' src='—'.'—'; no upstream wiki match and no source-code expression. NOTE: live UC comment (if any) is intentionally NOT used as a source — the live UC comment is the artifact we are trying to replace, not anchor against. |
| `ClearingHouseEffectiveDate` | transform='unknown' src='—'.'—'; no upstream wiki match and no source-code expression. NOTE: live UC comment (if any) is intentionally NOT used as a source — the live UC comment is the artifact we are trying to replace, not anchor against. |
| `IsFTD` | transform='unknown' src='—'.'—'; no upstream wiki match and no source-code expression. NOTE: live UC comment (if any) is intentionally NOT used as a source — the live UC comment is the artifact we are trying to replace, not anchor against. |
| `RefundVerificationCode` | transform='unknown' src='—'.'—'; no upstream wiki match and no source-code expression. NOTE: live UC comment (if any) is intentionally NOT used as a source — the live UC comment is the artifact we are trying to replace, not anchor against. |
| `MatchStatusID` | transform='unknown' src='—'.'—'; no upstream wiki match and no source-code expression. NOTE: live UC comment (if any) is intentionally NOT used as a source — the live UC comment is the artifact we are trying to replace, not anchor against. |
| `BonusStatusID` | transform='unknown' src='—'.'—'; no upstream wiki match and no source-code expression. NOTE: live UC comment (if any) is intentionally NOT used as a source — the live UC comment is the artifact we are trying to replace, not anchor against. |
| `BonusAmount` | transform='unknown' src='—'.'—'; no upstream wiki match and no source-code expression. NOTE: live UC comment (if any) is intentionally NOT used as a source — the live UC comment is the artifact we are trying to replace, not anchor against. |
| `BonusErrorCode` | transform='unknown' src='—'.'—'; no upstream wiki match and no source-code expression. NOTE: live UC comment (if any) is intentionally NOT used as a source — the live UC comment is the artifact we are trying to replace, not anchor against. |
| `ExTransactionID` | transform='unknown' src='—'.'—'; no upstream wiki match and no source-code expression. NOTE: live UC comment (if any) is intentionally NOT used as a source — the live UC comment is the artifact we are trying to replace, not anchor against. |
| `FundingTypeID` | transform='unknown' src='—'.'—'; no upstream wiki match and no source-code expression. NOTE: live UC comment (if any) is intentionally NOT used as a source — the live UC comment is the artifact we are trying to replace, not anchor against. |
| `IsRefundExcluded` | transform='unknown' src='—'.'—'; no upstream wiki match and no source-code expression. NOTE: live UC comment (if any) is intentionally NOT used as a source — the live UC comment is the artifact we are trying to replace, not anchor against. |
| `DocumentRequired` | transform='unknown' src='—'.'—'; no upstream wiki match and no source-code expression. NOTE: live UC comment (if any) is intentionally NOT used as a source — the live UC comment is the artifact we are trying to replace, not anchor against. |
| `UpdateDate` | transform='unknown' src='—'.'—'; no upstream wiki match and no source-code expression. NOTE: live UC comment (if any) is intentionally NOT used as a source — the live UC comment is the artifact we are trying to replace, not anchor against. |
| `ExpirationDateID` | transform='unknown' src='—'.'—'; no upstream wiki match and no source-code expression. NOTE: live UC comment (if any) is intentionally NOT used as a source — the live UC comment is the artifact we are trying to replace, not anchor against. |
| `CountryIDAsInteger` | transform='unknown' src='—'.'—'; no upstream wiki match and no source-code expression. NOTE: live UC comment (if any) is intentionally NOT used as a source — the live UC comment is the artifact we are trying to replace, not anchor against. |
| `StateIDAsInteger` | transform='unknown' src='—'.'—'; no upstream wiki match and no source-code expression. NOTE: live UC comment (if any) is intentionally NOT used as a source — the live UC comment is the artifact we are trying to replace, not anchor against. |
| `BankIDAsInteger` | transform='unknown' src='—'.'—'; no upstream wiki match and no source-code expression. NOTE: live UC comment (if any) is intentionally NOT used as a source — the live UC comment is the artifact we are trying to replace, not anchor against. |
| `AccountNameAsString` | transform='unknown' src='—'.'—'; no upstream wiki match and no source-code expression. NOTE: live UC comment (if any) is intentionally NOT used as a source — the live UC comment is the artifact we are trying to replace, not anchor against. |
| `AccountTypeAsString` | transform='unknown' src='—'.'—'; no upstream wiki match and no source-code expression. NOTE: live UC comment (if any) is intentionally NOT used as a source — the live UC comment is the artifact we are trying to replace, not anchor against. |
| `BankAccountAsString` | transform='unknown' src='—'.'—'; no upstream wiki match and no source-code expression. NOTE: live UC comment (if any) is intentionally NOT used as a source — the live UC comment is the artifact we are trying to replace, not anchor against. |
| `BankAddressAsString` | transform='unknown' src='—'.'—'; no upstream wiki match and no source-code expression. NOTE: live UC comment (if any) is intentionally NOT used as a source — the live UC comment is the artifact we are trying to replace, not anchor against. |
| `BankCodeAsDecimal` | transform='unknown' src='—'.'—'; no upstream wiki match and no source-code expression. NOTE: live UC comment (if any) is intentionally NOT used as a source — the live UC comment is the artifact we are trying to replace, not anchor against. |
| `BankDetailsAccountIDAsString` | transform='unknown' src='—'.'—'; no upstream wiki match and no source-code expression. NOTE: live UC comment (if any) is intentionally NOT used as a source — the live UC comment is the artifact we are trying to replace, not anchor against. |
| `BankIDAsString` | transform='unknown' src='—'.'—'; no upstream wiki match and no source-code expression. NOTE: live UC comment (if any) is intentionally NOT used as a source — the live UC comment is the artifact we are trying to replace, not anchor against. |
| `BankNameAsString` | transform='unknown' src='—'.'—'; no upstream wiki match and no source-code expression. NOTE: live UC comment (if any) is intentionally NOT used as a source — the live UC comment is the artifact we are trying to replace, not anchor against. |
| `BICCodeAsString` | transform='unknown' src='—'.'—'; no upstream wiki match and no source-code expression. NOTE: live UC comment (if any) is intentionally NOT used as a source — the live UC comment is the artifact we are trying to replace, not anchor against. |
| `CIDAsString` | transform='unknown' src='—'.'—'; no upstream wiki match and no source-code expression. NOTE: live UC comment (if any) is intentionally NOT used as a source — the live UC comment is the artifact we are trying to replace, not anchor against. |
| `v` | transform='unknown' src='—'.'—'; no upstream wiki match and no source-code expression. NOTE: live UC comment (if any) is intentionally NOT used as a source — the live UC comment is the artifact we are trying to replace, not anchor against. |
| `CustomerAddressAsString` | transform='unknown' src='—'.'—'; no upstream wiki match and no source-code expression. NOTE: live UC comment (if any) is intentionally NOT used as a source — the live UC comment is the artifact we are trying to replace, not anchor against. |
| `CustomerNameAsString` | transform='unknown' src='—'.'—'; no upstream wiki match and no source-code expression. NOTE: live UC comment (if any) is intentionally NOT used as a source — the live UC comment is the artifact we are trying to replace, not anchor against. |
| `FundingType` | transform='unknown' src='—'.'—'; no upstream wiki match and no source-code expression. NOTE: live UC comment (if any) is intentionally NOT used as a source — the live UC comment is the artifact we are trying to replace, not anchor against. |
| `MaskedAccountIDAsString` | transform='unknown' src='—'.'—'; no upstream wiki match and no source-code expression. NOTE: live UC comment (if any) is intentionally NOT used as a source — the live UC comment is the artifact we are trying to replace, not anchor against. |
| `PurseAsString` | transform='unknown' src='—'.'—'; no upstream wiki match and no source-code expression. NOTE: live UC comment (if any) is intentionally NOT used as a source — the live UC comment is the artifact we are trying to replace, not anchor against. |
| `RoutingNumberAsString` | transform='unknown' src='—'.'—'; no upstream wiki match and no source-code expression. NOTE: live UC comment (if any) is intentionally NOT used as a source — the live UC comment is the artifact we are trying to replace, not anchor against. |
| `SecureIDAsDecimal` | transform='unknown' src='—'.'—'; no upstream wiki match and no source-code expression. NOTE: live UC comment (if any) is intentionally NOT used as a source — the live UC comment is the artifact we are trying to replace, not anchor against. |
| `SortCodeAsString` | transform='unknown' src='—'.'—'; no upstream wiki match and no source-code expression. NOTE: live UC comment (if any) is intentionally NOT used as a source — the live UC comment is the artifact we are trying to replace, not anchor against. |
| `AccountBalanceAsDecimal` | transform='unknown' src='—'.'—'; no upstream wiki match and no source-code expression. NOTE: live UC comment (if any) is intentionally NOT used as a source — the live UC comment is the artifact we are trying to replace, not anchor against. |
| `AccountHolderAsString` | transform='unknown' src='—'.'—'; no upstream wiki match and no source-code expression. NOTE: live UC comment (if any) is intentionally NOT used as a source — the live UC comment is the artifact we are trying to replace, not anchor against. |
| `AccountIDAsDecimal` | transform='unknown' src='—'.'—'; no upstream wiki match and no source-code expression. NOTE: live UC comment (if any) is intentionally NOT used as a source — the live UC comment is the artifact we are trying to replace, not anchor against. |
| `ACHBankAccountIDAsInteger` | transform='unknown' src='—'.'—'; no upstream wiki match and no source-code expression. NOTE: live UC comment (if any) is intentionally NOT used as a source — the live UC comment is the artifact we are trying to replace, not anchor against. |
| `Address1AsString` | transform='unknown' src='—'.'—'; no upstream wiki match and no source-code expression. NOTE: live UC comment (if any) is intentionally NOT used as a source — the live UC comment is the artifact we are trying to replace, not anchor against. |
| `Address2AsString` | transform='unknown' src='—'.'—'; no upstream wiki match and no source-code expression. NOTE: live UC comment (if any) is intentionally NOT used as a source — the live UC comment is the artifact we are trying to replace, not anchor against. |
| `AdviseAsString` | transform='unknown' src='—'.'—'; no upstream wiki match and no source-code expression. NOTE: live UC comment (if any) is intentionally NOT used as a source — the live UC comment is the artifact we are trying to replace, not anchor against. |
| `AvailableBalanceAsDecimal` | transform='unknown' src='—'.'—'; no upstream wiki match and no source-code expression. NOTE: live UC comment (if any) is intentionally NOT used as a source — the live UC comment is the artifact we are trying to replace, not anchor against. |
| `BankCodeAsString` | transform='unknown' src='—'.'—'; no upstream wiki match and no source-code expression. NOTE: live UC comment (if any) is intentionally NOT used as a source — the live UC comment is the artifact we are trying to replace, not anchor against. |
| `BillNumberAsString` | transform='unknown' src='—'.'—'; no upstream wiki match and no source-code expression. NOTE: live UC comment (if any) is intentionally NOT used as a source — the live UC comment is the artifact we are trying to replace, not anchor against. |
| `BuildingNumberAsString` | transform='unknown' src='—'.'—'; no upstream wiki match and no source-code expression. NOTE: live UC comment (if any) is intentionally NOT used as a source — the live UC comment is the artifact we are trying to replace, not anchor against. |
| `CardHolderPhoneNumberBodyAsString` | transform='unknown' src='—'.'—'; no upstream wiki match and no source-code expression. NOTE: live UC comment (if any) is intentionally NOT used as a source — the live UC comment is the artifact we are trying to replace, not anchor against. |
| `CardHolderPhoneNumberPrefixAsString` | transform='unknown' src='—'.'—'; no upstream wiki match and no source-code expression. NOTE: live UC comment (if any) is intentionally NOT used as a source — the live UC comment is the artifact we are trying to replace, not anchor against. |
| `CardNumberAsString` | transform='unknown' src='—'.'—'; no upstream wiki match and no source-code expression. NOTE: live UC comment (if any) is intentionally NOT used as a source — the live UC comment is the artifact we are trying to replace, not anchor against. |
| `CityAsString` | transform='unknown' src='—'.'—'; no upstream wiki match and no source-code expression. NOTE: live UC comment (if any) is intentionally NOT used as a source — the live UC comment is the artifact we are trying to replace, not anchor against. |
| `CountryIDAsString` | transform='unknown' src='—'.'—'; no upstream wiki match and no source-code expression. NOTE: live UC comment (if any) is intentionally NOT used as a source — the live UC comment is the artifact we are trying to replace, not anchor against. |
| `CountryNameAsString` | transform='unknown' src='—'.'—'; no upstream wiki match and no source-code expression. NOTE: live UC comment (if any) is intentionally NOT used as a source — the live UC comment is the artifact we are trying to replace, not anchor against. |
| `CreatedAtAsString` | transform='unknown' src='—'.'—'; no upstream wiki match and no source-code expression. NOTE: live UC comment (if any) is intentionally NOT used as a source — the live UC comment is the artifact we are trying to replace, not anchor against. |
| `CurrentBalanceAsDecimal` | transform='unknown' src='—'.'—'; no upstream wiki match and no source-code expression. NOTE: live UC comment (if any) is intentionally NOT used as a source — the live UC comment is the artifact we are trying to replace, not anchor against. |
| `CustomerIDAsString` | transform='unknown' src='—'.'—'; no upstream wiki match and no source-code expression. NOTE: live UC comment (if any) is intentionally NOT used as a source — the live UC comment is the artifact we are trying to replace, not anchor against. |
| `EmailAsString` | transform='unknown' src='—'.'—'; no upstream wiki match and no source-code expression. NOTE: live UC comment (if any) is intentionally NOT used as a source — the live UC comment is the artifact we are trying to replace, not anchor against. |
| `EndPointIDAsString` | transform='unknown' src='—'.'—'; no upstream wiki match and no source-code expression. NOTE: live UC comment (if any) is intentionally NOT used as a source — the live UC comment is the artifact we are trying to replace, not anchor against. |
| `ErrorCodeAsString` | transform='unknown' src='—'.'—'; no upstream wiki match and no source-code expression. NOTE: live UC comment (if any) is intentionally NOT used as a source — the live UC comment is the artifact we are trying to replace, not anchor against. |
| `ErrorTypeAsString` | transform='unknown' src='—'.'—'; no upstream wiki match and no source-code expression. NOTE: live UC comment (if any) is intentionally NOT used as a source — the live UC comment is the artifact we are trying to replace, not anchor against. |
| `FirstNameAsString` | transform='unknown' src='—'.'—'; no upstream wiki match and no source-code expression. NOTE: live UC comment (if any) is intentionally NOT used as a source — the live UC comment is the artifact we are trying to replace, not anchor against. |
| `IBANCodeAsString` | transform='unknown' src='—'.'—'; no upstream wiki match and no source-code expression. NOTE: live UC comment (if any) is intentionally NOT used as a source — the live UC comment is the artifact we are trying to replace, not anchor against. |
| `InitialTransactionIDAsString` | transform='unknown' src='—'.'—'; no upstream wiki match and no source-code expression. NOTE: live UC comment (if any) is intentionally NOT used as a source — the live UC comment is the artifact we are trying to replace, not anchor against. |
| `IPAsString` | transform='unknown' src='—'.'—'; no upstream wiki match and no source-code expression. NOTE: live UC comment (if any) is intentionally NOT used as a source — the live UC comment is the artifact we are trying to replace, not anchor against. |
| `LanguageIDAsInteger` | transform='unknown' src='—'.'—'; no upstream wiki match and no source-code expression. NOTE: live UC comment (if any) is intentionally NOT used as a source — the live UC comment is the artifact we are trying to replace, not anchor against. |
| `LastNameAsString` | transform='unknown' src='—'.'—'; no upstream wiki match and no source-code expression. NOTE: live UC comment (if any) is intentionally NOT used as a source — the live UC comment is the artifact we are trying to replace, not anchor against. |
| `MD5AsString` | transform='unknown' src='—'.'—'; no upstream wiki match and no source-code expression. NOTE: live UC comment (if any) is intentionally NOT used as a source — the live UC comment is the artifact we are trying to replace, not anchor against. |
| `PayerAsString` | transform='unknown' src='—'.'—'; no upstream wiki match and no source-code expression. NOTE: live UC comment (if any) is intentionally NOT used as a source — the live UC comment is the artifact we are trying to replace, not anchor against. |
| `PayerBusiness` | transform='unknown' src='—'.'—'; no upstream wiki match and no source-code expression. NOTE: live UC comment (if any) is intentionally NOT used as a source — the live UC comment is the artifact we are trying to replace, not anchor against. |
| `PayerIDAsString` | transform='unknown' src='—'.'—'; no upstream wiki match and no source-code expression. NOTE: live UC comment (if any) is intentionally NOT used as a source — the live UC comment is the artifact we are trying to replace, not anchor against. |
| `PayerPurseAsString` | transform='unknown' src='—'.'—'; no upstream wiki match and no source-code expression. NOTE: live UC comment (if any) is intentionally NOT used as a source — the live UC comment is the artifact we are trying to replace, not anchor against. |
| `PayerStatus` | transform='unknown' src='—'.'—'; no upstream wiki match and no source-code expression. NOTE: live UC comment (if any) is intentionally NOT used as a source — the live UC comment is the artifact we are trying to replace, not anchor against. |
| `PaymentAmountAsDecimal` | transform='unknown' src='—'.'—'; no upstream wiki match and no source-code expression. NOTE: live UC comment (if any) is intentionally NOT used as a source — the live UC comment is the artifact we are trying to replace, not anchor against. |
| `PaymentDateAsDateTime` | transform='unknown' src='—'.'—'; no upstream wiki match and no source-code expression. NOTE: live UC comment (if any) is intentionally NOT used as a source — the live UC comment is the artifact we are trying to replace, not anchor against. |
| `PaymentGuaranteeAsString` | transform='unknown' src='—'.'—'; no upstream wiki match and no source-code expression. NOTE: live UC comment (if any) is intentionally NOT used as a source — the live UC comment is the artifact we are trying to replace, not anchor against. |
| `PaymentModeAsInteger` | transform='unknown' src='—'.'—'; no upstream wiki match and no source-code expression. NOTE: live UC comment (if any) is intentionally NOT used as a source — the live UC comment is the artifact we are trying to replace, not anchor against. |
| `PaymentProviderTransactionStatusAsString` | transform='unknown' src='—'.'—'; no upstream wiki match and no source-code expression. NOTE: live UC comment (if any) is intentionally NOT used as a source — the live UC comment is the artifact we are trying to replace, not anchor against. |
| `PaymentStatusAsInteger` | transform='unknown' src='—'.'—'; no upstream wiki match and no source-code expression. NOTE: live UC comment (if any) is intentionally NOT used as a source — the live UC comment is the artifact we are trying to replace, not anchor against. |
| `PaymentTypeAsString` | transform='unknown' src='—'.'—'; no upstream wiki match and no source-code expression. NOTE: live UC comment (if any) is intentionally NOT used as a source — the live UC comment is the artifact we are trying to replace, not anchor against. |
| `PlaidItemIDAsString` | transform='unknown' src='—'.'—'; no upstream wiki match and no source-code expression. NOTE: live UC comment (if any) is intentionally NOT used as a source — the live UC comment is the artifact we are trying to replace, not anchor against. |
| `PlaidNamesAsString` | transform='unknown' src='—'.'—'; no upstream wiki match and no source-code expression. NOTE: live UC comment (if any) is intentionally NOT used as a source — the live UC comment is the artifact we are trying to replace, not anchor against. |
| `PlatformIDAsInteger` | transform='unknown' src='—'.'—'; no upstream wiki match and no source-code expression. NOTE: live UC comment (if any) is intentionally NOT used as a source — the live UC comment is the artifact we are trying to replace, not anchor against. |
| `PromotionCodeAsString` | transform='unknown' src='—'.'—'; no upstream wiki match and no source-code expression. NOTE: live UC comment (if any) is intentionally NOT used as a source — the live UC comment is the artifact we are trying to replace, not anchor against. |
| `PSPCodeAsString` | transform='unknown' src='—'.'—'; no upstream wiki match and no source-code expression. NOTE: live UC comment (if any) is intentionally NOT used as a source — the live UC comment is the artifact we are trying to replace, not anchor against. |
| `RapidFirstNameAsString` | transform='unknown' src='—'.'—'; no upstream wiki match and no source-code expression. NOTE: live UC comment (if any) is intentionally NOT used as a source — the live UC comment is the artifact we are trying to replace, not anchor against. |
| `RapidLastNameAsString` | transform='unknown' src='—'.'—'; no upstream wiki match and no source-code expression. NOTE: live UC comment (if any) is intentionally NOT used as a source — the live UC comment is the artifact we are trying to replace, not anchor against. |
| `ResponseMessageAsString` | transform='unknown' src='—'.'—'; no upstream wiki match and no source-code expression. NOTE: live UC comment (if any) is intentionally NOT used as a source — the live UC comment is the artifact we are trying to replace, not anchor against. |
| `ResponseTimeAsString` | transform='unknown' src='—'.'—'; no upstream wiki match and no source-code expression. NOTE: live UC comment (if any) is intentionally NOT used as a source — the live UC comment is the artifact we are trying to replace, not anchor against. |
| `SecretKeyAsString` | transform='unknown' src='—'.'—'; no upstream wiki match and no source-code expression. NOTE: live UC comment (if any) is intentionally NOT used as a source — the live UC comment is the artifact we are trying to replace, not anchor against. |
| `ThreeDsAsJson` | transform='unknown' src='—'.'—'; no upstream wiki match and no source-code expression. NOTE: live UC comment (if any) is intentionally NOT used as a source — the live UC comment is the artifact we are trying to replace, not anchor against. |
| `ThreeDsResponseType` | transform='unknown' src='—'.'—'; no upstream wiki match and no source-code expression. NOTE: live UC comment (if any) is intentionally NOT used as a source — the live UC comment is the artifact we are trying to replace, not anchor against. |
| `TokenAsString` | transform='unknown' src='—'.'—'; no upstream wiki match and no source-code expression. NOTE: live UC comment (if any) is intentionally NOT used as a source — the live UC comment is the artifact we are trying to replace, not anchor against. |
| `TransactionIDAsString` | transform='unknown' src='—'.'—'; no upstream wiki match and no source-code expression. NOTE: live UC comment (if any) is intentionally NOT used as a source — the live UC comment is the artifact we are trying to replace, not anchor against. |
| `ZipCodeAsString` | transform='unknown' src='—'.'—'; no upstream wiki match and no source-code expression. NOTE: live UC comment (if any) is intentionally NOT used as a source — the live UC comment is the artifact we are trying to replace, not anchor against. |
| `BaseExchangeRate` | transform='unknown' src='—'.'—'; no upstream wiki match and no source-code expression. NOTE: live UC comment (if any) is intentionally NOT used as a source — the live UC comment is the artifact we are trying to replace, not anchor against. |
| `ExchangeFee` | transform='unknown' src='—'.'—'; no upstream wiki match and no source-code expression. NOTE: live UC comment (if any) is intentionally NOT used as a source — the live UC comment is the artifact we are trying to replace, not anchor against. |
| `ProtocolMIDSettingsID` | transform='unknown' src='—'.'—'; no upstream wiki match and no source-code expression. NOTE: live UC comment (if any) is intentionally NOT used as a source — the live UC comment is the artifact we are trying to replace, not anchor against. |
| `FunnelID` | transform='unknown' src='—'.'—'; no upstream wiki match and no source-code expression. NOTE: live UC comment (if any) is intentionally NOT used as a source — the live UC comment is the artifact we are trying to replace, not anchor against. |
| `AmountUSD` | transform='unknown' src='—'.'—'; no upstream wiki match and no source-code expression. NOTE: live UC comment (if any) is intentionally NOT used as a source — the live UC comment is the artifact we are trying to replace, not anchor against. |
| `SessionID` | transform='unknown' src='—'.'—'; no upstream wiki match and no source-code expression. NOTE: live UC comment (if any) is intentionally NOT used as a source — the live UC comment is the artifact we are trying to replace, not anchor against. |
| `PlatformID` | transform='unknown' src='—'.'—'; no upstream wiki match and no source-code expression. NOTE: live UC comment (if any) is intentionally NOT used as a source — the live UC comment is the artifact we are trying to replace, not anchor against. |
| `MOPCountry` | transform='unknown' src='—'.'—'; no upstream wiki match and no source-code expression. NOTE: live UC comment (if any) is intentionally NOT used as a source — the live UC comment is the artifact we are trying to replace, not anchor against. |
| `SwiftCodeAsString` | transform='unknown' src='—'.'—'; no upstream wiki match and no source-code expression. NOTE: live UC comment (if any) is intentionally NOT used as a source — the live UC comment is the artifact we are trying to replace, not anchor against. |
| `ClientBankNameAsString` | transform='unknown' src='—'.'—'; no upstream wiki match and no source-code expression. NOTE: live UC comment (if any) is intentionally NOT used as a source — the live UC comment is the artifact we are trying to replace, not anchor against. |
| `BankName` | transform='unknown' src='—'.'—'; no upstream wiki match and no source-code expression. NOTE: live UC comment (if any) is intentionally NOT used as a source — the live UC comment is the artifact we are trying to replace, not anchor against. |
| `CardCategory` | transform='unknown' src='—'.'—'; no upstream wiki match and no source-code expression. NOTE: live UC comment (if any) is intentionally NOT used as a source — the live UC comment is the artifact we are trying to replace, not anchor against. |
| `PaymentGeneration` | transform='unknown' src='—'.'—'; no upstream wiki match and no source-code expression. NOTE: live UC comment (if any) is intentionally NOT used as a source — the live UC comment is the artifact we are trying to replace, not anchor against. |
| `ProcessRegulationID` | transform='unknown' src='—'.'—'; no upstream wiki match and no source-code expression. NOTE: live UC comment (if any) is intentionally NOT used as a source — the live UC comment is the artifact we are trying to replace, not anchor against. |
| `MerchantAccountID` | transform='unknown' src='—'.'—'; no upstream wiki match and no source-code expression. NOTE: live UC comment (if any) is intentionally NOT used as a source — the live UC comment is the artifact we are trying to replace, not anchor against. |
| `IsSetBalanceCompleted` | transform='unknown' src='—'.'—'; no upstream wiki match and no source-code expression. NOTE: live UC comment (if any) is intentionally NOT used as a source — the live UC comment is the artifact we are trying to replace, not anchor against. |
| `RoutingReasonID` | transform='unknown' src='—'.'—'; no upstream wiki match and no source-code expression. NOTE: live UC comment (if any) is intentionally NOT used as a source — the live UC comment is the artifact we are trying to replace, not anchor against. |
| `IsRecurring` | transform='unknown' src='—'.'—'; no upstream wiki match and no source-code expression. NOTE: live UC comment (if any) is intentionally NOT used as a source — the live UC comment is the artifact we are trying to replace, not anchor against. |
| `FlowID` | transform='unknown' src='—'.'—'; no upstream wiki match and no source-code expression. NOTE: live UC comment (if any) is intentionally NOT used as a source — the live UC comment is the artifact we are trying to replace, not anchor against. |
| `IsAftSupportedAsBool` | transform='unknown' src='—'.'—'; no upstream wiki match and no source-code expression. NOTE: live UC comment (if any) is intentionally NOT used as a source — the live UC comment is the artifact we are trying to replace, not anchor against. |
| `IsAftEligibleAsBool` | transform='unknown' src='—'.'—'; no upstream wiki match and no source-code expression. NOTE: live UC comment (if any) is intentionally NOT used as a source — the live UC comment is the artifact we are trying to replace, not anchor against. |
| `IsAftProcessedAsBool` | transform='unknown' src='—'.'—'; no upstream wiki match and no source-code expression. NOTE: live UC comment (if any) is intentionally NOT used as a source — the live UC comment is the artifact we are trying to replace, not anchor against. |
| `etr_y` | transform='unknown' src='—'.'—'; no upstream wiki match and no source-code expression. NOTE: live UC comment (if any) is intentionally NOT used as a source — the live UC comment is the artifact we are trying to replace, not anchor against. |
| `etr_ym` | transform='unknown' src='—'.'—'; no upstream wiki match and no source-code expression. NOTE: live UC comment (if any) is intentionally NOT used as a source — the live UC comment is the artifact we are trying to replace, not anchor against. |
| `etr_ymd` | transform='unknown' src='—'.'—'; no upstream wiki match and no source-code expression. NOTE: live UC comment (if any) is intentionally NOT used as a source — the live UC comment is the artifact we are trying to replace, not anchor against. |

## Tier 4 candidates

_None._

## Cross-check mismatches

_None._

## Open questions

_None._

## Reviewer Corrections

> Domain-expert overrides applied as **Tier 5** on the next regeneration.
> Tier 5 is absolute — it overrides every other tier including Tier 1.

| Column | Correction | Current (wrong) | Reason | Reviewer |
|--------|------------|-----------------|--------|----------|
| _(none)_ | | | | |
