# Review-needed sidecar — `bi_output_vg_volume_amount`

Generated: 2026-06-19
Wiki: `knowledge/UC_generated/bi_output/Views/bi_output_vg_volume_amount.md`

## UNVERIFIED columns

| Column | Reason |
|--------|--------|
| `Date` | transform='unknown' src='—'.'—'; no upstream wiki match and no source-code expression. NOTE: live UC comment (if any) is intentionally NOT used as a source — the live UC comment is the artifact we are trying to replace, not anchor against. |
| `RealCID` | transform='unknown' src='—'.'—'; no upstream wiki match and no source-code expression. NOTE: live UC comment (if any) is intentionally NOT used as a source — the live UC comment is the artifact we are trying to replace, not anchor against. |
| `InstrumentTypeID` | transform='unknown' src='—'.'—'; no upstream wiki match and no source-code expression. NOTE: live UC comment (if any) is intentionally NOT used as a source — the live UC comment is the artifact we are trying to replace, not anchor against. |
| `IsSettled` | transform='unknown' src='—'.'—'; no upstream wiki match and no source-code expression. NOTE: live UC comment (if any) is intentionally NOT used as a source — the live UC comment is the artifact we are trying to replace, not anchor against. |
| `IsCopy` | transform='unknown' src='—'.'—'; no upstream wiki match and no source-code expression. NOTE: live UC comment (if any) is intentionally NOT used as a source — the live UC comment is the artifact we are trying to replace, not anchor against. |
| `IsBuy` | transform='unknown' src='—'.'—'; no upstream wiki match and no source-code expression. NOTE: live UC comment (if any) is intentionally NOT used as a source — the live UC comment is the artifact we are trying to replace, not anchor against. |
| `IsLeverage` | transform='unknown' src='—'.'—'; no upstream wiki match and no source-code expression. NOTE: live UC comment (if any) is intentionally NOT used as a source — the live UC comment is the artifact we are trying to replace, not anchor against. |
| `IsFuture` | transform='unknown' src='—'.'—'; no upstream wiki match and no source-code expression. NOTE: live UC comment (if any) is intentionally NOT used as a source — the live UC comment is the artifact we are trying to replace, not anchor against. |
| `IsCopyFund` | transform='unknown' src='—'.'—'; no upstream wiki match and no source-code expression. NOTE: live UC comment (if any) is intentionally NOT used as a source — the live UC comment is the artifact we are trying to replace, not anchor against. |
| `IsOpenedFromIBAN` | transform='unknown' src='—'.'—'; no upstream wiki match and no source-code expression. NOTE: live UC comment (if any) is intentionally NOT used as a source — the live UC comment is the artifact we are trying to replace, not anchor against. |
| `IsClosedToIBAN` | transform='unknown' src='—'.'—'; no upstream wiki match and no source-code expression. NOTE: live UC comment (if any) is intentionally NOT used as a source — the live UC comment is the artifact we are trying to replace, not anchor against. |
| `IsRecurring` | transform='unknown' src='—'.'—'; no upstream wiki match and no source-code expression. NOTE: live UC comment (if any) is intentionally NOT used as a source — the live UC comment is the artifact we are trying to replace, not anchor against. |
| `IsAirDrop` | transform='unknown' src='—'.'—'; no upstream wiki match and no source-code expression. NOTE: live UC comment (if any) is intentionally NOT used as a source — the live UC comment is the artifact we are trying to replace, not anchor against. |
| `VolumeOpen` | transform='unknown' src='—'.'—'; no upstream wiki match and no source-code expression. NOTE: live UC comment (if any) is intentionally NOT used as a source — the live UC comment is the artifact we are trying to replace, not anchor against. |
| `VolumeClose` | transform='unknown' src='—'.'—'; no upstream wiki match and no source-code expression. NOTE: live UC comment (if any) is intentionally NOT used as a source — the live UC comment is the artifact we are trying to replace, not anchor against. |
| `InvestedAmountOpen` | transform='unknown' src='—'.'—'; no upstream wiki match and no source-code expression. NOTE: live UC comment (if any) is intentionally NOT used as a source — the live UC comment is the artifact we are trying to replace, not anchor against. |
| `InvestedAmountClosed` | transform='unknown' src='—'.'—'; no upstream wiki match and no source-code expression. NOTE: live UC comment (if any) is intentionally NOT used as a source — the live UC comment is the artifact we are trying to replace, not anchor against. |
| `TotalVolume` | transform='unknown' src='—'.'—'; no upstream wiki match and no source-code expression. NOTE: live UC comment (if any) is intentionally NOT used as a source — the live UC comment is the artifact we are trying to replace, not anchor against. |
| `NetInvestedAmount` | transform='unknown' src='—'.'—'; no upstream wiki match and no source-code expression. NOTE: live UC comment (if any) is intentionally NOT used as a source — the live UC comment is the artifact we are trying to replace, not anchor against. |
| `CountOpenTransactions` | transform='unknown' src='—'.'—'; no upstream wiki match and no source-code expression. NOTE: live UC comment (if any) is intentionally NOT used as a source — the live UC comment is the artifact we are trying to replace, not anchor against. |
| `CountCloseTransactions` | transform='unknown' src='—'.'—'; no upstream wiki match and no source-code expression. NOTE: live UC comment (if any) is intentionally NOT used as a source — the live UC comment is the artifact we are trying to replace, not anchor against. |
| `CountTotalTransactions` | transform='unknown' src='—'.'—'; no upstream wiki match and no source-code expression. NOTE: live UC comment (if any) is intentionally NOT used as a source — the live UC comment is the artifact we are trying to replace, not anchor against. |
| `UpdateDate` | transform='unknown' src='—'.'—'; no upstream wiki match and no source-code expression. NOTE: live UC comment (if any) is intentionally NOT used as a source — the live UC comment is the artifact we are trying to replace, not anchor against. |
| `IsSQF` | transform='unknown' src='—'.'—'; no upstream wiki match and no source-code expression. NOTE: live UC comment (if any) is intentionally NOT used as a source — the live UC comment is the artifact we are trying to replace, not anchor against. |
| `IsMarginTrade` | transform='unknown' src='—'.'—'; no upstream wiki match and no source-code expression. NOTE: live UC comment (if any) is intentionally NOT used as a source — the live UC comment is the artifact we are trying to replace, not anchor against. |
| `IsC2P` | transform='unknown' src='—'.'—'; no upstream wiki match and no source-code expression. NOTE: live UC comment (if any) is intentionally NOT used as a source — the live UC comment is the artifact we are trying to replace, not anchor against. |
| `etr_y` | transform='unknown' src='—'.'—'; no upstream wiki match and no source-code expression. NOTE: live UC comment (if any) is intentionally NOT used as a source — the live UC comment is the artifact we are trying to replace, not anchor against. |
| `etr_ym` | transform='unknown' src='—'.'—'; no upstream wiki match and no source-code expression. NOTE: live UC comment (if any) is intentionally NOT used as a source — the live UC comment is the artifact we are trying to replace, not anchor against. |
| `etr_ymd` | transform='unknown' src='—'.'—'; no upstream wiki match and no source-code expression. NOTE: live UC comment (if any) is intentionally NOT used as a source — the live UC comment is the artifact we are trying to replace, not anchor against. |

## Tier 4 candidates

_None._

## Cross-check mismatches

_None._

## Open questions

_None._

## Reviewer Corrections

> Domain-expert overrides applied as **Tier 5** on the next regeneration.
> Tier 5 is absolute — it overrides every other tier including Tier 1.

| Column | Correction | Current (wrong) | Reason | Reviewer |
|--------|------------|-----------------|--------|----------|
| _(none)_ | | | | |
