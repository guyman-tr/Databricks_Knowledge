# Review-needed sidecar — `crm_case_v`

Generated: 2026-05-19
Wiki: `knowledge/UC_generated/etoro_kpi/Views/crm_case_v.md`

## UNVERIFIED columns

| Column | Reason |
|--------|--------|
| `CaseNumber` | transform='passthrough' src='main.crm.silver_crm_case'.'CaseNumber'; no upstream wiki match and no source-code expression. NOTE: live UC comment (if any) is intentionally NOT used as a source — the live UC comment is the artifact we are trying to replace, not anchor against. |
| `Status` | transform='passthrough' src='main.crm.silver_crm_case'.'Status'; no upstream wiki match and no source-code expression. NOTE: live UC comment (if any) is intentionally NOT used as a source — the live UC comment is the artifact we are trying to replace, not anchor against. |
| `Origin` | transform='passthrough' src='main.crm.silver_crm_case'.'Origin'; no upstream wiki match and no source-code expression. NOTE: live UC comment (if any) is intentionally NOT used as a source — the live UC comment is the artifact we are trying to replace, not anchor against. |
| `Subject` | transform='passthrough' src='main.crm.silver_crm_case'.'Subject'; no upstream wiki match and no source-code expression. NOTE: live UC comment (if any) is intentionally NOT used as a source — the live UC comment is the artifact we are trying to replace, not anchor against. |
| `Priority` | transform='passthrough' src='main.crm.silver_crm_case'.'Priority'; no upstream wiki match and no source-code expression. NOTE: live UC comment (if any) is intentionally NOT used as a source — the live UC comment is the artifact we are trying to replace, not anchor against. |
| `OwnerId` | transform='passthrough' src='main.crm.silver_crm_case'.'OwnerId'; no upstream wiki match and no source-code expression. NOTE: live UC comment (if any) is intentionally NOT used as a source — the live UC comment is the artifact we are trying to replace, not anchor against. |
| `ClosedDate` | transform='passthrough' src='main.crm.silver_crm_case'.'ClosedDate'; no upstream wiki match and no source-code expression. NOTE: live UC comment (if any) is intentionally NOT used as a source — the live UC comment is the artifact we are trying to replace, not anchor against. |
| `IsClosedOnCreate` | transform='passthrough' src='main.crm.silver_crm_case'.'IsClosedOnCreate'; no upstream wiki match and no source-code expression. NOTE: live UC comment (if any) is intentionally NOT used as a source — the live UC comment is the artifact we are trying to replace, not anchor against. |

## Tier 4 candidates

_None._

## Cross-check mismatches

_None._

## Open questions

_None._

## Reviewer Corrections

> Domain-expert overrides applied as **Tier 5** on the next regeneration.
> Tier 5 is absolute — it overrides every other tier including Tier 1.

| Column | Correction | Current (wrong) | Reason | Reviewer |
|--------|------------|-----------------|--------|----------|
| _(none)_ | | | | |
