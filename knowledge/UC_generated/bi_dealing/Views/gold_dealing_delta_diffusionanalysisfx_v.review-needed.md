# Review-needed sidecar — `gold_dealing_delta_diffusionanalysisfx_v`

Generated: 2026-05-19
Wiki: `knowledge/UC_generated/bi_dealing/Views/gold_dealing_delta_diffusionanalysisfx_v.md`

## UNVERIFIED columns

| Column | Reason |
|--------|--------|
| `USD_NOP` | transform='udf' src='main.bi_dealing.gold_dealing_delta_diffusionanalysisfx'.'—'; no upstream wiki match and no source-code expression. NOTE: live UC comment (if any) is intentionally NOT used as a source — the live UC comment is the artifact we are trying to replace, not anchor against. |
| `UnitsNOP` | transform='udf' src='main.bi_dealing.gold_dealing_delta_diffusionanalysisfx'.'—'; no upstream wiki match and no source-code expression. NOTE: live UC comment (if any) is intentionally NOT used as a source — the live UC comment is the artifact we are trying to replace, not anchor against. |
| `Delta` | transform='unknown' src='main.bi_dealing.gold_dealing_delta_diffusionanalysisfx'.'—'; no upstream wiki match and no source-code expression. NOTE: live UC comment (if any) is intentionally NOT used as a source — the live UC comment is the artifact we are trying to replace, not anchor against. |
| `DeltaRatio` | transform='unknown' src='main.bi_dealing.gold_dealing_delta_diffusionanalysisfx'.'—'; no upstream wiki match and no source-code expression. NOTE: live UC comment (if any) is intentionally NOT used as a source — the live UC comment is the artifact we are trying to replace, not anchor against. |
| `DeltaSquared` | transform='unknown' src='main.bi_dealing.gold_dealing_delta_diffusionanalysisfx'.'—'; no upstream wiki match and no source-code expression. NOTE: live UC comment (if any) is intentionally NOT used as a source — the live UC comment is the artifact we are trying to replace, not anchor against. |
| `DeltaSquaredRatio` | transform='unknown' src='main.bi_dealing.gold_dealing_delta_diffusionanalysisfx'.'—'; no upstream wiki match and no source-code expression. NOTE: live UC comment (if any) is intentionally NOT used as a source — the live UC comment is the artifact we are trying to replace, not anchor against. |
| `Sigma` | transform='coalesce' src='main.bi_dealing.gold_dealing_delta_diffusionanalysisfx'.'—'; no upstream wiki match and no source-code expression. NOTE: live UC comment (if any) is intentionally NOT used as a source — the live UC comment is the artifact we are trying to replace, not anchor against. |

## Tier 4 candidates

_None._

## Cross-check mismatches

_None._

## Open questions

- `PositionsTime`: blocked on upstream `main.bi_dealing.gold_dealing_delta_diffusionanalysisfx` wiki (in-scope, not yet authored).
- `InstrumentName`: blocked on upstream `main.bi_dealing.gold_dealing_delta_diffusionanalysisfx` wiki (in-scope, not yet authored).
- `InstrumentID`: blocked on upstream `main.bi_dealing.gold_dealing_delta_diffusionanalysisfx` wiki (in-scope, not yet authored).
- `HedgeServerID`: blocked on upstream `main.bi_dealing.gold_dealing_delta_diffusionanalysisfx` wiki (in-scope, not yet authored).
- `Mid`: blocked on upstream `main.bi_dealing.gold_dealing_delta_diffusionanalysisfx` wiki (in-scope, not yet authored).
- `T`: blocked on upstream `main.bi_dealing.gold_dealing_delta_diffusionanalysisfx` wiki (in-scope, not yet authored).
- `SigmaDate`: blocked on upstream `main.bi_dealing.gold_dealing_delta_diffusionanalysisfx` wiki (in-scope, not yet authored).

## Reviewer Corrections

> Domain-expert overrides applied as **Tier 5** on the next regeneration.
> Tier 5 is absolute — it overrides every other tier including Tier 1.

| Column | Correction | Current (wrong) | Reason | Reviewer |
|--------|------------|-----------------|--------|----------|
| _(none)_ | | | | |
