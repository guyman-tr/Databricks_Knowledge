-- =============================================================================
-- Databricks ALTER Script: bronze etoro.History.DepositAction
-- Generated: 2026-04-30 | tools/uc_bronze/generate_bronze_alters.py
-- Source wiki: knowledge/ProdSchemas/DB_Schema/etoro/Wiki/History/Tables/History.DepositAction.md
-- Layer: bronze
-- UC Target: main.general.bronze_etoro_history_depositaction
-- =============================================================================

-- ---- UC Target: main.general.bronze_etoro_history_depositaction (business_group=general) ----
ALTER TABLE main.general.bronze_etoro_history_depositaction SET TBLPROPERTIES (
    'comment' = 'Append-only event log recording every payment processing action taken on a deposit - each row captures one state transition in the deposit''s payment lifecycle (New -> InProcess -> Closed/Approved/Declined) across 37M+ events from 2014. Source: etoro.History.DepositAction on the etoro production database, ingested via the Generic Pipeline (Append strategy, 1440-minute refresh). Doc source: Tier 1 wiki (knowledge/ProdSchemas/DB_Schema/etoro/Wiki/History/Tables/History.DepositAction.md).'
);

ALTER TABLE main.general.bronze_etoro_history_depositaction SET TAGS (
    'layer' = 'bronze',
    'source_system' = 'SQL Server',
    'source_database' = 'etoro',
    'source_schema' = 'History',
    'source_table' = 'DepositAction',
    'business_group' = 'general',
    'pipeline' = 'generic_pipeline',
    'doc_source' = 'tier1_wiki',
    'doc_generated' = '2026-04-30',
    'copy_strategy' = 'Append',
    'refresh_minutes' = '1440'
);

-- Column Comments
ALTER TABLE main.general.bronze_etoro_history_depositaction ALTER COLUMN DepositActionID COMMENT 'Surrogate primary key, auto-incremented by 1. NOT FOR REPLICATION prevents identity re-seeding on subscriber nodes. NONCLUSTERED PK - the table is physically ordered by DepositID (see clustered index), so range scans by deposit are efficient. Returned as OUTPUT parameter from Billing.DepositActionAdd via SCOPE_IDENTITY(). (Tier 1 - upstream wiki, etoro.History.DepositAction)';
ALTER TABLE main.general.bronze_etoro_history_depositaction ALTER COLUMN DepositID COMMENT 'The deposit this action belongs to. Clustered index key (HDPA_DEPOSIT) - all rows for a single deposit are physically co-located, enabling fast retrieval of the full action history for one deposit. FK to Billing.Deposit (implicit - no formal constraint). (Tier 1 - upstream wiki, etoro.History.DepositAction)';
ALTER TABLE main.general.bronze_etoro_history_depositaction ALTER COLUMN PaymentActionStatusID COMMENT 'The processing state of this specific action event. FK to Dictionary.PaymentActionStatus: 1=New (submitted, not yet sent to provider), 2=InProcess (sent to payment gateway, awaiting response), 3=Closed (final outcome received). Distribution: New 43%, Closed 37%, InProcess 12%, 0=legacy 8%. (Tier 1 - upstream wiki, etoro.History.DepositAction)';
ALTER TABLE main.general.bronze_etoro_history_depositaction ALTER COLUMN PaymentActionTypeID COMMENT 'The type of payment action. FK to Dictionary.PaymentActionType: 1=PreAuthorization, 2=Purchase (82% of rows - the standard deposit action), 3=Cashout, 4=Refund, 5=Settle, 6=PostBack (10% - asynchronous provider callback confirming outcome), 7=Cancel (<0.1% - cancellation action). Legacy 0 values in 8% of rows predate the dictionary. (Tier 1 - upstream wiki, etoro.History.DepositAction)';
ALTER TABLE main.general.bronze_etoro_history_depositaction ALTER COLUMN PaymentStatusID COMMENT 'The deposit''s overall payment status at the time this action was recorded. FK to Dictionary.PaymentStatus: 1=New, 2=Approved, 3=Decline, 4=Technical, 5=InProcess, 6=Canceled, 11=Chargeback, 12=Refund, 13=Pending, 26=RefundAsChargeback, 35=DeclineByRRE, 39=ReversedDeposit (39 total values). Enables reconstructing the deposit''s status trajectory across all actions. (Tier 1 - upstream wiki, etoro.History.DepositAction)';
ALTER TABLE main.general.bronze_etoro_history_depositaction ALTER COLUMN ResponseID COMMENT 'Links this action to the raw payment provider response received. NULL for actions created before the provider responds. The NONCLUSTERED index HDPA_RESPONSE on ResponseID supports fast lookup of "which action corresponds to this provider response?" Used in Billing.GetLastDepositActionWithResponseCode. (Tier 1 - upstream wiki, etoro.History.DepositAction)';
ALTER TABLE main.general.bronze_etoro_history_depositaction ALTER COLUMN ManagerID COMMENT 'The back-office agent ID who triggered this action, or 0 for automated system processing. Non-zero values reference BackOffice.Manager and identify manual interventions (e.g., a BO agent canceling a stuck deposit via BackOffice.DepositCancel). NULL for legacy rows. (Tier 1 - upstream wiki, etoro.History.DepositAction)';
ALTER TABLE main.general.bronze_etoro_history_depositaction ALTER COLUMN ExchangeRate COMMENT 'Currency exchange rate applied when the deposit currency differs from USD (system base). Used to convert the deposit Amount to USD for internal accounting. NULL if no conversion was needed (USD deposits). (Tier 1 - upstream wiki, etoro.History.DepositAction)';
ALTER TABLE main.general.bronze_etoro_history_depositaction ALTER COLUMN ApprovalNumber COMMENT 'Payment provider''s approval/authorization number for this transaction. Used as a reference identifier in disputes, chargebacks, and manual investigation. Format varies by provider. (Tier 1 - upstream wiki, etoro.History.DepositAction)';
ALTER TABLE main.general.bronze_etoro_history_depositaction ALTER COLUMN AuthCode COMMENT 'Authorization code returned by the payment provider. Used alongside ApprovalNumber for payment verification and dispute resolution. (Tier 1 - upstream wiki, etoro.History.DepositAction)';
ALTER TABLE main.general.bronze_etoro_history_depositaction ALTER COLUMN ModificationDate COMMENT 'UTC datetime when this action row was inserted (set to GETDATE() by Billing.DepositActionAdd, or overridden via @Now parameter for batch/reprocessing scenarios). The composite NC index on (ModificationDate, DepositActionID) enables chronological queries across all deposits for a time window. (Tier 1 - upstream wiki, etoro.History.DepositAction)';
ALTER TABLE main.general.bronze_etoro_history_depositaction ALTER COLUMN ClearingHouseEffectiveDate COMMENT 'The date the payment clears the clearing house (bank settlement date). Different from ModificationDate (when the action was recorded) - represents the value date for accounting purposes. NULL for non-cleared actions. (Tier 1 - upstream wiki, etoro.History.DepositAction)';
ALTER TABLE main.general.bronze_etoro_history_depositaction ALTER COLUMN Amount COMMENT 'The deposit amount in the customer''s original currency. Set only on the first action for a deposit (the initial submission row). NULL on all subsequent action rows since the amount is already established. Stored as money type (4 decimal places) rather than dbo.dtPrice since this is a transacted fiat amount, not a price. (Tier 1 - upstream wiki, etoro.History.DepositAction)';
ALTER TABLE main.general.bronze_etoro_history_depositaction ALTER COLUMN CurrencyID COMMENT 'The currency of the Amount. FK to Dictionary.Currency (implicit): 1=USD. NULL when Amount is NULL. Populated only on the initial submission action. (Tier 1 - upstream wiki, etoro.History.DepositAction)';
ALTER TABLE main.general.bronze_etoro_history_depositaction ALTER COLUMN MatchStatusID COMMENT 'PSP reconciliation match status - tracks whether this deposit''s actions have been matched against payment provider settlement records. Carried forward from the previous row for the same DepositID by Billing.DepositActionAdd (all rows for a deposit share the same value until a matching operation changes it). Distribution: 0=Unmatched/default (99.9% of rows), NULL=legacy (0.1%). Used in Billing.DepositMatch and Billing.PSPMatchToEtoro for reconciliation workflows. (Tier 1 - upstream wiki, etoro.History.DepositAction)';
ALTER TABLE main.general.bronze_etoro_history_depositaction ALTER COLUMN Remark COMMENT 'Free-text note explaining the reason for this action (e.g., reason for cancellation, manual override justification). NULL for automated actions. Carries over from the SP caller context. (Tier 1 - upstream wiki, etoro.History.DepositAction)';
ALTER TABLE main.general.bronze_etoro_history_depositaction ALTER COLUMN SessionID COMMENT 'The customer''s web session ID at the time of the deposit action. Links the payment event to the customer session for fraud analysis and investigation. NULL for system-generated actions with no user session context. (Tier 1 - upstream wiki, etoro.History.DepositAction)';
ALTER TABLE main.general.bronze_etoro_history_depositaction ALTER COLUMN DepotID COMMENT 'Identifies the payment gateway/depot (provider routing) used for this action. Set when the deposit is assigned to a specific processor. NULL for initial actions before gateway assignment and for closure rows. Example: DepotID=87 in recent data. (Tier 1 - upstream wiki, etoro.History.DepositAction)';
ALTER TABLE main.general.bronze_etoro_history_depositaction ALTER COLUMN ExchangeFee COMMENT 'Fee charged for currency exchange, in the smallest currency unit (cents). NULL for USD deposits or when no exchange fee applies. (Tier 1 - upstream wiki, etoro.History.DepositAction)';
ALTER TABLE main.general.bronze_etoro_history_depositaction ALTER COLUMN BaseExchangeRate COMMENT 'The base (pre-markup) exchange rate, as opposed to ExchangeRate which may include the spread. Enables fee calculation: fee = Amount * (ExchangeRate - BaseExchangeRate). (Tier 1 - upstream wiki, etoro.History.DepositAction)';
ALTER TABLE main.general.bronze_etoro_history_depositaction ALTER COLUMN PaymentGeneration COMMENT 'Identifies the generation or version of the payment processing flow used for this deposit. Distinguishes between different payment processing implementations deployed over time (e.g., legacy vs. modern payment stack). (Tier 1 - upstream wiki, etoro.History.DepositAction)';
ALTER TABLE main.general.bronze_etoro_history_depositaction ALTER COLUMN ProcessRegulationID COMMENT 'The regulatory processing framework applied to this deposit. References a regulatory classification that determines which processing rules and compliance checks apply. May correspond to jurisdiction or entity (e.g., Cyprus vs. US regulatory environment). (Tier 1 - upstream wiki, etoro.History.DepositAction)';
ALTER TABLE main.general.bronze_etoro_history_depositaction ALTER COLUMN MerchantAccountID COMMENT 'The merchant account within the payment gateway used for this transaction. Works in conjunction with DepotID: DepotID identifies the gateway, MerchantAccountID identifies the specific merchant account on that gateway. Example: MerchantAccountID=7 with DepotID=87 in recent data. (Tier 1 - upstream wiki, etoro.History.DepositAction)';

