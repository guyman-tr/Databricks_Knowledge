-- =============================================================================
-- Databricks ALTER Script: bronze etoro.History.HedgeServer
-- Generated: 2026-04-30 | tools/uc_bronze/generate_bronze_alters.py
-- Source wiki: knowledge/ProdSchemas/DB_Schema/etoro/Wiki/History/Tables/History.HedgeServer.md
-- Layer: bronze
-- UC Target: main.trading.bronze_etoro_history_hedgeserver
-- =============================================================================

-- ---- UC Target: main.trading.bronze_etoro_history_hedgeserver (business_group=trading) ----
ALTER TABLE main.trading.bronze_etoro_history_hedgeserver SET TBLPROPERTIES (
    'comment' = 'SQL Server system-versioned temporal history table for Trade.HedgeServer, automatically recording every configuration change to hedge server records with precise row-validity timestamps (SysStartTime/SysEndTime) to enable point-in-time reconstruction of any hedge server''s configuration state. Source: etoro.History.HedgeServer on the etoro production database, ingested via the Generic Pipeline (Append strategy, 60-minute refresh). Doc source: Tier 1 wiki (knowledge/ProdSchemas/DB_Schema/etoro/Wiki/History/Tables/History.HedgeServer.md).'
);

ALTER TABLE main.trading.bronze_etoro_history_hedgeserver SET TAGS (
    'layer' = 'bronze',
    'source_system' = 'SQL Server',
    'source_database' = 'etoro',
    'source_schema' = 'History',
    'source_table' = 'HedgeServer',
    'business_group' = 'trading',
    'pipeline' = 'generic_pipeline',
    'doc_source' = 'tier1_wiki',
    'doc_generated' = '2026-04-30',
    'copy_strategy' = 'Append',
    'refresh_minutes' = '60'
);

-- Column Comments
ALTER TABLE main.trading.bronze_etoro_history_hedgeserver ALTER COLUMN HedgeServerID COMMENT 'Identifier of the hedge server node. Matches Trade.HedgeServer.HedgeServerID (PK_THSR). Multiple rows with the same HedgeServerID represent successive configuration versions over time. (Tier 1 - upstream wiki, etoro.History.HedgeServer)';
ALTER TABLE main.trading.bronze_etoro_history_hedgeserver ALTER COLUMN IPAddress COMMENT 'IP address of the hedge server node. DEFAULT ''127.0.0.1'' in Trade.HedgeServer. In staging environment all rows show 127.0.0.1; production would show actual server IPs. (Tier 1 - upstream wiki, etoro.History.HedgeServer)';
ALTER TABLE main.trading.bronze_etoro_history_hedgeserver ALTER COLUMN Port COMMENT 'TCP port the hedge server listens on. DEFAULT 0 in Trade.HedgeServer. Port=0 may indicate the server uses a default or dynamically assigned port. (Tier 1 - upstream wiki, etoro.History.HedgeServer)';
ALTER TABLE main.trading.bronze_etoro_history_hedgeserver ALTER COLUMN IsActive COMMENT 'Whether this hedge server was active (1=active, 0=inactive) during this version window. Indexed in Trade.HedgeServer via Idx_Trade_HedgeServer_IsActive. DEFAULT 1 (active) on creation. When set to 0, the server stops accepting hedge requests. (Tier 1 - upstream wiki, etoro.History.HedgeServer)';
ALTER TABLE main.trading.bronze_etoro_history_hedgeserver ALTER COLUMN HedgingMode COMMENT 'Numeric mode controlling the hedging behavior pattern of this server. Values observed: 0 (no periodic hedging) and 1 (periodic hedging enabled - works alongside PeriodicHedgeIntervalMinutes). Exact semantics are configuration-level and not enumerated in DDL. (Tier 1 - upstream wiki, etoro.History.HedgeServer)';
ALTER TABLE main.trading.bronze_etoro_history_hedgeserver ALTER COLUMN IsDummy COMMENT 'Indicates if this is a test/dummy server (1) or a real production server (0). Dummy servers are used for testing and simulation without executing real trades against liquidity providers. (Tier 1 - upstream wiki, etoro.History.HedgeServer)';
ALTER TABLE main.trading.bronze_etoro_history_hedgeserver ALTER COLUMN ConsiderOpenRequestsSec COMMENT 'Timeout in seconds for how long to consider open hedge requests valid. DEFAULT 60 (1 minute) in Trade.HedgeServer. Requests older than this threshold may be discarded or re-evaluated. (Tier 1 - upstream wiki, etoro.History.HedgeServer)';
ALTER TABLE main.trading.bronze_etoro_history_hedgeserver ALTER COLUMN HedgeStrategyModeID COMMENT 'FK to Dictionary.HedgeStrategyMode.HedgeStrategyModeID in Trade.HedgeServer (WITH CHECK). Defines the hedging strategy algorithm used by this server. DEFAULT 0. See _glossary.md for HedgeStrategyMode values if documented. (Tier 1 - upstream wiki, etoro.History.HedgeServer)';
ALTER TABLE main.trading.bronze_etoro_history_hedgeserver ALTER COLUMN ExecutionFactor COMMENT 'Scaling factor applied to hedge position sizes. DEFAULT 1.0 (full-size hedging). Values < 1.0 mean under-hedging (partial coverage); values > 1.0 mean over-hedging. Used by the hedge server when calculating position sizes to submit to liquidity providers. (Tier 1 - upstream wiki, etoro.History.HedgeServer)';
ALTER TABLE main.trading.bronze_etoro_history_hedgeserver ALTER COLUMN AllowMajor COMMENT 'Whether this server is permitted to hedge major currency pairs. DEFAULT 0 (not allowed). When 1, the server can execute hedges on major pairs in addition to its configured instrument set. (Tier 1 - upstream wiki, etoro.History.HedgeServer)';
ALTER TABLE main.trading.bronze_etoro_history_hedgeserver ALTER COLUMN CircuitBreakerLimit COMMENT 'Maximum threshold for a circuit breaker that halts hedging activity when a risk limit is breached. The exact metric (USD exposure, lot count, PnL) is determined by the hedge server''s configuration. A breach triggers automatic halting of hedge operations. (Tier 1 - upstream wiki, etoro.History.HedgeServer)';
ALTER TABLE main.trading.bronze_etoro_history_hedgeserver ALTER COLUMN CircuitBreakerWarningLimit COMMENT 'Warning threshold below CircuitBreakerLimit that triggers an alert before the hard stop is reached. Smaller precision (12,4 vs 14,4) allows for a warning that fires at a lower threshold. (Tier 1 - upstream wiki, etoro.History.HedgeServer)';
ALTER TABLE main.trading.bronze_etoro_history_hedgeserver ALTER COLUMN InstrumentIDToHedgeOn COMMENT 'When set, restricts this hedge server to hedge only on this specific instrument ID. NULL means the server can hedge on any instrument in its configured set. References Trade.Instrument or Dictionary.Instrument. (Tier 1 - upstream wiki, etoro.History.HedgeServer)';
ALTER TABLE main.trading.bronze_etoro_history_hedgeserver ALTER COLUMN DbLoginName COMMENT 'SQL Server login name (suser_name()) of the session that made the change captured in this version. Computed column in Trade.HedgeServer, materialized here at version creation time. Enables "who changed this" audit queries. (Tier 1 - upstream wiki, etoro.History.HedgeServer)';
ALTER TABLE main.trading.bronze_etoro_history_hedgeserver ALTER COLUMN AppLoginName COMMENT 'Application-level login identifier from SQL Server context_info() at the time of the change. Populated by applications that set context_info before modifying Trade.HedgeServer. NULL if not set by the application. (Tier 1 - upstream wiki, etoro.History.HedgeServer)';
ALTER TABLE main.trading.bronze_etoro_history_hedgeserver ALTER COLUMN SysStartTime COMMENT 'UTC timestamp when this row version became active in Trade.HedgeServer. GENERATED ALWAYS AS ROW START on the source table. The row was valid from this instant until SysEndTime. Precision to 100-nanosecond intervals (datetime2(7)). (Tier 1 - upstream wiki, etoro.History.HedgeServer)';
ALTER TABLE main.trading.bronze_etoro_history_hedgeserver ALTER COLUMN SysEndTime COMMENT 'UTC timestamp when this row version was superseded (by an UPDATE or DELETE on Trade.HedgeServer). GENERATED ALWAYS AS ROW END. CLUSTERED index key (leading column) enabling efficient temporal range scans. The combination SysStartTime=SysEndTime indicates a zero-duration version (INSERT immediately followed by UPDATE). (Tier 1 - upstream wiki, etoro.History.HedgeServer)';
ALTER TABLE main.trading.bronze_etoro_history_hedgeserver ALTER COLUMN HostName COMMENT 'Network hostname (host_name()) of the client machine that made the change. Computed column in Trade.HedgeServer, materialized at version creation. Identifies which server or workstation updated this configuration. (Tier 1 - upstream wiki, etoro.History.HedgeServer)';
ALTER TABLE main.trading.bronze_etoro_history_hedgeserver ALTER COLUMN OperationalMode COMMENT 'Operational behavior mode of the hedge server. Values observed: 1 (standard operation), 2 (alternative mode), 5 (specialized mode). DEFAULT 1 in Trade.HedgeServer. Exact semantics are defined in the hedge server application configuration. (Tier 1 - upstream wiki, etoro.History.HedgeServer)';
ALTER TABLE main.trading.bronze_etoro_history_hedgeserver ALTER COLUMN PriceSource COMMENT 'Identifier for the price data source this server uses when calculating hedge rates. DEFAULT 1. Value 1 is the only value observed in live data. Exact source mapping is defined in the hedge server application. (Tier 1 - upstream wiki, etoro.History.HedgeServer)';
ALTER TABLE main.trading.bronze_etoro_history_hedgeserver ALTER COLUMN PeriodicHedgeIntervalMinutes COMMENT 'When periodic hedging is enabled (HedgingMode=1), this is the interval in minutes between scheduled hedge rebalancing cycles. NULL when periodic hedging is not configured. (Tier 1 - upstream wiki, etoro.History.HedgeServer)';
ALTER TABLE main.trading.bronze_etoro_history_hedgeserver ALTER COLUMN PeriodicHedgeHours COMMENT 'Schedule string defining which hours of the day periodic hedging is allowed to run. Likely a comma-delimited or range-format string (e.g., "9-17" for market hours). NULL when not configured or when all hours are permitted. (Tier 1 - upstream wiki, etoro.History.HedgeServer)';
ALTER TABLE main.trading.bronze_etoro_history_hedgeserver ALTER COLUMN UnitRoundingMethod COMMENT 'Method used to round position sizes to valid lot increments when submitting orders to the liquidity provider. The specific rounding algorithm (floor, ceil, nearest) is determined by the value. NULL uses the hedge server''s default rounding. (Tier 1 - upstream wiki, etoro.History.HedgeServer)';
ALTER TABLE main.trading.bronze_etoro_history_hedgeserver ALTER COLUMN StrategyName COMMENT 'Human-readable name of the hedging strategy used by this server. Identifies the strategy configuration applied (e.g., "EMS" strategies). NULL if no named strategy is assigned. (Tier 1 - upstream wiki, etoro.History.HedgeServer)';
ALTER TABLE main.trading.bronze_etoro_history_hedgeserver ALTER COLUMN StrategyGroup COMMENT 'Numeric group identifier for categorizing hedge servers by strategy family. Servers in the same group may share configuration or be coordinated during rebalancing. NULL if not grouped. (Tier 1 - upstream wiki, etoro.History.HedgeServer)';
ALTER TABLE main.trading.bronze_etoro_history_hedgeserver ALTER COLUMN SystemName COMMENT 'Name of the system managing this hedge server. DEFAULT ''EMS'' (Execution Management System). All rows in live data show ''EMS'', indicating all hedge servers are managed by the EMS platform. (Tier 1 - upstream wiki, etoro.History.HedgeServer)';
ALTER TABLE main.trading.bronze_etoro_history_hedgeserver ALTER COLUMN RequestedAlertIntervalSeconds COMMENT 'Interval in seconds for requested alert notifications from this server. DEFAULT 180 (3 minutes). Configures how frequently the server should send status/health alerts to monitoring systems. (Tier 1 - upstream wiki, etoro.History.HedgeServer)';
ALTER TABLE main.trading.bronze_etoro_history_hedgeserver ALTER COLUMN ManagedExposurePeriodSec COMMENT 'Duration in seconds for the managed exposure monitoring window. Defines how long a period of exposure is tracked before being evaluated or reset by the exposure management algorithm. NULL if not configured. (Tier 1 - upstream wiki, etoro.History.HedgeServer)';
ALTER TABLE main.trading.bronze_etoro_history_hedgeserver ALTER COLUMN AllowOMSPricingPartialFill COMMENT 'Whether this server allows partial fills when using OMS (Order Management System) pricing. DEFAULT 0 (partial fills not allowed). When 1, the server accepts orders where only part of the requested quantity is filled. (Tier 1 - upstream wiki, etoro.History.HedgeServer)';
ALTER TABLE main.trading.bronze_etoro_history_hedgeserver ALTER COLUMN `HostName (duplicate)` COMMENT 'Note: HostName appears once in position 18 above. The DDL lists 30 distinct columns with no duplicates. (Tier 1 - upstream wiki, etoro.History.HedgeServer)';
-- == LAST EXECUTION ==
-- Timestamp: 2026-05-03 10:35:25 UTC
-- Bronze deploy: etoro batch 1
-- ====================
