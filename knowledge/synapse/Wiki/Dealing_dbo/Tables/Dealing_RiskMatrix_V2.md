# Dealing_dbo.Dealing_RiskMatrix_V2

| Property | Value |
|----------|-------|
| **Schema** | Dealing_dbo |
| **Object** | Dealing_RiskMatrix_V2 |
| **Type** | Table |
| **ETL SP** | None found — no writer SP in SSDT repository |
| **Refresh** | ⚠️ STALE — single snapshot 2024-06-02; no active ETL |
| **Distribution** | ROUND_ROBIN |
| **Index** | HEAP |
| **Rows** | ~87.6K |
| **Date Range** | 2024-06-02 only (single snapshot ⚠️) |
| **PII** | None |

---

## 1. Business Meaning

NOP (Net Open Position) stress-test matrix for Real Stocks and ETFs, capturing how the hedge book exposure would shift under 26 price movement scenarios — from +1% to +900% and -1% to -100%. Each row represents a specific slice of the NOP book (by instrument × buy/sell × leverage × regulation × region) with a set of simulated NOP values at each price shock level.

The table appears to have been a **one-time experimental snapshot** from June 2024 — no writer SP exists in the SSDT repository and no OpsDB scheduling entry exists. The HEAP storage (rather than CLUSTERED INDEX) is consistent with a temporary/ad-hoc write.

Purpose: stress-testing the real stocks hedge book — understanding how LP hedge requirements change under extreme market scenarios.

---

## 2. Grain

One row = one HedgeServerID × InstrumentID × IsBuy × Leverage × Regulation × Region combination at a single `PositionsTime` snapshot (all rows are 2024-06-02 08:01:49).

---

## 3. Key Columns & Elements

| Column | Type | Description |
|--------|------|-------------|
| `PositionsTime` | datetime | Snapshot timestamp (2024-06-02 08:01:49 for all rows) |
| `HedgeServerID` | int | LP/hedge server identifier |
| `InstrumentID` | int | Instrument identifier |
| `InstrumentName` | nvarchar | Instrument name (denormalized) |
| `InstrumentType` | varchar | Asset class |
| `IsBuy` | bit | 1=Long, 0=Short — client position direction |
| `Leverage` | int | Leverage tier |
| `Regulation` | varchar | Regulatory jurisdiction |
| `Region` | varchar | Geographic region |
| `Bid` | float | Bid price at snapshot time |
| `Ask` | float | Ask price at snapshot time |
| `ConversionRate` | float | FX rate to USD |
| `UnitsNOP` | float | Current net open position in units |
| `IsSettled` | int | 1 = real asset, 0 = CFD asset. (Tier 5 — Expert Review) |
| `UnitsNOP+1%` … `UnitsNOP+900%` | float | Simulated NOP value if price increases by X% (15 tiers: 1,2,3,4,5,10,15,20,25,30,35,40,50,100,900) |
| `UnitsNOP-1%` … `UnitsNOP-100%` | float | Simulated NOP value if price decreases by X% (11 tiers: 1,2,3,4,5,10,15,20,25,50,100) |
| `UpdateDate` | datetime | ETL timestamp (2024-06-02 08:02:49) |

---

## 4. Common Query Patterns

```sql
-- NOP breakdown by instrument at max stress
SELECT InstrumentName, IsBuy, SUM(UnitsNOP) AS CurrentNOP,
       SUM([UnitsNOP+100%]) AS NOP_Up100pct,
       SUM([UnitsNOP-100%]) AS NOP_Down100pct
FROM Dealing_dbo.Dealing_RiskMatrix_V2
GROUP BY InstrumentName, IsBuy
ORDER BY ABS(SUM(UnitsNOP)) DESC;
```

> ⚠️ **Single snapshot** — all data from 2024-06-02. This is a historical point-in-time snapshot, not live. Column names include special characters (`+`, `%`, `-`) — use bracket quoting in all queries.

---

## 5. Known Issues & Quirks

- **No writer SP in SSDT**: ETL pipeline unknown — possibly generated by a Python script, Databricks notebook, or ad-hoc query
- **No OpsDB entry**: Not scheduled; will not be refreshed automatically
- **Single snapshot**: All 87.6K rows share PositionsTime = 2024-06-02 08:01:49 — appears to be a one-time run
- **HEAP storage**: No clustered index — unusual for a persisted table; consistent with ad-hoc/experimental use
- **Column name quoting**: Scenario columns use `[UnitsNOP+1%]`, `[UnitsNOP-1%]` etc. — must use square brackets in SQL
- **26 stress scenarios**: 15 upside (+1% through +900%) and 11 downside (-1% through -100%) tiers

---

## 6. Lineage Summary

Source unknown — no writer SP found in SSDT repository. Likely computed from DWH_dbo.Dim_Position (NOP aggregation) + DWH_dbo.Dim_Instrument + a stress-test calculation engine. See `.lineage.md` for available details.

---

## 7. Related Objects

| Object | Relationship |
|--------|-------------|
| `DWH_dbo.Dim_Position` | Likely source — NOP position aggregation |
| `DWH_dbo.Dim_Instrument` | Likely source — instrument metadata |

---

*Quality score: 4.0/10 — stale single snapshot, no active ETL, no OpsDB scheduling*
