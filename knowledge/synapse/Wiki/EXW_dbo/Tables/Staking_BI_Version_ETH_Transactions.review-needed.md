# EXW_dbo.Staking_BI_Version_ETH_Transactions — Review Needed

**Generated**: 2026-04-20 | **Batch**: 8 | **Object**: #4 of 6

## Tier 4 Items (Require Business Owner Verification)

| # | Column | Question | Current Assumption |
|---|--------|----------|-------------------|
| RN-001 | ALL | ETL mechanism: no writer SP in SSDT. Is this populated by the same Databricks/ADF job as other Staking BI tables? | Assumed: same external ETL pipeline as Staking_BI_Version_WalletUserRewards |
| RN-002 | UpdateDate | Is UpdateDate the ETL write time or related to the staking operation completion? | Assumed: ETL write timestamp |
| RN-003 | StakingMonthID | Is StakingMonthID the reward month (month rewards were calculated) or the transaction initiation month? | Assumed: reward month — a staking operation from Jun 2021 may have StakingMonthID=202107 if first rewarded in July |
| RN-004 | Row Count | What is the actual row count? WalletDB.Staking.Staking had 2,181 rows at wiki generation; EligibleTransactions in WalletUserRewards shows 1–5+ per user-month. | Assumed: ~2,000–35,000 rows — needs MCP query to confirm |

## Tier 3 Columns Awaiting Confirmation

| Column | Current Inference | Verification Needed |
|--------|------------------|---------------------|
| IsStakingEligible | 1=eligible to contribute to rewards; 0=not eligible | Confirm eligibility criteria (minimum holding period? ETH-specific rule?) |
| EffectiveStakingStartDate | First day position was staking-eligible for the reward month | Confirm: is this the staking initiation date or a computed eligibility start? |
| EligibleStakingDaysCount | Days within reward month position was eligible | Confirm: calendar days or business days? Does partial month count at month-end? |
| AverageDailyPositionPerTransaction | Average ETH across eligible days for this transaction | Confirm calculation: Amount × EligibleStakingDaysCount / DaysInMonth, or different? |
| ClientMonthlyStakingReward | Reward attributed to this specific transaction | Confirm: SUM across GCID+StakingMonthID should equal MonthlyRewards in WalletUserRewards? |

## Cross-Object Consistency

- `SUM(ClientMonthlyStakingReward) WHERE IsStakingEligible=1` grouped by GCID+StakingMonthID should equal `MonthlyRewards` in `Staking_BI_Version_WalletUserRewards` — assumed consistent; not yet verified via query.
- `COUNT(*) WHERE IsStakingEligible=1` grouped by GCID+StakingMonthID should equal `EligibleTransactions` in `Staking_BI_Version_WalletUserRewards`.
- `StakingMonthID` values should be a subset of those in `Staking_BI_Version_WalletUserRewards` (202107–202305).
- `WalletID` (uniqueidentifier) matches BI version of WalletUserRewards — no CAST needed for joins between these two tables.

## T1 Upstream Fidelity Check

| Column | Upstream Description (WalletDB.Staking.Staking) | Wiki Description | MATCH? |
|--------|------------------------------------------------|------------------|--------|
| Id | "Auto-incrementing surrogate key. PK referenced by Staking.StakingStatuses.StakingId and Staking.StakingTransactions.StakingId via explicit FKs." | "Auto-incrementing surrogate key from WalletDB.Staking.Staking.Id (IDENTITY). Uniquely identifies the staking operation." | YES — verbatim base; DWH context added |
| WalletID | "The customer wallet from which crypto is being staked. FK to Wallet.Wallets.WalletId." | "The wallet from which ETH was staked. FK to Wallet.Wallets.WalletId. Stored as native uniqueidentifier (correctly typed)." | YES — verbatim base; type note added |
| Amount | "Quantity of cryptocurrency being staked, in the asset's native units (e.g., 1.345547 ETH). Summed by Staking.GetStakingTotals for total staked calculation (only Completed operations). High precision (18 decimals) to handle sub-unit amounts." | "Quantity of ETH staked in this operation, in native ETH units (e.g., 1.345547 ETH). High precision (18 decimals). Summed across Completed operations for GetStakingTotals in production." | YES — verbatim base; SP reference retained |
| CorrelationID | "Idempotency key for the staking operation. Generated by the calling service to uniquely identify each staking request. Used by InsertStaking for duplicate detection (NOT EXISTS check)." | "Idempotency key for the staking operation, generated by the calling service. Used by WalletDB for duplicate detection (NOT EXISTS check before insert). Preserved for cross-system reconciliation." | YES — verbatim base; DWH note added |
| CryptoID | "The cryptocurrency being staked. FK to Wallet.CryptoTypes.CryptoID. Currently all records are CryptoId=2 (ETH) in this environment." | "The cryptocurrency being staked. FK to Wallet.CryptoTypes.CryptoID. Value is always 2 (ETH) in this dataset — this is an ETH-only staking program." | YES — verbatim base; DWH scope note added |
| Staking_DateTime | Maps to WalletDB.Staking.Staking.Occurred: "Timestamp when the staking operation was initiated. Defaults to UTC now." | "Timestamp when the staking operation was initiated (maps to WalletDB.Staking.Staking.Occurred, which defaults to UTC now). Rename only." | YES — verbatim base; rename documented |

| Column | Upstream Description (WalletDB.Staking.StakingTransactions) | Wiki Description | MATCH? |
|--------|-------------------------------------------------------------|------------------|--------|
| EtoroFee | "eToro's service fee for processing the staking delegation, in the staked crypto's units. Currently 0 across all records — staking transfers are fee-free for users." | "eToro's service fee for processing the staking delegation, in ETH units. Currently 0 across all records — eToro absorbs staking costs." | YES — verbatim base; ETH units made explicit |
| BlockchainEstFee | "Estimated blockchain network fee (gas fee) for the staking transaction, in the staked crypto's units. Currently 0 across all records — blockchain fees absorbed by eToro." | "Estimated blockchain gas fee for the staking transaction, in ETH units. Currently 0 across all records — blockchain fees absorbed by eToro." | YES — verbatim base |
