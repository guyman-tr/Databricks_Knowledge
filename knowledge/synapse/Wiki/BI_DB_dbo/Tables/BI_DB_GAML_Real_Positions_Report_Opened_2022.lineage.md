# Column Lineage -- BI_DB_dbo.BI_DB_GAML_Real_Positions_Report_Opened_2022

**Writer SP**: `BI_DB_dbo.SP_Finance_Non_US_Settlement_Report` (Priority 99 -- FinanceReportSPS)
**ETL Pattern**: `DELETE FROM ... WHERE OpenDateID = @dateID`; `INSERT ... SELECT` from `#relPos2`
**Slice**: `RegulationID = 10` (GAML only)

---

## Source tables (via #relPos2)

| Source | Alias | Role |
|--------|-------|------|
| BI_DB_dbo.BI_DB_PositionPnL | pl | DateID = @dateID, Amount, PositionPnL → **EOD_Value** as Total_Open_$ |
| DWH_dbo.Dim_Instrument | di | Type, name, ISIN, exchange |
| DWH_dbo.Fact_SnapshotCustomer | fsc | RealCID, RegulationID, validity |
| DWH_dbo.Dim_Range | dr | Snapshot window |
| DWH_dbo.Dim_Country | dc | Country (rolled in #relPos2) |
| DWH_dbo.Dim_PlayerLevel | dpl | Player level |
| DWH_dbo.Dim_Regulation | | Regulation.Name |
| DWH_dbo.Dim_Position | dp | Open/close IDs, InitialAmountCents, MirrorID, RegulationIDOnOpen, OpenOccurred |
| BI_DB_dbo.V_GermanBaFin | gb | IsGermanBaFin |

---

## Column-level lineage

| BI_DB Column | Source column / transform |
|-------------|---------------------------|
| PositionID | pl.PositionID |
| CID | pl.CID |
| OpenDateID | dp.OpenDateID |
| InstrumentID | pl.InstrumentID |
| Instrument_Name | di.Name |
| ISINCode | di.ISINCode |
| Initial_Amount | dp.InitialAmountCents / 100 |
| Regulation_on_Open | dp.RegulationIDOnOpen |
| Current_IsSettled | pl.IsSettled |
| UpdateDate | GETDATE() |
| OpenOccurred | dp.OpenOccurred |
| OpenEOM | EOMONTH(dp.OpenOccurred) |
| InstrumentType | di.InstrumentType |
| Is_Copy | CASE WHEN pl.MirrorID > 0 THEN 1 ELSE 0 END |
| Exchange | di.Exchange |
| EOD_Value | pl.Amount + pl.PositionPnL (aliased Total_Open_$ in #relPos2) |
| Same_Day_OC | CASE WHEN dp.OpenDateID = dp.CloseDateID THEN 1 ELSE 0 END |
| Regulation_Name | Dim_Regulation.Name |
| IsGermanBaFin | CASE WHEN gb.CID IS NOT NULL THEN 1 ELSE 0 END |

---

*Generated by DWH Semantic Documentation Pipeline -- Batch 5*
