# BI_DB_dbo.BI_DB_Client_Balance_Aggregate_Level_New -- Review Needed

> Items flagged for offline domain expert review. The pipeline continues end-to-end; review happens asynchronously. Add corrections to `## Reviewer Corrections` and trigger a review-rerun to regenerate and re-deploy.

## Summary

| Metric | Value |
|--------|-------|
| Total columns | 173 |
| Tier 2 (SP + CID rollup) | 170 |
| Tier 3 (insert-time only) | 3 |
| Tier 4 (inferred / UNVERIFIED) | 0 |
| Columns needing review | 0 Tier 4 |

## Review status

All 173 columns are documented from `SP_Client_Balance_New` (`#RegAgg` over `#CIDAgg`) with semantic inheritance from `BI_DB_Client_Balance_CID_Level_New`. Seven dimension **names** (Regulation, AccountType, Label, Country, MifidCategory, Club, PlayerStatus) remain Tier 1-eligible via the same Dictionary-backed path as the CID wiki when reading transitive lineage.

## Reviewer corrections

> **Instructions**: Add corrections below. Each row becomes a Tier 5 (domain-expert confirmed) override on the next pipeline rerun. Use `glossary` in the Scope column if the term should also be added to `knowledge/glossary.md`.

| Column / topic | Current (wrong) | Correction | Scope | Reviewer | Date |
|----------------|-----------------|------------|-------|----------|------|

## Tier 4 (UNVERIFIED) columns

None. All columns trace to SP + CID table definitions.

## Columns needing clarification

1. **Additive vs interpretable totals**: Confirm with Finance whether `SUM(OpeningBalance)` or `SUM(ClosingBalance)` at this grain is ever used for official balance reporting, or whether CID-level reconciliation is always required first.

2. **Double rows and segment reports**: Analysts filtering on `TransferDirection = 1` only -- is that sufficient for all aggregate dashboards, or do some reports need explicit handling of `TransferDirection = -1` rows?

3. **UC outbound table name**: Confirm the deployed Unity Catalog gold table name matches `bi_db.gold_sql_dp_prod_we_bi_db_dbo_bi_db_client_balance_aggregate_level_new` if this table is replicated to UC.

4. **DepositConversionFee / WithdrawConversionFee**: Same question as CID table -- permanent placeholders or planned implementation?

## Structural notes

- **Upstream wiki depth**: Rich column semantics live in `BI_DB_Client_Balance_CID_Level_New.md`. If that wiki changes, rerun aggregate documentation to refresh inherited descriptions.
- **Outliers / CB-validity**: Customers with `IsCreditReportValidCB` changes still produce multiple paths; aggregate rows reflect that split at the chosen grain.

## Tier 5 re-review needed

| Column | Tier 5 correction | Was based on (old Tier 1--3) | New Tier 1--3 | Change summary |
|--------|-------------------|-------------------------------|---------------|------------------|

---

*Generated by DWH Semantic Documentation Pipeline -- Batch 6.*
