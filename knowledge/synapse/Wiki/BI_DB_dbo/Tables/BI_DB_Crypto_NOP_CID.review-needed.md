# Review Sidecar -- BI_DB_dbo.BI_DB_Crypto_NOP_CID

## Auto-Generated Verification

| Check | Status | Notes |
|-------|--------|-------|
| Column count matches SP INSERT | OK | 36 insert columns in `SP_Crypto_NOP` |
| All columns have tier suffix in wiki | OK | 35 Tier 2 + 1 Tier 3 (`UpdateDate`) |
| Writer SP confirmed | OK | `SP_Crypto_NOP` per OpsDB (Priority 99, Daily, FinanceReportSPS) |
| Sample data reviewed | **PASSED** | Live prod Synapse: `SELECT TOP 5 * ORDER BY Date DESC`, 7-day Regulation distribution, `COUNT_BIG(*)`. `COUNT(*)` alone errors with int overflow; documented in main wiki. Subagent lacked Synapse MCP; used `synapse_connect.py` against `sql_dp_prod_we`. |
| Distribution vs clustered | OK | HASH(`CID`), clustered `Date`, `CID` per DDL |

## Items for Human Review

| # | Column / Section | Confidence | Question |
|---|------------------|------------|----------|
| 1 | CID nullability | Medium | DDL allows NULL `CID`; live **TOP 5** samples showed non-null **CID** only. |
| 2 | Invested amounts | High | `InitialAmount` is `InitialAmountCents/100` -- confirm finance uses same definition as other reports. |

## Reviewer Corrections

*(Empty -- awaiting human review)*

## Tier Distribution

| Tier | Count | Notes |
|------|-------|-------|
| Tier 2 | 35 | Business and SP-derived columns |
| Tier 3 | 1 | `UpdateDate` (`GETDATE()`) |

---

*Generated by DWH Semantic Documentation Pipeline -- Batch 5*
