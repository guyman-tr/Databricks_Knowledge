# Review Sidecar -- BI_DB_dbo.BI_DB_Crypto_NOP

## Auto-Generated Verification

| Check | Status | Notes |
|-------|--------|-------|
| Column count matches SP INSERT | OK | 38 insert columns in `SP_Crypto_NOP` |
| All columns have tier suffix in wiki | OK | 37 Tier 2 + 1 Tier 3 (`UpdateDate`) |
| Writer SP confirmed | OK | `SP_Crypto_NOP` per OpsDB (Priority 99, Daily, FinanceReportSPS) |
| Sample data reviewed | **PASSED** | Live prod Synapse (`sql_dp_prod_we`): `SELECT TOP 5 * ORDER BY Date DESC`, 7-day Regulation / InstrumentName / TanganyStatus / IsBuy / IsDLTUser distributions, `COUNT(*)`. Subagent session lacked Synapse MCP; verification executed via `synapse_connect.py` (same pool as MCP default prod). |
| ETH staking branch logic | OK | Documented from `#NOP_1` / `#NOP_2` (opt-out vs opt-in join inversion) |

## Items for Human Review

| # | Column / Section | Confidence | Question |
|---|------------------|------------|----------|
| 1 | Row grain | High | Confirm reporting consumers treat full `GROUP BY` as grain (many dimensions). |
| 2 | Total_NOP_ReversedUnits | Medium | Confirm business meaning when `BidSpreaded` is NULL (live samples often NULL). |

## Reviewer Corrections

*(Empty -- awaiting human review)*

## Tier Distribution

| Tier | Count | Notes |
|------|-------|-------|
| Tier 2 | 37 | All business and derived-from-source columns |
| Tier 3 | 1 | `UpdateDate` (`GETDATE()`) |

---

*Generated by DWH Semantic Documentation Pipeline -- Batch 5*
