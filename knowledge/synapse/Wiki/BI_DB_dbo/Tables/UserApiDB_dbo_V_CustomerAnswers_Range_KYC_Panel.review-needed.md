# Review Needed: BI_DB_dbo.UserApiDB_dbo_V_CustomerAnswers_Range_KYC_Panel

## Summary

All 13 columns are **Tier 3** — production source identified as `UserApiDB.dbo.V_CustomerAnswers` but no upstream wiki exists for this database/view. The `_no_upstream_found.txt` marker is present.

## Items for Human Review

### 1. Production Source Verification

- The table is loaded via `SP_Create_UserApiDB_dbo_V_CustomerAnswers_Range` which issues a `COPY INTO` from Parquet files at `/internal-sources/Bronze/UserApiDB/dbo/V_CustomerAnswers/`.
- **Action needed**: Confirm that `UserApiDB.dbo.V_CustomerAnswers` is the correct production source view. If a wiki exists for this view in a repo not covered by the routing file, Tier 3 columns could be upgraded to Tier 1.

### 2. QuestionId Mapping Completeness

- The QuestionId-to-subject mapping was extracted from SP_KYC_Panel comment headers (lines 44-77). The following QuestionIds appear in sample data but are not in the comment block:
  - QuestionId 28 ("Profiling Consent") — observed in sample data
  - QuestionId 110 ("Crypto Trading Knowledge Assessment") — observed in sample data
- **Action needed**: Verify whether these questions are intentionally excluded from the downstream KYC Panel pivoting or if the comment block is simply outdated.

### 3. Table Transience

- This table is dropped and recreated on each SP run. The current 295K-row snapshot is from the most recent run and may not be representative of typical volumes.
- **Action needed**: Confirm whether this table should be documented as a permanent DWH object or if it is purely an ETL artifact. If the latter, consider adding a blacklist entry.

### 4. etr_* Partition Columns

- The `etr_y`, `etr_ym`, `etr_ymd` columns are typed as `varchar(max)` in the DDL but contain structured date partition values. They are generated by the Bronze export pipeline path structure, not by the production source.
- **Action needed**: No action required unless these columns are used downstream (they are not referenced by SP_KYC_Panel or SP_KYC_Questions_Answers_Row_Data_46).

### 5. Sibling Tables

- Two sibling tables exist with identical structure: `UserApiDB_dbo_V_CustomerAnswers_Range_KYC_Panel_Full` and `UserApiDB_dbo_V_CustomerAnswers_Range_KYC_Panel_Fix`.
- **Action needed**: Determine if these siblings need separate documentation or are purely ad-hoc variants.
