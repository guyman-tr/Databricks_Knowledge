# Column Lineage -- BI_DB_dbo.BI_DB_GAML_Real_Positions_Report_Closed

**Writer SP**: `BI_DB_dbo.SP_Finance_Non_US_Settlement_Report` (Priority 99 -- FinanceReportSPS)
**ETL Pattern**: `DELETE FROM ... WHERE CloseDateID = @dateID`; `INSERT ... SELECT` from `#relPos2`
**Slice**: `RegulationID = 10` AND `CloseDateID <> 0`

---

## Source tables (via #relPos2)

| Source | Alias | Role |
|--------|-------|------|
| BI_DB_dbo.BI_DB_PositionPnL | pl | DateID = @dateID, IsSettled, Amount, AmountInUnitsDecimal, PositionPnL, HedgeServerID |
| DWH_dbo.Dim_Instrument | di | InstrumentTypeID filter, Name, ISIN, InstrumentType, Exchange |
| DWH_dbo.Fact_SnapshotCustomer | fsc | RealCID, RegulationID, CountryID, PlayerLevelID, IsCreditReportValidCB |
| DWH_dbo.Dim_Range | dr | Snapshot date range |
| DWH_dbo.Dim_Country | dc | Country.Name (in #relPos2) |
| DWH_dbo.Dim_PlayerLevel | dpl | PlayerLevel.Name |
| DWH_dbo.Dim_Regulation | | Regulation.Name as Regulation |
| DWH_dbo.Dim_Position | dp | Open/Close dates, InitialAmountCents, Amount, NetProfit, MirrorID, RegulationIDOnOpen |
| BI_DB_dbo.V_GermanBaFin | gb | IsGermanBaFin |

---

## Column-level lineage

| BI_DB Column | Source column / transform |
|-------------|---------------------------|
| PositionID | pl.PositionID |
| CID | pl.CID |
| CloseDateID | dp.CloseDateID |
| InstrumentID | pl.InstrumentID |
| Instrument_Name | di.Name |
| ISINCode | di.ISINCode |
| Initial_Amount | dp.InitialAmountCents / 100 |
| Regulation_on_Open | dp.RegulationIDOnOpen |
| Current_IsSettled | pl.IsSettled |
| UpdateDate | GETDATE() |
| CloseOccurred | dp.CloseOccurred |
| CloseEOM | EOMONTH(dp.CloseOccurred) |
| InstrumentType | di.InstrumentType |
| Is_Copy | CASE WHEN pl.MirrorID > 0 THEN 1 ELSE 0 END |
| Exchange | di.Exchange |
| Realized_Equity | dp.Amount + dp.NetProfit |
| Same_Day_OC | CASE WHEN dp.OpenDateID = dp.CloseDateID THEN 1 ELSE 0 END |
| Regulation_Name | Dim_Regulation.Name (joined as Regulation in #relPos2) |
| IsGermanBaFin | CASE WHEN gb.CID IS NOT NULL THEN 1 ELSE 0 END |

---

*Generated by DWH Semantic Documentation Pipeline -- Batch 5*
