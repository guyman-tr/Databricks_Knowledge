# Column Lineage -- BI_DB_dbo.BI_DB_Finance_Non_US_Settlement_Report

**Writer SP**: `BI_DB_dbo.SP_Finance_Non_US_Settlement_Report` (Priority 99 -- FinanceReportSPS)
**ETL Pattern**: DELETE-INSERT by `ReportDate = @dateID` (daily)
**Population filter**: Real stocks (`InstrumentTypeID IN (5,6)`), `IsSettled = 1`, `IsCreditReportValidCB = 1`, `RegulationID NOT IN (6,7,8)` in `#relPos2`

**Gap_Type**: Current `#finalPrep` uses literal **'NA'**. **Historical table rows** may still show custodian/hedge gap categories from pre-2022 LP logic (verified live: multi-value distribution on prod).

**Custodian dollar/unit columns**: Literals **0** after LP removal (Apr 2022).

---

## Source chain

```
BI_DB_PositionPnL (pl)
  + Dim_Instrument, Fact_SnapshotCustomer, Dim_Range, Dim_Country, Dim_PlayerLevel, Dim_Regulation, Dim_Position
  + V_GermanBaFin (#GermanBafin)
        |
        v
    #relPos2 (position-level, filtered)
        |
        v
    #relPos1 (CID x Instrument x HedgeServer ...)
        |
        v
    #relPos (Instrument x HedgeServer x Date ...)
        |
        v
#dailyV_ClientsPositions_HistoryCurrency (regulation splits; custodian cols = 0)
        |
        v
    #joined (+ Dim_Instrument display name, #hedgeServers provider)
        |
        v
    #finalPrep (+ #prices; Gap_Type = 'NA')
        |
        v
INSERT BI_DB_Finance_Non_US_Settlement_Report
```

Supporting: `#usdConversion` from `Dim_GetSpreadedPriceUSDConversionRate`; `#prices` from `Fact_CurrencyPriceWithSplit` x USD rate.

---

## Column-level lineage

| BI_DB Column | Source / transform |
|-------------|---------------------|
| Gap_Type | Literal `'NA'` in current `#finalPrep`; legacy rows from older pipeline |
| ProviderID | `#dailyV_ClientsPositions_HistoryCurrency.ProviderID` (= `#relPos.HedgeServerID`) |
| Provider | `#hedgeServers.Provider` by HedgeServerID |
| InstrumentID | `#joined.InstrumentID` |
| Instrument_Name | `Dim_Instrument.InstrumentDisplayName` |
| ReportDate | `@dateID` |
| ISINCode | `Dim_Instrument.ISINCode` |
| Symbol | `Dim_Instrument.Symbol` |
| Closing_Rate_Price_Unspreaded | `#prices` Bid x USD |
| Closing_Rate_Price_Spreaded | `#prices` BidSpreaded x USD |
| Total_Client_Holdings_In_Units | `#dailyV_ClientsPositions_HistoryCurrency.InstrumentInventory` |
| Total_Custodian_Settled_Positions_In_Units | Literal 0 |
| Custodian_vs_Client_Holdings_Difference_In_units | Literal 0 |
| Total_Clients_Holdings_in_$ | `#dailyV_ClientsPositions_HistoryCurrency.Total_Clients_Holdings_In_$` |
| Total_Custodian_Settled_Positions_in_$ | Literal 0 |
| Custodian_vs_Client_Holdings_Difference_In_$ | Literal 0 |
| ASIC_* / CySEC_* / FCA_* / GAML_* / Seychelles_* / FinCENFINRA_* | CASE on `RegulationID` in `#dailyV_ClientsPositions_HistoryCurrency` |
| UpdateDate | `GETDATE()` |
| Actual_Avg_Price | `Total_Clients_Holdings_In_$ / InstrumentInventory` when inventory &lt;&gt; 0 |
| IsGermanBaFin | CASE from `#GermanBafin` on CID, aggregated |
| Country | `Dim_Country.Name` |
| PlayerLevel | `Dim_PlayerLevel.Name` |

---

*Generated by DWH Semantic Documentation Pipeline -- Batch 5*
